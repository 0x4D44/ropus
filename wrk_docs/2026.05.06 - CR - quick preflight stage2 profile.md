# Quick Preflight Stage 2 Profile Code Review

## Findings

No blocking issues found.

## Issue-Level Notes

- **Optional: `/home/md/language/ropus/full-test/src/cli.rs:63`** - The revised help text matches the HLD, but there is still no direct assertion around rendered `print_help()` output. Existing CLI tests cover parsing only. This is not blocking because the HLD phrased help-test work as conditional on an existing capture pattern, and no such helper appears to exist here.

## Assessment

The uncommitted source diff matches the signed revised HLD. `options.quick && options.release_preflight` selects `Stage2Profile::QuickReleasePreflight` in `/home/md/language/ropus/full-test/src/main.rs:260`, while all other flag combinations keep `Stage2Profile::FullWorkspace`. The policy is covered by `/home/md/language/ropus/full-test/src/main.rs:280`.

The quick-preflight profile injects Cargo package excludes for exactly `conformance`, `ropus-harness-control`, and `ropus-harness-deep-plc` before `--lib --tests` in both plain test and `cargo llvm-cov ... test` paths at `/home/md/language/ropus/full-test/src/tests.rs:86` and `/home/md/language/ropus/full-test/src/tests.rs:116`. I confirmed those names match workspace packages.

Normal Stage 2 command construction remains unchanged for `Stage2Profile::FullWorkspace`, with regression tests at `/home/md/language/ropus/full-test/src/tests.rs:399`, `/home/md/language/ropus/full-test/src/tests.rs:417`, and `/home/md/language/ropus/full-test/src/tests.rs:547`.

The IETF vector fallback remains a libtest tail skip of only `--skip testvector` at `/home/md/language/ropus/full-test/src/tests.rs:148`, and the synthetic failure path is still appended independently at `/home/md/language/ropus/full-test/src/tests.rs:284`. The quick-preflight regression test at `/home/md/language/ropus/full-test/src/tests.rs:493` specifically guards against reintroducing `extensions`.

## Previous `--skip extensions` Behavior

I found no remaining `--skip extensions` behavior in the modified source. The remaining `extensions` references under `full-test/src/html.rs` are pre-existing conformance category names, not Stage 2 skip logic. The new reference in `/home/md/language/ropus/full-test/src/tests.rs:499` is a negative assertion.

## Validation

Reviewed:

- `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - quick preflight stage2 profile.md`
- `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - quick preflight stage2 profile implementation.md`
- Current uncommitted source diff for `/home/md/language/ropus/full-test/src/cli.rs`, `/home/md/language/ropus/full-test/src/main.rs`, and `/home/md/language/ropus/full-test/src/tests.rs`

Commands run during review:

```text
timeout 120 cargo test -p full-test --locked
git diff --check -- full-test/src/cli.rs full-test/src/main.rs full-test/src/tests.rs
cargo metadata --no-deps --format-version 1
```

`cargo test -p full-test --locked` passed with 152 tests. `git diff --check` reported no whitespace errors.

## Residual Risks

I did not rerun the full 240s workspace package-exclude command or the 300s user-facing release smoke; I treated the implementer-reported passes as supplemental evidence and verified the cheaper command-shape and package-name checks locally.

Coverage-enabled quick-preflight command shape is unit-tested, but its runtime remains an HLD-accepted residual risk rather than something proven by this review.
