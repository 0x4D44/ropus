# Code Review: Performance Thresholds Final

Date: 2026.05.06

Scope: final independent review of the performance threshold slice after
feedback application.

## Findings

No `Blocker` or `Issue` findings.

The feedback was correctly applied:

- The `8.74x` live decode smoke is documented honestly as an unresolved
  release-blocking signal in
  `/home/md/language/ropus/wrk_docs/2026.05.06 - MEA - performance threshold baseline.md:65`.
  The threshold was not weakened: `/home/md/language/ropus/full-test/src/bench.rs:268`
  still keeps `CELT 48k mono sine 1k loud` decode at a `1.26x` release-fail
  threshold via `/home/md/language/ropus/full-test/src/bench.rs:273`.
- The implementation journal also records the explicit decision not to claim a
  clean release-performance pass until a calibrated sweep clears or explains
  the local result:
  `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - performance thresholds implementation.md:92`.
- Missing-threshold and release-thresholded crashed-row cases now have useful
  synthetic tests:
  `/home/md/language/ropus/full-test/src/bench.rs:1243` and
  `/home/md/language/ropus/full-test/src/bench.rs:1278`.
- The underlying blocking logic remains direct and release-scoped:
  missing thresholds are surfaced at
  `/home/md/language/ropus/full-test/src/bench.rs:414`, crashed rows at
  `/home/md/language/ropus/full-test/src/bench.rs:426`, and banner failure only
  consumes `bench.banner_fail()` at
  `/home/md/language/ropus/full-test/src/banner.rs:95`.

## Contract Check

No new contract drift found. The profile selection keeps non-quick
`--release-preflight` release-thresholded, including when `--skip-benchmarks`
is supplied:
`/home/md/language/ropus/full-test/src/main.rs:107`.

The JSON and HTML report surfaces expose the claim, threshold source, and
release-blocking issues without implying broad performance coverage:
`/home/md/language/ropus/full-test/src/report.rs:134` and
`/home/md/language/ropus/full-test/src/html.rs:858`.

Scope stayed inside the performance runner/reporting and oracle-matrix docs. I
did not find codec, harness parity, dependency, or versioned-file churn in the
reviewed diff.

## Validation

Passed:

```text
timeout 120 cargo test -p full-test bench
timeout 120 cargo test -p full-test banner
timeout 120 cargo fmt --check
timeout 30 git diff --check
```

## Verdict

Go for committing this slice.

Residual risk: this is not a clean release-performance pass. The local
`48k_sine1k_loud.wav` decode smoke would fail the new gate and still needs a
calibrated `--iters 30` same-machine sweep or performance investigation before
any release-performance green claim.
