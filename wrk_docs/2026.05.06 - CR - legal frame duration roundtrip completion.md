# Completion Review: Legal Frame Duration Roundtrip

Date: 2026.05.06

Scope: final completion verification for Phase 3.3 Slice A, legal frame-duration fuzz dimensions for `fuzz_roundtrip`.

Reviewed context:

- `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - legal frame duration fuzz dimensions.md`
- `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - legal frame duration roundtrip.md`
- Current uncommitted source diff for `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs`
- New helper/test files under `/home/md/language/ropus/tests/fuzz/`

## Go/No-Go

Go for Slice A.

No blocking findings were found in the current uncommitted implementation for the requested scope.

## Completion Checks

- Helper coverage: pass. `/home/md/language/ropus/tests/fuzz/fuzz_targets/frame_duration.rs` defines 2.5, 5, 10, 20, 40, and 60 ms via half-millisecond metadata and maps them to the expected sample counts for 8000, 12000, 16000, 24000, and 48000 Hz.
- Helper tests: pass. `/home/md/language/ropus/tests/fuzz/tests/frame_duration_tests.rs` verifies the full HLD sample-rate/duration matrix, stable labels, selector wrapping, and `data[7]` high-bit selection behavior.
- `fuzz_roundtrip` minimum guard: pass. The initial byte guard is lowered from `8 + 320` to `8 + 40`, matching the minimum legal payload for 8 kHz, mono, i16, 2.5 ms.
- `fuzz_roundtrip` bytes-needed guard: pass. After parsing sample rate, channel count, PCM mode, and selected duration, the target computes `samples_needed = frame_size * channels`, selects 2-byte or 4-byte samples by PCM mode, and returns if `pcm_bytes.len() < bytes_needed`.
- Selected `frame_size` use: pass. The selected `frame_size` is used for Rust encode, Rust decode capacity/request, C encode, packet sample-count checks, decoded sample-count checks, and PCM/SNR oracle comparison sizing in both i16 and float paths.
- Oracle strength: pass. No source diff is present for `/home/md/language/ropus/tests/fuzz/fuzz_targets/oracle.rs`; CELT equality, SILK/Hybrid SNR, sample-count, and skip-gate behavior are preserved. The change only adds frame-duration/frame-size context to failure messages.
- Campaign behavior: pass. This is not a fuzz campaign change. Validation built the target but did not run a fuzz campaign.
- Stateful targets: pass. No source diff is present for `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_encode_multiframe.rs` or `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_decode_plc_seq.rs`.
- Docs/journals: pass. HLD, prior review, implementation/review journals, and this completion review/journal are present in the requested locations.

## Validations

All requested validation commands passed:

```text
timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests
```

Result: passed. 4 tests passed.

```text
timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests
```

Result: passed. 20 tests passed, 1 ignored operator-report test.

```text
timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_roundtrip
```

Result: passed.

```text
timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check
```

Result: passed.

```text
timeout 120 cargo fmt --check
```

Result: passed.

```text
git diff --check
```

Result: passed.

## Residual Risks

- This verification did not run a fuzz campaign; it confirms build/test/format/scope readiness for Slice A.
- Duration selection reuses `data[7] >> 5`, so existing corpus entries remain parseable when long enough but may exercise a different frame duration than before. This is explicitly accepted by the HLD.
- Selectors 6 and 7 wrap to 2.5 ms and 5 ms, so those durations are slightly overrepresented when deriving from three high bits. This matches the helper's modulo behavior and the prior review's accepted residual risk.
- There are unrelated uncommitted journal files in the worktree. They do not affect the Slice A source verification.
