# Quick Preflight Stage 2 Profile Completion Review

Date: 2026.05.06

## Decision

Go.

I found no completion blockers. The current uncommitted implementation matches the signed HLD for the quick-preflight Stage 2 package profile, and every requested validation passed.

## HLD Match

- `options.quick && options.release_preflight` selects the quick Stage 2 package profile in `/home/md/language/ropus/full-test/src/main.rs`.
- The quick profile uses Cargo package excludes for exactly:
  - `conformance`
  - `ropus-harness-control`
  - `ropus-harness-deep-plc`
- The rejected `--skip extensions` behavior is absent from Stage 2 command construction. The remaining implementation-side `extensions` reference in this slice is a negative regression assertion.
- The IETF missing-vector fallback remains `--skip testvector`, and the synthetic failure path is still preserved so the gate cannot pass green without required vectors.
- Normal non-quick Stage 2 command shape remains unchanged and is covered by regression tests.
- CLI help text now transparently describes the narrower Stage 2 smoke profile and has direct unit test coverage.

## Validation

Passed:

```text
timeout 120 cargo test -p full-test --locked
```

Result: 153 passed, 0 failed.

Passed:

```text
timeout 240 cargo test --workspace --exclude conformance --exclude ropus-harness-control --exclude ropus-harness-deep-plc --lib --tests --no-fail-fast -- --test-threads=1
```

Result: all scheduled package tests passed under the package-excluded profile, including `ropus` library tests and workspace integration tests.

Passed:

```text
timeout 300 cargo run --release -p full-test -- --quick --release-preflight --skip-coverage
```

Observed Stage 2 command:

```text
cargo test --workspace --exclude conformance --exclude ropus-harness-control --exclude ropus-harness-deep-plc --lib --tests --no-fail-fast -- --test-threads=1
```

Runner result:

```text
PASS — ropus 0.12.6 @ main/79a4b96 — 2257 passed, 0 failed, 6 ignored
```

Passed:

```text
timeout 120 cargo fmt --check
```

Passed:

```text
git diff --check
```

## Residual Risks

Coverage-enabled quick-preflight runtime was not part of Arthur's required validation list for this completion pass. The command shape is unit-tested, but runtime remains the HLD-accepted residual risk.

The quick-preflight smoke intentionally excludes all of `conformance` and both PLC harness packages. That is the signed design tradeoff, not a gap in this slice.
