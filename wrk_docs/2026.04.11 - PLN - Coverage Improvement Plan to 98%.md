# Coverage Improvement Plan: Line/Region/Function Coverage to 98%

**Date:** 2026-04-11  
**Baseline:** Lines 96.29% | Regions 96.65% | Functions 97.60% | Branches 82.53%  
**Target:** 98% lines (primary), 98% regions, 98% functions  
**Delta:** Need to cover ~638 more lines, ~765 more regions, reduce missed functions from 50 to ~42

## Strategy

Focus on the 7 files responsible for 67% of all missed lines. Write targeted unit tests that exercise uncovered branches and error paths. Each stage targets a specific file or group, with a commit after each stage.

## Stage 1: silk/encoder.rs (309 missed lines → target <70)

The largest single gap. Target areas:
- **NSQ delayed-decision path** (`silk_nsq_del_dec`): Viterbi backward path search, state rewinds
- **Stereo M/S encoding**: `silk_stereo_lr_to_ms` mid-only flag decision, stereo width thresholds
- **NLSF encoding trellis**: `silk_nlsf_encode` delta-quantization decision tree
- **LTP gain codebook selection**: `silk_quant_ltp_gains` rate-distortion comparison
- **Noise shape quantizer unvoiced path**: TYPE_UNVOICED LTP prediction
- **Bandwidth switching**: `silk_control_audio_bandwidth` state machine
- **LBRR/FEC setup**: `silk_setup_lbrr` packet loss weighting
- **High-complexity paths**: complexity 5-10 configuration

Expected impact: ~240 lines covered → brings silk/encoder.rs from 95.52% to ~99%+

## Stage 2: opus/encoder.rs (170 missed lines → target <40)

Target areas:
- **Mode transition redundancy**: SILK→CELT bandwidth switch redundancy encoding
- **DTX activation**: Silence detection threshold crossing, 1-byte DTX packet
- **Hybrid mode rate interpolation**: SILK rate for hybrid, CBR-to-VBR cap
- **FEC hysteresis**: `decide_fec` with last_fec=1 subtract path
- **Stereo width edge cases**: Low-bitrate stereo width reduction in hybrid
- **LFE channel mode**: Forces CELT-only narrowband
- **RESTRICTED_LOWDELAY**: Forced CELT-only, zero delay compensation
- **VBR constraint with CELT**: Constraint enforcement

Expected impact: ~130 lines → brings opus/encoder.rs from 94.05% to ~99%+

## Stage 3: opus/decoder.rs (131 missed lines → target <30)

Target areas:
- **Redundancy decoding**: Hybrid mode redundancy, extension decoding
- **FEC decode path**: decode_fec=true LBRR processing
- **Mode transition PLC**: CELT→SILK and SILK→CELT transition buffer logic
- **Bandwidth detection**: SILK_ONLY 8/12/16kHz mapping

Expected impact: ~100 lines → brings opus/decoder.rs from 91.61% to ~98%+

## Stage 4: celt/encoder.rs + celt/bands.rs (181 missed lines → target <40)

Target areas:
- **Constrained VBR**: CVBR upper bound clamping, shrinking logic
- **Silent frame encoding**: VBR silence reservoir update
- **Mid/side rebalancing**: Joint encoding paths, rebalance conditions
- **Extreme gain denormalization**: Very low/high band energy paths

Expected impact: ~140 lines → brings both files to ~99%+

## Stage 5: opus/repacketizer.rs + silk/decoder.rs (121 missed lines → target <25)

Target areas:
- **Extension payload parsing**: Long extensions, variable-length, cascade
- **Self-delimited frame handling**: Variable size, CBR/VBR mismatch
- **Conditional coding**: Delta-lag decoding, pitch contour interpolation
- **LTP gain decoding**: Per-index ICDF table branches

Expected impact: ~96 lines → brings both files to ~99%+

## Stage 6: Remaining files cleanup (~340 missed lines across 22 files → target <100)

Target areas:
- **dnn/core.rs**: Linear layer edge cases, activation clipping
- **celt/ec_ctx.rs**: Encoder snapshot/restore, decoder-only methods
- **celt/range_coder.rs**: Error handling paths
- **celt/lpc.rs**: Edge cases in LPC fitting
- **silk/common.rs**: Rarely-used utility paths
- **dnn/lpcnet.rs**: PLC branch coverage gaps
- **celt/mdct.rs**: Branch coverage (65% → needs attention)

Expected impact: ~240 lines → brings overall to 98%+

## Execution Plan

Each stage:
1. Agent reads the specific file and existing tests
2. Agent writes new tests in the file's `#[cfg(test)] mod tests` block
3. Run `cargo +nightly llvm-cov --branch` to verify improvement
4. Commit when tests pass and coverage improves

## Success Criteria

| Metric     | Current | Target |
|------------|---------|--------|
| Lines      | 96.29%  | 98.0%  |
| Regions    | 96.65%  | 98.0%  |
| Functions  | 97.60%  | 98.0%  |
| Branches   | 82.53%  | 90%+   |
