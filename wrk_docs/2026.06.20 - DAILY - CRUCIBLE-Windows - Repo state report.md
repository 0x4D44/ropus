# 2026.06.20 — DAILY — CRUCIBLE / Windows — Repo state report

**Pass:** read-only daily observation (the first in this series — no prior `DAILY` report exists in `wrk_docs/`, so this establishes the baseline).
**Host / env:** CRUCIBLE / Windows. **Worktree:** `C:/worktrees/ropus/sched-daily-report-CRUCIBLE-20260620-050007` on branch `task/sched-daily-report-CRUCIBLE-20260620-050007`.
**Repo state:** clean tree, `main` mirrors `origin/main` at `8173030`, crate version **0.12.18**.
**Method:** four parallel readers (commit arc, design backlog, defect/quality, project state) → adversarial verification (3 skeptics per interpretive trend claim, 2-of-3 must fail to refute) → direct `git` verification of every cited hash/date. Refuted and unverified items are quarantined below, not reported as fact.

---

## Headline trend

**The codec port and its tooling are essentially feature-complete and shipped (v0.12.18 on crates.io), and the repo has been deliberately quiet for five weeks.** After a heavy April–early-May integration burst, the last code/feature commit landed **2026-05-12**; the only commit since is a cosmetic chore (deltic toast icon) on **2026-06-17**. There are **no known open defects** as of the last campaign closeout (2026-05-08: tests green, fuzz clean). The open threads are not bugs — they are **deferred decisions** about testing depth (coverage 98% target, mutation audit never run) and project direction (resume integration vs. declare a milestone).

This is a *decide-direction* day, not a firefight day. Nothing is red.

---

## Direction of travel — cadence

Commits/day since 2026-05-01 (verified via `git log`):

```
05-01 …  10        05-08  10
05-02 …  29        05-09   6
05-03 …  59 ◄peak  05-10   1
05-04 …  17        05-11   3   (release 0.12.18)
05-06 …  23        05-12   3   (fb2k live-VBR — last code commit)
05-07 …  23        ──────── 36-day gap ────────
                   06-17   1   (chore: deltic toast icon)
```

203 commits in the last 60 days, but all of the signal is in 2026-05-01..12. The project crossed from "build" into a **dormant/parked integration phase** around mid-May. CLAUDE.md still describes the current focus as "integration — full encode/decode round-trip validation"; that phase is paused, not failed.

---

## Key commits since the active baseline (2026-05-08 → 2026-06-17)

All hashes and dates below are `git`-verified.

| Hash | Date | Significance |
|------|------|--------------|
| `8173030` | 2026-06-17 | **chore:** add `.deltic-toast.png` icon. No code/feature change — the sole commit in five weeks. |
| `2912eba` | 2026-05-12 | fb2k live-VBR: version bumps + audit closeout (last code/feature commit). |
| `8cdc252` | 2026-05-12 | fb2k shim: live VBR bitrate in the foobar2000 status bar (EWMA over decoded bytes). |
| `ec12e38` | 2026-05-12 | fb2k FFI: report bytes-consumed from `decode_next` (feeds the VBR estimate). |
| `6267221` | 2026-05-11 | perf: recalibrate README + release thresholds against the fixed bench. |
| `54fef85` | 2026-05-11 | bench: amortise codec construction across iters (removed a measurement artefact). |
| `91c4e59` | 2026-05-11 | **Release 0.12.18** — catch-up crates.io publish since 0.12.6, consolidating unpublished 0.12.7–0.12.17 (per commit msg + `ropus/CHANGELOG.md`). |
| `84a54eb` | 2026-05-10 | SILK drift attribution — **test-framing** fix (see Defects §). |
| `c0c145d`…`755eb3b` | 2026-05-09 | DRED bitrate plumbing — staged TDD port (Stage 2/3/5/7/8), byte-exact in f32. |
| `9d9d9cc` | 2026-05-08 | Fix CELT prediction sequencing for CELT→SILK redundancy (fuzz-found). |
| `55e224c` | 2026-05-08 | Fix SILK LBRR `ec_prev` state-mutation parity (fuzz-found). |
| `9b20775` | 2026-05-08 | Document feat-18 PitchDNN residual + add `tanh_approx` pin test. |
| `9b8c552` | 2026-05-07 | Fix PLC short-frame `delay_buffer` contamination (fuzz-found). |

### Recent features (the last burst)
- **fb2k live-VBR bitrate** in the foobar2000 status bar — single-pole EWMA over decoded bytes, only the bytes that produced kept samples count (seek-honest). Plugin (`ropus-fb2k` 0.3.1) is shippable.
- **DRED bitrate plumbing** — `compute_dred_bitrate`/`estimate_dred_bitrate` ported, validated across a 15-vector FFI matrix (FEC on/off, loss 0–30%, 5k–120k, multiple frame sizes / sample rates).
- **Performance recalibration** — a per-iteration codec-construction artefact (DNN-weight deserialization, ~3.3 ms/iter) was inflating decode timings; with construction hoisted out of the loop the honest steady state is ~**1.06× encode / ~1.01× decode** vs C on i7-14700KF / WSL2, and README + thresholds were regenerated from that baseline.

---

## Design backlog

Themes drawn from the ~147 `wrk_docs/` files, cross-checked against `git`. **Important correction:** an earlier draft mis-dated the conformance/CLI/opus-tools work to "late May 2026"; the hashes are real but landed on **2026-04-18/19**. Dates below are the verified commit dates.

| Item | Status | Evidence (verified hashes/dates) |
|------|--------|-----------------------------------|
| Publish `ropus` to crates.io | **COMPLETE** | `c1ecc6b` (04-18 LICENSE+metadata), `aff71c0` (04-18 gates green), `f8206d8` (04-19 readiness), `91c4e59` (05-11 release 0.12.18) |
| ropus-cli split (enc/dec/info/play) | **COMPLETE** | `12ef7c2` (04-19) |
| opus-tools flag parity | **COMPLETE** | `c4e7640`,`39605b6`,`a124281` (all 04-19) |
| Conformance / IETF projection | **COMPLETE** | `14ba004`,`da86a6a`,`2973c44`,`4cf2d0e` (all **04-19** — *not* a May push) |
| foobar2000 component (`foo_ropus`) | **COMPLETE** | M1–M6 journals; live-VBR `8cdc252`/`2912eba` (05-12) |
| DRED port + bitrate plumbing | **COMPLETE** | `c0c145d` closeout (05-09) |
| All 26 codec modules ported | **COMPLETE** | CLAUDE.md; Tier-1 bit-exact / Tier-2 SNR-bounded per `design_docs/` |
| SIMD acceleration (beyond `wide`) | **DESIGNED → DEFERRED** | HLD `2026.04.08 - HLD - SIMD acceleration.md`; `wide` trial `458e8f5` (04-08); deferral documented `bf1f0ca` (04-19) |
| Branch coverage → 98% | **NOT REACHED (deferred)** | see Defects § — settled high-80s%; closing needs product-code changes |
| Mutation testing | **INFRA REVIVED, NEVER RUN** | `ff113bc`+`863ac77` (05-06) revive the runner; no audit executed, no score exists |
| Torture/stress testing | **BRIEF ONLY** | `2026.04.08 - BRIEF - stress and torture testing.md`; no HLD/impl |

Net: **nothing is "designed but un-started" and forgotten.** SIMD-beyond-`wide`, full 98% coverage, mutation *audits*, and torture testing are the four explicitly-deferred backlog items.

---

## Defect / quality state

**No structured `bugs/` ledger exists in this repo** — defect tracking has been entirely journal-based (`wrk_journals/` numbered bug sessions + fuzz closeouts). The paper trail is thorough, but there are no stable IDs, no two-eyes closure, and no queryable state machine.

**Verified status (corrections in bold):**

- **No known open defects at last closeout.** The 2026-05-08 fuzz expansion campaign closed with **445,581 executions, 0 crashes, 0 new artifacts** across its 4 new/modified targets (the broader fuzz suite is ~10+ targets); the one crash it surfaced (`mode-transitions-stereo-hybrid-state-divergence`) was fixed (`9d9d9cc`) and archived as a regression seed. Tests reported green (1868/1868).
- **Bug #13 (SILK wideband garbage-decode) is FIXED**, not open. ✗ The earlier draft listed it as "the last open issue" — refuted 3/3 by skeptics and confirmed by `git`: fixed **2026-04-14 in `f6b6e50`** (missing `nlsf_interp_coef_q2` write-back on `first_frame_after_reset`), with a regression test; SILK WB probe went 187/200 → 200/200 bit-exact.
- **feat-18 / feat-19 PitchDNN drift — accepted Tier-2, pinned (no action).** feat-18 ≈ `1.013279e-5`, feat-19 ≈ `1.79e-7`; root cause is the **C reference's** SSE2 `_mm_rcp_ps` approximate reciprocal, Rust is spec-compliant. Locked + pin-tested (`9b20775`). Acoustically inaudible. *Survived 3/3 skeptics.*
- **SILK "drift" was a harness precision artefact, not a codec bug.** `harness-deep-plc` builds the C reference in **float** mode (xiph forbids `FIXED_POINT + DEEP_PLC`) while ropus mirrors **fixed-point** SILK; the ~24 dB SNR is the expected cross-precision floor. Against the *fixed-point* C reference, Rust SILK is **byte-exact** (16k mono VOIP CBR, {16,24,32,40,64} kbps). `84a54eb` changed only test/expectation files. *Survived 2/3; the dissent agreed no codec bug exists, only quibbling "byte-exact" scope.*
- **Branch coverage plateaued in the high-80s%, 98% not reached — but "unreachable" is too strong.** ✗ The 2026-04-17 final report: **87.58% → 89.70%** (workspace) after +459 tests; a later, narrower `-p ropus --lib` measurement on 2026-05-05 read **87.40%**. The report does **not** declare 98% impossible — it lists five product-code strategies (expose internals, remove defensive branches, enable the dnn feature, delete dead code, refactor asserts) reaching ~98.3%, explicitly **deferred** as out-of-scope for a test-additions-only campaign. *Original "remaining gap is unreachable" claim refuted 2/3 and softened here.*
- **Mutation testing: infra revived, audit never run.** ✗ "Never revived" is wrong — `ff113bc`+`863ac77` (2026-05-06) fixed the runner (path drift, baseline-skip removal, zero-mutant guards) and merged to `main`. But **no mutation audit has been executed since** (deferred as too slow; no logs, no mutation score). Net: ready-but-unexercised. *Refuted 2/3 as worded; corrected here.*

---

## Branches, worktrees, release state

- **Branches:** `main` + this task branch only. No stale feature branches. `origin/main` = `8173030`.
- **Worktrees:** main clone `C:/language/ropus` (on `main`, trunk-holder) + this report worktree. Clean.
- **Release:** v0.12.18 (tag, 2026-05-11) is the published library; `ropus/CHANGELOG.md` records the crates.io catch-up. CLI crates (`ropusenc` 0.1.3, `ropusdec` 0.1.0, `ropusinfo` 0.1.0, `ropusplay` 0.2.0), `capi` 0.2.2, `ropus-fb2k` 0.3.1, `harness` 0.0.3, `full-test` 0.5.3 are versioned independently and unpublished by design.
- **Oracles:** `wrk_docs/testing-oracle-matrix.md` (last touched 2026-05-06) defines ~14 oracle classes and asset-gated preflight lanes (core / neural-dred / corpus / performance / platform / sanitizer).

---

## Verification ledger (transparency)

| Claim | Verdict |
|-------|---------|
| Repo dormant since 2026-05-12; lone chore 2026-06-17 | **CONFIRMED** (git) |
| v0.12.18 = real crates.io catch-up publish (2026-05-11) | **CONFIRMED** (git + CHANGELOG) |
| Backlog dates = 2026-04-18/19 (not late May) | **CONFIRMED** (git) |
| feat-18/19 drift accepted reference-side artefact, pinned | **SURVIVED 3/3** |
| SILK "drift" = harness float-vs-fixed, no codec bug | **SURVIVED 2/3** (substance unanimous) |
| May fuzz campaign clean (445,581 runs, 0 crashes) | **SURVIVED 2/3** (target-count nuance noted) |
| "Bug #13 is the last open defect" | **REFUTED 3/3** — fixed 2026-04-14 (`f6b6e50`) |
| "Mutation testing never revived" | **REFUTED 2/3** — infra revived 2026-05-06, never run |
| "Remaining coverage gap is unreachable" | **REFUTED 2/3** — deferred, needs product-code changes |
| Proptest seed 2296 frame-2 energy divergence | **UNVERIFIED** — surfaced only in coverage notes; held, see #7 |

---

## Tracked issues (key items)

1. **Integration phase parked 5+ weeks.** Decide: resume integration validation, or formally declare a milestone / hand-off and update CLAUDE.md's "current focus".
2. **Coverage 98% unmet (~89.7% workspace / 87.4% ropus-lib).** Decide: pursue (needs product-code changes — expose internals / prune defensive branches) or formally accept the high-80s% and close the goal.
3. **Mutation testing revived but never run.** No mutation score exists. Either execute one scoped audit (e.g. `celt/math_ops.rs`) to get a real number, or formally park the lane.
4. **No structured `bugs/` ledger.** Consider adopting the `bugs/` ledger for durable post-1.0 triage; current journal-only tracking has no stable IDs / two-eyes closure.
5. **Coverage measurement-scope ambiguity.** 2026-04-17 (workspace, 89.70%) vs 2026-05-05 (`-p ropus --lib`, 87.40%) measure different scopes — pick one canonical command so the number is comparable across reports.
6. **feat-18/19 PitchDNN Tier-2 drift** — accepted + pinned (reference-side). *No action; monitoring.*
7. **Proptest seed 2296 frame-2 energy divergence** — flagged during coverage work; **UNVERIFIED** here. Verify whether it still reproduces on `main` and whether it's a real latent bug before it's either tracked or dismissed.
8. **DRED WAV-level reconstruction is Tier-2** (per CLAUDE.md) — confirm whether closing the remaining DRED reconstruction gap is still a goal or accepted as-is.
9. **CLI crates unpublished.** Decide whether `ropusenc/dec/info/play` should ship to crates.io alongside the library, or remain workspace-internal.
10. **fb2k component (0.3.1) shippable** — decide on distribution (it's feature-complete with live-VBR display; no open design debt).

---

## Conclusions & plan of action for today

**Conclusion.** ropus is in a healthy, shipped, quiescent state: the codec and all tooling are complete, 0.12.18 is published, the last known defect closed in April, and the May fuzz/test posture was clean. The five-week silence is the single most significant signal — the project has effectively reached the end of its build phase and is waiting on a direction decision. The remaining items are *choices about test depth and scope*, not breakage.

**Proposed plan (read-only pass — these are recommendations for Arthur, no code touched today):**

1. **Make the direction call first** (#1). Everything else hangs on "resume integration vs. milestone-close." If milestone-closing, update CLAUDE.md's "current focus" so it stops claiming an active integration phase.
2. **If resuming, the highest-value, lowest-risk first move is to verify the proptest seed 2296 divergence** (#7) — it's the only thing resembling a live latent bug, and it's currently unverified. A 30-minute repro settles whether there's real work hiding behind the dormancy.
3. **Then resolve the two deferred testing decisions** (#2, #3): run one scoped mutation audit to get an actual score (the runner is ready), and decide whether 98% coverage is worth product-code changes or should be formally accepted at ~90%.
4. **Low-effort hygiene:** pick a canonical coverage command (#5) and consider standing up the `bugs/` ledger (#4) so future daily passes have a real queue to trend against — this is the first report, and the absence of a ledger is the main gap in this pass's coverage.

**Risks / caveats.** This pass could not exercise the build/tests (read-only); all "green" statements are sourced from the last closeout journals (2026-05-08), now five weeks stale — a fresh `cargo test` / fuzz smoke would re-confirm. Item #7 is explicitly unverified. No external publish state was probed (crates.io live status is asserted from the commit/CHANGELOG, not queried).
