# Fuzz Oracle Debt Reporting Completion Review

Date: 2026.05.06
Reviewer: final Phase 2.2 completion verifier
Scope: current uncommitted diff in `/home/md/language/ropus`; no code edits performed.

## Verdict

Go.

I found no blockers and no required changes. The implementation satisfies the signed HLD and the follow-up review feedback: the report format is deterministic labeled text, not CSV-like output with ambiguous comma-bearing prose fields.

## Completion Checks

- `DecodeOracleClass` has stable external names through `stable_name()`, debt status through `is_weakened()`, asserted properties through `asserted_property()`, and weakened-class debt descriptions through `unasserted_property()`.
- The stable class order is fixed by `DECODE_ORACLE_CLASSES`, and `DecodeOracleClassCounts` uses fixed fields rather than map iteration, so rows and reports are deterministic.
- Count/report helpers are pure with respect to fuzz runtime behavior. They take supplied packet/class data and return counts or formatted strings; no global counters, randomness, filesystem reads, or campaign state are inside `oracle.rs`.
- Active fuzz targets remain quiet for this slice. The only new `println!` is inside the ignored integration test `decode_oracle_class_distribution_report`; no always-on campaign report, distribution threshold, or global counter was added.
- Tests cover metadata uniqueness/debt boundaries, count totals, deterministic report formatting including zero-count classes, decode classification boundaries, and the bounded ignored operator report.
- The ignored report is bounded to committed `fuzz_decode` seeds/crashes plus the known recovery fixture and labels itself as `committed fuzz_decode seeds, crashes, and known failures`. It is not presented as live fuzz campaign coverage.
- `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md` splits the weakened fuzz classes into `sample-count-only`, `recovery-or-dtx-only`, and `error-agreement-only`, and its asserted/unasserted wording aligns with the source metadata.
- No changed shell wrapper is present in the uncommitted diff, and no Phase 3.3 schema/frame-duration expansion work was introduced.

## Validation

Passed:

```bash
timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests
```

Result: 20 passed, 0 failed, 1 ignored. The build emitted the existing C reference optimization warning only.

Passed:

```bash
timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests -- --ignored --nocapture decode_oracle_class_distribution_report
```

Result: 1 passed. Reported bounded distribution:

```text
total: 41
strong: 33
weakened: 8
celt-coded-comparable: 19
silk-hybrid-snr-comparable: 14
sample-count-only: 6
recovery-or-dtx-only: 2
error-agreement-only: 0
```

Passed:

```bash
timeout 120 cargo fmt --check
```

Passed:

```bash
git diff --check
```

## Residual Risks

- The ignored distribution report is bounded fixture/corpus reporting, not live campaign coverage. That is aligned with the HLD, but release notes should avoid treating the percentages as fuzz exploration coverage.
- Matrix/source wording can drift in future edits because there is no generated-doc check tying `/home/md/language/ropus/tests/fuzz/fuzz_targets/oracle.rs` to `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`.
- `summarize_multistream_decode_packets` is implemented but not directly exercised by a test in this slice. It is a thin wrapper over the tested fixed count machinery and existing multistream classifier, so I do not consider this a completion blocker for Phase 2.2.
