# Code Review: Legal Frame Duration Roundtrip

Date: 2026.05.06

Scope: Phase 3.3 Slice A review of current uncommitted source changes for legal frame-duration fuzz dimensions in `fuzz_roundtrip`.

Reviewed source files:

- `/home/md/language/ropus/tests/fuzz/fuzz_targets/frame_duration.rs`
- `/home/md/language/ropus/tests/fuzz/tests/frame_duration_tests.rs`
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs`

Explicitly checked out of scope files:

- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_encode_multiframe.rs`
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_decode_plc_seq.rs`

## Findings

No required changes found.

## Residual Risks

- This review did not run a fuzz campaign; `cargo fuzz build` only proves the target compiles.
- Existing corpus inputs remain parseable when long enough, but old inputs can now select different frame durations because `data[7]` high bits are intentionally reused.
- Duration selection from `data[7] >> 5` gives selectors 0 and 1 two high-bit patterns each after modulo wrapping, so 2.5 ms and 5 ms are slightly overrepresented. This matches the HLD's accepted reuse of existing bits and is not a blocker.

## Review Notes

- `/home/md/language/ropus/tests/fuzz/fuzz_targets/frame_duration.rs:1` defines the expected legal duration table `[5, 10, 20, 40, 80, 120]` in half-milliseconds.
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/frame_duration.rs:7` maps all supported sample rates to the HLD sample counts: 2.5 ms through 60 ms.
- `/home/md/language/ropus/tests/fuzz/tests/frame_duration_tests.rs:27` verifies the full sample-rate/duration matrix from the HLD.
- `/home/md/language/ropus/tests/fuzz/tests/frame_duration_tests.rs:40` verifies selectors wrap instead of rejecting inputs.
- `/home/md/language/ropus/tests/fuzz/tests/frame_duration_tests.rs:53` verifies `fuzz_roundtrip` uses `data[7]` high bits as the selector without adding a schema byte.
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:116` lowers the initial byte-slice guard to `8 + 40`, which is the minimum legal i16 mono frame payload at 8 kHz and lets the later parsed `bytes_needed` guard enforce exact per-mode length.
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:139` through `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:146` keeps the existing DTX/loss interpretation and derives duration from the high bits as specified.
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:195`, `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:205`, and `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:210` use the selected `frame_size` for float Rust encode, Rust decode, and C encode.
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:240` through `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:304` keep parseability, packet sample-count, decoded sample-count, bit-exact CELT, and SNR oracle checks tied to the selected `frame_size`.
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:320`, `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:329`, and `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:335` use the selected `frame_size` for i16 Rust encode, Rust decode, and C encode.
- `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:360` through `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs:419` keep packet parseability, sample-count invariants, CELT equality, and SILK/Hybrid SNR oracle behavior intact while comparing against the selected `frame_size`.
- No source diff is present for `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_encode_multiframe.rs` or `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_decode_plc_seq.rs`.

## HLD Compliance

The implementation complies with `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - legal frame duration fuzz dimensions.md` for Slice A:

- Adds the small shared helper under `tests/fuzz/fuzz_targets/`.
- Adds integration tests that include the helper by path.
- Reuses `data[7]` high bits rather than adding a new byte.
- Lowers the initial minimum-length guard to `8 + 40`.
- Keeps the dynamic `bytes_needed` guard.
- Uses selected `frame_size` in encode/decode and assertions.
- Does not edit `/home/md/language/ropus/tests/fuzz/fuzz_targets/oracle.rs`.
- Does not touch Slice B targets.

## Validation

All requested validation commands passed:

```text
timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests
```

Result: passed, 4 tests passed.

```text
timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests
```

Result: passed, 20 tests passed, 1 ignored operator report.

```text
timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_roundtrip
```

Result: passed.

```text
timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check
```

Result: passed.
