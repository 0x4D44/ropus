# Code Review: Fuzz Sanity Completion

Date: 2026.05.06
Reviewer: final completion verifier
Scope: current uncommitted Phase 1.2 fuzz sanity gate diff

## Verdict

GO for commit.

I found no completion-blocking gaps in the current uncommitted diff. The prior review feedback appears applied: quick inventory and shell sanity now agree on malformed manifest handling and recursive committed crash `.bin` semantics, and crash replay now passes the fixed input before `-- -runs=0`.

## Confirmations

- `tools/fuzz_run.sh --sanity` does not start fuzz campaigns. It skips findings directory creation, builds selected targets, replays committed crash inputs, prints per-target summary rows, and exits before the main fuzzing loop.
- `tools/fuzz_run.sh --check-crashes` remains compatible as an alias for the same bounded sanity path.
- Default `full-test` does not require fuzz tooling. `full-test/src/fuzz.rs` returns `not_requested` unless `--release-preflight` is set.
- Non-quick `--release-preflight` runs the bounded shell sanity command `timeout 300 bash tools/fuzz_run.sh --sanity`; nonzero launch/build/replay/tooling outcomes become fuzz `fail` and banner `FAIL`.
- Quick `--release-preflight` is inventory-only. It does not invoke `tools/fuzz_run.sh`, `cargo fuzz`, or `cargo +nightly fuzz`; tool absence is report WARN when inventory is otherwise clean.
- Crash replay command ordering is fixed in `tools/fuzz_run.sh`: `cargo +nightly fuzz run --fuzz-dir tests/fuzz <target> <crash_file> -- -runs=0`.
- Quick inventory and shell sanity now agree on the reviewed manifest/crash semantics: malformed `[[bin]]` headers fail, `name_suffix` is not accepted as a target name, undeclared committed `.bin` crash files fail, and recursive `.bin` files are counted.
- Required docs/journals are present for HLD, review, diagnosis, feedback/status context, and implementation history.

## Validation Results

All requested validations passed, run in order:

```text
PASS timeout 10 bash -n tools/fuzz_run.sh
PASS timeout 10 bash -n tools/fuzz_targets.sh
PASS timeout 10 bash tools/fuzz_run.sh --list
PASS timeout 120 cargo test -p full-test --locked
PASS timeout 300 bash tools/fuzz_run.sh --sanity --target fuzz_decode
PASS timeout 300 cargo run --release -p full-test -- --quick --release-preflight --skip-coverage
PASS timeout 120 cargo fmt --check
PASS git diff --check
```

Key observed outputs:

- `tools/fuzz_run.sh --list` listed 12 manifest targets; `fuzz_decode` reported 4 committed crash files.
- `cargo test -p full-test --locked` passed 167 tests.
- `tools/fuzz_run.sh --sanity --target fuzz_decode` built `fuzz_decode`, replayed all 4 crash files, and ended with `RESULT: All crash regressions pass.`
- Quick release preflight completed without cargo-fuzz output and reported `PASS 2271 passed, 0 failed, 6 ignored`.

## Residual Risks

- I did not run non-quick `cargo run --release -p full-test -- --release-preflight --skip-coverage` because it was not in Arthur's requested validation sequence. The non-quick policy was verified by code inspection and unit coverage, plus direct shell sanity replay for `fuzz_decode`.
- The quick inventory parser intentionally mirrors the shell parser locally rather than invoking `tools/fuzz_targets.sh`; current tests cover the prior mismatch cases, but future parser changes must keep both sides aligned.
- The quick release-preflight run generated a normal HTML report under `tests/results/`; that output path appears outside the uncommitted git diff.
