# Code Review - Mutation Testing Revival HLD Signoff

Date: 2026.05.06

Scope: supervisor review of `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - mutation testing revival.md`.

## Decision

Signed off for implementation.

Mutation testing remains the final testing-infrastructure item from the
original plan. The implementation should revive it as a periodic audit lane,
not as a release gate.

## Required Implementation Contract

Implementation is approved for:

- fixing stale `src/...` assumptions in `tools/mutation_test.sh`,
- using `--package ropus` and `ropus/src/...` as the authoritative package/path
  convention,
- adding a zero-mutant guard so stale or misresolved files are non-green,
- making baseline policy explicit,
- removing hidden baseline skips from `.cargo/mutants.toml` or moving them
  behind an explicit script mode,
- producing a clear audit summary under `logs/mutation-testing/<timestamp>/`,
- running a narrow first validation/audit target if feasible.

The first scoped target is:

```text
ropus/src/celt/math_ops.rs
```

## Policy

- Mutation testing is periodic/on-demand only.
- Mutation testing is non-release-blocking.
- The first revived lane is unit-test-only.
- Reports must state that the C-reference differential harness is not inside
  the mutation loop.
- No kill-rate threshold is introduced in this slice.
- Survivor triage quality matters more than a headline score.

## Scope Boundaries

Allowed files:

- `tools/mutation_test.sh`
- `.cargo/mutants.toml`
- `wrk_docs/testing-oracle-matrix.md` only if a reproducible audit report exists
- implementation/review/journal artifacts

Forbidden in this slice:

- codec implementation changes,
- adding tests to kill mutants,
- full-workspace mutation campaign,
- CI provider config,
- release-preflight or `full-test` integration,
- custom C-reference mutation wrapper,
- performance/platform/corpus/DRED/PLC work.

## Validation Expectations

Focused validation should include:

- `timeout 30 bash -n tools/mutation_test.sh`
- `timeout 30 cargo mutants --version`
- `timeout 60 bash tools/mutation_test.sh --list`
- `timeout 60 bash tools/mutation_test.sh --file celt/math_ops --list`
- `timeout 120 cargo test -p ropus --lib`
- `timeout 30 git diff --check -- .cargo/mutants.toml tools/mutation_test.sh`

Run the scoped mutation audit only if feasible in a bounded timeout. If it is
too slow, document the blocker and commit the revived lane only after the cheap
path and baseline behavior are validated.

## Residual Risk

This revives mutation testing as an audit surface. It does not prove C-reference
parity, and it does not close survivors by itself.
