# Code Review: Quick Smoke Conformance Skip

Date: 2026.05.06

Scope reviewed: current uncommitted source diff in:

- `/home/md/language/ropus/full-test/src/main.rs`
- `/home/md/language/ropus/full-test/src/tests.rs`
- `/home/md/language/ropus/full-test/src/cli.rs`

Context: implementation of signed HLD `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - quick smoke conformance skip.md`.

## Findings

No blocking source-code defects found in the uncommitted patch.

Issue: acceptance remains incomplete for the broader smoke command.

- Evidence: implementer validation says `/home/md/language/ropus/full-test` printed the intended Stage 2 command with `--skip extensions`, but the full smoke command still timed out:
  `cargo run -p full-test -- --release-preflight --quick --skip-coverage --skip-benchmarks --skip-ambisonics`.
- Relevant implementation: `/home/md/language/ropus/full-test/src/main.rs:260` derives the policy from `quick && release_preflight`, and `/home/md/language/ropus/full-test/src/tests.rs:125` appends `--skip extensions`.
- Assessment: the patch appears correct as a partial fix for the known slow `extensions` conformance test, but it does not yet prove the HLD's end-to-end goal of a bounded quick smoke command. The timeout now likely indicates additional slow Stage 2 work, or that the timeout budget is too small for the remaining workspace tests. That should be tracked before calling the quick smoke path fully fixed.

## HLD Match

The source diff matches the HLD's mandatory implementation points:

- `/home/md/language/ropus/full-test/src/main.rs:260` keeps the skip internal and true only for `--quick --release-preflight`.
- `/home/md/language/ropus/full-test/src/tests.rs:89` extends `build_cargo_args(...)` with `skip_slow_conformance`.
- `/home/md/language/ropus/full-test/src/tests.rs:125` appends the libtest tail `--skip extensions` when active.
- `/home/md/language/ropus/full-test/src/tests.rs:135` renders the Stage 2 header with the active skip filters.
- `/home/md/language/ropus/full-test/src/cli.rs:63` updates help text without adding a new parser flag.
- `/home/md/language/ropus/full-test/src/tests.rs:448`, `/home/md/language/ropus/full-test/src/tests.rs:456`, and `/home/md/language/ropus/full-test/src/tests.rs:472` cover the required command-shape cases.

The optional `tests.skip_reason` report note for the slow conformance filter was not added. That is acceptable because the HLD made it optional and required command-shaped visibility instead.

## Validation Notes

I did not edit source files.

I ran a targeted check of the actual libtest filter behavior:

```text
timeout 60 cargo test -p conformance --test extensions -- --test-threads=1 --skip extensions
```

Result: passed immediately with `running 0 tests` and `1 filtered out`. This confirms the specific `--skip extensions` mechanism avoids executing `/home/md/language/ropus/tests/conformance/tests/extensions.rs:57`.

I did not rerun the full smoke command because the known result is a timeout and the review scope is source diff inspection. The timeout should be treated as remaining release-smoke work, not evidence that this patch failed to filter the extension stress test.

## Residual Risk

`libtest --skip extensions` remains a global substring filter. That risk is explicitly accepted by the HLD for the `--quick --release-preflight` path only, and the implementation keeps it constrained to that path.
