# Code Review - Platform/Sanitizer Implementation

Date: 2026.05.06

Scope: independent review of Dirac's uncommitted platform/sanitizer breadth implementation.

## Findings

No `Blocker` or `Issue` findings.

`Optional`: `/home/md/language/ropus/full-test/src/html.rs:710` renders command vectors with a plain `" "` join. The new generic-smoke command contains one argv item with a spaced env value from `/home/md/language/ropus/full-test/src/platform.rs:420` (`RUSTFLAGS=-C target-cpu=x86-64`). The implementation command vector is correct for `std::process::Command`, and JSON preserves it as a vector, so this is not a release-gate correctness issue. A future polish pass could shell-quote HTML command display to make copy/paste reproduction less surprising.

## Contract Review

The implementation matches the signed HLD/signoff scope.

- Default and quick release-preflight profiles do not claim platform or sanitizer breadth: `/home/md/language/ropus/full-test/src/platform.rs:37`, `/home/md/language/ropus/full-test/src/platform.rs:301`, `/home/md/language/ropus/full-test/src/platform.rs:362`.
- Non-quick `--release-preflight` selects `ReleaseBreadth`: `/home/md/language/ropus/full-test/src/platform.rs:37`.
- On `x86_64`, the generic lane is claimed and release-blocking, and a command failure or launch error becomes `fail`/`unsupported`: `/home/md/language/ropus/full-test/src/platform.rs:309`, `/home/md/language/ropus/full-test/src/platform.rs:317`, `/home/md/language/ropus/full-test/src/platform.rs:328`, `/home/md/language/ropus/full-test/src/platform.rs:348`.
- Off `x86_64`, the generic lane reports `not_applicable` and is not marked as a pass: `/home/md/language/ropus/full-test/src/platform.rs:309`.
- Sanitizer breadth is satisfied only by existing fuzz `FullSanity` passing, and quick inventory cannot satisfy it: `/home/md/language/ropus/full-test/src/platform.rs:371`, `/home/md/language/ropus/full-test/src/platform.rs:398`.
- Banner failure includes claimed platform/sanitizer failures: `/home/md/language/ropus/full-test/src/banner.rs:92`, `/home/md/language/ropus/full-test/src/banner.rs:112`.
- `main` wires the platform outcome into banner, HTML, and JSON reporting: `/home/md/language/ropus/full-test/src/main.rs:82`, `/home/md/language/ropus/full-test/src/main.rs:87`, `/home/md/language/ropus/full-test/src/main.rs:140`, `/home/md/language/ropus/full-test/src/main.rs:155`, `/home/md/language/ropus/full-test/src/main.rs:196`.
- JSON exposes `platform_breadth`: `/home/md/language/ropus/full-test/src/report.rs:20`, `/home/md/language/ropus/full-test/src/report.rs:41`.
- HTML exposes claim state, lane state, command, note, and release-blocking status: `/home/md/language/ropus/full-test/src/html.rs:690`.
- CLI/preflight wording is honest about quick no-claim and non-quick breadth claims: `/home/md/language/ropus/full-test/src/cli.rs:64`, `/home/md/language/ropus/full-test/src/cli.rs:69`, `/home/md/language/ropus/full-test/src/preflight.rs:61`.
- The oracle matrix scopes the platform and sanitizer claims without implying broad OS, full-workspace sanitizer, or quick-inventory coverage: `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md:23`, `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md:50`.

The generic-profile proof is documented sufficiently for this slice. `/home/md/language/ropus/wrk_docs/2026.05.06 - MEA - platform sanitizer smoke.md` records a `cargo rustc --print cfg` comparison showing default `x86-64-v3` features versus generic `fxsr/sse/sse2`, and records a passing generic `cargo test -p ropus --lib` smoke.

No forbidden scope observed: no codec implementation, `.cargo/config.toml`, CI/provider, full-workspace sanitizer, mutation, performance, DRED/PLC, or corpus implementation changes were included in the reviewed target diff. No versioned files were modified in the reviewed target set.

## Validation

Passed:

```text
timeout 120 cargo test -p full-test platform
timeout 120 cargo test -p full-test banner
timeout 120 cargo test -p full-test report
timeout 120 cargo test -p full-test html
timeout 120 cargo test -p full-test cli::tests
timeout 120 cargo test -p full-test preflight
timeout 120 cargo fmt --check
timeout 30 git diff --check
```

## Go/No-Go

Go for committing the platform/sanitizer breadth slice.

Feedback application is not required. The optional HTML command quoting polish can be left for a later report-usability pass.
