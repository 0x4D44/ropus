# Code Review - Platform/Sanitizer Breadth HLD Signoff

Date: 2026.05.06

Scope: supervisor review of `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - platform sanitizer breadth.md`.

## Decision

Signed off for implementation with verification constraints.

The next slice should add a narrow release-breadth signal: one generic x86_64
core smoke outside the repository's default `x86-64-v3` assumption, plus an
explicit sanitizer claim derived from the existing cargo-fuzz ASan sanity path.

## Required Implementation Contract

Implementation is approved for:

- a platform/breadth stage in `full-test`,
- explicit claim states for generic x86_64 and sanitizer breadth,
- non-quick `--release-preflight` requiring generic x86_64 smoke on x86_64
  hosts,
- quick `--release-preflight` making no platform or sanitizer claim,
- sanitizer claim reporting tied to the existing non-quick fuzz full-sanity
  path,
- JSON/HTML/banner/help/oracle-matrix updates,
- focused unit tests with synthetic outcomes.

Required profile behavior:

- default full-test: platform/sanitizer not claimed;
- quick release-preflight: platform/sanitizer not claimed;
- non-quick release-preflight on x86_64: generic x86_64 smoke is claimed and
  release-blocking;
- non-quick release-preflight off x86_64: generic x86_64 lane is
  `not_applicable`, not pass;
- non-quick release-preflight sanitizer claim is satisfied only when the
  existing fuzz full-sanity lane ran and passed.

## Verification Constraint

The implementation must not assume that `RUSTFLAGS="-C target-cpu=x86-64"`
overrides `.cargo/config.toml`. It must either:

- use a command form that demonstrably avoids the repo `x86-64-v3` rustflags,
  or
- run and document a bounded local smoke showing the generic command is accepted
  and represents the intended profile.

If exact rustc flag precedence cannot be proven in this slice, the generic
platform lane must report the uncertainty honestly instead of claiming a pass.

## Scope Boundaries

Allowed files:

- `full-test/src/*` as needed for a platform/breadth stage,
- `wrk_docs/testing-oracle-matrix.md`,
- HLD/review/journal artifacts.

Avoid changing `.cargo/config.toml`; the goal is to test outside the optimized
default, not remove it.

Forbidden in this slice:

- codec implementation changes,
- full CI/provider configuration,
- broad OS/platform matrix,
- full-workspace sanitizer,
- mutation testing work,
- performance optimization or threshold recalibration,
- DRED/PLC/corpus debt work.

## Validation Expectations

Focused validation:

- `timeout 120 cargo test -p full-test platform` or equivalent module filter,
- `timeout 120 cargo test -p full-test breadth` if that is the module name,
- `timeout 120 cargo test -p full-test banner`,
- `timeout 120 cargo test -p full-test report`,
- `timeout 120 cargo test -p full-test html`,
- `timeout 120 cargo test -p full-test cli::tests`,
- `timeout 120 cargo fmt --check`,
- `timeout 30 git diff --check`.

Run the generic x86_64 smoke if feasible. For sanitizer, do not add a new broad
sanitizer command; reuse and report the existing fuzz full-sanity result.

## Residual Risk

This slice adds release-breadth smoke. It does not prove Windows, macOS, ARM,
WebAssembly, full-workspace sanitizer safety, or all C ABI/harness paths.
