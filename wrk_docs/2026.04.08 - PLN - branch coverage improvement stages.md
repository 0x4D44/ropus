# Branch Coverage Improvement Stages

Date: 2026-04-08

Current measured state:

- Lines: `95.92%`
- Functions: `98.15%`
- Regions: `96.19%`
- Branches: `80.93%`

Target:

- reach `98%` coverage with branch-aware measurement tracked for lines, functions, regions, and branches

Primary measurement command:

```powershell
mdtimeout 5400 cargo +nightly llvm-cov --workspace --all-features --branch --summary-only
```

Supporting detail command:

```powershell
mdtimeout 1800 cargo +nightly llvm-cov report --branch --text --show-missing-lines --output-dir target/llvm-cov/current-text
```

Measurement note:

- the JSON summary export became unreliable in this workspace; the direct text summary is now the trusted metric source

## Stage 0: Stable Runner

Goal:

- keep branch-aware coverage repeatable and fast enough to iterate

Actions:

- always use `cargo +nightly llvm-cov --branch`
- trust `--summary-only` text output for totals
- use the text missing-lines report for hotspot inspection
- treat any new test failure as a blocker and fix it before chasing more coverage

Exit criteria:

- coverage command runs cleanly without manual cleanup

## Stage 1: Deterministic Gap Recovery

Status:

- completed

Files already pushed:

- `src\dnn\lpcnet.rs`
- `src\opus\multistream.rs`
- `src\opus\decoder.rs`
- `src\opus\repacketizer.rs`

Measured gain from baseline:

- lines `+1.79`
- functions `+3.96`
- regions `+1.57`
- branches `+1.07`

## Stage 2: Encoder Control Paths

Status:

- in progress

Files already pushed:

- `src\opus\encoder.rs`
- `src\silk\encoder.rs`

Measured gain from the encoder-focused waves:

- lines `+4.47`
- functions `+5.39`
- regions `+4.10`
- branches `+4.36`

What still remains here:

- `silk::encoder` stereo LR/MS, LBRR, budget-damage-control, and deeper delayed-decision paths
- `opus::encoder` redundancy placement, transition prefills, and remaining CELT/SILK mode-budget edges

## Stage 3: Residual Decoders And Packet Paths

Primary files:

- `src\celt\bands.rs`
- `src\silk\decoder.rs`
- `src\opus\repacketizer.rs`
- `src\opus\decoder.rs`
- `src\celt\decoder.rs`

Why:

- they still expose meaningful deterministic branch debt
- they are cheaper to attack than the deepest encoder state machines
- they remain good follow-up work once an encoder wave stalls

## Stage 4: CELT Branch Cleanup

Primary files:

- `src\celt\encoder.rs`
- `src\celt\math_ops.rs`

Why:

- they are no longer background noise; the branch miss counts are now large enough to matter
- they are still safer than forcing more intrusive scaffolding into the biggest Opus/SILK state machines

## Stage 5: Fresh Rerank After Every Wave

Goal:

- stop following stale assumptions and retarget from current coverage data

Actions after every meaningful test wave:

1. rerun `cargo +nightly llvm-cov --workspace --all-features --branch --summary-only`
2. refresh the top missed-branch files from the latest report
3. only attack files that still dominate the totals

Decision rule:

- continue a file when new tests buy material branch movement
- stop when the next point would require brittle or behavior-changing scaffolding

## Next Execution Order

Start here:

1. `src\silk\encoder.rs`
2. `src\opus\encoder.rs`
3. `src\celt\bands.rs`
4. `src\opus\repacketizer.rs`
5. `src\silk\decoder.rs`
6. `src\celt\encoder.rs`

Execution style:

- one agent per file or file-pair with disjoint edit scopes
- add tests inside existing `#[cfg(test)]` modules
- run focused file/module tests first
- rerun full branch-aware coverage after each committed wave

## Stop Condition Review

The stated target is `98%`, but the repo should not fake progress.

Current reality:

- functions are over the line already
- the real problem is branches: `975` more covered branches are still required to reach `98%`
- that remaining branch debt lives mostly in large encoder state machines, not forgotten helper wrappers

Working rule:

- keep pushing while the next wave is still deterministic and high-signal
- surface the target as unrealistic the moment progress depends on broad, brittle scaffolding rather than real behavior coverage
