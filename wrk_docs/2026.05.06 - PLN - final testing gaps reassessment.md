# Final Testing Gaps Reassessment

Date: 2026.05.06

Scope: documentation-only reassessment after commit
`e25a0fe Add platform sanitizer release breadth gate`.

No source code was implemented, no tests were changed, and no commit was made.

## Evidence Read

- `/home/md/language/ropus/wrk_docs/2026.05.05 - testing gaps review.md`
- `/home/md/language/ropus/wrk_docs/2026.05.05 - PLN - testing improvement plan.md`
- `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`
- Recent 2026.05.06 HLDs, reviews, measurement notes, plans, and journals
  through the platform/sanitizer release breadth gate.
- Recent commit history through `e25a0fe`.
- Remaining mutation-testing candidates:
  - `/home/md/language/ropus/.cargo/mutants.toml`
  - `/home/md/language/ropus/tools/mutation_test.sh`
  - `/home/md/language/ropus/ropus/src/**`

Read-only tool evidence:

- `HEAD` is `e25a0fe Add platform sanitizer release breadth gate`.
- `cargo-mutants 27.0.0` is installed.
- `/home/md/language/ropus/tools/mutation_test.sh` still targets top-level
  `src/...` paths.
- There is no top-level `/home/md/language/ropus/src`; the relevant crate
  sources live under `/home/md/language/ropus/ropus/src`.
- `/home/md/language/ropus/.cargo/mutants.toml` still skips known baseline
  failing tests.

## Bottom Line

The release-confidence infrastructure campaign is complete in the practical
sense: the original false-green risks are now either release-blocking,
WARN-only by explicit policy, or named debt in the oracle matrix.

The original plan is not literally 100% complete if every phase item is counted,
because Phase 4.3, mutation testing revival, remains undone. That remaining
item is testing infrastructure, but it is a periodic test-quality audit lane,
not a release gate and not a blocker for saying the release-preflight contract is
now honest.

Recommendation: run one final HLD-first slice for mutation testing revival only
if Arthur wants the original plan closed literally. Do not keep extending the
release gate campaign into codec repair, broad corpus curation, performance
optimization, or full platform matrices.

## Completed Infrastructure Areas

| Area | Status |
|---|---|
| Harness compare integrity | Completed. Byte/sample comparisons are length-aware and headline `encode`, `decode`, and `roundtrip` commands fail automation on real mismatches. |
| Core release preflight | Completed. Core reference, conformance, IETF, and fuzz-manifest prerequisites are explicit; missing required core assets are no longer silent false greens. |
| Quick release smoke profile | Completed. Quick release preflight has a bounded core-smoke profile and explicitly does not claim neural/DRED, corpus, performance, platform, or sanitizer breadth. |
| Fuzz target discovery | Completed. Fuzz target lists are manifest-derived rather than duplicated stale arrays. |
| Fuzz sanity gate | Completed. The standard gate can build active fuzz targets and replay committed crash corpora. |
| Fuzz oracle debt reporting | Completed. Weakened fuzz oracle classes have stable names and matrix rows, so "no fuzz findings" is interpretable by oracle strength. |
| Oracle matrix | Completed and actively maintained. The matrix distinguishes exact byte parity, exact PCM parity, bounded drift, SNR-gated parity, sample-count-only, recovery-only, error-agreement-only, smoke-only, ignored, asset-skipped, performance, platform, and sanitizer lanes. |
| Legal frame-duration fuzzing | Completed for the planned targets. `fuzz_roundtrip`, `fuzz_encode_multiframe`, and `fuzz_decode_plc_seq` now exercise legal 2.5/5/10/20/40/60 ms frame sizes. |
| PLC/recovery drift visibility | Completed as infrastructure. Decode24 PLC has an active exact-prefix plus bounded-drift oracle; full-frame exact parity remains visible debt. |
| DRED/DNN release claim policy | Completed as infrastructure. Non-quick release preflight requires neural/DRED assets before claiming those gates; quick/default do not overclaim. |
| Real-world/generated corpus gate | Completed as infrastructure. Non-quick release preflight has a manifest-backed generated non-reference Ogg family-0 corpus claim with non-green handling for missing/skipped-only/zero-audio coverage. |
| Performance release thresholds | Completed as infrastructure. Non-quick release preflight has a thresholded benchmark smoke claim with release-blocking build, parser, missing row, crash, missing-threshold, skipped-benchmark, and severe-ratio failures. |
| Platform/sanitizer release breadth | Completed as infrastructure. Non-quick release preflight claims generic x86_64 smoke on x86_64 hosts and ASan-backed fuzz full-sanity as the sanitizer lane; default and quick make no breadth claim. |

## Remaining Testing-Infrastructure Gaps

| Gap | Why it remains | Concrete next scope |
|---|---|---|
| Mutation testing revival | This is the only original-plan infrastructure item still open. The runner references stale `src/...` paths, while current sources are under `ropus/src/...`; the cargo-mutants config still hides baseline failures with global skips; previous reports warn that the C reference oracle is outside the cargo-mutants loop. | HLD-first periodic audit slice. Correct path handling, make baseline skips visible instead of hidden, run one scoped mutation audit on a high-value module, and document whether the audit is unit-test-only or includes a harness/differential test tool. |
| HTML command quoting polish | The platform review found that HTML renders command vectors with simple space joins, which is awkward for `RUSTFLAGS=-C target-cpu=x86-64`. JSON and actual command execution are correct. | Optional report-usability cleanup. Not a correctness or release-gate blocker. |

No other remaining item looks like testing-infrastructure gate plumbing. The
remaining high-risk items below are now visible because the new infrastructure
labels them correctly.

## Exposed But Out-Of-Scope Debts

| Debt | Current infrastructure status | Why it is out of scope for this campaign |
|---|---|---|
| DRED payload-byte parity | Explicit `ignored` exact-byte-parity debt in the oracle matrix. Non-quick release preflight does not claim it. | Requires LPCNet/FARGAN/DRED parity work, not more gate plumbing. |
| DRED PCM reconstruction | Explicitly pinned as `OPUS_UNIMPLEMENTED`; no release claim is made. | Codec feature implementation and oracle design remain required. |
| PLC full-frame exact decode24 parity | Bounded drift oracle is active and documented; full exact parity remains debt. | Requires codec drift investigation. The testing gap is no longer hidden. |
| Fuzz weakened oracle classes | `sample-count-only`, `recovery-or-dtx-only`, and `error-agreement-only` are named and reported. | Strengthening these means solving hard recovery/malformed decode oracle questions or codec behavior, not infrastructure. |
| Real-world corpus breadth | The current gate is a generated FFmpeg-native Ogg family-0 smoke and says what it does not cover. | Adding WebM, seek, pre-skip, gain, granule, messaging voice notes, or third-party media is asset/provenance and container semantics work. |
| Performance regression evidence | The performance gate honestly records that the `48k_sine1k_loud.wav` decode smoke measured `8.74x` against a `1.26x` threshold. | Needs calibrated measurement or optimization. The gate is doing its job by exposing it. |
| Full platform matrix and broad sanitizer coverage | The platform gate is intentionally one generic x86_64 smoke plus cargo-fuzz ASan sanity. | Windows/macOS/ARM/WebAssembly, full-workspace ASan, Miri, TSAN, and MSAN are breadth expansions, not required to close the original false-green campaign. |

## Recommendation

If "100% complete" means the release-preflight/testing-confidence contract from
the 2026.05.05 success criteria, stop this campaign. The infrastructure now
makes green, warn, fail, skipped, unsupported, and debt states explicit enough
to avoid the false-confidence problem the review identified.

If "100% complete" means every phase item in the original plan, do one final
HLD-first slice:

```text
Mutation testing revival as a periodic audit lane
```

Recommended scope:

1. Do not add mutation testing to the default or release-preflight fast path.
2. Update the runner design from stale top-level `src/...` assumptions to the
   current crate layout under `/home/md/language/ropus/ropus/src/...`.
3. Replace hidden global baseline skips with explicit baseline status in the
   mutation report.
4. Decide in the HLD whether mutation testing remains unit-test-only or uses
   cargo-mutants `--test-tool` or an equivalent wrapper to include a small
   C-reference differential oracle.
5. Run one scoped audit module only, such as
   `/home/md/language/ropus/ropus/src/silk/common.rs`, and record the mutation
   score plus surviving-mutant triage.
6. Add a maintained mutation audit row to
   `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md` only if the
   result is reproducible enough to be interpreted later.

Do not mix that slice with codec parity fixes, performance investigation,
corpus curation, sanitizer expansion, or CI provider work.
