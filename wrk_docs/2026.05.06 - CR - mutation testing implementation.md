# Code Review - Mutation Testing Revival Implementation

Date: 2026.05.06

Scope: independent review of the uncommitted mutation testing revival
implementation and fix-finish pass, limited to:

- `/home/md/language/ropus/tools/mutation_test.sh`
- `/home/md/language/ropus/.cargo/mutants.toml`
- associated mutation testing journals

## Findings

No `Blocker` or `Issue` findings.

`Optional`: `/home/md/language/ropus/tools/mutation_test.sh:396` uses
`--baseline skip` for the cargo-mutants run after the script has already run
its own strict baseline at `/home/md/language/ropus/tools/mutation_test.sh:352`.
That matches the revived-lane design, but cargo-mutants warns that
`--baseline=skip` is better paired with an explicit timeout rather than only a
timeout multiplier. This is not a commit blocker for the current slice because
the HLD allowed audit deferral, the runner is expected to be wrapped with
`timeout`, and no completed mutation score is being claimed. Before a long
unattended audit, add an explicit cargo-mutants timeout option or document the
external timeout requirement in the command examples.

## Scope Match

The implementation matches the signed HLD/signoff scope.

- Current default modules use `ropus/src/...`, not stale top-level `src/...`:
  `/home/md/language/ropus/tools/mutation_test.sh:19`.
- Short path, stale `src/...`, current `ropus/src/...`, and absolute workspace
  path inputs are normalized through the `ropus/src` prefix:
  `/home/md/language/ropus/tools/mutation_test.sh:69`.
- cargo-mutants inventory and run commands are scoped to `--package ropus`:
  `/home/md/language/ropus/tools/mutation_test.sh:119`,
  `/home/md/language/ropus/tools/mutation_test.sh:325`,
  `/home/md/language/ropus/tools/mutation_test.sh:392`.
- Zero-mutant selected files are non-green in both list mode and run inventory:
  `/home/md/language/ropus/tools/mutation_test.sh:180`,
  `/home/md/language/ropus/tools/mutation_test.sh:340`.
- Hidden baseline skips were removed from
  `/home/md/language/ropus/.cargo/mutants.toml:1`, and skip-degraded behavior
  is explicit via `--skip-test`: `/home/md/language/ropus/tools/mutation_test.sh:230`.
- The runner and generated summary text state that the lane is unit-test-only
  and the C-reference differential harness is not in the loop:
  `/home/md/language/ropus/tools/mutation_test.sh:5`,
  `/home/md/language/ropus/tools/mutation_test.sh:54`,
  `/home/md/language/ropus/tools/mutation_test.sh:166`,
  `/home/md/language/ropus/tools/mutation_test.sh:291`.

The implementation did not modify codec code, release-preflight/full-test code,
CI provider config, or C-reference harness behavior.

## Audit Deferral

Deferring the full `celt/math_ops` mutation audit is acceptable for this slice.
The HLD signoff explicitly allowed bounded audit deferral when mutation
execution proved too slow, provided the cheap path and strict baseline were
validated. That condition is met: list/inventory paths resolve real mutants,
the strict `ropus` library baseline passes, and the journals record the runtime
blocker and lack of a published mutation score.

No oracle matrix update is required yet because there is no completed
reproducible audit report to record.

## Validation

Passed:

```text
timeout 30 bash -n tools/mutation_test.sh
timeout 30 cargo mutants --version
timeout 60 bash tools/mutation_test.sh --list
timeout 60 bash tools/mutation_test.sh --file celt/math_ops --list
timeout 60 bash tools/mutation_test.sh --file src/celt/math_ops --list
timeout 60 bash tools/mutation_test.sh --file ropus/src/celt/math_ops.rs --list
timeout 120 cargo test -p ropus --lib
timeout 30 git diff --check -- .cargo/mutants.toml tools/mutation_test.sh
```

Observed key results:

```text
cargo-mutants 27.0.0
ropus/src/celt/math_ops.rs: 230 mutants
strict library baseline: 1866 passed; 0 failed
```

Also checked that the old hidden skip filters are absent from the reviewed
config/script:

```text
rg -n "additional_cargo_test_args|test_compute_frame_features|test_plc_load_model|test_encode_multiframe_silk_special" .cargo/mutants.toml tools/mutation_test.sh
```

No matches.

## Verdict

Go for commit after including the requested review/journal artifacts.
Feedback application is not required for this slice. The optional timeout polish
can wait until the first deliberately scheduled long mutation audit.
