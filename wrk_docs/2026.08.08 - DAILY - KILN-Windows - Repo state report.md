# 2026.08.08 — DAILY — KILN / Windows — Repo state report

**Pass:** scheduled read-only daily observation (sixth in the series, first from KILN).
**Host / env:** KILN / Windows.
**Worktree:** `D:/worktrees/ropus/20260808-OPS-CLA@KILN-daily-report-020036` on branch
`task/20260808-OPS-CLA@KILN-daily-report-020036`.
**Repo state:** clean tree; `HEAD` = `origin/main` = **`9546504`**, dated **2026-08-02 10:15:47 +0100**.
Crate version **0.12.18** (unchanged since 2026-05-11).
**Prior reports trended against:** `2026.08.01 — CRUCIBLE` (landed as `0d82e07`), and the
**2026.08.02 — CRUCIBLE report, which never reached `origin/main`** — it is stranded on
`origin/task/20260802-OPS-CLA@CRUCIBLE-daily-report-021843` (`88b647b`). Its numbers are used
here as a mid-drain data point.
**Method:** four fresh-context readers covered the bug ledger, the commit delta, the design
backlog and doc rot, and a line-by-line audit of the prior report's 16 tracked issues and 8 plan
steps. Two load-bearing conclusions went to adversarial skeptics; **both were refuted or
materially narrowed**, and the corrected forms are published below — see §Verification ledger.
Every count, sha, timestamp and file fact was re-read at source by the lead. All readers
returned; nothing here is unverified through rate-limiting.

---

## Headline

**The bug ledger went to zero, and then the repo went silent for six days.**

66 records, **all Closed**. Zero Open, zero Fixed, zero Blocked. That is the largest single
movement in this repo's tracked history: 204 commits, 239 files, +14,156/−2,514, all landed
across 2026-08-01 and 2026-08-02. Nothing has landed since.

Two things stop that from being the good-news story it looks like, and both survived
adversarial checking:

**1. Zero Open is not zero defects.** Nine confirmed defects are known and unfiled right now.
Six of them are from a code review that ran *on 2026-08-02* and could not file anything because
`deltic bugs new` failed closed on an inaccessible clone-wide lock
(`wrk_docs/2026.08.02 - CR - 20260802-REV-ROP-CLA@CRUCIBLE-code-review-061944.md:22-27`). Three
more were carried unfiled from the 2026-08-01 report. **The ledger reads clean because filing
broke, not because the findings went away.**

**2. The drain's evidence is strong for the last third and thin for the first two-thirds.** I
drafted "all 66 are genuinely closed on recorded evidence" and a skeptic refuted it on counts I
then verified myself. 24 closures carry an explicit fails-before/passes-after re-derivation. The
41 in the 2026-08-01 wave do not — they are passes-after-only, in templated three-bullet notes
that reuse *shared whole-suite pass counts* across many bugs, closed in a 25-minute window
(`69a312c` 22:13:59 → `b4ef399` 22:39:14) that cannot fit the per-bug gate runs the commit
message claims. And **63 of 66 files still declare `## Fix` = `<unfixed — raised only>`.**

The binding constraint has moved again. It is no longer triage, and it is no longer the queue.
**It is that nobody has decided what happens next** — and the machinery that was supposed to
notice that (the daily report lane) has itself been failing.

---

## Direction of travel — cadence

```
2026-03:    30
2026-04:   412     ◄ port + integration peak
2026-05:   184     ◄ 0.12.18 published 05-11
2026-06:     3
2026-07:    39     ◄ review campaign; ledger created 07-30
2026-08:   204     ◄ ALL on 08-01 (155) and 08-02 (49); nothing since
```

**The repo has been idle for ~6 days.** The 2026-08-01 report's claim that "codec logic has been
static for 81 days" is now **retired**: `git diff --stat v0.12.18..HEAD -- ropus/src/` is
**+442/−64 across 10 files**, including `opus/decoder.rs`, `opus/extensions.rs`,
`opus/multistream.rs`, `opus/repacketizer.rs`, `silk/encoder.rs`, `celt/mdct.rs` and
`celt/fft.rs`.

### Where the 204 commits went

| Kind | Count |
|---|---:|
| `claim(ID): fix owner` | 65 |
| `clear(ID): stale fix owner` | 2 |
| Real fix / code commits | 66 |
| `fixed(ID): manual land` (ledger flips, no code) | 63 |
| `close(bugs): two-eyes verify …` | 7 |
| `review: autonomous code-review pass` | 1 |

**63 distinct bug IDs** completed claim → fix → land. Splitting the diff: ledger/docs churn is
132 files / +2,981; **real code is 107 files / +11,175 / −2,366.**

| Area | files | +/− |
|---|---:|---|
| `ropus-tools-core/` + the four CLI binaries | 32 | +4,995 / −1,129 |
| `harness/`, `harness-deep-plc/`, `harness-control/` | 32 | +3,372 / −804 |
| `ropus-fb2k/` | 5 | +914 / −113 |
| `capi/` | 11 | +696 / −139 |
| `full-test/` | 14 | +688 / −120 |
| **`ropus/src/` (published crate)** | **10** | **+440 / −61** |

Net **+166 `#[test]` functions** and three new test files
(`capi/tests/ms_decoder_ctl.rs`, `ropus-tools-core/tests/build_provenance.rs`,
`harness-deep-plc/tests/support/finite_oracle.rs`). The single largest change is `552461a` —
the `capi/` allocation-failure rework (+566/−134, new `capi/src/alloc.rs`), which threads a real
`OPUS_ALLOC_FAIL` through every C ABI constructor and unwinds partially-built multistream
handles instead of leaking them.

---

## The bug ledger

| State | 07-31 | 08-01 | 08-02 (mid) | **Today** | Δ vs 08-01 |
|---|---:|---:|---:|---:|---:|
| **Open** | 58 | 63 | 4 | **0** | **−63** |
| **Fixed** | 3 | 1 | 6 | **0** | −1 |
| **Closed** | 0 | 2 | 56 | **66** | **+64** |
| Total | 61 | 66 | 66 | 66 | 0 |

**No new bug IDs.** The highest is still `ROP-BUG-FLUX-00072`. Whole-ledger severity: 0 Critical,
11 High, 43 Medium, 12 Low. Priority: 18 Must, 41 Should, 7 Could. Every record carries
`Owner: -` and `Attempts: fix=0`.

The gaps in the ID run (00014, 00015, 00041, 00058, 00059, 00067) are not missing bugs — they are
held by the sibling `reqs/` ledger under the shared per-host number space.

### The queue was drained, not triaged

This matters for reading the prior report's plan. Plan step 1 was "run the human Gate-1 triage",
called "the highest-value action available". It **was not executed — it was bypassed.**
`git diff 0d82e07..HEAD -- bugs/` changes **zero** `Owner:`, `Priority:` or `Severity:` lines.
Nothing was routed to `reqs/`, nothing was wontfixed, no human assigned anything. The ledger's
state vocabulary (`bugs/README.md:62`) has no triage state at all. An automated fix campaign
consumed the whole queue instead.

That is a legitimate outcome — but it means the queue's *priority ordering was never validated
by a human*, and the report should not be read as "triage happened".

### Closure quality — the honest split

`bugs/README.md:66-67` states the rule in full: *"A bug moves to `Closed` only after a second
pair of eyes verifies the fix (regression test green, root cause understood)."* No cross-host or
cross-family requirement. So the 64 closures by one verifier on host `flux` **satisfy the rule as
written**; that is not a violation, and I am not reporting it as one.

The substantive concern is the *strength* of the record, which is uneven:

| Closure population | Count | Evidence |
|---|---:|---|
| Fails-before **and** passes-after, bug-specific | **24** | `00001`, `00002`, and the 2026-08-02 batches `00044`–`00072`. Genuinely strong — e.g. `bugs/ROP-BUG-FLUX-00047.md:40-46` splices the fix's tests onto pre-fix code at `1d39892~1` and records "3 of 8 tests failed exactly as predicted", naming them. `00060` re-proved three tests against `62bff7d~1`. `00062` reverted ten source files. |
| Passes-after only | **41** | The entire 2026-08-01 wave: `b4ef399`'s 36, plus `00038`–`00040` and `00043`. Templated three-bullet notes; **20 files repeat one of three identical whole-suite figures** (`75 passed, 0 failed` / `1888 passed` / `every suite green`). That is one suite run per crate, transcribed into many bug files as though it were per-bug evidence. |
| Platform-specific genuine repro | **1** | `00003` — see below. |

Two further facts about the record, both verified directly:

- **66 of 66 State-history lines record `gate=manual`.** No closure ran `deltic integrate`. The
  `fixed(ID)` commits are pure ledger flips carrying no code (`6cb58d8` touches exactly one file).
- **63 of 66 `## Fix` fields still read `<unfixed — raised only>`** — including `00007` and
  `00042`, whose Notes describe landed fixes in detail. `bugs/README.md:118-119` designates
  `## Fix` as the accepted-fix summary. That field was abandoned across 95% of the ledger.

### The best artifact in the ledger

`ROP-BUG-FLUX-00003` closed properly, and it is worth naming because it is the counter-example
that calibrates everything above. On 2026-07-31 a KILN verifier **refused** to close it:
*"this host cannot reproduce the original observation, so closing it here would be a fabricated
closure"* (`bugs/ROP-BUG-FLUX-00003.md:38-41`), and wrote down the exact next step. On 2026-08-01
a verifier on Apple Silicon ran that exact repro and closed it on the evidence
(`:59-71`). The refusal was honoured and discharged on the correct platform. That is what the
two-eyes rule is for, and it worked.

---

## The false zero — nine confirmed, unfiled defects

The `foo_ropus/` review landed on 2026-08-02 (`66f0954`), confirmed **six defects through seven
review roles plus a devil's advocate**, and filed **none** of them. The blocker was a sandbox
permission failure writing the clone-wide mint lock — a harness problem, not a repo defect, and
the review correctly refused to hand-mint IDs rather than break the ledger's collision-safety
contract.

| ID in doc | Sev | Finding |
|---|---|---|
| CR-FOO-01 | High | Forward-only (live HTTP / archive) streams open, then `decode_initialize()` unconditionally seeks — the backend rejects every seek when `file_size` is unknown, so playback throws `INVALID_STREAM` before the first sample. The signed HLD requires live HTTP playback. |
| CR-FOO-02 | High | Tag-callback exceptions can cross the Rust FFI boundary. |
| CR-FOO-03 | High | `OpusTags` import has no resource budget. |
| CR-FOO-04 | Medium | Nominal bitrate rounding can overflow signed `int`. |
| CR-FOO-05 | Medium | Huge finite seek times reach an undefined conversion. |
| CR-FOO-06 | Low | Packaging removes the last good archive too early. |

`foo_ropus/` has **no ledger record of any kind** (grep across `bugs/` and `reqs/` → no match),
**no `.deltic-integrate.toml` component**, **no row in `review-coverage.md`**, and was last
touched 2026-05-12 — 88 days ago.

Carried unfiled from 2026-08-01, all three re-verified as still live today:

- **Windows `clippy -D warnings` dead code.** `unpinned_manifest` (`full-test/src/corpus.rs:1788`)
  and `sha256_hex` (`:1808`) are defined unconditionally; every call site (`:1528`, `:1568`,
  `:1621`, `:1644`, `:1673`) is behind `#[cfg(unix)]`. `bugs/ROP-BUG-FLUX-00002.md:59-68` records
  this and ends *"it needs its own ledger entry"* — **and none was ever created.** (Line numbers
  in that note are now stale; the code moved.)
- **`full-test/src/bench.rs:312,317` attributes calibration to "Apple M1 Pro"**, pinned by the
  test assertion at `bench.rs:1272`, while the workstream's own HLD says "M4" three times.
- **`ropus/README.md:167` claims "six" upstream NEON translation units.** `harness/build.rs:240-246`
  lists **five**; the sixth is the harness-local scalar shim pushed separately at `:446-448`. This
  is in the *published* crate README and was introduced only nine days ago.

Also still unfiled: the hanging `ropusplay` CoreAudio tests (no `CoreAudio` match anywhere in
`bugs/`).

---

## Shipped code: a breaking release is sitting unbumped

This is the sharpest actionable finding of the pass, and the number is bigger than I first
drafted.

`ropus/src` changed by **+442/−64** since tag `v0.12.18`, and **no version bump landed anywhere in
204 commits**. Every fix says `manual land`, so the integration step that owns the bump never ran.
`ropus/CHANGELOG.md` is **byte-identical to the tag**; its top entry is still
`## [0.12.18] - 2026-05-11` with no `[Unreleased]` section.

`ropus/src/lib.rs:110,113` declare `pub mod celt;` and `pub mod silk;`, so everything below was
externally nameable at 0.12.18. **Thirteen breaking public-API changes are pending:**

**Eleven `pub` → `pub(crate)` removals:**
`opus_fft`, `opus_fft_impl` (`celt/fft.rs`); `xcorr_kernel_simd`, `celt_maxabs32_simd`,
`denormalise_band_simd`, `mdct_window_simd` (`celt/simd.rs`); `silk_lpc_analysis_filter`
(`silk/common.rs:433`); `silk_warped_autocorrelation`, `silk_corr_matrix`, `silk_corr_vector`,
`silk_burg_modified` (`silk/encoder.rs`).

**Two signature changes:**
`celt/fft.rs:453` — `pub fn opus_ifft` → **`pub unsafe fn opus_ifft`** (hard compile break for any
external caller); `opus/multistream.rs:1527` — `set_expert_frame_duration(&mut self, v: i32)` now
returns `i32`, on a type re-exported at the crate root (`lib.rs:235`).

Plus behavioural tightening that is semver-relevant without changing signatures: the multistream
constructors now reject undersized mappings (`multistream.rs:910,1613`), and the extension parser
returns `OPUS_BAD_ARG` on inputs it previously accepted (`extensions.rs:44-66,96-118`).

Under Cargo's 0.x rules the minor is the compatibility axis, so **this is 0.13.0, not 0.12.19.**

### What that means for users of 0.12.18 — narrowed

I drafted "users of 0.12.18 have reachable UB". A skeptic narrowed it and the narrowing is right.
The supportable form: **`ropus 0.12.18` exposes a safe `pub fn silk_lpc_analysis_filter` whose
caller-controlled indices reach `get_unchecked` via the `uc!` macro (`lib.rs:227`), and the path
`ropus::silk::common::silk_lpc_analysis_filter` is nameable from any external crate.** The UB is
*statically* reachable. It was never demonstrated by execution — `bugs/ROP-BUG-FLUX-00007.md:25`
says *"Static review only; no code was executed"*, and the closure evidence is a `compile_fail`
doctest proving the path is no longer nameable. That proves a reachability change, not that the UB
was exploitable. There is no PoC, no miri/ASan run, and no advisory.

**But the release cannot be cut today.** No `[Unreleased]` changelog section, no version bump, no
`RELEASING.md`, and no release workflow anywhere in the repo. The available action is *prepare*
the release, not tag it.

---

## Design backlog

**`reqs/` — zero delta for eight days.** Still exactly 6 requirements (`00014`, `00015`, `00041`,
`00058`, `00059`, `00067`), all `Draft` / `Should` / **`Flow: heavy`**, all raised 2026-07-31, all
with empty traceability, none claimed. `deltic reqs --ready` is empty: readiness needs `Accepted`,
and every one is `heavy`, which the `/loop` runner reports and skips even after triage. **This is
a zero-throughput queue, and it is the only queue left.** All six are structural refactors
(bounded-memory streaming, unified header/decode policy, centralised Ogg parsing, pure playback
controller, heap-allocation removal in hot paths, DRED-to-PCM) — five of the six target exactly
the tools layer that generated the entire defect wave.

**`issues/`** — README only. Scaffolded 2026-07-30, never used.

**Review coverage is now 0-of-13 current, not 12-of-12 complete.** `wrk_docs/review-coverage.md`
lists 12 areas all dated 2026-07-31 — and the drain put commits into **every one of them** after
that date, a fact the review pass itself recorded
(`…code-review-061944.md:226-232`). `foo_ropus/` was reviewed on 2026-08-02 but the shared table
was deliberately not updated; its result went to a *separate* file
(`wrk_docs/review-coverage/20260802-REV-….md`), so two divergent coverage mechanisms now exist.

**Design pipeline.** 71 HLDs; newest is `2026.07.30 - HLD - macos arm optimisation.md` (9 days).
Newest PLN is `2026.05.07` — **93 days**. The 204 commits produced **zero** design docs.

### The four long-deferred items

| Item | Status |
|---|---|
| SIMD beyond `wide` | **No movement, 111 days.** No `simd_neon.rs`/`simd_avx2.rs`; **zero** `std::arch` or `is_*_feature_detected` hits anywhere in `ropus/src`. `ropus/README.md:160-164` still declares them deferred. |
| Branch coverage → 98% | **Not achieved and measurement stopped.** 89.70% against a 98% target (8.30pp gap); newest coverage report is 2026-04-17, **113 days**. The report's own heading says the target is *"Not Attainable From Test Additions Alone"* — yet the 98% goal is still asserted in six PLN filenames and was never formally retired. |
| Mutation-testing audit | **Dormant 94 days.** `.cargo/mutants.toml` and `tools/mutation_test.sh` exist, untouched since 2026-05-06. Appears in **zero** of the 21 oracle-matrix rows — it is not a release gate. |
| Torture / stress testing | **Code live, design abandoned.** `harness/src/cli.rs:cmd_torture` is exercised; its governing docs are 122 days old with no successor, and it too appears in none of the 21 matrix rows. |

---

## Branches, worktrees, and a failing report lane

- **Worktrees (4):** main clone `D:/language/ropus` (trunk-holder, on `main`, **20 behind
  `origin/main`**), plus KILN daily-report worktrees for 2026-08-04, 2026-08-06 and this pass.
- **The 08-04 and 08-06 worktrees' branches sit exactly at `origin/main` with no commits of their
  own.** Those two scheduled passes landed nothing. (Whether they hold uncommitted drafts is
  unknown — they are outside this worktree and were not readable.)
- **Six daily reports have ever been authored; only four are in `main`.** Verified with
  `git log --all --diff-filter=A -- 'wrk_docs/*DAILY*'`. Stranded: the **2026-06-21 KILN** report
  on `task/sched-daily-report-KILN-20260621-050004` (upstream `[gone]`, still unreaped) and the
  **2026-08-02 CRUCIBLE** report on `origin/task/20260802-OPS-CLA@CRUCIBLE-daily-report-021843`.
- **No daily report exists anywhere for 2026-08-03 through 2026-08-07.**
- **Only one commit anywhere is not reachable from `origin/main`** — `88b647b`, the stranded
  report. There is **no unmerged engineering work**.
- 3 tags: `v0.11.1`, `v0.12.0`, `v0.12.18`. (The 2026-08-01 report listed a fourth, `v0.5.0`; it is
  not in this clone's tag set. Cross-host tag-fetch difference, not verified either way — no fetch
  ran.)

So: of the last four scheduled daily passes, **one produced a stranded report and two produced
nothing.** This report is the first to land in six days.

---

## Integration policy — much better, with two pointed gaps

`.deltic-integrate.toml` now names **13 components**, up from 8. `tools-core`, `ropusenc`,
`ropusdec`, `ropusinfo`, `ropusplay` and `fetch-assets` were added during the drain (`8d7e17a`,
`7da7d20`). That substantially discharges the prior report's item 12.

**Two of the 14 workspace members still have no component**, and they are the two that matter most:

- **`capi/`** — which took the single largest code change in the range (+696/−139, the whole
  allocation-failure rework) and has four distinct defects recorded in `lessons_learnt.md:188-201`.
- **`tests/conformance`** — which `testing-oracle-matrix.md:33` designates `required-core` with
  `exact-byte-parity`: **the strongest oracle in the repo.**

Plus the non-Cargo `foo_ropus/`. A change confined to any of the three falls through to the
workspace `fallback` gate — which runs `clippy --workspace --all-targets -D warnings` and therefore
**fails on Windows** on the unfiled dead-code defect above. Correction to my own first draft: that
redness does *not* block a `ropus`-scoped release, because a release commit touching only
`ropus/Cargo.toml` and `ropus/CHANGELOG.md` maps to the `core` component and runs `-p ropus` only.

---

## Documentation

**One item genuinely retired.** `lessons_learnt.md` now exists at the repo root, created
2026-08-01 (`1c8b95c`) and maintained through the drain — **58 entries**, index-v1 format, one per
fix. It is where the engineering learning from these 204 commits actually went. It is also
**well past its ~25-entry convention and due a curation pass** (a human-invoked one — not
something to trim mid-task).

Everything else is unchanged or worse:

- **`scratchpad.md` still does not exist** — and `git log --all` shows it never has. The nine
  unfiled defects above are the direct cost.
- **`CLAUDE.md` and `AGENTS.md` last touched 2026-04-19 — 111 days.** `:18` still says
  "Currently in the **integration** phase"; `:33` still lists `ropus-cli/`, **a directory that does
  not exist**; `:8,:23` still say `mdopus`. The architecture tree names 4 of 14 workspace members
  and **omits 10**, plus `foo_ropus/` and all four ledgers. `:95` claims "no platform-specific
  intrinsics", now contradicted by `harness/build.rs:236-246`.
- **26 corrupt `*_review.md` files** in `wrk_docs/design_docs/` — re-verified: 26 of 26 contain
  only `ERROR: [WinError 2] The system cannot find the file specified`. **111 days.** (Byte size
  not re-measured; the size-reporting commands were permission-denied. Content is confirmed
  exactly.)
- **`ropus/CHANGELOG.md` is 89 days behind** and is the only changelog in the repo — the five
  shipping binaries have none.
- **`testing-oracle-matrix.md` is 93 days stale** and still covers only the codec. **Zero rows for
  the tools layer** — exactly the crates that produced the whole defect wave. Substantial tools
  tests *did* land (`ropus-tools-core/tests/round_trip.rs` +1,027, new `build_provenance.rs`, four
  CLI `tests/cli.rs`), but the matrix was never updated to claim them, so no oracle-strength
  statement exists for any user-facing tool.

---

## Verification ledger

| Claim | Check | Verdict |
|---|---|---|
| **H1** — "all 66 bugs are genuinely closed on recorded evidence; a real drain, not a rubber-stamp" | 1 skeptic, then lead re-verification | **REFUTED as stated.** 24 closures carry a fails-before re-derivation; 41 are passes-after-only with templated notes reusing shared whole-suite counts. Lead confirmed the timing at source (`69a312c` 22:13:59 → `b4ef399` 22:39:14 = 25m15s for 36 bugs across 4 crates) and the `## Fix` count (63/66 `<unfixed>`). Published in split form. The skeptic's sub-attack on the two-eyes rule itself **failed** — `bugs/README.md:66-67` imposes no host/family requirement, so the rule was met. |
| **H3a** — "users of 0.12.18 have reachable UB" | 1 skeptic | **SURVIVES-NARROWED.** Path publicity independently confirmed by the lead (`lib.rs:113 pub mod silk`). But UB was never executed — `00007:25` says static review only. Republished as *statically* reachable. |
| **H3b** — "the pending release is breaking: 0.13.0, not 0.12.19" | 1 skeptic | **SURVIVES — and I had understated it.** Lead re-derived the full list directly from `git diff v0.12.18..HEAD -- ropus/src/`: **11** narrowings + **2** signature changes = **13** breaking items, not the 1 I first cited. (The skeptic said 10 narrowings; its own list contained 11. Lead count stands.) |
| **H3c** — "cutting the release is the highest-value action today" | 1 skeptic | **REFUTED.** No `[Unreleased]` section, no bump, no `RELEASING.md`, no workflow. Also my blocker citation was wrong: the Windows clippy failure would not gate a `ropus`-scoped release. Republished as *prepare* the release. |
| **H2** — "the scheduled daily-report lane is failing" | lead, direct | **FACT.** `git log --all --diff-filter=A -- 'wrk_docs/*DAILY*'` gives 6 reports authored, 4 in `main`; `git branch -vv` puts the 08-04 and 08-06 branches at `origin/main` with no commits. Not panelled — deterministic. |
| Deterministic lookups — 66 files all Closed; 204 commits; 239 files ±14,156/2,514; `ropus/src` +442/−64; 6 heavy Draft reqs; 13 components vs 14 members; 26 corrupt files; 63/66 `<unfixed>`; the `cfg(unix)` call sites | lead, direct | **FACT** — each read once at its source. Two readers disagreed on the commit count (204 vs "~180"); lead settled it at **204** via `git rev-list --count`. |

---

## Tracked issues (key items)

1. **Nine confirmed defects are unfiled, and filing is what broke.** Six `foo_ropus/` findings
   (3 High, one breaking live HTTP playback outright) plus three carried. Cheapest high-value act
   available: re-run the filing from a session with normal clone-lock access. **⟶ NEW, top.**
2. **A breaking 0.13.0 release is sitting unbumped and undocumented.** 13 breaking public-API
   changes in `ropus/src` since `v0.12.18`, including a soundness narrowing; changelog
   byte-identical to the tag; no `RELEASING.md`; no workflow. **⟶ NEW.**
3. **41 of 66 closures rest on passes-after-only, templated evidence**, 66/66 at `gate=manual`,
   63/66 with `## Fix` = `<unfixed — raised only>`. The ledger's confidence is lower than its state
   field implies. **⟶ NEW.**
4. **The scheduled daily-report lane is unreliable** — 2 of 6 reports stranded off `main`, 2
   consecutive KILN passes produced nothing, 6-day gap. The repo's self-observation is the thing
   that failed. **⟶ NEW.**
5. **The reqs ledger is a zero-throughput queue and now the only queue.** 6 Draft / Should / heavy,
   untouched 8 days, `--ready` empty. Human Gate-1 triage is still unrun; the bug queue was drained
   around it rather than through it. *Carried, re-scoped.*
6. **`capi/` and `tests/conformance` have no integration component** — the crate with the largest
   change in the range, and the repo's strongest oracle. `foo_ropus/` has none either. *Carried,
   sharpened; 5 of 7 gaps closed.*
7. **Windows `clippy --workspace -D warnings` still fails** on `full-test/src/corpus.rs:1788,1808`.
   Makes the fallback gate red on this platform. *Carried — and `00002` explicitly said it needed
   its own entry.*
8. **Review coverage is 0-of-13 current, not 12-of-12 complete**, and two divergent coverage files
   now exist. **⟶ NEW.**
9. **Zero testing oracles claimed for the tools layer.** Real tools tests landed; the 93-day-stale
   matrix was never updated to claim them. *Carried, partially discharged in code only.*
10. **`CLAUDE.md`/`AGENTS.md` 111 days stale + 26 corrupt review files.** Fourth consecutive report.
    `CLAUDE.md` documents a directory that does not exist and omits 10 of 14 members. Cheap.
11. **`lessons_learnt.md` exists (good) and is at 58 entries — past its convention, due a
    human-invoked prune.** `scratchpad.md` still does not exist. *Half-retired.*
12. **Carried, unmoved:** SIMD beyond `wide` (111 days); 98% coverage unmet with measurement
    stopped 113 days ago and the target never retired; mutation testing dormant 94 days and not a
    gate; torture testing live in code but design-abandoned; proptest seed 2296 still unverified
    across six reports.

---

## Conclusions and plan of action for today

**Conclusion.** The repo did something genuinely hard: it took a 63-deep static-review backlog to
zero in about 36 hours, with real code behind it — +11,175 lines, +166 tests, a full C-ABI
allocation-failure rework, and API-surface narrowing in the published crate. The last third of
those closures is properly evidenced, and `ROP-BUG-FLUX-00003` shows the two-eyes rule working
exactly as designed: a verifier refused to close what it could not observe, wrote down the repro,
and a verifier on the right platform discharged it.

Then everything stopped. Six days, no commits, two scheduled passes producing nothing, one report
stranded. And in that silence, three things quietly went wrong: nine confirmed defects have nowhere
to live because the filing tool failed closed; a breaking release is sitting unbumped with 13
public-API changes and a byte-identical changelog; and the ledger's `0 Open` is being read as
`0 defects` when it is not.

The binding constraint is no longer triage, and no longer the queue. **It is that the automated
lane finished its work and nothing decided what comes next.** The reqs ledger — six heavy Drafts —
is the only queue left, and by construction it cannot move without a human.

**Proposed plan (read-only pass — recommendations only; nothing was changed today):**

1. **Re-file the nine unfiled defects (#1).** Run `deltic bugs new` from a session with normal
   clone-lock access and mint the six `foo_ropus/` findings — they are already bug-ready with
   title, severity, priority, fix and regression oracle in
   `wrk_docs/2026.08.02 - CR - 20260802-REV-ROP-CLA@CRUCIBLE-code-review-061944.md`. Then the three
   carried ones, Windows-clippy first. This is minutes of work and it is the difference between a
   ledger that means something and one that doesn't.
2. **Prepare the 0.13.0 release (#2)** — *prepare*, not tag. Author the `[Unreleased]` → `[0.13.0]`
   changelog section covering all 13 breaking items; confirm the semver call; write the missing
   `RELEASING.md`. The bump itself belongs to the integration step. Cutting it today is not
   available and should not be attempted.
3. **Run the human Gate-1 triage on `reqs/` (#5).** Six heavy Drafts, zero throughput, and it is
   now the only queue. Accept, reject, or split each into light slices — a heavy req that nothing
   can route is indistinguishable from an unfiled idea.
4. **Fix the daily-report lane (#4).** Reap `task/sched-daily-report-KILN-20260621-050004`, decide
   what to do with the stranded 2026-08-02 report, and find out why the 08-04 and 08-06 passes
   landed nothing. A self-observation loop that silently skips is worse than no loop, because its
   silence reads as "nothing to report".
5. **Add integration components for `capi/` and `tests/conformance` (#6),** and decide whether
   `foo_ropus/` gets one. Both fall through to a fallback gate that is red on Windows, and one of
   them is the repo's strongest oracle.
6. **Spot-check the 41 thin closures (#3)** rather than re-verifying all of them. Take the High/Must
   items in that population, re-derive fails-before for two or three, and see whether the wave holds
   up. If it does, note it and move on; if it doesn't, that is a much bigger finding than anything
   else in this report.
7. **Cheap hygiene (#10, #11):** refresh `CLAUDE.md`/`AGENTS.md` (Project Status, the crate tree,
   the `mdopus` and `ropus-cli/` references); delete or regenerate the 26 corrupt `*_review.md`
   files; create `scratchpad.md`; queue a human-invoked `lessons_learnt.md` prune; fix
   `ropus/README.md:167` ("six" → five upstream plus one local shim).
8. **Reconcile review coverage (#8)** — merge the `foo_ropus/` row into `review-coverage.md`, retire
   the second mechanism, and mark all 12 areas stale, because the drain touched every one of them.

**Risks and caveats.** Read-only pass: no build, no tests, no clippy, no app run, nothing fixed.
Every quality claim rests on what the ledger and commits *record*, not on observed green output —
including the release-readiness assessment. The Windows clippy defect was established by reading
`#[cfg(unix)]` attributes against unguarded definitions, not by running the gate. No `git fetch`
ran, so branch, `[gone]` and tag facts rest on the local remote-tracking database; the `v0.5.0`
discrepancy against the prior report is unresolved for that reason. Files outside this worktree
were not readable, so the 08-04 and 08-06 passes are characterised by branch state only. And the
central caution of this report is about its own headline: **`0 Open` is a ledger state, not a
statement about the software.**
