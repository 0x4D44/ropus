# Code Review — 2026-08-19 — harness-deep-plc/

Pass: 20260819-REV-ROP-CLA@KILN-code-review-112641 (Claude Fable 5, KILN).
Area: `harness-deep-plc/` (~7,000 lines: float-mode DEEP_PLC/DRED differential harness — build.rs + C shims + FFI surface + 13 differential test files).
Selection: MOVED tier — last reviewed 2026-07-31, last commit 2026-08-02 (the fix wave for the 2026-07-31 FLUX bug batch).
Method: six-lens fan-out (correctness/FFI, oracle validity, C-shim safety, build reliability, maintainability, devil's advocate), every reported finding then independently re-verified by the lead against the source. Static review only — no build, test, or app ran. The git-ignored `reference/` checkout is absent on this host; claims depending on it are labelled UNVERIFIED.

## Confirmed findings — filed as bugs

| ID | Sev | Finding | Anchor |
|---|---|---|---|
| ROP-BUG-KIL-00021 | High | Nine live DRED gates skip-and-pass when `WEIGHTS_BLOB` is empty; `ropus/build.rs` writes an empty blob on any blob-build failure with only a cargo warning, so the whole DRED differential surface can go green with zero comparisons. Fix effort: small, mechanical (nine sites → the in-crate `require_weights` assert pattern). | `harness-deep-plc/tests/dred_rdovae_enc_diff.rs:111` (+8 sites) |
| ROP-BUG-KIL-00022 | Medium | SNR oracles accept degenerate output: `compute_snr_db` returns INFINITY on zero noise, release gates assert only `snr > threshold`, so bit-identical (incl. both-silent) output passes; the sibling calibration helper already has the divergence assert the release gates lack. Also a dead NEG_INFINITY guard ordering in `dred_bitrate_plumbing_nonzero_diff.rs:260-266`. Effort: small. | `harness-deep-plc/tests/tier2_snr.rs:221-223,276,393` |
| ROP-BUG-KIL-00023 | Medium | Bit-exact gates accept shared NaN and zip-truncate on length mismatch — three files bypass `tests/support/finite_oracle.rs` (`dred_decode_payload_diff.rs:48`, `dred_encode_payload_diff.rs:141`, `burg_cepstral_analysis_diff.rs:35`), plus the NaN-permissive `any(|f| *f != 0.0)` at `dred_integrated_encode.rs:433`. Residue of the ROP-BUG-FLUX-00069 class. Effort: small. | `harness-deep-plc/tests/dred_decode_payload_diff.rs:48-55` |
| ROP-BUG-KIL-00024 | Medium | DRED shim FFI lacks contracts: unbounded caller `n` in eight memcpy copy/set helpers (writes into heap `DREDEnc`); C-macro write sizes never tied to the Rust constants sizing the destination buffers (constant drift → silent stack corruption); five unchecked `opus_encoder_ctl` returns (differently-configured encoders compared as equal); a shared borrow cast to `*mut` for a writable C parameter. Effort: medium. | `harness-deep-plc/dred_encode_shim.c:103-167,190-218,253-279` |
| ROP-BUG-KIL-00025 | Medium | `peek.c` hand-mirrors private decoder layouts with an `#ifdef ENABLE_DEEP_PLC` block but never includes `config.h` (the only place the macro is defined; the sibling `harness/` shims do include it) — and has zero static/runtime guards, no positive-path test, plus dead `peek_celt_old_log_e2`. Corroboration: `state_divergence.rs:11-15` records "garbage" where an `OPUS_CLEAR`ed decoder should read zeros. Effort: small-medium. | `harness-deep-plc/c/peek.c:14-18,66-72` |
| ROP-BUG-KIL-00026 | Medium | RDOVAE "Tier-1" gates silently downgrade to a 60 dB Tier-2 fallback on bit-exactness loss (`if all_bit_exact { return; }`); the CLAUDE.md Tier-1 status is unenforced. Effort: small. | `harness-deep-plc/tests/dred_rdovae_enc_diff.rs:206-239` |
| ROP-BUG-KIL-00027 | Low | `build.rs` sentinel guard probes 3 files but compiles 5 more `*_data.c` from the same tarball — a partial extraction defeats the friendly diagnostic and yields a raw cc error. Effort: trivial. | `harness-deep-plc/build.rs:91-93` vs `:273-315` |
| ROP-BUG-KIL-00028 | Medium | Drift-lock bounds are host-architecture-specific: `config.h` claims "scalar only" but only NEON is disabled; the crate's own root-cause note proves `vec_avx.h`'s `_mm_rcp_ps` is the live C kernel on x86, so the locked bounds and the 34-index bit-exact lock encode x86 SIMD output, ungated by `target_arch`. Same family as closed ROP-BUG-FLUX-00004. Effort: medium. | `harness-deep-plc/tests/dred_lpcnet_feature_drift.rs:102-140` |

## Design/debt — filed as Draft reqs

- **ROP-REQ-KIL-00029** — add the missing C differential for `OpusDREDDecoder::process`/`fec_features` (the composed reconstruction chain is gated only in halves; its sole value assertion is the NaN-permissive check above).
- **ROP-REQ-KIL-00030** — consolidate duplicated test support (WAV parser ×5, `vectors_path` ×6, `weights_or_skip` ×7 with live signature drift, SNR helpers ×4) into `tests/support/`.
- **ROP-REQ-KIL-00031** — build-time reference provenance + mirrored-layout/constant guards (fetch-time pin is accepted silently on mismatch; one FNV fingerprint block is the only build-time drift detector).
- **ROP-REQ-KIL-00032** — tier-2 gates must prove DEEP_PLC engagement (50 dB threshold sits at the top of the measured 35–50 dB classical band; no engagement accessor reachable; needs API design).

## Confirmed minor findings — report only (no ledger entry)

- Stale "Failure semantics" doc block claims `unimplemented!()` stubs that no longer exist — `harness-deep-plc/tests/dred_dtx_first_frame_diff.rs:29-34` (both functions are implemented at `ropus/src/opus/encoder.rs:724,770`). A maintainer un-ignoring the test would expect a TDD panic signal that won't come.
- Misleading failure text: the PCM SNR gate in `dred_bitrate_plumbing_nonzero_diff.rs:344-363` blames "DRED reconstruction ... drifting" but `decode_all_rust` never reconstructs DRED (no loss, no `OpusDREDDecoder`); the SNR sees only SILK bit-allocation effects. The extension-presence gate beside it is sound (post-FLUX-00070).
- Contradictory comments about the same 32 kbps/FEC/loss-20/dur-100 config: `dred_bitrate_plumbing_diff.rs:231-234` says DRED budget clamps to 0 there; `dred_integrated_encode.rs:221-224` asserts extensions are emitted at it. One is stale.
- Self-referential check: the q-tag verification in `dred_decode_payload_diff.rs:222-235` derives its expected value from the same ropus `compute_quantizer` under test; it catches only an unwritten slot. Redundant, not dangerous — the C comparison above it covers the real property.
- Uniform fixture: all-ones `activity_mem` (`dred_encode_payload_diff.rs:212-213`, `dred_decode_payload_diff.rs:82`) cannot detect an activity-indexing bug on either side (documented as deliberate; noted as an oracle blind spot).
- No non-vacuity companion for `estimate_dred_bitrate_byte_exact_against_c_ref` (`dred_compute_bitrate_ffi_diff.rs:103-122`) — five vector rows could all return (0,0) on both sides and pass; the sibling `compute` gate has exactly such a companion (`:197-213`).
- `state_divergence.rs` stage7b3 tests are assertion-free printers with a stale header ("16.67 dB, still below the 60 dB target") — explicitly diagnostics, but they present as green tests and are the only consumers of the peek surface.
- `wrk_docs`-level note: the headline byte-parity gate `dred_encode_silk_frame_bytes_match_c_reference` is `#[ignore]`d (documented: blocked by LPCNet feature drift), and `dred_dtx_first_frame_diff.rs` contains no C side at all — the "byte-exact DRED payload" claim currently rests on the bounded-drift proxy.
- Tautological assert `manifest.sources() == manifest.watched_sources()` in shared `harness/reference_build_manifest.rs:29-35` (both return the same field) — outside this area, noted only.
- Inert gap: `cargo:rerun-if-changed=config.h` (`build.rs:383`) is skipped on the `no_reference` early-return path; harmless because nothing was compiled in that state.
- `DEC_QFRAME_WIDTH` defined but bypassed by bare `80` literals in the second test of `dred_rdovae_dec_diff.rs:366,376`.
- Cosmetic/tooling: `deltic bugs new` on this host now mints host token `KIL` while all pre-existing records use `KILN` (sequence correctly unified at 21+). Worth a look at deltic's host-token derivation.

## Not reported / already tracked

- Loss-pattern mismatch between `harness-control/tests/control_snr.rs` and `tier2_snr.rs` (control's "byte-for-byte identical" comment is false; 14 vs 13 losses) — already tracked as open **ROP-BUG-KILN-00016**; the cross-crate fixture centralization is **ROP-REQ-KILN-00020**. Not duplicated.
- `#![cfg(not(no_reference))]` making the crate report "0 tests, ok" without `reference/` — deliberate, documented design (workspace builds on fresh clones); the residual risk window is covered by KIL-00021's fix.
- One reviewer claimed `peek_celt_trailing_base` fails to skip xiph's `lpc[]` trailing block; another verified it consistent with the in-repo architecture doc and the sibling `debug_helper.c`. **UNVERIFIED either way without `reference/`** — folded into KIL-00025 as an explicit verification step, not asserted as a defect.
- Verified clean (so nobody re-litigates): all 56 FFI declarations match their C definitions in arity/width/constness; `checked_peek_range`/`checked_decode_output_len`/`validate_peek_capacities` are correct and unit-tested; every-packet parser validation (post-FLUX-00071) present; RDOVAE running minima NaN-safe via finite_oracle; C and Rust weights share one provenance; `#[path]` bin/bin_inner mechanism sound; rerun-if-changed coverage complete for all compiled sources.

## Next pass

Per `wrk_docs/review-coverage.md` all areas were last reviewed 2026-07-31; anything with commits since then is MOVED. Suggested order: `ropus/` (largest, most-changed core), then `harness-control/` (interacts with KILN-00016), then `capi/`.
