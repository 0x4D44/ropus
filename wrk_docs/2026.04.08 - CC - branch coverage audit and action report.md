# Branch Coverage Audit And Action Report

Date: 2026-04-08

Repo: `C:\language\mdopus`

## Scope

This audit is based on repeated clean branch-aware coverage runs across the current workspace with all crate features enabled.

Trusted measurement command:

```powershell
mdtimeout 5400 cargo +nightly llvm-cov --workspace --all-features --branch --summary-only
```

Useful detail command:

```powershell
mdtimeout 1800 cargo +nightly llvm-cov report --branch --text --show-missing-lines --output-dir target/llvm-cov/current-text
```

Important facts:

- `--branch` still requires nightly on this machine
- there were no persistent test failures blocking coverage
- the only initial failure was mechanical: `target/llvm-cov` did not exist for a requested output path
- `cargo llvm-cov ... --json --summary-only` produced inconsistent denominators later in the session; the direct text summary stayed stable and is the trusted source for totals
- the measured run includes the crate, the harness build path, and the existing test targets exercised by `cargo llvm-cov --workspace --all-features`

## Validation State

Coverage run status:

- `737` unit tests passed
- `3` smoke coverage tests passed
- branch-aware coverage completed successfully on nightly

## Current Totals

| Metric | Covered | Total | Percent |
| --- | ---: | ---: | ---: |
| Lines | 34,752 | 36,554 | 95.07% |
| Functions | 1,992 | 2,034 | 97.94% |
| Regions | 52,822 | 55,351 | 95.43% |
| Branches | 4,580 | 5,710 | 80.21% |

Distance from `98%`:

- Lines: `1,071` more covered lines
- Functions: `2` more covered functions
- Regions: `1,422` more covered regions
- Branches: `1,016` more covered branches

The branch number is the limiting metric by a large margin.

## Progress So Far

Measured progress from the fresh baseline:

| Wave | Lines | Functions | Regions | Branches |
| --- | ---: | ---: | ---: | ---: |
| Baseline | 91.45% | 92.76% | 92.09% | 76.57% |
| Wave 1: DNN + multistream + decoder/repacketizer | 93.24% | 96.72% | 93.66% | 77.64% |
| Wave 2: encoder-heavy pass | 94.54% | 97.85% | 94.96% | 78.90% |
| Wave 3: multistream + silk decoder cleanup | 94.62% | 97.86% | 95.04% | 79.05% |
| Wave 4: opus encoder/decoder/repacketizer pass | 94.93% | 97.92% | 95.32% | 79.96% |
| Wave 5: silk encoder helper/state pass | 95.07% | 97.94% | 95.43% | 80.21% |

Committed coverage work landed in:

- `src\dnn\lpcnet.rs`
- `src\opus\encoder.rs`
- `src\opus\decoder.rs`
- `src\opus\multistream.rs`
- `src\opus\repacketizer.rs`
- `src\silk\decoder.rs`
- `src\silk\encoder.rs`

## High-Level Read

The repo is healthy on many helper-heavy modules and materially weaker in large control-flow modules. The current suite is broad, and the cheap helper wins have mostly been harvested already. The remaining debt is concentrated in large encoder state machines plus a second tier of packet/decode orchestration files.

This is not a “find two missing tests” situation. The remaining gap to `98%` is still over a thousand uncovered branches, which means the target is now dominated by expensive control-flow work inside large stateful modules.

## Largest Hotspots By Missed Branches

| File | Missed branches | Branches | Missed lines | Lines |
| --- | ---: | ---: | ---: | ---: |
| `src\silk\encoder.rs` | 240 | 76.79% | 582 | 91.23% |
| `src\opus\encoder.rs` | 145 | 79.40% | 143 | 94.66% |
| `src\silk\decoder.rs` | 117 | 70.45% | 111 | 94.95% |
| `src\celt\encoder.rs` | 104 | 81.88% | 105 | 95.48% |
| `src\celt\bands.rs` | 98 | 78.70% | 86 | 94.79% |
| `src\opus\repacketizer.rs` | 92 | 77.78% | 72 | 96.33% |
| `src\opus\decoder.rs` | 61 | 81.74% | 88 | 94.51% |
| `src\celt\decoder.rs` | 53 | 78.46% | 177 | 89.53% |
| `src\opus\multistream.rs` | 47 | 86.34% | 48 | 97.80% |
| `src\silk\common.rs` | 23 | 86.31% | 51 | 96.37% |

## Most Important Coverage Interpretation

There are four different coverage problems in the repo:

1. `src\silk\encoder.rs` and `src\opus\encoder.rs` are still the dominant debt buckets. They are large, stateful, and branch-heavy.
2. `src\silk\decoder.rs`, `src\opus\repacketizer.rs`, and `src\opus\decoder.rs` remain worthwhile because they still expose deterministic branch debt without the full encoder-state cost.
3. `src\celt\encoder.rs` and `src\celt\bands.rs` are now more relevant than earlier because many easier Opus/DNN gains have already been captured.
4. Functions are almost solved. Branches and regions are the honest constraint now.

## Best Near-Term Return

The fastest realistic next wave is:

1. `src\silk\encoder.rs`
2. `src\opus\encoder.rs`
3. `src\opus\repacketizer.rs`
4. `src\silk\decoder.rs`
5. `src\celt\encoder.rs`
6. `src\celt\bands.rs`

Why this order:

- `silk::encoder` is still the only file with enough remaining branch debt to move the repo materially by itself
- `opus::encoder` remains the second-biggest realistic target and already proved it can pay back targeted state-path tests
- `opus::repacketizer` and `silk::decoder` still offer deterministic cleanup branches without deep parity-risk
- `celt::encoder` and `celt::bands` are now the next best non-Opus branch buckets

## Clean-Run Blockers

There are currently no test failures blocking branch coverage.

The blockers found during this pass were:

- nightly is required for `cargo llvm-cov --branch`
- the output directory for custom artifacts must exist before writing to it
- the JSON summary export became unreliable for this workspace, so the direct text summary is now the trusted measurement path

Neither blocker required source fixes.

## Risks To The 98% Target

The target is still technically measurable but expensive.

Main risks:

- branch coverage is still only `80.21%`, so line-only helper tests are no longer enough
- the largest remaining debt lives in orchestrator-style encoders with wide state surfaces
- later gains will get more expensive per point because most low-risk helpers are already covered
- `98%` branch coverage from the current position would require covering `1,016` more branches, which is a large engineering project rather than a tidy cleanup pass

## Recommended Next Actions

1. Keep using `cargo +nightly llvm-cov --workspace --all-features --branch --summary-only` as the source of truth.
2. Add another deterministic `silk::encoder` wave aimed at VAD, NLSF, stereo LR/MS, and budget-damage-control branches that are still cold.
3. Add another `opus::encoder` wave aimed at redundancy placement, transition prefills, and remaining CELT/SILK mode-budget branches.
4. Follow with `opus::repacketizer`, `silk::decoder`, and `celt::encoder` branch cleanup.
5. Re-rank from the text summary after every meaningful wave and stop pretending the repo is close to `98%` until the remaining branch debt actually collapses.
