# Worktree Triage Review

**Date:** 2026-04-08
**Scope:** Classify uncommitted work in the repository root before commit/push.

## Summary

The dirty worktree broke into three buckets:

1. **Commit-worthy project artifacts**
   - `wrk_docs/2026.04.08 - Codec Deep Dive.md`
   - `wrk_docs/2026.04.08 - HLD - DNN pipeline integration.md`
   - `wrk_docs/2026.04.08 - HLD - SIMD acceleration.md`
   - `wrk_docs/2026.04.08 - HLD - unit tests for lpc rate common.md`
   - `wrk_journals/2026.04.08 - JRN - DNN+Tests+SIMD sprint.md`
2. **Local workflow residue that should be ignored**
   - `.codex-home/`
   - `.codex-tmp/`
   - `.wrk_validate/`
   - `.wrk_validate_stage1.patch`
3. **Low-value code change that should not be committed**
   - `src/main.rs`

## Disposition

### Commit-worthy artifacts

The `wrk_docs` files are substantive research/design outputs: one codec status review plus three HLDs covering DNN pipeline integration, SIMD acceleration, and unit-test expansion. The journal captures the same design sprint from the workflow/process side. These belong in version control because they describe real engineering decisions and planned work.

### Ignore-only artifacts

`.codex-home/` and `.codex-tmp/` are local Codex runtime caches copied into the repository root. `.wrk_validate/` is a nested validation sandbox with its own checkout, build outputs, and generated coverage data. `.wrk_validate_stage1.patch` is an exported patch artifact from that sandbox. None of these are repository source; they create noise and should stay ignored.

### Hold-back decision on `src/main.rs`

The only tracked code change was a tiny `main_runs()` smoke test that just calls `main()`. It looks like coverage padding rather than meaningful regression protection. It was reverted instead of committed so the final history does not accumulate no-signal tests.

## Naming check

The new `wrk_docs` and `wrk_journals` files kept for commit all follow the repository's date-prefixed naming pattern. Existing tag usage such as `HLD`, `CR`, and `JRN` was preserved for consistency with the established convention in this repository.
