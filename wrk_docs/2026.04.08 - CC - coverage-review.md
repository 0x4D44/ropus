# Coverage Review

Date: 2026-04-08
Repo: `C:\language\mdopus`
Command: `mdtimeout 300 cargo +nightly llvm-cov --branch --json --summary-only --output-path target\llvm-cov-summary.json`

## Executive Summary

The current baseline is far below the requested 98% target.

- Lines: 48.95% (14,344 / 29,306)
- Functions: 64.31% (946 / 1,471)
- Regions: 50.36% (21,475 / 42,644)
- Branches: 35.10% (1,851 / 5,274)

This is not a "small top-up" situation. The repo is missing roughly 14,962 covered lines and 3,423 covered branches. Reaching 98% will require broad execution coverage across the encoder/decoder stacks, not just more helper tests.

## Coverage Collection Notes

- `cargo llvm-cov --branch` required nightly on this machine.
- The first clean run was blocked by Cargo trying to test the implicit `src/main.rs` binary under coverage on Windows.
- That blocker was fixed by setting `autobins = false` in `Cargo.toml` and relying on the explicit `mdopus-compare` bin target.
- Source size from `mdkloc src`:
  - Rust files: 37
  - Code lines: 35,825
  - Comment lines: 6,780
  - Blank lines: 4,745

## What Is Covered Well

These modules already have strong line coverage and are not the best first targets for more work:

- `src/celt/cwrs.rs`: 98.30% lines, 91.30% branches
- `src/celt/fft.rs`: 97.45% lines, 80.00% branches
- `src/celt/mdct.rs`: 98.34% lines, 65.00% branches
- `src/celt/modes.rs`: 97.06% lines, 100.00% branches
- `src/celt/quant_bands.rs`: 96.19% lines, 72.73% branches
- `src/dnn/lossgen.rs`: 96.78% lines, 90.00% branches

The current suite is strongest around math-heavy helper code and table-driven logic.

## Worst Coverage Files

By line coverage:

| File | Line % | Function % | Branch % | Covered / Total Lines |
|---|---:|---:|---:|---:|
| `src/celt/ec_ctx.rs` | 6.45 | 5.56 | 0.00 | 6 / 93 |
| `src/silk/encoder.rs` | 9.59 | 35.04 | 6.21 | 571 / 5,952 |
| `src/silk/decoder.rs` | 14.47 | 48.21 | 2.69 | 248 / 1,714 |
| `src/dnn/fargan.rs` | 20.45 | 57.14 | 0.00 | 126 / 616 |
| `src/silk/common.rs` | 34.92 | 35.14 | 28.38 | 300 / 859 |
| `src/dnn/lpcnet.rs` | 38.98 | 43.75 | 58.33 | 568 / 1,457 |
| `src/opus/encoder.rs` | 40.93 | 35.65 | 31.09 | 828 / 2,023 |
| `src/dnn/core.rs` | 43.92 | 66.30 | 57.89 | 556 / 1,266 |
| `src/celt/bands.rs` | 46.65 | 75.76 | 31.39 | 731 / 1,567 |
| `src/opus/decoder.rs` | 53.96 | 63.16 | 37.76 | 620 / 1,149 |
| `src/dnn/pitchdnn.rs` | 55.40 | 80.00 | 100.00 | 195 / 352 |
| `src/opus/multistream.rs` | 55.40 | 47.97 | 48.08 | 856 / 1,545 |

By raw missed lines:

| File | Missed Lines | Missed Branches |
|---|---:|---:|
| `src/silk/encoder.rs` | 5,381 | 966 |
| `src/silk/decoder.rs` | 1,466 | 362 |
| `src/opus/encoder.rs` | 1,195 | 419 |
| `src/dnn/lpcnet.rs` | 889 | 15 |
| `src/celt/bands.rs` | 836 | 317 |
| `src/dnn/core.rs` | 710 | 32 |
| `src/opus/multistream.rs` | 689 | 162 |
| `src/celt/encoder.rs` | 643 | 337 |
| `src/silk/common.rs` | 559 | 106 |
| `src/opus/decoder.rs` | 529 | 178 |

## Files With No Inline Tests

These files currently have no `#[cfg(test)]` or `#[test]` blocks:

- `src/silk/common.rs`
- `src/silk/tables.rs`
- `src/celt/rate.rs`
- `src/celt/lpc.rs`
- `src/celt/ec_ctx.rs`
- `src/lib.rs`
- `src/opus/multistream_tables.rs`
- `src/main.rs`
- module wiring files under `src/*/mod.rs`

Important nuance: some of these are still partially covered indirectly, but several are functionally "test deserts".

## Main Findings

1. The current suite is heavily skewed toward low-level helper tests.
2. The largest gaps are in stateful codec execution paths:
   - `silk::encoder`
   - `silk::decoder`
   - `opus::encoder`
   - `opus::decoder`
   - `opus::multistream`
3. Branch coverage is much worse than line coverage, which means the suite is touching surfaces without exercising the real decision trees.
4. The repo already contains a large harness binary (`tests/harness/main.rs`) with many executable comparison commands, but that binary currently contributes almost no coverage because it had zero actual tests.
5. The fastest honest way to move coverage is to turn those harness commands into automated tests, then fill remaining holes with direct unit tests in zero-test modules.

## Risk Assessment

- 98% overall coverage is not close. It is a major testing project.
- The repo is large and parity-sensitive; careless assertions can create brittle tests.
- The biggest gains are likely from integration-style execution, not from adding more micro-tests to already-covered math modules.
- Branch coverage will be the hardest metric to drag upward, especially in SILK and Opus control flow.

## Immediate Recommendation

Prioritize the existing harness first:

1. Convert stable `mdopus-compare` command paths into actual automated tests.
2. Use those tests to drive end-to-end encode/decode, PLC, FEC, DTX, repacketizer, packet parsing, and multistream flows.
3. Only after that, attack zero-test source files directly.

This is the only path that has a realistic chance of producing large coverage gains quickly.

## Stage 1 Result

After turning existing harness flows into real tests and adding focused source-side tests in selected low-coverage modules, the coverage moved to:

- Lines: 77.41% (22,975 / 29,678)
- Functions: 79.32% (1,197 / 1,509)
- Regions: 78.63% (34,121 / 43,394)
- Branches: 65.69% (3,655 / 5,564)

That is a large improvement from the original baseline:

- Lines: +28.46 points
- Functions: +15.01 points
- Regions: +28.27 points
- Branches: +30.59 points

The biggest wins came from:

- executing the `mdopus-compare` harness as real smoke tests
- new tests in `src/celt/lpc.rs`
- new tests in `src/celt/ec_ctx.rs`
- new tests in `src/celt/rate.rs`
- new tests in `src/silk/common.rs`
- new scenario tests in `src/opus/encoder.rs`

## Stage 2 Result

After the next targeted batch in DNN and multistream, the coverage moved again to:

- Lines: 85.48% (26,053 / 30,478)
- Functions: 85.95% (1,352 / 1,573)
- Regions: 86.35% (38,592 / 44,690)
- Branches: 70.35% (3,920 / 5,572)

That is another real step forward from Stage 1:

- Lines: +8.07 points
- Functions: +6.63 points
- Regions: +7.72 points
- Branches: +4.66 points

The biggest Stage 2 wins came from:

- broad branch-driving tests in `src/dnn/core.rs`
- zero-weight model execution tests in `src/dnn/fargan.rs`
- deferred-frame and PLC bookkeeping tests in `src/dnn/lpcnet.rs`
- multistream construction, packet parsing, and roundtrip tests in `src/opus/multistream.rs`

This stage improved the shape of the repo, not just the total. DNN is no longer a near-empty hole, and multistream moved from weak to respectable. The remaining gap is now concentrated in the larger state machines and packet logic rather than in obvious test-desert modules.

## Current Post-Stage-2 Priorities

The repo is in a much healthier place, but 98% is still a long way off. The next targets by missed lines are now:

| File | Missed Lines | Line % | Missed Branches | Branch % |
|---|---:|---:|---:|---:|
| `src/silk/encoder.rs` | 1,059 | 82.21 | 345 | 66.63 |
| `src/dnn/lpcnet.rs` | 633 | 58.60 | 71 | 37.72 |
| `src/opus/encoder.rs` | 554 | 74.62 | 246 | 64.86 |
| `src/opus/decoder.rs` | 270 | 76.50 | 118 | 63.35 |
| `src/opus/repacketizer.rs` | 267 | 80.12 | 186 | 54.85 |
| `src/opus/multistream.rs` | 259 | 84.67 | 98 | 71.68 |
| `src/silk/decoder.rs` | 228 | 86.70 | 136 | 63.44 |
| `src/silk/common.rs` | 201 | 78.46 | 44 | 71.79 |

The risk profile has shifted:

- The deepest remaining hole is now `silk::encoder`, by a wide margin.
- `dnn::lpcnet` is still weak enough to justify another direct unit-test pass.
- The most attractive branch-yield targets now sit in Opus packet/control code:
  - `opus::encoder`
  - `opus::decoder`
  - `opus::repacketizer`
- `opus::multistream` is no longer a crisis, but it still has enough cold branches to justify cleanup if Stage 3 stalls.

At this point, the most efficient route to 98% is likely:

1. targeted `silk::encoder` and `opus::encoder` matrix tests
2. packet/decode branch closure in `opus::decoder` and `opus::repacketizer`
3. another narrow unit-test pass in `dnn::lpcnet`
4. branch-directed cleanup in `silk::decoder`, `silk::common`, and `opus::multistream`

## Stage 3 Result

After the next coordinated batch across SILK, Opus packet/control code, and LPCNet, the coverage moved to:

- Lines: 86.81% (26,869 / 30,951)
- Functions: 88.06% (1,416 / 1,608)
- Regions: 87.57% (39,923 / 45,588)
- Branches: 71.83% (4,004 / 5,574)

Delta from Stage 2:

- Lines: +1.33 points
- Functions: +2.11 points
- Regions: +1.22 points
- Branches: +1.48 points

The Stage 3 work covered:

- more SILK encoder control and filter-transition branches in `src/silk/encoder.rs`
- Opus encoder control, helper, and live encode branches in `src/opus/encoder.rs`
- self-delimited, invalid-shape, and padding cases in `src/opus/repacketizer.rs`
- packet-parse, LBRR, FEC fallback, and wrapper paths in `src/opus/decoder.rs`
- additional PLC/FEC and synthesis-delay branches in `src/dnn/lpcnet.rs`

This stage was still worthwhile, but the return per test is now much smaller. That is the expected shape once the big easy holes are gone. The remaining gap is mostly dense decision logic inside large state machines.

## Current Post-Stage-3 Priorities

The repo is still far from 98%. The next targets by missed lines are now:

| File | Missed Lines | Line % | Missed Branches | Branch % |
|---|---:|---:|---:|---:|
| `src/silk/encoder.rs` | 965 | 84.04 | 332 | 67.89 |
| `src/dnn/lpcnet.rs` | 590 | 62.85 | 62 | 44.64 |
| `src/opus/encoder.rs` | 487 | 78.75 | 225 | 67.86 |
| `src/opus/multistream.rs` | 259 | 84.67 | 98 | 71.68 |
| `src/opus/repacketizer.rs` | 238 | 83.05 | 175 | 57.52 |
| `src/silk/decoder.rs` | 228 | 86.70 | 136 | 63.44 |
| `src/silk/common.rs` | 201 | 78.46 | 44 | 71.79 |
| `src/opus/decoder.rs` | 176 | 86.45 | 97 | 70.25 |
| `src/dnn/pitchdnn.rs` | 157 | 55.40 | 0 | 100.00 |

The risk profile has shifted again:

- `silk::encoder` is still the dominant line and branch hole.
- `dnn::lpcnet` remains under-covered enough that more direct unit tests are still justified.
- `opus::repacketizer` has become a branch-heavy cleanup target rather than a line-heavy one.
- `dnn::pitchdnn` is now a plausible quick-win file because it has low line coverage but no branch debt.

At this point, the most efficient route to 98% is likely:

1. another focused pass on `silk::encoder`
2. line-heavy cleanup in `dnn::lpcnet` and `dnn::pitchdnn`
3. branch-heavy cleanup in `opus::repacketizer`, `silk::decoder`, and `opus::decoder`
4. only then return to `opus::multistream`, `silk::common`, and any stubborn CELT branch gaps
