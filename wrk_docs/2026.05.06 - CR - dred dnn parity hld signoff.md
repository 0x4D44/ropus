# Code Review - DRED/DNN Parity Gate HLD Signoff

Date: 2026.05.06

Scope: supervisor review of `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - dred dnn parity gate.md`.

## Decision

Signed off for implementation.

The revised HLD now makes the testing contract precise enough to implement:

- Default runs remain asset-tolerant and report-only.
- `--quick --release-preflight` remains a core smoke profile and explicitly does not claim neural/DRED coverage.
- Non-quick `--release-preflight` claims core plus neural/DRED gate coverage and must banner-block when required DNN/DRED assets are absent.
- The reporting surface must not continue saying only `core`/`C1 core` when neural assets are banner-blocking.
- `preflight::capture` must receive full CLI policy context, preferably through a small derived policy enum, not only a boolean.

## Signoff Conditions

Implementation is approved only within this scope:

- Change `full-test` policy/reporting/help/tests as needed.
- Update `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`.
- Add focused unit tests using tempdir-simulated assets.
- Keep `harness-deep-plc` DRED test code unchanged.
- Keep codec implementation files unchanged.
- Keep exact DRED payload-byte parity ignored as named debt.
- Do not implement DRED PCM reconstruction.

## Expected Validation

The implementation agent should run the focused `full-test` unit checks affected by the policy/report changes, plus formatting. A later review agent should rerun the same checks and inspect the diff against this signoff.

## Residual Risk

This slice improves release-gate honesty. It does not prove full DRED payload parity, recovered PCM correctness, or broad neural model quality. Those remain separate debts and must stay visible in the oracle matrix.
