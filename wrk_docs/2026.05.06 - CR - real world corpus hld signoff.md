# Code Review - Real World Corpus Gate HLD Signoff

Date: 2026.05.06

Scope: supervisor review of `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - real world corpus gate.md`.

## Decision

Signed off for implementation with constraints.

The HLD correctly identifies the current false-green risk: `corpus_diff`
protects the totally empty directory case, but skipped-only candidate sets can
still report success, and `full-test` does not currently make or deny a
real-world corpus claim.

## Required Implementation Contract

Implementation is approved for a narrow slice:

- Harden `corpus_diff` or its full-test wrapper so a corpus claim cannot pass
  with zero decoded-and-compared required entries.
- Add visible corpus claim/reporting to `full-test`.
- Non-quick `--release-preflight` may claim corpus coverage only if the
  required generated non-reference smoke is actually decoded and compared
  against the C reference.
- Quick `--release-preflight` must explicitly make no corpus claim.
- Default/non-release runs must remain report-only.
- Use existing checked-in WAV fixtures as source material.
- Use FFmpeg native `-c:a opus` for the first non-reference generated smoke.
- If FFmpeg/native-opus is unavailable and non-quick `--release-preflight`
  claims corpus coverage, the result must be non-green rather than a pass.
- Reports must state the limited oracle: exact PCM parity against the C
  reference for supported Ogg family-0 packet decode only.

## Asset And Provenance Constraints

- Do not commit `.opus`, `.ogg`, or `.webm` binaries in this slice.
- Do not add third-party media.
- Do not add trust-on-first-use network downloads.
- Do not admit `TBD` checksums into any gate.
- A tracked manifest/provenance document is acceptable, but generated media
  must remain local/temporary/ignored unless Arthur separately approves a
  measured binary exception.

## Scope Boundaries

Allowed implementation areas:

- `harness/src/bin_inner/corpus_diff.rs`
- `harness/src/bin/corpus_diff.rs` if needed
- `full-test/src/*` for corpus policy/reporting
- `tests/vectors/real_world/README.md`
- `tests/vectors/real_world` metadata files
- `tools/fetch_corpus.sh` or a new provisioning helper if needed
- `wrk_docs/testing-oracle-matrix.md`
- implementation/review journals

Forbidden in this slice:

- codec implementation changes,
- WebM/Matroska parsing,
- seek/output-gain/pre-skip/granule-position/FEC/PLC semantics,
- CI provider configuration,
- default `cargo test` or default `full-test` failing because the corpus is
  absent.

## Validation Expectations

The implementation agent should add focused tests for:

- empty-candidate directories,
- skipped-only directories,
- at least one decoded-and-compared supported Ogg entry,
- default/quick/non-quick `full-test` corpus claim policy,
- JSON/HTML/report wording for claimed vs unclaimed corpus coverage.

Run focused `ropus-harness` and `full-test` tests plus formatting. If FFmpeg is
available locally, also run the generated-smoke path; if it is not available,
document the missing-tool behavior and ensure the tests cover the policy.

## Residual Risk

This slice creates a small generated non-reference smoke, not a curated
real-world corpus. It should not be marketed as broad end-user playback
coverage.
