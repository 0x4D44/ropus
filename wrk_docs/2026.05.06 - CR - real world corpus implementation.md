# Code Review - Real World Corpus Implementation

Date: 2026.05.06

Scope: independent review of Lagrange's uncommitted real-world corpus gate
implementation against:

- `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - real world corpus gate.md`
- `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - real world corpus hld signoff.md`
- `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - corpus gate assessment.md`

## Findings

No Blocker or Issue findings.

Optional: `/home/md/language/ropus/full-test/src/corpus.rs:477` - The
generated-smoke success path still depends on local FFmpeg plus a live
`cargo run -p ropus-harness --bin corpus_diff` invocation. The unit tests cover
the no-claim modes, missing-manifest branch, missing-FFmpeg branch, native-opus
probe parsing, and `CORPUS_DIFF_SUMMARY` parsing, but this sandbox cannot
exercise the full pass path because FFmpeg is not installed. This is acceptable
for the signed slice because missing FFmpeg fails the non-quick release claim
honestly; a later hardening pass could inject the command runner/generator so
the success branch is unit-testable without local media tooling.

## Scope Match

The implementation matches the signed HLD/signoff scope.

- `/home/md/language/ropus/full-test/src/corpus.rs:207` selects the required
  three modes: default report-only, quick release no-claim, and non-quick
  generated smoke.
- `/home/md/language/ropus/full-test/src/corpus.rs:337` makes missing FFmpeg or
  missing native `opus` a failing generated-smoke outcome.
- `/home/md/language/ropus/full-test/src/corpus.rs:477` runs `corpus_diff`
  against a temporary generated corpus directory rather than committed media.
- `/home/md/language/ropus/tests/vectors/real_world/corpus_manifest.toml:7`
  adds manifest metadata only; no `.opus`, `.ogg`, or `.webm` binaries are
  added.
- `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md:34` records the
  limited exact-PCM oracle and explicit non-claims.

I saw no codec implementation changes, WebM/Matroska parsing, network
downloads, third-party media, or broad player/container-semantics claims in the
target diff.

## Contract Checks

Skipped-only and zero-decoded corpus runs are non-green. The harness summary
contract now treats zero decoded-and-compared files as exit 1 at
`/home/md/language/ropus/harness/src/bin_inner/corpus_diff.rs:369`, with
zero-audio tracked separately at
`/home/md/language/ropus/harness/src/bin_inner/corpus_diff.rs:379`.

Default/no-release remains report-only:
`/home/md/language/ropus/full-test/src/corpus.rs:217` records manifest status
without setting `claimed=true` or banner-failing.

Quick release-preflight makes no corpus claim:
`/home/md/language/ropus/full-test/src/preflight.rs:59` and
`/home/md/language/ropus/full-test/src/corpus.rs:207` both state the quick
profile as no neural/DRED or real-world corpus claim.

Non-quick release-preflight claims and fails appropriately:
`/home/md/language/ropus/full-test/src/preflight.rs:65` marks the generated
corpus gate as claimed, `/home/md/language/ropus/full-test/src/corpus.rs:337`
fails missing FFmpeg/native-opus provisioning, and
`/home/md/language/ropus/full-test/src/banner.rs:93` promotes a claimed corpus
failure into the final FAIL banner.

Reports are honest about the limited oracle. HTML renders the corpus oracle
text at `/home/md/language/ropus/full-test/src/html.rs:640`, JSON exposes the
corpus stage at `/home/md/language/ropus/full-test/src/report.rs:31`, and the
README narrows the claim to supported Ogg family-0 packet decode at
`/home/md/language/ropus/tests/vectors/real_world/README.md:43`.

Version-field rule: no versioned files are modified in this slice, so there is
no missing version bump to request.

## Validation

Passed:

- `timeout 120 cargo test -p ropus-harness corpus`
- `timeout 120 cargo test -p full-test corpus`
- `timeout 120 cargo test -p full-test preflight`
- `timeout 120 cargo test -p full-test banner`
- `timeout 120 cargo test -p full-test report`
- `timeout 120 cargo test -p full-test html`
- `timeout 120 cargo test -p full-test cli::tests`
- `timeout 120 cargo fmt --check`
- `timeout 30 git diff --check`

Environment check:

- `timeout 30 bash -lc 'command -v ffmpeg'` exited 1, confirming FFmpeg is not
  installed here. The live generated-media success path was therefore not run.

## Review Outcome

No required feedback application is needed. The optional testability note can
be handled in a later hardening slice.
