/target
/reference
/logs
__pycache__
*.pyc
.claude/
.codex-home/
.codex-tmp/
.wrk_validate/
.wrk_validate*.patch
.wrk_cov*/

# Debug trace/output artifacts
/*.txt
notes/*.txt
notes/*.log
/*.log
/build.rs

# Mutation testing output directories (cargo-mutants runs)
/mutants_*/
/mutants.out*/

# Fuzz build artifacts (keep crashes/, fuzz_targets/, Cargo.*)
/tests/fuzz/target/
/tests/fuzz/corpus/

# Transient fuzz panic-capture dir (crash artifacts saved by Windows hook)
/fuzz_crashes/

# Real-world corpus binaries (size + redistribution ambiguity).
# README.md stays committable; fetch script pulls into this directory.
/tests/vectors/real_world/*.opus
/tests/vectors/real_world/*.ogg
/tests/vectors/real_world/*.webm

# foobar2000 SDK (not redistributable; tools/fetch-fb2k-sdk.ps1 fetches it).
/fb2k-ropus/sdk/

# CMake build output for the fb2k-ropus C++ component.
/fb2k-ropus/build/

# IETF RFC 6716 / RFC 8251 test vectors (~71 MB; fetched by
# tools/fetch_ietf_vectors.sh / .ps1 and used by the conformance
# `ietf_vectors` test).
/tests/vectors/ietf/
