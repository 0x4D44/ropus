[package]
name = "capi"
version = "0.2.2"
edition = "2024"
license = "BSD-3-Clause"
publish = false

[lib]
name = "mdopus_capi"
crate-type = ["staticlib", "rlib"]

[dependencies]
ropus = { path = "../ropus" }

[build-dependencies]
cc = "1"
