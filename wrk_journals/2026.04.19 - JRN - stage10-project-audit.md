# 2026-04-19 — JRN — Stage 10 project audit close-out

**Date.** 2026-04-19
**Role.** Close-out feedback-applier (Stage 10).
**Supervisor verdict.** Ship Stage 10; apply only the items listed below.

## Stream journals consolidated here

- Stream A (Stages 1-5, infrastructure + CTL plumbing):
  `wrk_journals/2026.04.19 - JRN - stage10-audit-stream-A.md`
- Stream B (Stages 6-7, codec-heavy ports):
  `wrk_journals/2026.04.19 - JRN - stage10-audit-stream-B.md`
- Stream C (Stages 8-9, DRED + platform-SIMD deferral):
  `wrk_journals/2026.04.19 - JRN - stage10-audit-stream-C.md`
- Stream D (Adversarial, Stages 1-9 integration-gap scan):
  `wrk_journals/2026.04.19 - JRN - stage10-audit-stream-D.md`

## Consolidated per-stage verdict

| Stage | Scope | Verdict | Notes |
|-------|-------|---------|-------|
| 1 | Ambisonic / projection matrices | green | 5 orders × roundtrip all byte/sample-identical. |
| 2 | CVBR padding verification | green | Test-only stage; oracle confirms spread narrowing + peak cap. |
| 3 | Real-world bitstream corpus | green (scaffolded) | Diff mechanism live; curated list deferred per HLD. |
| 4 | Fuzzer cadence operationalisation | green | `tools/fuzz_cycle.sh` + README ship; no CI dependency by design. |
| 5 | Independent CTL plumbing | green | 5 CTLs round-trip; `ropus` 0.5.1→0.6.0. |
| 6 | Analysis / tonality detection | tier-2 | Bit-exact at the module layer; ~5% byte-drift on music encoding accepted (0.000 dB PCM SNR delta). |
| 7 | DNN wiring + adjacent CTLs | tier-2 | Weight blob embedded; DEEP_PLC SNR 52.59 dB at the 50 dB gate (calibrated against a 42.33 dB classical ceiling). |
| 8 | DRED encode/decode-parse + decode-process | tier-1 (payload) | Range-coded payload byte-exact; PCM reconstruction deferred (see follow-up HLD). |
| 9 | Platform SIMD | green (doc-only) | Portable `wide` SIMD at C parity; hand-tuned backends explicitly deferred. |

## Settling the `tier2_snr` discrepancy (Change 1)

Stream C reported `dnn_plc_tier2_snr_above_50db` failing at 9.29 dB.
Stream D reported it passing at 52.59 dB. Re-run this pass:

```
mdtimeout 300s cargo test -p ropus-harness-deep-plc tier2_snr
```

Result: **1 passed, 0 failed** (`dnn_plc_tier2_snr_above_50db ... ok`,
1.39 s). Stream C's reading was stale — likely a cold-build state before
the weights blob was regenerated, matching the exact G-7.1 failure mode
Stream B documented: if `reference/dnn/*_data.c` is missing at build
time the blob is silent-empty and tier-2 tests fail at ~9 dB.

No blocker; Stream D's reading is the live reality.

## Actions applied in this pass

1. **Documentation drift (Stream D finding #2).**
   - `ropus/README.md:55-67`: replaced the "Deferred" block listing
     Analysis / DNN wiring / OSCE+DRED with separate shipped-feature and
     truly-deferred blocks. Analysis tier-2 with 0.000 dB PCM SNR delta;
     DNN wiring for PLC at tier-2 50 dB; DRED encoder + decoder-parse
     shipped with FARGAN reconstruction linked to the new follow-up HLD;
     OSCE and platform-specific SIMD remain explicitly deferred.
   - `README.md:81`: replaced the stale "Deferred; a separate HLD will
     scope it" line for `test_opus_dred.c` with a reference to
     `dred-port.md` (Stage 8 shipped) and the fargan-dred follow-up HLD
     (open).
   - `CLAUDE.md:10-16`: replaced the "All 26 modules ported and passing
     bit-exact comparison tests" line with a tight tier-1/tier-2
     breakdown — tier-1 on range coder / CELT primitives / integer paths
     / RDOVAE forward pass / DRED range-coded payload; tier-2 SNR-bounded
     on Analysis / LPCNet features / DEEP_PLC / DRED WAV-level.

2. **FARGAN-DRED follow-up tracker (Stream D finding #1 + #3).**
   Created `wrk_docs/2026.04.19 - HLD - fargan-dred-joint-followup.md`
   (~80 lines) covering mission, context, scope (the 3 `OpusDREDDecoder::
   decode*` methods), prerequisites, exit criterion (trip-wire tests in
   `opus/dred.rs` flip from `Err(OPUS_UNIMPLEMENTED)` to successful PCM),
   and the Stage 8.6 drift link.

3. **Stream B small gaps (both applied).**
   - **G-7.1 cold-build ordering trap.** Extended the
     `cargo:warning=` in `ropus/build.rs:61-68` to cite the
     tier-2 SNR test consequence — a cold build without
     `reference/dnn/*_data.c` now warns that
     `ropus-harness-deep-plc::tier2_snr` will fail at ~9 dB with
     silent classical PLC fallback.
   - **G-6.1 Stage 6.4c TODO marker.** Added a `TODO(stage-6-tier-1)`
     comment at `ropus/src/celt/encoder.rs:216-222` naming
     `delayed_intra`, `tonal_average`, and `spec_avg` as the three
     diverging fields and pointing at Stream B's G-6.1 entry for the
     dump-bandE hand-off.

## Items deferred (not applied this pass, by supervisor instruction)

- **Stage 8 adversarial concern #5 (`dred_target_chunks` stale on
  repeated `set_dred_duration` calls).** Documented in Stream D and in
  the Stage 8 adversarial audit; supervisor instruction is to not touch.
  Low-impact (latent; no current caller reconfigures DRED duration
  mid-stream).
- **Stage 8 adversarial concern #6 (DRED integrated test's
  "at-least-one-frame" oracle vs per-frame byte equality).** Tied to
  the Stage 8.6 LPCNet-drift un-ignore path; strengthening criterion
  stays in the test file.
- **Stream D concern #4 (`prop_sequence_invariants` pre-existing
  failure at seed 8969).** Tracked under the encoder/SIMD perf
  workstream per MEMORY.md guidance — not ours to fix, not ours to
  parallel-investigate.
- **Stream A G-3.1 (real-world corpus missing six named encoder
  sources).** Explicit HLD deferral to operator curation.
- **FARGAN-DRED code itself.** Out of scope for Stage 10; lives in the
  follow-up HLD created in action (2) above.

## Verification

```
mdtimeout 60s cargo check -p ropus
mdtimeout 300s cargo test -p ropus-harness-deep-plc
```

Both green (see tail output in the same session). The `tier2_snr`
re-run was Change 1's settling step; `cargo check -p ropus` picks up
the CELT encoder comment addition and the build.rs warning text
change.

## Overall verdict

**Stage 10 ships.** All nine upstream stages land at their respective
tiers; the only surfaced gap with material impact (FARGAN-DRED
decoder PCM reconstruction) now has a tracking HLD, and the two
small Stream B signposting items (G-7.1 cold-build warning, G-6.1
CELT TODO) are applied inline.
