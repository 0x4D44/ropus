# Fuzz sanity gate feedback journal

## 2026-05-06

Read the feedback-application skill, Raman review, Lovelace crash diagnosis,
and signed HLD before editing. The feedback is coherent: quick Rust inventory
had drifted from the shell runner in two places, and the shell replay command
was placing the fixed input after `--`, which turns the committed crash file
into a libFuzzer-side argument instead of cargo-fuzz's fixed-input argument.

Applied a small scoped fix rather than redesigning ownership. The shell runner
still owns cargo-fuzz execution. Rust quick preflight still owns inventory, but
its parser now mirrors the shell grammar more closely and crash counting is
recursive like the shell `find ... -type f -name '*.bin'` path.

Emotional temperature: mildly wary because the repository already had
uncommitted Phase 1.2 implementation work in the same files. I avoided
touching unrelated changes and kept the patch limited to the review issues.

Validation completed:

- `timeout 10 bash -n tools/fuzz_run.sh`: pass.
- `timeout 10 bash tools/fuzz_run.sh --list`: pass, listed 12 targets and 4
  `fuzz_decode` crash files.
- Focused `timeout 120 cargo test -p full-test fuzz --locked`: pass, 14 fuzz
  and related banner tests.
- `timeout 120 cargo test -p full-test --locked`: pass, 167 tests.
- `timeout 300 bash tools/fuzz_run.sh --sanity --target fuzz_decode`: pass,
  `fuzz_decode build=pass crashes=4 replay=pass`.
- `timeout 300 cargo run --release -p full-test -- --quick --release-preflight
  --skip-coverage`: pass, 2271 passed, 0 failed, 6 ignored; report at
  `tests/results/full_test_20260506_071506.html`.
- `timeout 120 cargo fmt --check`: pass.

Emotional temperature after validation: calmer. The key risk was the committed
crash corpus failing for the wrong reason; after the argument-order fix it
passes honestly, which matches the diagnosis and keeps the gate meaningful.
