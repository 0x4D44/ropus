# Journal: Legal Frame Duration Roundtrip Review

Date: 2026.05.06

## 07:48 BST

Started independent code review for Phase 3.3 Slice A. Read the code-reviewer skill and the repository instructions supplied for `/home/md/language/ropus`. I kept the scope review-only and source-diff-only, with source edits explicitly off the table.

I read the signed HLD at `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - legal frame duration fuzz dimensions.md`, then inspected the current uncommitted source changes. The tracked diff only showed `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs`; the new helper and tests are untracked, so I read them directly with line numbers. I also checked that `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_encode_multiframe.rs` and `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_decode_plc_seq.rs` had no diff.

Review impressions: the implementation is small and matches the signed design closely. I was initially cautious about the lower minimum-length guard, but the later parsed `bytes_needed` guard is the real correctness check, so `8 + 40` is appropriate for admitting the minimum i16 mono 2.5 ms case without weakening parse safety. I also checked that the selected `frame_size` flows through Rust/C encode calls, Rust decode requests/capacity, packet sample assertions, decoded sample assertions, and existing oracle checks.

Emotional temperature: focused and calm. Slight irritation at the broad initial AGENTS search hanging longer than useful, but no blocker; I switched to targeted reads and kept the review moving.

Validation run:

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`: passed, 4 tests passed.
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`: passed, 20 tests passed, 1 ignored operator report.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_roundtrip`: passed.
- `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`: passed.

Outcome: no required changes found. I saved the formal review to `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - legal frame duration roundtrip.md`.
