# Stage 10 audit — Stream A (Stages 1-5)

**Date:** 2026-04-19
**Scope:** Infrastructure / harness / CTL plumbing (Stages 1-5 of the
deferred-work-closeout program). Excludes codec ports (Stages 6-9).
**Parent plan:** `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md`
**Execution journal:** `wrk_journals/2026.04.19 - JRN - deferred-work-closeout-execution.md`

## Summary

All five Stream A stages are shipped, test-backed, and mapped to a single identifiable commit each. The parent HLD names all ten priorities in order, so intent is unambiguous. Acceptance met in four cases outright (1, 2, 4, 5) and met-as-documented-scaffolding in one (3, real-world corpus — mechanism live, curation deferred per execution journal). No test failures; no gaps requiring re-work.

## Stage 1 — Ambisonic / projection matrices (P0)

- **Plan identified.** `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md:47-62` (section "1. Ambisonic / projection matrices").
- **Exit criterion.** "New harness target `ropus-compare projection-roundtrip <foa.wav>` encodes + decodes a 4-channel FOA input against the C reference. Byte-for-byte encode match; sample-for-sample decode match. Add one fixture under `tests/vectors/ambisonic/`." (HLD line 57-58). Devils-advocate review extended this to all five orders.
- **Implementation delivered.** Commit `1f43890` — "feat(projection): port ambisonics mapping matrix data". Touched `ropus/src/opus/multistream_tables.rs` (zero-filled arrays replaced with ten matrices ported from `reference/src/mapping_matrix.c:289-996`; now `C:\language\mdopus\ropus\src\opus\multistream_tables.rs` is 715 lines), plus new `harness/src/bin/projection_roundtrip.rs`, `harness/src/bindings.rs` projection CTL additions, five fixtures at `tests/vectors/ambisonic/ambisonic_order{1..5}_100ms.wav`, `tools/make_ambisonic_fixture.rs`.
- **Test evidence.** `cargo test -p ropus --lib projection` — 5 passed. `cargo test -p ropus --lib ambisonic` — 9 passed. I also ran the harness binary directly on two fixtures: `projection_roundtrip.exe ambisonic_order1_100ms.wav` reported `5/5 frames byte-identical` + `5/5 frames sample-identical`; same for `order5_100ms.wav` (36-channel 5HOA). Build clean.
- **Verdict.** ✅ met.
- **Gaps.** None.

## Stage 2 — CVBR verification (P1)

- **Plan identified.** `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md:66-81` (section "2. CVBR padding verification").
- **Exit criterion.** "Green test asserting CVBR output-size behaviour" (HLD line 79). HLD noted flag is *already* wired at `opus/encoder.rs:2452`; task was verification, not implementation.
- **Implementation delivered.** Commit `646fb5c` — "test(encoder): verify CVBR shapes packet size distribution". Touched only `ropus/src/opus/encoder.rs` under existing `#[cfg(test)]`, no production code. Test at `ropus/src/opus/encoder.rs:7961` (`test_cvbr_narrows_packet_size_distribution_vs_vbr`) asserts three properties: spread narrowing, strict peak cap, mean-budget floor (≥ 3/4 of target). The pre-existing `test_vbr_constraint_celt_only_path` at line 5481 confirms the dispatch wiring. CELT consumer sites (`celt/encoder.rs:2265, 2127, 1485, 1548, 2830, 2899-2911`) were traced against the reference and confirmed unchanged.
- **Test evidence.** Both `test_cvbr_narrows_packet_size_distribution_vs_vbr` and `test_vbr_constraint_celt_only_path` pass individually. At 64 kbps / 20 ms / mono the test's own observations: VBR spread 116 vs CVBR spread 52.
- **Verdict.** ✅ met. Verification-only stage executed exactly as the HLD scoped it.
- **Gaps.** None. (Devils-advocate had flagged SILK-only mode as undocumented; journal entry at line 87 confirms resolution — SILK does not consume `vbr_constraint`, it is exclusively forwarded to CELT.)

## Stage 3 — Real-world bitstream corpus (P2)

- **Plan identified.** `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md:85-103` (section "3. Real-world bitstream corpus").
- **Exit criterion.** "`cargo run --bin corpus_diff -- tests/vectors/real_world` returns zero exit when all files decode identically; prints a diff summary on mismatch. Corpus README lists at least: FFmpeg (libopus), FFmpeg (native), WebRTC endpoint capture, `opusenc` from `opus-tools`, one YouTube audio extraction, one WhatsApp voice note sample." (HLD line 97-99).
- **Implementation delivered.** Commit `49baa61` — "feat(harness): corpus-diff scaffolding for real-world Opus decode testing". New files: `harness/src/bin/corpus_diff.rs` (377 lines), `tools/fetch_corpus.sh` (174 lines), `tests/vectors/real_world/README.md`. `harness/Cargo.toml` gained `ogg = "0.9"` + binary registration. `.gitignore` excludes `.opus` / `.ogg` / `.webm` under `real_world/` but keeps the README.
- **Test evidence.** Binary builds clean. `corpus_diff.exe` on an empty directory returns exit code 2 (explicit "populate the corpus" signal, distinct from 0 = all matched, 1 = mismatch). TBD-checksum entries in `fetch_corpus.sh` hard-refuse.
- **Verdict.** ⚠️ met with documented caveat. The mechanism is live and tested; the curated list does NOT yet include FFmpeg-native / WebRTC / YouTube / WhatsApp samples as HLD line 98 specified. The execution journal at line 103 frames this correctly: "Honest ship: diff mechanism is live, scope gap documented rather than swept under the rug." The README at `tests/vectors/real_world/README.md:7-22` flags this explicitly as "ships scaffolding; real-world coverage is gated on corpus population by the operator." Given the HLD's own footnote ("Why no checked-in fixtures. Real-world files are large and redistribution-encumbered"), treating curation as future operator work is consistent with the plan.
- **Gaps.** (G-3.1) The list of six specific encoder sources from HLD line 98 is not yet populated — only opus-codec.org + FATE reference-encoded samples are listed. Noted but not blocking — the HLD anticipated this.

## Stage 4 — Fuzzer cadence (P6)

- **Plan identified.** Primary: `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md:190-202` (section "8. Fuzzer cadence"). The older brief `wrk_docs/2026.04.08 - BRIEF - fuzz testing operationalization.md` sets acceptance criteria for the fuzz infrastructure itself (seed corpus, differential fuzzing, crash corpus, automation). The execution plan folded Stage 4 into the later HLD's scope ("Operational, not code. One shell script plus documentation.").
- **Exit criterion.** "Script runs cleanly against all current targets; README explains triage. No CI changes — run cadence is a human process pre-1.0." (HLD line 200).
- **Implementation delivered.** Commit `2886e73` — "chore(fuzz): operationalize fuzz cadence with weekly cycle script + README". Two new files only: `tools/fuzz_cycle.sh` (216 lines) and `tests/fuzz/README.md` (184 lines). Default duration 600s per target across 9 targets (~1.5h full cycle); `mdtimeout`/`timeout` wrapping; per-target logs under `logs/fuzz_cycle_<ts>/`; EXIT/INT trap; mid-run crash notification. No Rust changes.
- **Test evidence.** Script syntax valid (argument parser emits "ERROR: duration must be a non-negative integer" on bogus input). 9 fuzz targets present under `tests/fuzz/fuzz_targets/` (minus `c_reference.rs` which is a helper). Seed corpora populated with 100s-1000s of inputs (e.g. `fuzz_decode`: 1847 seeds, `fuzz_roundtrip_safety`: 5631 seeds). Crash corpus directories exist at `tests/fuzz/crashes/fuzz_{decode,encode,packet_parse,repacketizer,roundtrip}/`.
- **Verdict.** ✅ met vs Stage 4's HLD-scoped acceptance. The older 2026.04.08 BRIEF had a broader scope (differential fuzzing, configuration coverage expansion); per the `deferred-work-closeout.md` line 192, Stage 4 was explicitly trimmed to cadence-operationalization — any broader scope belongs to earlier workstreams (see fuzz campaigns 1-9 journals).
- **Gaps.** None relative to this stage's scope.

## Stage 5 — Independent CTL plumbing (P7)

- **Plan identified.** `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md:206-228` (section "9. Independent CTL plumbing").
- **Exit criterion.** "Conformance suite exercises each new CTL without returning `OPUS_UNIMPLEMENTED`. Unit tests on the Rust side cover set-then-get round-trips." (HLD line 224). Five specific CTLs listed: `SET/GET_IGNORE_EXTENSIONS` (dec), `GET_IN_DTX` (enc), `SET/GET_VOICE_RATIO` (enc), `SET_LFE` (enc + MS), `SET_ENERGY_MASK` (enc + MS).
- **Implementation delivered.** Commit `469a100` — "feat(ctl): wire five independent CTLs through the capi shim". Touched `capi/src/ctl.rs` (request-code constants + dispatch match arms, see lines 61-92), `capi/src/ctl_shim.c`, `ropus/src/opus/encoder.rs` (new methods at lines 3767/3775/3783/3807/3824), `ropus/src/opus/decoder.rs` (at lines 1542/1547), `ropus/src/opus/multistream.rs` (MS dispatch for SET_LFE + SET_ENERGY_MASK). Versions bumped: `ropus` 0.5.1 → 0.6.0 (minor for new public API), `capi` 0.1.0 → 0.2.0. Devils-advocate found four MUST-FIXes that were applied pre-commit: stereo ENERGY_MASK length uses `21 * channels`, `energy_masking` field added on `OpusEncoder`, multistream dispatch added, version bumped correctly.
- **Test evidence.** `cargo test -p ropus --lib test_ctl_` runs 16 tests — all pass, including `test_ctl_set_lfe_roundtrip`, `test_ctl_set_energy_mask_roundtrip`, `test_ctl_get_in_dtx_drives_through_encode`, `test_ctl_voice_ratio_boundary_with_encode`. `cargo test -p ropus --lib voice_ratio` runs 6 tests — all pass. `cargo test -p ropus --lib ignore_extensions` runs 2 tests — all pass.
- **Verdict.** ✅ met. Every CTL has a Rust-side round-trip test; all pass.
- **Gaps.** None.

## Cross-cutting findings

- **HLD discoverability.** The parent plan for Stages 1-10 is one document: `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md`. Filename does not announce "Stage 1-10"; a `^## \d` grep reveals the full mapping.
- **Version management.** Stages 1 and 5 bumped versions (patch + minor). Stages 2-4 were test-only / operational-only with no public-API change, so no bump was required per CLAUDE.md semver rules.
- **Concurrency hygiene.** The execution journal documents awareness of the concurrent perf workstream on `opus/encoder.rs`; Stage 5 (`469a100`) waited for the perf agent's landing (`3009a6b`, `2c07087`, `ddfd151`). No collisions observed.
- **Pre-existing warning.** `cargo test -p ropus` emits one `unused_mut` warning at `celt/encoder.rs:4924`. Unrelated to Stream A — flag only.

## Gap list

Prioritised, actionable:

1. **(G-3.1, low priority)** `tests/vectors/real_world/` corpus lists only reference-encoded samples; HLD line 98 called for six specific non-reference encoder sources (FFmpeg-native, WebRTC, YouTube, WhatsApp, etc.). Mechanism is live; curating the samples is operator work, explicitly deferred by the execution journal. Action: either (a) land a first genuine non-reference sample (e.g., one ffmpeg `-c:a opus` re-encode of an existing fixture) to prove the corpus is non-empty, or (b) drop the specific encoder list from the HLD and declare "baseline scaffolding" the permanent scope. Current state is honest but the HLD and shipped artefact disagree in letter.
2. **(G-X1, housekeeping)** `ropus/src/celt/encoder.rs:4924` has an `unused_mut` warning. Not Stream A's code — flag for perf-workstream owner.

No gaps on Stages 1, 2, 4, 5.

## Overall verdict (Stream A)

Stages 1-5 are complete: four ✅, one ⚠️ (Stage 3, met-with-documented-caveat); no ❌ or ⏭️; all tests green; the single documentation-level gap (G-3.1) is low-priority and explicitly acknowledged in the execution journal.
