# Legal Frame Duration Stateful Review Journal

## 2026-05-06 14:34 BST

Task: independent code review for Phase 3.3 Slice B, focused on the uncommitted stateful fuzz duration changes.

Read the requested code-reviewer skill, repository instructions, the Slice B HLD, and the uncommitted diff. The diff is scoped to the shared frame-duration helper/tests, `fuzz_encode_multiframe.rs`, `fuzz_decode_plc_seq.rs`, and journals/docs. `fuzz_roundtrip.rs` and `oracle.rs` have no source diff.

Review notes:

- Multiframe selector policy matches the HLD: one shape byte selects both legal duration and `2..=16` frames, with the selected frame size used for input sizing, Rust encode, C encode, and decode sample-count sanity.
- PLC selector policy matches the HLD: duration derives from `drop_mask` high bits, and the same selected `frame_size` is used for encode and both decode API calls.
- Rust PLC output buffers stay at `5760 * channels`.
- The existing C helper internally allocates `frame_size * channels`, because its API takes only the decode `frame_size`. This is not a changed source file and does not weaken the oracle for legal Opus calls, but it is a literal nuance against the HLD phrase about output buffers remaining `5760 * channels`.
- Oracle taxonomy and `oracle.rs` are untouched. No campaign behavior was added.

Validation run:

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`: passed, 7 tests.
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`: passed, 20 passed, 1 ignored.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe`: passed.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`: passed.
- `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`: passed.
- `git diff --check` for touched source files: passed.

Emotional temperature: calm and mildly cautious. The implementation is small and mostly lines up cleanly with the HLD. My only discomfort is the C helper capacity wording: I do not think it is a practical correctness defect for the current target, but I want the review to be explicit so Arthur can decide whether strict literal buffer symmetry matters enough to expand scope.
