# Coverage Campaign Journal

## 2026-04-08

Started from the instruction set Arthur gave for a full coverage campaign instead of a one-off summary. Wrote a fresh detailed review and an updated staged plan under `wrk_docs`, both based on a current `cargo +nightly llvm-cov --branch --json --summary-only` run rather than stale notes.

Three worker commits landed cleanly on `master`:

- `80a2782` for `src/types.rs`
- `934b732` for `src/opus/repacketizer.rs`
- `03367dc` for `src/dnn/lpcnet.rs`

Current verified totals after those commits:

- lines: `88.77%`
- functions: `90.85%`
- regions: `89.62%`
- branches: `73.52%`

The branch number is still the hard truth. We are not “close to 98” in any honest sense. The next real work is in `opus::encoder`, `silk::encoder`, decoder-side PLC/FEC cleanup, and whatever can be harvested from smaller branch-heavy helpers without colliding with other agents.

Emotional temperature: clear-eyed, not discouraged, but the target is still aggressive enough that pretending otherwise would be dishonest. The work is moving, just not on a magical slope.

Later checkpoint after more worker commits and one local decoder batch. New commits landed for LPCNet helper coverage and SILK decoder coverage, and I added a small `opus::decoder` batch for wrapper error edges, PLC chunking, and empty-packet handling. Fresh verified totals from `cargo +nightly llvm-cov --branch --summary-only` are:

- lines: `89.29%`
- functions: `91.53%`
- regions: `90.13%`
- branches: `73.90%`

That is real movement, but it still proves the same point: the repo is now bottlenecked by a handful of hard files rather than broad neglect. `opus::encoder` and `silk::encoder` remain the obvious next expensive targets. `opus::repacketizer`, `silk::decoder`, and `opus::decoder` are still productive, but each batch there buys less than earlier stages.

Emotional temperature: still steady. The test loop is solid now, which matters. The bad news is that the slope toward `98%` is still brutally shallow.

Local follow-up in `src/silk/decoder.rs`: added deterministic tests for stereo predictor decoding, the mid-only flag, `MS->LR` interpolation and steady-state behavior, and the private down-FIR interpolation branches. First pass had two overconfident assertions; fixed them instead of pretending the helpers promised more than they do.

Current verified totals after that decoder batch:

- lines: `89.22%`
- functions: `91.46%`
- regions: `90.08%`
- branches: `73.73%`

`silk::decoder` itself is now at `92.80%` line coverage and `69.25%` branch coverage. Useful progress, but the campaign is still bottlenecked by `opus::encoder` and `silk::encoder`.

Emotional temperature: still disciplined. The curve is flatter now, but it is moving in the right direction.

Another pass in `src/opus/repacketizer.rs`, this time focused on extension iteration and direct parse/generate error paths rather than frame concatenation. Added tests for `frame_max`, invalid frame-separator increments, parse buffer-too-small behavior, and generate-time bad-arg / too-small-buffer cases.

Current verified totals after that repacketizer batch:

- lines: `89.29%`
- functions: `91.53%`
- regions: `90.13%`
- branches: `73.90%`

`opus::repacketizer` itself is now at `89.78%` line coverage and `66.91%` branch coverage. Better, but still branch-heavy enough that it is not finished.

Emotional temperature: still sober. Progress is real, but the repo is nowhere near 98% branch coverage and the remaining debt is concentrated in the hardest files.

Later checkpoint after a local `src/dnn/lpcnet.rs` helper pass. Added deterministic tests for complex helpers, inverse band energy, interpolated gains, cepstrum-to-LPC weighting, float energy/inner-product helpers, FIR/pitch-xcorr helpers, and preemphasis/biquad state updates. The first attempt had bad expected values in three assertions, which is irritating but ordinary; fixed those instead of papering over them.

Current verified totals after that batch:

- lines: `89.03%`
- functions: `91.34%`
- regions: `89.88%`
- branches: `73.63%`

`dnn::lpcnet` itself moved to `74.11%` lines and `58.62%` branches. Real progress, but the total branch metric is still dominated by the same hard files: `opus::encoder`, `silk::encoder`, `opus::repacketizer`, `silk::decoder`, and `opus::decoder`.

Emotional temperature: still steady. The repo is healthier than when this started, but 98% remains a campaign, not a quick closeout.
