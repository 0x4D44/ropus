# 2026.05.05 - JRN - testing-review

## Initial Brief
- Arthur asked for an agent-team review of testing in the repo.
- Deliverable: write a report to `wrk_docs` named `YYYY.MM.DD - <desc>.md`.
- Need to identify gaps, poorly tested areas, likely bug locations, worries, and improvement opportunities.
- Include a devil's advocate perspective that challenges assumptions and the other review work.

## Working Notes
- Read the workspace `AGENTS.md`; testing truth source is `mdopus-compare`, with unit tests inline in `src/`.
- There is an unrelated untracked journal from another task; leaving it untouched.

## Emotional Temperature
- Clear task, but the risk is shallow inventory masquerading as insight. I need the report to point at concrete failure modes and verification blind spots, not just count tests.

## Agent Findings Incorporated
- Harness/parity audit found two serious gate-quality issues: length-mismatch tails can compare equal if the missing values are zeros, and headline `ropus-compare encode|decode|roundtrip` commands print mismatch output without returning non-zero.
- Fixture/fuzz/CI audit found stale fuzz target lists in scripts and docs: active `tests/fuzz/Cargo.toml` has 12 bins, but the shell runners still target old names and omit newer CTL, DNN, multistream, and PLC-sequence targets.
- Unit/deep-code audit found DRED/DNN weak spots: ignored DRED payload-byte parity, weight-dependent tests that return early on empty blobs, explicit DRED reconstruction stubs, and narrowed PLC/recovery oracles.
- Devil's advocate challenged the confidence narrative: no enforced CI gate, asset-dependent soft skips, weakened oracles, and coverage tests that often prove survival/shape rather than parity.

## Local Validation Notes
- `cargo test -- --list` completed and showed the workspace currently exposes a large test inventory: `ropus` alone listed 1866 library tests, plus conformance, harness, CLI, fb2k, deep PLC, and tools-core tests.
- `cargo llvm-cov --branch --summary-only -p ropus --lib -- --test-threads=1` failed on stable because branch coverage requires nightly.
- `cargo +nightly llvm-cov --branch --summary-only -p ropus --lib -- --test-threads=1` passed: 1866 passed, 0 failed, 0 ignored. Summary: 97.18% line coverage, 87.40% branch coverage for `ropus`.
- Coverage weak spots from that scoped run: `dnn/dred.rs` 84.60% lines / 50.00% branches; `opus/dred.rs` 76.47% lines / 36.84% branches; `opus/extensions.rs` 0.00% lines; `opus/analysis.rs` 78.89% branches; `opus/soft_clip.rs` 75.47% branches; `celt/fft.rs` 71.43% branches; `celt/mdct.rs` 72.50% branches.

## Emotional Temperature Update
- More worried about gate integrity than raw test volume. The repo has a lot of serious testing work, but several important paths can appear green without actually exercising the oracle claimed by the docs.

## Report Written
- Saved final report to `wrk_docs/2026.05.05 - testing gaps review.md`.
- Main thesis: testing breadth is strong, but release confidence needs oracle-strength labeling and harder preflights.
- Strongest concrete risks surfaced:
  - `ropus-compare` comparison helpers and exit codes can make failures look green to automation.
  - Fuzz runner scripts are stale relative to `tests/fuzz/Cargo.toml`.
  - `reference/` and DNN weights absence can downgrade or skip important oracle-backed tests.
  - DRED payload parity/reconstruction and full decode24 PLC parity remain weak spots.
  - Real-world corpus and CI enforcement are not in place.

## Final Emotional Temperature
- Satisfied with the report's honesty. I still dislike how easy it is for this project to say "covered" when the actual oracle is smoke-only or sample-count-only, but that is now named clearly enough to act on.
