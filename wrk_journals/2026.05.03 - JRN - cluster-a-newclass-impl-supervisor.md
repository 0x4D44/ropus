---
title: Cluster A NEW class — Phase C implementation supervisor journal
date: 2026-05-03
supervisor: Claude (Opus 4.7, 1M context)
parent_hld: wrk_docs/2026.05.03 - HLD - multistream-encode-bytes-divergence-V2.md
parent_supervisor: wrk_journals/2026.05.02 - JRN - encoder-divergence-fixes-supervisor.md
---

# Cluster A NEW class — Phase C implementation supervisor journal

Implementation of HLD V2 (`wrk_docs/2026.05.03 - HLD - multistream-encode-bytes-divergence-V2.md`,
sign-off Q1=a, Q2=full-vectors, Q3=yes, Q4=in-scope, Q5=18h cap, Q6=a). Goal: localise and close
the multistream-encode-bytes-divergence class (5 repros, all mono SILK NB family=0, first
divergence at boundary 6 of V1 Phase B grid).

## Mandate

- Supervisor protects own context — no testing/debugging directly. All work via agents.
- Agent team: implementation → `/code-reviewer` → `/code-review-apply-feedback` → devil's-advocate
  per stage. Devil's-advocate explicitly challenges the impl + review + apply chain.
- TDD where applicable (the 5 repros are the oracle for the fix; positive control via
  closed multiframe regression seeds per Q3).
- Quality bar: CLAUDE.md "minimal code, no premature abstraction, alignment over speed."
- Journal updated to disk regularly. Commit after each stage.

## Stage plan

| # | Stage | Depends on | Agent type |
|---|---|---|---|
| 1 | Harness C-side trace plumbing (new traced encode_frame_FIX.c, debug_silk_trace.c push_payload, build.rs, bindings.rs, silk_enc_api_traced.c retarget) | — | impl + review + apply + devil's |
| 2 | Rust-side `silk_trace::Tuple` extension (`payload: Vec<i32>`, `iter: i32`, `push_payload` helper) | — (parallel with 1) | impl + review + apply + devil's |
| 3 | 10 trace-push call-sites inside `silk_encode_frame_fix` per §4.1 payload schema | 2 | impl + review + apply + devil's |
| 4 | `fuzz_repro_diff` print-path: per-boundary payload decode + named-column print + first-divergent-element highlight | 1, 2 | impl + review + apply + devil's |
| 5 | Run Phase C diagnostic on 5 multistream repros + 4 closed multiframe regression seeds (positive control). Capture report, journal findings. | 1-4 | impl agent (data gathering only) |
| 6 | Production-source fix per Phase C findings (anti-fix-creep §5.6 — per-repro triage) | 5 | impl + review + apply + devil's |
| 7 | Validation matrix per §6 (5 repros byte-equal, fuzz_encode clean, multiframe seeds clean, cargo test, 1h smoke fuzz_multistream) | 6 | impl agent |
| 8 | Migrate repros to permanent seeds, delete known_failures dir, version bump | 7 | impl agent |
| 9 | Completeness audit: implementation vs HLD V2 plan; iterate until 100% match | 8 | audit agent |

## Decision log

### 2026-05-03 ~03:50 BST — Supervisor journal opened

Stages 1 + 2 dispatched in parallel (independent: harness C side vs ropus Rust side).

### 2026-05-03 ~04:10 BST — Stage 1 + 2 agents back

**Stage 1 (`a2365cd835708d1d1`)** — landed `harness/silk_encode_frame_FIX_traced.c` (659
lines, both externals + LBRR helper renamed `_traced`), extended `debug_silk_trace.c` with
parallel payload ring (64 records × 384 i32s ≈ 96 KB static), wired `build.rs` and
`bindings.rs`, retargeted `silk_enc_api_traced.c`. Build clean, all 22 c_ref_differential
tests pass, fuzz_repro_diff still reproduces the V1 boundary-6 finding. One minor mechanical
deviation: had to fix up `fuzz_repro_diff.rs` Copy moves because Stage 2 had to drop `Copy`
from `Tuple` (Vec doesn't impl Copy) — flagged transparently.

**Stage 2 (`a4f88e0fd0316b2d5`)** — extended `silk_trace::Tuple` with
`iter: i32` (default 0) + `payload: Vec<i32>` (default empty). Dropped `Copy`. Single push
function (no separate push_payload — caller fills new fields as needed). Forced to add
`..Default::default()` to the 7 V1 push sites in `silk/encoder.rs` because Rust named-init
is exhaustive — that scope-deviation flagged explicitly. Builds clean both with and without
the feature flag.

Their changes don't overlap (Stage 1 touched harness + `fuzz_repro_diff.rs`; Stage 2 touched
`lib.rs` + the 7 V1 sites in `silk/encoder.rs`). No conflicts.

Code-reviewer + devil's-advocate dispatched in parallel.
