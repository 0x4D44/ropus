# Journal — publish-as-crate implementation

**Date:** 2026-04-18
**Role:** Supervisor (Claude Opus 4.7). Delegating implementation to subagents; not coding/testing directly.
**Plan:** `wrk_docs/2026.04.18 - HLD - publish-as-crate.md`

## Orchestration model

Per stage:
1. **Implement** — one (or parallel) implementation agents. Each gets the HLD + scoped task.
2. **Review in parallel** — `code-reviewer` agent + `devils-advocate` agent. Independent reads.
3. **Apply feedback** — if reviews raise issues, launch an apply-feedback agent with explicit file:line references.
4. **Supervisor gate** — read the diff, decide go/no-go.
5. **Commit** atomic per stage.

## Stages

1. Workspace skeleton + file moves
2. Library cleanup (features, env var, main.rs)
3. Peripheral migration (build.rs paths, fuzz, tooling)
4. Public API (re-exports, stability contract)
5. Metadata, LICENSE, README
6. Verification (MSRV, test, doc, dry-run publish)
7. Completeness audit vs HLD

## Log

### 2026-04-18 — Stage 1 (workspace skeleton + file moves)

**Implementation agent:** delegated. Moved 40 files via `git mv`; wrote `ropus/Cargo.toml` (minimal, features kept for now), `harness/Cargo.toml` (publish=false, 8 binaries), workspace root `Cargo.toml` (resolver=3, 2 members, excludes). Copied `tests/harness/config.h` → `harness/config.h`. `cargo build -p ropus` verified passing.

**Pre-existing in-flight work detected.** Working tree arrived with uncommitted edits on `README.md`, `ropus/src/silk/{common,encoder}.rs`, `harness/src/main.rs`, `harness/build.rs`, `tests/harness/config.h`, `tools/pgo_build.sh`, plus stash `sync-with-origin-1776147702`. Arthur confirmed Option 2: commit only Stage 1's work, leave in-flight mods untouched in the working tree.

**Reviews.** Code-reviewer approved. Devils-advocate caught three issues:
1. `tests/harness/debug_helper.c`, `debug_silk_trace.c` not in HLD move list, but referenced by `harness/build.rs` — **fixed in-stage** with additional `git mv`.
2. `harness/config.h` initially held pre-existing dirty SIMD-enabled content — **fixed in-stage** by overwriting with clean HEAD content of `tests/harness/config.h`.
3. `tools/repro_bug_d.rs` (no `_2`) present but not in HLD move list. Last touched in earlier Bug D campaign; superseded by `_d2`. Leaving in `tools/` for Arthur's decision.

**Known deferred:** `ropus/src/main.rs` stub stays for Stage 2 to delete. All feature flags intact (Stage 2). `harness/build.rs` path literals still reference `tests/harness/` (Stage 3). Fuzz subcrate and tooling scripts still say `mdopus` (Stage 3).

**Commit:** `fa031ab` — staged only my work (renames + new manifests + docs + journal); pre-existing unstaged mods left in working tree.

### 2026-04-18 — Stage 2 (library cleanup + FFI test migration)

**Implementation.** Two-agent sequence. First agent: deleted `ropus/src/main.rs`; simplified `uc!`/`uc_set!`/`uc_mut!` macros to safe indexing; removed `[features]` from `ropus/Cargo.toml`; removed `default-features=false, features=["simd"]` from harness's `ropus` dep; removed `MDOPUS_SKIP_REFERENCE` env var in `harness/build.rs`; swept all `#[cfg(feature = "simd"|"unchecked-indexing"|"dnn")]` gates across ~10 codec files. Grep confirms zero residuals.

**FFI test escalation.** `cargo test -p ropus --lib` failed to link: 6 tests + 1 helper + extern C block used `opus_*` and `silk_LPC_*` symbols. Launched second agent to migrate them to `harness/tests/c_ref_differential.rs` (the harness crate has the C reference linked via build.rs). Zero `pub` visibility bumps needed — all referenced items already `pub`. Library tests now pass 1773/0/0.

**Reviews.** Code-reviewer approved. Devils-advocate raised mixed claims: most "scope creep" was actually pre-existing in-flight work (silk/common.rs iterator rewrites, harness/src/main.rs bench refactor, root Cargo.toml capi additions) — NOT Stage 2. One real catch: `harness/build.rs` and `ropus/src/silk/encoder.rs` had both Stage 2 edits AND pre-existing mods mixed in.

**Untangling.** Third agent: for each mixed file, saved working-tree copy, reverted to HEAD, re-applied only Stage 2 edits, restaged, restored working-tree copy so Set B (pre-existing work) survives as unstaged. Verified staged diff contains only Stage 2 scope. Library tests still pass 1773/0/0.

**Parallel commit landed mid-flight.** `c27089e capi: phase 1 — skeleton` absorbed part of the pre-existing work (capi/, tests/conformance/, workspace-member additions, related HLD/journal). Rebased implicitly; no conflicts with Stage 2.

**Commit:** `5abd1a9` — staged 17 files (pure Stage 2). Set B pre-existing work remains in working tree.

### 2026-04-18 — Stage 3 (peripheral migration)

**Implementation.** Two-agent sequence. First agent executed HLD-listed renames: `harness/build.rs` path literals (`.join("reference")` → `.join("../reference")`, `.join("tests/harness")` → `.join(".")`), `harness/Cargo.toml` bin names (`mdopus-compare` → `ropus-compare`, `mdopus-interframe` → `ropus-interframe`), `tests/fuzz/Cargo.toml` dep rename + package rename + strip obsolete features, 9 fuzz-target imports, 6 tooling scripts. MIXED-file handling on `harness/build.rs` and `tools/pgo_build.sh` preserved pre-existing deltas as unstaged.

**Gap caught.** First agent reported `cargo build` fails on pre-existing Stage 1/2 leftovers; I disagreed — these were Stage 3 scope (harness must build). Escalated:
- `harness/src/main.rs` had 51 `use mdopus::...` call sites + banner strings (pure rename)
- 6 `harness/src/bin/*.rs` had `use mdopus::...` + broken `#[path = "../tests/harness/bindings.rs"]` → `"../bindings.rs"` (interframe.rs had an even-more-broken `"bindings.rs"`)

**Second agent** (fix-up): applied these renames via clean-isolation pattern on the MIXED file `harness/src/main.rs` (save WT → `git checkout HEAD --` → apply renames → `git add` → restore WT). 156-line staged diff (78+/78-, bidirectional rename). All other bin files pure rename.

**Parallel session active during execution.** Added `ropus/src/opus/soft_clip.rs`, `capi/src/{decoder,encoder,lib,ctl}.rs`, `capi/src/ctl_shim.c`, `tests/conformance/*` edits. Mid-session stash/pop conflict resolved — kept parallel-session versions.

**Verification.** `cargo build` (whole workspace): PASS (includes xiph/opus C compile). `cargo test -p ropus --lib`: 1776 passed. `cargo check --manifest-path tests/fuzz/Cargo.toml`: PASS. `cargo test -p ropus-harness` partial: `c_ref_differential` integration test passes (5/0/1-ignored), 6 bin targets compile and have no tests, but 3 smoke tests in `ropus-compare` bin fail with `cannot read tests/vectors/48000hz_mono_noise.wav` — pre-existing bug exposed by the cwd change (manifest dir is now `harness/`, not workspace root). Deferred to Stage 6 verification.

**Residual `mdopus`** in `harness/src/`: zero. Repo-wide: only historical `wrk_docs/`, `wrk_journals/`, `notes/` + project-name mentions in `CLAUDE.md`, `AGENTS.md`, `README.md` + capi/conformance parallel work. None are within Stage 3's rename scope.

**Commit:** 28 files staged (+148/-149). Set B pre-existing work remains unstaged: silk/common.rs, silk/encoder.rs, ropus/opus/mod.rs, harness/src/main.rs bench refactor, harness/build.rs SIMD doc, tools/pgo_build.sh bench tuning, README.md validation section, capi/, conformance/, soft_clip.rs, panic-unwind profiles.


