# Phase 3.4 and 3.1 Supervisor Journal

Date: 2026.05.06
Supervisor: Claude (Opus 4.7, 1M)
Working partner: Arthur

## Mission

Drive Phase 3.4 (real-world corpus policy/provisioning gate) and Phase 3.1 (DRED/DNN
parity diagnostic) to 100% completion using agent teams. Implementation, review, and
feedback application are all delegated. Supervisor provides direction, decisions, and
commits. A devil's-advocate role critiques each major output.

## Workflow per stage

1. Scope (Explore agent) — what exists, what is missing, which files matter.
2. HLD (Plan agent) — implementation-ready design doc with explicit acceptance.
3. Devil's-advocate review of HLD — challenge weak assumptions.
4. Adjust HLD as needed.
5. Implementation (general-purpose agent, TDD where applicable).
6. Code review (code-reviewer skill).
7. Devil's-advocate review of code review — challenge soft findings or missed risks.
8. Apply feedback (code-review-apply-feedback skill).
9. Verification — agent confirms gates green and scope respected.
10. Supervisor commit.

After both stages: comparison agents check implementation against plan; iterate to 100%.

## Inputs

- `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - testing plan next slice after frame durations.md`
  recommends Phase 3.4 corpus policy/provisioning, then Phase 3.1 DRED diagnostic.
- Phase 3.2 PLC recovery drift oracle is already landed (commit `36774f7`).
- Phase 3.3 legal frame duration fuzzing is landed (commit `917f003`).
- Working tree is clean apart from one untracked release journal
  (`wrk_journals/2026.05.04 - JRN - release-and-refuzz.md`), parked.

## Quality bar (from CLAUDE.md)

- Minimize requirements ruthlessly. Every removed requirement collapses complexity.
- Don't predict the future; no premature abstractions.
- Design for testability up front. Oracles encode "what correct looks like".
- Tests are scaffolding; tear them down if they cost more than the confidence they provide.
- Push back honestly. Don't be agreeable just to be nice.

## Stage 3.4 kickoff

Spawning the scoping agent to map the current corpus state and constraints before
any HLD is written.

## Stage 3.4 scoping findings (Explore agent)

Surprise: corpus gate plumbing already landed in commit `d70730f`. Existing artifacts:
- `full-test/src/corpus.rs` (~1100 lines) — gate logic, FFmpeg probe, manifest loader, corpus_diff invocation, mode selection.
- `full-test/src/preflight.rs` — `real_world_corpus` asset policy key.
- `full-test/src/html.rs` and `report.rs` — corpus outcome section.
- `harness/src/bin_inner/corpus_diff.rs` (~159 lines) — Ogg parser + per-file decode compare.
- `tests/vectors/real_world/corpus_manifest.toml` — single required entry: `ffmpeg-native-sine-32k`.
- Oracle matrix has a corpus row with `required-corpus` only under `--release-preflight` non-quick.

So Phase 3.4 isn't "build a gate"; it's "tighten the gate's policy until 'corpus claim' is
honestly falsifiable". Real gaps:

1. Empty-corpus = non-green is *claimed* in the README but not enforced — `corpus_diff`
   exit code 2 ("no candidates") is treated as soft, not as a release-preflight failure.
2. Generated FFmpeg sample is not SHA256-pinned in manifest, so encoder version drift is
   silent.
3. Manifest has one entry; the plan asks for a small pinned corpus (FFmpeg, Ogg edge
   cases — WebRTC/voice note are excluded by licensing).
4. `.webm` candidates are silently skipped; should be loud-defer with documented reason.
5. Source-add policy / licensing sign-off is undocumented.

## Supervisor decisions for the Stage 3.4 HLD

Recording these so the HLD agent doesn't have to re-derive them:

- **Scope: policy/provisioning tightening, not encoder breadth.** No WebRTC capture, no
  voice-note ingestion. Stay inside what the repo can produce or fetch with clear
  licensing.
- **WebM deferred explicitly.** Loud skip — `corpus_diff` must log a clearly visible
  "container deferred" line per skipped file, and the release-preflight summary must
  mention deferred files separately from compared files. No silent skips.
- **Determinism contract.** Each manifest entry gets an `expected_sha256` field
  (optional for entries explicitly marked non-deterministic). Generated entries with a
  pinned hash must match; mismatch is a hard fail with FFmpeg version reported.
  Non-pinned generated entries are allowed but only count toward "smoke", not
  "required-corpus".
- **Empty/degenerate corpus = non-green under release-preflight.** Exit code 2 from
  `corpus_diff` must surface as a preflight failure when corpus claim is required.
  Quick mode and report-only mode keep their current soft behavior.
- **Manifest growth: small and pinned.** Add at most three additional entries: a second
  FFmpeg-native bitrate target (low-bitrate exercise), one Ogg edge case (short
  pre-skip or unusual output_gain — exact form to be measured during implementation),
  and one libopus-encoded baseline used only for parity-against-self (already covered
  by sanity baseline; do not duplicate). HLD should be explicit about not bloating.
- **Source sign-off policy.** A short `SOURCES.md` next to the manifest documenting
  per-source license/origin and the rule that any new source needs Arthur's review
  before commit. Lightweight, no automation required.

These decisions are autonomous because Arthur is offline; they follow the next-slice plan
verbatim where it speaks and pick the conservative option where it doesn't. Any of them
can be revisited at the code review stage.

Dispatching the HLD agent next.

## Stage 3.4 HLD persisted, then devil's-advocate review

HLD lives at `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - corpus policy provisioning.md`.

Devil's-advocate found three real blockers and several real risks. Highlights:

- **Blocker A**: `corpus.rs:667-668` `generate_entry` hardcodes `-b:a 32k` and ignores
  the parsed `generation_command`. With a 24k-mono entry plus another rate corner, the
  SHA256 pin would never match because every entry would be re-encoded at 32k.
- **Blocker B**: `opusenc` writes a vendor tag from the linked libopus version with no
  `--no-comments` flag; SHA256 of `opusenc` output is not stable across builds. The
  HLD's path-(a) recipe for the Ogg-edge fixture cannot deliver a reproducible digest.
- **Blocker C**: `corpus.rs` has no `Command`-injectable seam, so HLD tests 8-11
  ("stub Command execution") are speculative until that seam is designed.

Real risks worth recording:
- FFmpeg `-strict -2` is experimental — the bitstream is allowed to change across
  versions. The HLD's R1 already documents this and we will live with it knowingly.
- Three counters (`decoded_and_compared`, `compared_required_entries`,
  `compared_pinned_entries`) are near-synonyms; rename for clarity.

## Supervisor decisions resolving the blockers

A. **Per-entry generation parameters.** Add `bitrate_kbps: u32` and `channels: u32` to
   the manifest schema and to `ManifestEntry`. `generate_entry` reads them and uses
   structured-arg `ffmpeg` invocation. Keep `generation_command` as a human-readable
   documentation field (no shell-out, no injection surface).

B. **Drop opusenc entirely.** Replace the `ogg-edge-preskip-or-output-gain` entry with
   a second FFmpeg-native pinned entry that exercises a different rate corner — e.g.
   `ffmpeg-native-speech-48k-stereo` using `tests/vectors/speech_48k_mono.wav` (or the
   stereo equivalent if present) at 48 kbps stereo. Same provenance/recipe profile as
   the other two entries, no opus-tools dependency, same R1/R2 risk model. The point
   of the second pinned entry is "catch encoder drift in a different rate corner", not
   "carry an unusual OpusHead field" — the latter framing was too clever for the
   actual gate semantics.

C. **CommandRunner seam.** Introduce `trait CommandRunner { fn run(&self, program: &str,
   args: &[String]) -> std::io::Result<Output>; fn version(&self, program: &str) ->
   std::io::Result<Output>; }`. Default impl `RealCommandRunner` shells out via
   `std::process::Command`. Tests inject `FakeCommandRunner` keyed on `(program,
   args)` returning canned `Output`. Threaded through `detect_ffmpeg`, `generate_entry`,
   and the `corpus_diff` invocation. Public API of `corpus.rs` gains a non-breaking
   overload that defaults to `RealCommandRunner` for the existing call sites.

D. **Counter naming.** Rename `compared_pinned_entries` → `pinned_entries_matched` to
   distinguish from `compared_required_entries`. Rename intent: "matched" is the new
   contract; "compared" is the precondition.

Editing the HLD now to bake these in, then dispatching the implementation agent.

## Stage 3.4 implementation results

Implementation agent reported all 12 acceptance criteria green, +13 tests, all
focused validations passing. Files changed are inside the expected scope.

Notable agent decisions to validate at code review:

1. **Bitexact FFmpeg flags** — agent added `-fflags +bitexact -flags +bitexact` to
   the generation argv after empirically discovering FFmpeg writes a random Ogg page
   serial per run (without them, SHA256 pin is unreproducible by design). This is a
   correctness-critical deviation and the right call; reviewer must validate it.
2. **Source-WAV fallback** — no stereo speech WAV in repo; agent fell back to mono
   speech at 40 kbps per HLD's permitted fallback. But the manifest id was kept
   verbatim as `ffmpeg-native-speech-48k-stereo`, which is now misleading. Rename
   target: `ffmpeg-native-speech-40k-mono`.
3. **Pure-Rust SHA256** — agent added ~80 LOC of pure-Rust SHA256 to keep production
   zero-dep. Reasonable rationale but introduces crypto code that needs review;
   alternative is making `sha2` a runtime dep (already a dev-dep). Reviewer should
   weigh whether the 80 LOC of crypto belongs in production.
4. **`pinned_entries_matched` carried through failure outcomes** — sensible.
5. **`full-test` version bump 0.4.4 → 0.5.0** — correct per project rule for a
   feature addition.
6. **11 pre-existing clippy errors** unchanged — agent verified they pre-date the
   slice. Reviewer should sample one or two to confirm.

Locked artifacts:
- FFmpeg version: `ffmpeg version 7.0.2-static`
- `ffmpeg-native-sine-24k-mono.opus` SHA256: `50c4939f96b3d8b4c19cee3aaeb5f5cd0a0dfba70bf71f21c0fe336bb740ce1b`
- `ffmpeg-native-speech-48k-stereo.opus` (mono 40k) SHA256: `51ae2ebf17b279acd191114565aabec399c0203a164871db2f8efbb661f1d33f`

Dispatching the code-reviewer next.

## Stage 3.4 code review verdict: NO-PASS

Reviewer surfaced a load-bearing defect plus 5 other Required and 5 Important
findings. Empirical evidence: 10 consecutive runs of the locked FFmpeg recipe
produced 10 distinct SHA256 digests; the bitexact flags do NOT stabilize the
Ogg muxer's randomized stream serial on the static 7.0.2 build the agent
locked against. Pin contract is broken by default.

### Supervisor decisions on the review findings

**Required:**

1. SHA256 pin redesign — go with the reviewer's option (a): hash the **Opus
   packet payload bytes only**, after walking Ogg pages and dropping framing
   plus the two header pages (OpusHead + OpusTags). This pins encoder output
   without depending on muxer state or metadata vendor strings. As a side
   effect, drop the `-fflags +bitexact -flags +bitexact` flags entirely
   (their advertised purpose was suppressing vendor strings, but if we don't
   hash them, we don't need to suppress them) and update the comment that
   credits them with stripping page serials (per Suggested #14, that's wrong).
2. Update `generation_command` for entry 1 to match actual argv. Without the
   bitexact flags after step 1, all three `generation_command` strings just
   need to drop them — easier alignment.
3. Add a fixture-directory WebM integration test asserting the
   `^  DEFER .*reason=webm-matroska-container-deferred$` line.
4. Add a test that reads the live `corpus_manifest.toml` and asserts the
   exact set of required IDs.
5. Rename `ffmpeg-native-speech-48k-stereo` to `ffmpeg-native-speech-40k-mono`
   in manifest, README, and journal.
6. Gate `ScriptedRunner`-using tests with `#[cfg(unix)]` for now. Portable
   rewrite is out of scope.

**Important:**

7. Add direct SHA256 test vectors (NIST FIPS-180-2 — empty, "abc",
   multi-block).
8. **Swap inline SHA256 for `sha2` crate.** The reviewer found `sha2` is
   already a transitive workspace dep via `tools/fetch-assets`, so the
   "zero-dep production" rationale is false. Drop ~150 LOC of locally-
   maintained crypto + the shell-out fallback. Add `sha2` to `full-test`'s
   `[dependencies]`.
9. Tighten `max_size_bytes` for the speech entry from 4_000_000 to
   1_500_000 (still > the ~780KB actual file).
10. Add exit code 3 row to README's exit-code table.
11. Add HTML rendering test for `pinned_entries_matched` + `deferred` strings.

**Suggested:** 12 (comment about scripted-runner dispatch routing) and 13
(`failure_outcome` 10th param) are nice-to-have. 14 (bitexact comment lies)
becomes moot once we drop the flag in step 1. 15 (sha256_of shell-out is
dead code) becomes moot once we switch to `sha2` in step 8.

Knock-on effects of redesigning the pin to hash payload bytes only:
- No re-locking against FFmpeg version drift for muxer/metadata changes; pin
  only fires on actual encoder output drift. This is *strictly stronger*
  than the original contract, not weaker.
- The locked digests change: the apply-feedback agent must regenerate them
  from the same FFmpeg version, since they're computed differently.
- The pin is now portable across maintainer machines that have any FFmpeg
  build that produces the same encoder output (which, for the same FFmpeg
  release, is what we want; cross-version drift is still detected).

Dispatching apply-feedback agent next with these decisions baked in.

## Stage 3.4 apply-feedback results

All 11 Required + Important findings addressed. Suggested #13 also done. Empirical
reproducibility check: 5/5 identical payload SHA256 per pinned entry (vs 5/5 distinct
full-file SHA256 confirming the diagnosis). The pin contract is now load-bearing.

New locked digests (payload-only):
- `ffmpeg-native-sine-24k-mono`: `d9b990ee3f2b3fe4d18c413cc550b4e5165d8710935b17f43a15c5f32fb01d39`
- `ffmpeg-native-speech-40k-mono`: `4e81fb84245420e87f7a9f864103d7e02ce35ccddaf3a57ab581b93a8d0bbfa1`

Test count: full-test 216 → 225 (+9), new harness integration test, all green.
Diff scope: all changes within full-test/, harness/, tests/vectors/real_world/, and
oracle matrix. No codec source touched.

## Stage 3.4 committed

`29f54d6 Tighten corpus claim with payload-pinned digests`

14 files changed, +2130 / -95. Stage 3.4 closed.

## Stage 3.1 kickoff: DRED/DNN parity diagnostic

Per the next-slice plan: diagnostic-only. Goal is to isolate where LPCNet feature
drift originates so the existing ignored DRED byte-parity tests have a named
blocker rather than a hand-wave. Not attempting to fix the upstream issue; not
trying to make the ignored test green. Output is an active diagnostic + an HLD
that names the specific upstream blocker.

Dispatching the scoping agent.

## Stage 3.1 scoping findings (Explore agent)

Phase 3.1 sits behind a known LPCNet feature drift blocker. Existing artifacts:

- `harness-deep-plc/tests/dred_encode_payload_diff.rs` — full byte-parity test, currently
  `#[ignore = "Stage 7 LPCNet drift: Ex[]/FFT-path bit-exactness pending"]`. Uses a
  deterministic 440 Hz tone fixture and asserts byte-exact payload via FFI.
- The FFI shim `ropus_test_dredenc_copy_lpcnet_features()` already exposes the C-side
  feature vector — the C side is reachable.
- DRED gate plumbing already landed (commit `85c510a`); a release-preflight key exists.
- Tier-1 byte-exact: range-coder Laplace payload, DRED static quant tables. Tier-2 /
  weakened: LPCNet features and DRED PCM reconstruction.
- **No frame-feature differential test exists.** The pipeline is
  `LPCNet features → RDOVAE → range coder` and only the final byte is gated. Where
  drift originates is invisible.

## Supervisor decisions for Stage 3.1 HLD

Resolving the seven open questions before HLD:

1. **Granularity**: per-frame *feature-vector* drift first. If we can name max abs and
   RMS drift per feature index per frame from a deterministic input, the next
   remediation slice has measurable starting numbers. Latents and state are next-
   slice-of-the-next-slice.
2. **Capture**: in-process drift report (mirrors the PLC drift oracle pattern from
   `harness/tests/c_ref_differential.rs`). No CSV/JSON dump in this slice; if needed
   later, add an opt-in env var. Don't bloat.
3. **Thresholds**: measure-first, lock-to-observed bounds — same discipline as the
   PLC drift slice. Implementation agent records the observed bounds (max abs drift,
   max RMS drift, drifting-frame count, first-divergent-frame index) and pins the
   active oracle exactly there. Don't invent acceptance numbers.
4. **Feature-only sub-test**: yes, this is the heart of the diagnostic. A test that
   compares ropus's LPCNet feature output against C's, with no RDOVAE/range-coder
   involvement, is what proves the blocker is upstream. Use the existing FFI shim.
5. **Release-gate vs report-only**: tiered. The new feature-drift oracle is *active*
   with locked bounds (release-gate behavior — like PLC drift). The existing ignored
   byte-exact test stays ignored as debt with the same comment. The diagnostic
   doesn't claim byte-exact parity; it pins observed feature drift so a regression
   that worsens the drift fires.
6. **Fixture stability**: keep the single 440 Hz tone fixture. Adding signal types is
   future scope. Don't bloat.
7. **Ignored-test relationship**: leave it ignored. Add a new diagnostic test that
   exposes the drift quantitatively and update the ignore comment to point at the
   new diagnostic so the reader sees the measured baseline immediately.

Dispatching the HLD agent.

## Stage 3.1 HLD persisted, then devil's-advocate review

HLD lives at `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - dred lpcnet feature drift diagnostic.md`.

Devil's-advocate verified all structural HLD claims (FFI bindings exist, `pub`
fields are reachable, `-ffp-contract=off` is in effect, the structural template
test exists, the dred-dnn-parity gate already lifted weights to required). No
blockers; five refinements:

1. "Where drift originates" overstated — soften to "drift at the LPCNet
   feature-extraction boundary".
2. Add a per-feature-index max-drift array (length 36) to the report so the
   next remediation slice sees which feature dimension is worst.
3. Add reproducibility check before locking: 3 consecutive `--nocapture` runs
   produce identical primary numerics. Supervisor does NOT have re-lock
   authority on a reproducibility miss.
4. Promote `n_compared_frames >= 4` from Risk #5 to acceptance criterion.
5. Document the drifting-count's bit-exact definition explicitly in the panic
   context so it can't be silently relaxed.

All five baked into the HLD's "Supervisor revisions" section.

Dispatching implementation agent next.

## Stage 3.1 implementation results

All acceptance criteria green. Test-and-docs-only as designed: no production
code or FFI plumbing changed.

Locked numerics:
- `MAX_ABS_FEATURE_DRIFT = 3.2544136e-5` (frame 16, feat 20)
- `MAX_RMS_PER_FRAME_DRIFT = 7.896632e-6` (frame 16)
- `MAX_DRIFTING_FRAME_COUNT = 50` (50/50 frames drift)
- `FIRST_DIVERGENT_FRAME_AT_LEAST = 0`

Per-feature top 3: feat 20 (3.25e-5), feat 21 (3.24e-5), feat 19 (1.86e-5).
Drift concentrated in cepstral cluster (indices 18-21); features 22-35 bit-exact.

This is exactly the "concrete starting numbers" payload the slice was designed
to produce. Next remediation slice knows: target the cepstral cluster, max
drift is ~3.3e-5, every frame drifts.

Reproducibility (Revision C): 3/3 consecutive `--nocapture` runs produced
identical primary numerics + identical per-feature array + identical
first-mismatch tuples.

Lock host: Intel i7-14700KF / x86_64 / WSL2 Ubuntu 24.04 / glibc 2.39 /
rustc 1.95.0 / gcc 13.3.0 / `-ffp-contract=off` enforced.

Files changed:
- New: `harness-deep-plc/tests/dred_lpcnet_feature_drift.rs`
- Edit: `harness-deep-plc/tests/dred_encode_payload_diff.rs:151` (ignore reason)
- Edit: `wrk_docs/testing-oracle-matrix.md` (new active row + class def + cross-ref)
- New: `wrk_journals/2026.05.06 - JRN - dred lpcnet feature drift implementation.md`

Test counts: +5 tests in new file (4 helper + 1 active diagnostic). Existing
DRED suites unaffected. `cargo fmt --check` clean.

Dispatching code-reviewer.
