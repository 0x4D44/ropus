# Legal Frame Duration Stateful HLD Journal

Date: 2026.05.06

## Entry 1

Read `/home/md/language/ropus/AGENTS.md`, the broad legal frame-duration HLD, Slice A helper/tests, `fuzz_roundtrip`, `fuzz_encode_multiframe`, and `fuzz_decode_plc_seq`.

Current state:

- Slice A exists and is already wired into `fuzz_roundtrip`.
- The shared helper is intentionally tiny and covers the six legal Opus durations.
- `fuzz_encode_multiframe` still uses `sample_rate / 50` in both `Arbitrary` and runtime.
- `fuzz_decode_plc_seq` still uses `sample_rate / 50` for encode/input sizing, then passes `5760` as the decode frame-size argument to both Rust and C sequence decode.

Main insight:

The PLC `5760` is not just capacity. In the decode call it is also the requested duration for PLC when the packet is dropped. For Slice B, replacing that requested duration with the selected legal frame size is the right design. The output buffer can remain `5760 * channels` so capacity does not become the thing under test.

Decision:

I recommend one implementation slice for both remaining stateful targets. The changes are parallel and small: derive a selector, compute legal frame size through the shared helper, propagate it through sizing/encode/decode, and add scoped tests/debug context. Splitting further would be cleaner for blame but probably not worth the extra process.

Emotional temperature:

Calm and slightly wary. The helper and roundtrip work make this slice straightforward, but the PLC target has a subtle API semantic trap. I am glad Arthur called out the `5760` point explicitly; otherwise it would be easy to preserve the old call and accidentally fuzz a different dimension.

Blockers:

None.

Next:

Implementation agent should extend helper tests if selector helper functions are added, then update `fuzz_encode_multiframe` and `fuzz_decode_plc_seq` with no oracle taxonomy changes and no fuzz campaigns.
