# Build Cleanup Session - 2026.04.06

## Goal
Get the build compiling with zero warnings/errors, cargo fmt clean, and clippy clean.

## Initial State
- **Build**: Compiles with 15 warnings (unused variables in bands.rs, encoder.rs, test harness)
- **cargo fmt**: 2201 lines of formatting diff
- **clippy**: 13 errors + 1665 warnings
  - Errors: 7 approximate float constants, 6 "always returns zero" operations
  - Warnings: too_many_arguments, assign_op_pattern, unnecessary_cast, manual_clamp, useless_vec, etc.

## Work Log

### Formatting
- [ ] Run cargo fmt to auto-fix all formatting

### Clippy Errors (13)
- Approximate float constants (7): Need to decide approach - these are C-port values that must be bit-exact
- "Always returns zero" (6): Need to investigate if these are real bugs or intentional C patterns

### Build Warnings (15)
- Unused variables in debug state destructuring (encoder.rs) - likely debug helpers
- Unused `lowband_scratch` parameter (bands.rs) - potential missing implementation
- Unused `qab_frame` variable (bands.rs) - potential missing implementation
- Unused `ctx_avoid_split_noise` assignments (bands.rs) - potential logic issue

## Findings

*(updated as work progresses)*
