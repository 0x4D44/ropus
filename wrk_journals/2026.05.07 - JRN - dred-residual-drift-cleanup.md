# 2026.05.07 — DRED residual drift cleanup

Continuation of `2026.05.07 - JRN - integration-roundtrip-validation.md`.
That session shipped 13 commits closing 6 real C-vs-Rust parity bugs and
27/36 DRED LPCNet features; **two real items remain open**, deferred at the
prior session's stopping point.

## Goal
Close the two open parity items:
1. **`compute_burg_cepstrum` powi/pow** at `ropus/src/dnn/lpcnet.rs:983`.
   Same shape as the f32→f64 fixes that landed earlier; classical-PLC path.
   No current test gates it.
2. **DRED feat 18-19 xcorr_features cascade**. Residual drift after Phase
   7 of the prior session. Needs deeper investigation than the f32→f64
   spotting that yielded six wins.

## Operating mode
- Supervisor: I direct, agents do legwork.
- **HLD-first discipline**: each item starts with an HLD that I review and
  sign off before any code is written. Agents iterate on the design until
  I'm satisfied.
- Implementation agents follow the signed-off HLD.
- Each implementation goes through `/code-reviewer` and
  `/code-review-apply-feedback`.
- Journal updated after each phase.

## Plan
1. Dispatch HLD-writing agents for items 1 and 2 in parallel.
2. Review each HLD; iterate with agent until signed off.
3. Dispatch implementation agents from signed-off HLDs.
4. Code review + apply feedback per item.
5. Verify, commit, journal closeout.

## Progress log
- 2026-05-07: Journal scaffold created. Dispatched HLD agents for item 1
  (`compute_burg_cepstrum`) and item 2 (xcorr cascade) in parallel.

### HLD sign-off (2026-05-07)

Both HLDs reviewed in full. Both are sound; signing off with answers to
the open questions.

**Item 1 — `wrk_docs/2026.05.07 - HLD - burg-cepstrum-pow-fix.md`**
- Verdict: SIGN OFF.
- Notable: agent's audit found a *second* deviation (`log10(1e-2 +
  Eburg[i])` at `lpcnet.rs:1011`) alongside the headline `powi`. Both
  are real C-vs-Rust mismatches matching the prior six fixes' shape.
- Open Q1 (test home): use `harness-deep-plc` — already wired for FFI.
- Open Q2 (noise fixture in addition to sine): yes, add it. Cheap, and
  broadens `burg_lpc[i]` magnitude coverage.

**Item 2 — `wrk_docs/2026.05.07 - HLD - xcorr-features-cascade-fix.md`**
- Verdict: SIGN OFF.
- Notable: high-confidence diagnostic via paired instrumentation. The
  fixed-point sibling `celt::lpc::celt_fir` already does the C-correct
  reversed accumulation; only the float helper diverges.
- Verified the indexing math by hand: HLD's proposed Rust fix
  reproduces C's per-output sum sequence exactly
  (`x[ord+i] + num[ord-1]*x[i] + num[ord-2]*x[i+1] + ... +
  num[0]*x[i+ord-1]`).
- Implementation note (non-blocking): the `vec![0.0; ord]` allocation
  per frame is wasteful since `ord` is `LPC_ORDER` (16) at all call
  sites. Implementation agent should prefer either a stack array
  (`[0.0_f32; LPC_ORDER]`) or direct reverse indexing of `num[]`
  (`num[ord - 1 - j]`) over a `Vec`. Either preserves the bit-exact
  contract; pick whichever is cleanest.
- Bit-exact (0.0) re-lock confirmed as the desired outcome.
- Out-of-scope list approved (no shared celt::lpc/dnn::lpcnet refactor;
  no kernel vectorisation).

## Implementation outcomes (2026-05-07)

### Item 1 — commit `6dc7f2e`
Two surgical changes in `compute_burg_cepstrum` + new differential test.

- Fix A: `lpcnet.rs:998` — `0.995_f64.powi(...)` with f64 multiply,
  narrowed at store. Matches `freq.c:169`.
- Fix B: `lpcnet.rs:1011` — f64 add + f64 log10, narrowed at store.
  Matches `freq.c:174`.
- New file: `harness-deep-plc/tests/burg_cepstral_analysis_diff.rs`
  asserts bit-exact equality across all 36 outputs on two fixtures
  (440 Hz sine, seeded xorshift32 white noise) via a new
  `ropus_test_burg_cepstral_analysis` FFI thunk.
- Versions: ropus 0.12.9 → 0.12.10, harness-deep-plc 0.1.4 → 0.1.5.

Code review: **APPROVE**, no critical/major. Implementation agent
correctly noted a sub-ULP `powi(integer)` vs `pow(integer-double)`
implicit assumption now pinned by the new test.

### Item 2 — commit `da62d7a`
HLD prediction (bit-exact 0.0 across all 36 features) was **partly
wrong**. Reality:
- 34 of 36 features now bit-exact (0-17, 20-35) — the FIR fix really did
  close the cascade for non-PitchDNN paths.
- Feat 19 (frame_corr): 8.34e-7 → 1.79e-7 (4.7x reduction).
- Feat 18 (dnn_pitch): essentially unchanged (1.01e-5 vs prior
  9.89e-6) — a sub-ULP shift because PitchDNN now sees bit-exact inputs
  but its own internal f32 noise is unaffected.

Implementation agent correctly STOPPED on the test failure rather than
papering over with looser locks. Supervisor:
1. Decided to ship the FIR fix anyway (34 features bit-exact is a real
   net win).
2. Dispatched a focused agent to re-lock with **per-feature bit-exact
   bounds** (assert 0.0 on all 34 newly-bit-exact features) plus
   per-feature ceilings on feat 18 and feat 19. The aggregate bound
   slightly loosens (1.013e-5 vs 9.89e-6) but the per-feature locks
   are STRICTER than the prior aggregate lock would have been.
3. The re-lock agent caught that feat 19 isn't bit-exact (1.79e-7
   residual) — the brief had assumed it was, but the agent verified
   measurement and locked per-feature instead. Better than
   instructed.

Versions: ropus 0.12.10 → 0.12.11, harness-deep-plc 0.1.5 → 0.1.6.

Code review: **APPROVE**, no critical/major. Reviewer hand-traced
the FIR indexing AND re-derived the existing unit test's expected
values from the new code to verify bit-exact correctness; symmetry
sweep found no other reversed-order accumulation sites. Verdict
holds.

### Concurrent activity (NOT this session's work)
A parallel session was running an 8-hour overnight fuzz campaign,
which surfaced and committed:
- `9b8c552 Fix PLC short-frame delay_buffer contamination`
  (`ropus/src/opus/encoder.rs`).
- New known-failure fixtures under `tests/fuzz/known_failures/` for
  `multiframe-dtx-celt-divergence` and
  `multiframe-silk-fec-state-divergence`.
Per multi-agent protocol, this session left those untouched.

### Final state
- Commits this session: `6dc7f2e` + `da62d7a` (plus this journal).
- All gates green in own scope: 2/2 burg_diff, 5/5 dred drift,
  17/17 param grid, 1866/1866 lib, clippy/fmt/build clean.
- DRED LPCNet feature drift on the locked diagnostic: 34/36
  bit-exact, feat 18 residual (PitchDNN inference) tracked for
  future, feat 19 residual (1.79e-7) tracked for future.

### Tracked for future
- **Feat 18 PitchDNN inference residual** (~1.01e-5). Drift now lives
  inside the neural network forward pass, not the FIR. Next slice:
  audit Conv1D/Dense layers in `dnn/pitchdnn.rs` and `dnn/core.rs`
  for f32-vs-f64 mismatches or summation-order issues; this is a
  different investigation pattern than the f32→f64 spotting that
  closed eight bugs across two sessions.
- **Feat 19 frame_corr residual** (1.79e-7). Sub-ULP at unit scale;
  may also live in pitchdnn or the inner_prod / softplus chain.
- The optional belt-and-braces `nb_available_bytes = 2` parity-aligning
  insert in CELT silence branch (style only, not a bug).
