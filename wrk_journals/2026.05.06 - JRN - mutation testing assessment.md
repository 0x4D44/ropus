# Mutation Testing Assessment Journal

Date: 2026.05.06

## 2026.05.06 - Documentation-only assessment

Arthur asked for a documentation-only explorer pass over mutation-testing config
and scripts. I kept to the two owned files and did not implement code or commit.

Files inspected:

- `/home/md/language/ropus/.cargo/mutants.toml`
- `/home/md/language/ropus/tools/mutation_test.sh`
- `/home/md/language/ropus/Cargo.toml`
- `/home/md/language/ropus/ropus/Cargo.toml`
- `/home/md/language/ropus/harness/Cargo.toml`
- mutation-testing plans/reports in `/home/md/language/ropus/wrk_docs/`

Cheap commands run:

```text
timeout 30 bash -n tools/mutation_test.sh
timeout 30 cargo mutants --help
timeout 30 cargo mutants --version
timeout 30 cargo mutants --list-files --package ropus
timeout 30 cargo mutants --package ropus --file src/silk/common.rs --list
timeout 30 cargo mutants --package ropus --file ropus/src/silk/common.rs --list
timeout 30 bash tools/mutation_test.sh --list
timeout 30 ls -l tools/mutation_test.sh
timeout 30 rg -n "test_compute_frame_features|test_plc_load_model|test_encode_multiframe_silk_special" ropus/src
```

Key findings:

- `cargo-mutants 27.0.0` is installed.
- The workspace root has no `src/`; codec sources live under
  `/home/md/language/ropus/ropus/src`.
- The runner still normalizes short paths to top-level `src/...`.
- `bash tools/mutation_test.sh --list` reports zero mutants for every priority
  module because of stale paths.
- `cargo mutants --package ropus --file ropus/src/silk/common.rs --list`
  lists 1199 mutants, so the zero inventory is definitely false.
- The script is not executable, despite usage examples saying
  `./tools/mutation_test.sh`.
- `.cargo/mutants.toml` still has global skipped test filters. One appears stale
  and two match current test-name substrings, so the mutation baseline remains
  shaped invisibly.
- The old pilot's C-reference oracle warning is still valid: the strongest
  oracle is in harness binaries, not in the default cargo-mutants `cargo test`
  loop.

Emotional temperature: mildly annoyed, but in a useful way. The problem is not
subtle: a stale path can make the tool say "0 mutants" and quietly destroy the
meaning of the lane. The good part is that the minimal fix is bounded; this does
not need to sprawl into a giant campaign.

Recommendation recorded in the plan:

- HLD-first implementation slice.
- Fix path/package handling.
- Fail on zero mutants.
- Remove or isolate hidden baseline skips.
- Keep the first revived run unit-test-only and label it honestly.
- Validate on `ropus/src/celt/math_ops.rs`, not the broad old default module set.
- Keep mutation testing periodic/on-demand, outside release preflight.

No blockers. No code was modified.
