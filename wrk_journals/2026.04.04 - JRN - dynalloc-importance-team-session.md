# 2026-04-04 — Dynalloc Importance Team Session

## Starting State
- 40/120 encode tests pass (all silence, all sample rates/channels/bitrates)
- 80/120 non-silence encode tests fail

## Team Structure
- **dynalloc-fixer**: Deep-dive follower/importance computation
- **devil's-advocate**: Audit approach, check assumptions, find systemic issues  
- **silk-investigator**: Trace SILK encoder frame-2 divergence
- **spread-auditor**: Audit spreading_decision + patch_transient_decision
- **dynalloc-boost-auditor**: Audit dynalloc boost/encoding loop
- **trim-fixer**: Fix alloc_trim_analysis (13 bugs)
- **transient-fixer**: Fix patch_transient_decision (7 bugs)

## Bugs Found and Fixed

### Commit 1: E_MEANS + CBR halving + VBR follow
1. **Wrong E_MEANS table** — used tables.rs [9,10,10,11,...] not quant_bands.rs [103,100,92,...]
2. **CBR halving logic** — `(vbr==0||cvbr)&&!trans` not `vbr==0||(cvbr&&!trans)`
3. **VBR follow precedence** — `(follow-c).max(t)` not `follow.max(t)-c`

### Commit 2: alloc_trim_analysis (13 bugs)
- Spectral tilt formula completely wrong (different algorithm)
- Bitrate frac scaling missing >>10
- Stereo: missing EXTRACT16, minXC loop, logXC conversion, trim/saving formulas
- surround_trim, tf_estimate formulas
- **Result: 40→54 passing tests**

### Commit 3: SILK prefill
- Missing payloadSize_ms=10 and complexity=0 override

### Commit 4: patch_transient_decision + CBR cap
- 5 bugs in patch_transient_decision (spread_old, loop bounds, per-channel)
- 2 bugs at call site (bandLogE2 += vs =, loop bound end vs nb_ebands)
- CBR cap: `(!vbr||(cvbr&&!trans))` not `((!vbr||cvbr)&&!trans)`

## Final State: 54/120 encode tests pass

### Passing patterns:
- All 40 silence tests: PASS
- All 10 mono noise @32k+: PASS (new!)
- 4 others

### Near-miss tests (99%+ matching):
- 8kHz mono noise @64k: 8097/8100 (99.96%)
- 12kHz mono sine @64k: 7997/8100 (98.7%)
- 8kHz mono sine @128k: 15963/16100 (99.15%)
- 48kHz mono sine @64k: 2006/8100 (24.8%, improving)

### Still failing patterns:
- @16kbps: SILK/hybrid mode issues (seed, gains diverge)
- Stereo: additional stereo-specific bugs likely
- Sine waves: more sensitive to tiny accumulated errors

## Devil's Advocate Recommendations (still open)
1. Systematic function-by-function audit of remaining CELT helpers
2. Missing activity detection path (peak_signal_energy)
3. compute_vbr is a stub (matters for VBR mode)
4. Test decode separately from encode
5. Stray uncommitted changes in silk/, opus/ from concurrent sessions — need review

## What Went Well
- Agent team approach found 25+ bugs in one session
- E_MEANS table fix was the single biggest impact (root cause of importance divergence)
- alloc_trim_analysis rewrite was highly effective (13 bugs → 14 new passing tests)

## What Could Be Better
- Stray changes from concurrent sessions kept interfering (had to git checkout --)
- Need better isolation between sessions
- Some fixes need deeper testing to confirm they actually help (SILK prefill, patch_transient)
