# Mutation Testing Fix Finish Journal

Date: 2026.05.06

## 17:23 - Scope

I am acting as the focused finish worker for the mutation testing revival slice.
I read the HLD, HLD signoff, implementation journal, and current diff before
editing. The allowed write scope is narrow: the mutation runner, cargo-mutants
config, and journals only. No commit.

Feedback item: live audit mode emitted a Bash nameref warning. I agree this is
real and worth fixing because warning noise in an audit runner undermines trust
even when the underlying command still works.

Decision: fix the warning by changing only the local nameref names and ensuring
the helper passes the original array variable name through. No behavioral policy
change.

Audit status: deferred. Prior full and 1/16 mutation attempts were too slow for
this environment. I will run cheap syntax/list/baseline validation and avoid a
full mutation campaign.

Emotional temperature: focused and slightly wary of shell edge cases. I am
keeping this finish pass boring on purpose.

## 17:31 - Final Status

Fix applied:

- `append_baseline_skips` now uses a `baseline_cmd_ref` nameref.
- `append_cargo_mutants_test_args` now uses a `mutants_cmd_ref` nameref.
- The cargo-mutants helper passes the original array variable name through to
  the baseline skip helper, avoiding the prior local nameref shadowing.

Validation passed:

```text
timeout 30 bash -n tools/mutation_test.sh
timeout 30 cargo mutants --version
timeout 60 bash tools/mutation_test.sh --list
timeout 60 bash tools/mutation_test.sh --file celt/math_ops --list
timeout 120 cargo test -p ropus --lib
timeout 30 git diff --check -- .cargo/mutants.toml tools/mutation_test.sh
```

`cargo mutants --version` reported `cargo-mutants 27.0.0`. The full default
inventory and the `celt/math_ops` short-path inventory both reported nonzero
mutant counts. The strict `ropus` library baseline passed with 1866 tests.

I also ran one bounded live warning check:

```text
timeout 45 bash -c 'set -o pipefail; bash tools/mutation_test.sh --file celt/math_ops --shard 1/230 --jobs 1 --timeout-multiplier 1 2>&1 | tee /tmp/ropus_mutation_warning_check.log'
```

It intentionally exited with timeout status 124 after reaching `Found 1 mutant
to test`. The captured output contained no Bash nameref/circular-reference
warning. No mutation processes remained afterward.

Audit deferral rationale: mutation execution is still too slow for this finish
pass. The revived lane is script-clean and baseline/list validated, but a
completed audit report and oracle-matrix update should wait for a deliberately
scheduled bounded audit.

Remaining risk: cargo-mutants emitted its own warning about needing an explicit
test timeout when using `--baseline=skip`; that is separate from the Bash
warning and should be considered in a later runner polish pass.
