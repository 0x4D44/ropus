# Journal — Official xiph/opus test suite integration

**Started:** 2026-04-18
**HLD:** `wrk_docs/2026.04.18 - HLD - official-test-suite.md`
**Supervisor:** Claude (Arthur's directive: run agent teams; protect main context; TDD where applicable; devil's-advocate pass per phase; commit after each phase; final completeness audit)

## Team roles

- **Implementer** — spawns per phase; writes code against the HLD.
- **Reviewer** — invokes `code-reviewer` skill against the implementer's output.
- **Applier** — invokes `code-review-apply-feedback` skill to work the review's findings.
- **Devil's advocate** — independent challenge pass each phase: "what would make this fail? what did the others miss?"
- **Final auditor** — end-of-project: compares delivery against HLD; blocks completion until 100%.

## Phase plan

| # | Phase | HLD est. | Success condition |
|---|---|---|---|
| 1 | Skeleton + api test | 2d | `cargo test -p conformance api` green |
| 2 | Decode + soft_clip | 2d | `test_opus_decode.c` green |
| 3 | Padding + repacketizer | 1d | `test_opus_padding.c` green |
| 4 | Encode + multistream | 2d | `test_opus_encode.c` green (ENABLE_DRED undefined) |
| 5 | Packet extensions | 3d | `test_opus_extensions.c` green |
| Final | Completeness audit | — | 100% HLD coverage |

## Log

### 2026-04-18 — Setup

- Tasks 1–6 created.
- HLD v2 already on disk.
- Kicking off phase 1.

### 2026-04-18 — Phase 1 scope correction

Phase-1 implementer agent flagged that my brief was self-contradictory: `test_opus_api.c` (1916 lines) actually exercises multistream + repacketizer + packet_pad/unpad in full — it's the broadest-surface test, not the narrowest. The HLD's own phasing had this inconsistency too (said phase-1 test was api but put repacketizer in phase 3 and multistream in phase 4).

Good catch. Reordered phases by actual API surface:

| # | Test file | Scope |
|---|---|---|
| 1 | `test_opus_padding.c` | encoder/decoder + `opus_packet_pad/unpad` (narrowest, 80 lines) |
| 2 | `test_opus_decode.c` | + `opus_pcm_soft_clip` + decoder CTLs |
| 3 | `test_opus_api.c` | + multistream + repacketizer (biggest surface — 1916 lines) |
| 4 | `test_opus_encode.c` | + `OPUS_SET_FORCE_MODE` |
| 5 | `test_opus_extensions.c` | + extensions port + private-impl exports |

HLD updated. Tasks 1, 3, 4 descriptions amended. Resuming phase-1 agent with narrower brief.

Agent name: `a3c02c1c04fdb9678` (received blocker report; awaits revised direction).

### 2026-04-18 — Phase 1 implementer complete

Agent `a3c02c1c04fdb9678` reports `cargo test -p conformance -- --test-threads=1` green on the `padding` test. Deliverables match the narrowed brief.

**Notable deviations (flagged for reviewer):**
- Crate name is `ropus`, not `mdopus` (concurrent agent restructured root Cargo.toml). Capi now depends on `ropus`.
- Rust 2024 edition required `#[unsafe(no_mangle)]` attribute syntax and `unsafe extern "C" { ... }` blocks. Applied uniformly.
- Used runtime `force_link()` + `black_box` to anchor no_mangle symbols on Windows/MSVC (can't use `#[used]` static with fn-ptr-to-usize under const-eval rules). Legitimate workaround.
- Pre-existing `ropus-harness` build.rs failure (expects `harness/reference/` that doesn't exist) — unrelated; confirmed repros without capi changes.

**Next:** reviewer agent (code-reviewer skill) on the phase-1 delta.

### 2026-04-18 — Phase 1 review + apply + DA

**Reviewer (agent `a47a6c93c0ce52918`) — clean bill with three hardening items:**
1. `capi/src/lib.rs` opus_strerror — use `.get()`/`unwrap_or` pattern.
2. `capi/src/decoder.rs` opus_packet_pad — guard `new_len < len`.
3. `capi/src/encoder.rs` + `decoder.rs` — `checked_mul(frame_size, channels)` to defend 32-bit overflow.

All three applied by agent `a330e0254cfddd4ca` (after concurrent-work blocker resolved).

**Devil's advocate (agent `aeff459b5fd9f20a9`) — eight findings:**

| # | Finding | Disposition |
|---|---|---|
| 1 | `test_opus_api.c:244-248` does `memcpy(dec2, dec, get_size(2))`; our Vec-holding OpusDecoder gets aliased heap pointers → phase-3 blocker | **Flagged for phase 3 decision** (see below) |
| 2 | HLD required `panic = "unwind"` but not set in any Cargo.toml | **Fixed** in workspace profile |
| 3 | `opus_packet_pad` constructs `from_raw_parts_mut(data, new_len)` before validation — UB if caller's buffer is smaller | Caller-contract; not fixable in capi without violating C API contract. Note. |
| 4 | `opus_decode(data=NULL, len>0)` — C treats as PLC, we return BAD_ARG | Phase-2 concern; fix when wiring decoder fully |
| 5 | `force_link()` may not survive LTO/strip in release; `#[used]` statics would be more robust | Watch. Not fixing; current pattern works and "don't speculate on future phases" |
| 6 | `test_opus_common.h` declares `regression_test()`; both test_opus_encode.c and test_opus_decode.c define it → linker collision when compiling into one test binary | **Phase 3+ blocker — needs test crate restructure (one `[[test]]` per opus test)** |
| 7 | `_init` twice leaks Vecs (ptr::write skips drop); test_opus_api.c may exercise | Phase-3 concern |
| 8 | `get_size()` could skew from `size_of::<State>` across compiles | **Fixed** via debug_assert_eq in `_init` |

Applier agent `ada28395445d954a6` applied fixes 2 and 8. Build + test still green.

**Known cargo warning:** `[profile.test] panic = "unwind"` is ignored (cargo inherits from dev). Harmless; kept for documentation intent.

### Phase-3 strategic concerns flagged now

**Architectural:** Issue #1 (`memcpy`) and #6 (`regression_test` collision) will both surface in phase 3 (test_opus_api.c). Need resolution strategy before phase 3 begins:
- #1 (memcpy): options include (a) redesign OpusDecoder/OpusEncoder in `ropus/` to be repr(C) with inline fixed arrays, (b) ship-it and hope test_opus_api.c's `memcmp` happens to pass despite aliased Vecs (only UB if either copy is later freed/dropped; the test does free dec2 via C free which skips Rust Drop, so *in practice* this might work — but it's textbook UB). (c) Accept partial test_opus_api.c coverage.
- #6: trivial — split tests/conformance/tests/run.rs into one file per test (`tests/padding.rs`, `tests/decode.rs`, `tests/api.rs`, etc.). Each becomes its own `[[test]]` binary with its own symbol scope.

**Phase 1 is DONE.** Committing next.

### 2026-04-18 — Phase 2 implementation

Phase 2 had significant scope (soft_clip port + decoder CTLs + PLC fix + per-test-file restructure + test_opus_decode.c passing). Work happened in four agent rounds:

**Round 1 — implementer (`a08e769008acac173`):** delivered soft_clip port, CTL plumbing, PLC fix, per-test-file split, but **hit STATUS_HEAP_CORRUPTION on MSVC** when running `test_opus_decode.c`. Minimal-C and minimal-Rust reproducers both passed; only the real test crashed. Agent couldn't isolate and reported blocked.

**Round 2 — debug agent (`af7b7ab2ae758a5f9`):** root-caused at confidence 5/5.
- **Root cause:** our `OpusDecoder` (4888 bytes) holds `Vec<_>` fat pointers (in `SilkDecoderState::exc_q14`, `out_buf`, plus six `Vec<i32>` fields in `CeltDecoder`). The test does `memcpy(decbak, dec, get_size(c))` to snapshot state. This aliases Vec backing allocations between two "copies." On mode transitions `SilkDecoder::init()` drops+realloc Vec contents, freeing heap blocks still referenced by the aliased copy. Use-after-free + double-free → heap corruption.
- **Fix:** introduced POD `OpusDecoderHandle` in `capi/src/decoder.rs` — layout `{ u64 magic, *mut OpusDecoder inner, padding }` sized to `size_of::<OpusDecoder>()`. Handle stored in test-allocated storage; real decoder lives on Rust heap, unreferenced by the bytes the test memcpys. `resolve_handle()` validates magic before every deref. `_destroy` is a no-op leak (intentional — test processes short-lived, avoids CRT/Rust heap-boundary issues).
- Release build passes in 8s, debug in 155s. Both `padding` and `decode` green.

**Round 3 — reviewer (`ac846ed645589f0bb`) — two required fixes + clarifications:**
1. `decoder.rs:62` const assertion used hardcoded `4888` — changed to type-based size check.
2. `encoder.rs:55` dangling rustdoc link — fixed to `state_free`.
3. `soft_clip.rs` ambiguous "returns 0" comment — clarified re: within1 hint vs clamping.
4. `soft_clip.rs:48` dead-code binding — added placeholder comment for future SIMD hook.

Applier agent `aed2bfde66aeec6e6` applied all four.

**Round 4 — devil's advocate (`a1eb8d1ca282f4587`):** one real fix, several phase-3/4 traps.
- **Fixed:** `state_free` dead wiring. Decoder destroy now routes through the same chokepoint as encoder (applier agent `ab10dc47d351bc8e2`).

**Phase-3+ architectural concerns to carry forward:**

1. **Aliased handles share one inner decoder.** `test_opus_decode.c:328-333` memcpys `dec[0] → decbak` expecting a state reset — with our handle indirection, both aliases point at the same real decoder. Test passes today (assertions only check `out_samples >= 20`), but any future byte-exact cross-copy test will fail. If phase 4 test_opus_encode.c does byte-exact copy-and-diverge checks, we need a different strategy (serialize/deserialize state via ropus-side Clone, OR make OpusDecoder POD). **Flag this in the phase-3 brief.**
2. **OpusEncoder has the same Vec-aliasing issue** — will bite in phase 4. Handle-indirection pattern applies mechanically. Phase 4 must replicate.
3. **`opus_packet_has_lbrr` diverges from reference for code-0 packets** — may surface in test_opus_api.c (phase 3).
4. **`opus_decoder_init` on a live handle leaks the old inner Box** — not a bug under "always leak" design, but aliased handles pointing at the detached old Box continue to work silently. Document.
5. **Leak accounting:** ~5-10 kB per decoder × number of creates. Fine for current tests; monitor in phase 3 where loops get tighter.
6. **OPUS_SET_COMPLEXITY** unwired for encoder (phase 4 will need it).

**Phase 2 is DONE.** Both `padding` and `decode` green. Committing next.

### 2026-04-18 — Phase 3 implementation

Biggest phase — `test_opus_api.c` is 1916 lines, widest API surface. Landed in three rounds:

**Round 1 — implementer (`aa9fdfcb4bc734ef5`):** hit an interrupted-session on first try; resumed cleanly. Delivered:
- Encoder refactored to handle indirection (magic `MDOPUSEC`, 24-byte prefix, **generation counter** bumped on reset — novel pattern to satisfy `test_opus_api.c:247` memcmp-after-reset assertion under handle aliasing).
- `capi/src/ms_encoder.rs`, `ms_decoder.rs` — handle-indirection multistream wrappers including per-stream sub-handles for `OPUS_MULTISTREAM_GET_{ENCODER,DECODER}_STATE`.
- `capi/src/repacketizer.rs` — handle over `Box<OpusRepacketizer<'static>>` (lifetime erased under documented "caller keeps input alive" invariant; deviates from HLD's POD variant — ~50 LOC saved, still satisfies all test assertions).
- `capi/src/packet_parse.rs` — `opus_packet_parse` wrapper.
- Full encoder CTL dispatchers (15 setters, 17 getters, uint32 getter), MS encoder/decoder CTL dispatchers, `GET_{ENCODER,DECODER}_STATE` fan-out.
- `tests/conformance/tests/api.rs` + `arch.h` stub (15 lines).
- Decoder + encoder `generation: u64` field; handle magic per state type.

Visibility escalations (all minor pub additions): `set_application` new pub fn, `MAX_FRAMES` pub const, `opus_packet_parse_impl` pub fn. No semantic changes.

Three novel design decisions flagged for design-soundness review:
1. **Generation counter** — bumped on `OPUS_RESET_STATE` so aliased handles diverge after reset.
2. **MS `_get_size()` decoupled** — advertises `16*1024*streams` (test constraint: `2048 < size <= (1<<18)*streams`), our real MS state is unrelated.
3. **Repacketizer lifetime erasure** — `'static` Box, caller must keep input alive.

**Round 2 — reviewer (`acef4a6da2654f5ca`):** three real fixes + one doc improvement:
1. `capi/src/ctl.rs` aliasing hazard in GET_{ENCODER,DECODER}_STATE: `&` + `&mut` to same Inner was UB. Fixed by routing all access through a single `&mut Inner` via the `sub_*_handle_ptr` helpers (now taking `c_int` and folding bounds check in).
2. `capi/src/repacketizer.rs:127` UB read of uninitialized `(*h).magic`. Fixed: always wipe + fresh alloc, mirroring encoder/decoder `_init`.
3. `capi/src/ctl.rs:277-289` double reset in `mdopus_ms_encoder_ctl_reset` (shim walked streams then ms.reset() also walks). Fixed: single `ms.reset()` call.
4. Doc note on repacketizer input-buffer lifetime invariant.

Applier `a78e0587f30e433a6` applied all four. Tests remained green.

Also fixed a symmetric version of (1) in the decoder mirror that the reviewer cited. Good catch.

**Round 3 — devil's advocate (`adc1f0bc5a3362ca2`):** two real fixes + four phase-4/5 traps:
1. **MS `OPUS_SET_APPLICATION` double-dispatch torn-state window.** Per-sub loop validates-and-mutates each stream; if validation fails on stream 1+, stream 0 already mutated → inconsistent state. Fix: validate value up-front (tri-value set), then single `ms.set_application()` call. Phase 4 would have hit this.
2. **`opus_packet_parse(frames=NULL)` rejected** where C reference accepts. Fix: remove the null check; conditionally write frame pointers only when non-null.
3. **`arch.h` stub missing `IMIN`/`IMAX`** — phase 4 `test_opus_encode.c:264` compile blocker. Fix: add both macros to stub.
4. **MS `OPUS_SET_FORCE_CHANNELS` same torn-state pattern** as #1. Phase 4 risk. Fix: same pattern.

Applier `a507bfaf9e9df1868` applied all four. Audit confirmed no other MS CTL dispatchers had the same pattern.

**Unexpected finding** worth carrying to phase 4: `ms.set_application` in ropus sets `self.application = v` unconditionally then relies on sub-encoder validation to filter. Without the capi-layer up-front guard, a bad value would corrupt `ms.application` (read back by `get_application`) without taking effect per-stream. The capi-layer validation is load-bearing for correct get-after-set roundtrip on rejection.

**Phase-4/5 watch-list (deferred):**
- MS encoder `_get_size()` integer overflow at `streams > 131071` (unreachable in tests).
- Repacketizer `_init` leak growth in tight loops (phase-4 `test_opus_encode.c` may surface).
- Sub-handle identity after MS destroy (leak-all means no UAF, but stale pointers are weird).

**Phase 3 is DONE.** All three tests (`padding`, `decode`, `api`) green on release and debug builds. Committing next.

### 2026-04-18 — Phase 4 + bug discovery

Phase 4 implementer (`aa8f3fc042071f4ce`) landed all the capi work:
- `OPUS_SET_FORCE_MODE` CTL wired (both encoder + MS encoder with up-front validation).
- `opus_private.h` stub (~30 lines: MODE_SILK_ONLY/HYBRID/CELT_ONLY + OPUS_SET_FORCE_MODE).
- Force-include plumbing (`/FI` MSVC, `-include` others) intercepts `#include "../src/opus_private.h"` from the test source.
- `regression_test()` no-op stub in `tests/conformance/src/regression_test_stub.c` — upstream `opus_encode_regressions.c` requires surround+projection encode, explicitly out of scope per publish-as-crate HLD.
- `tests/conformance/tests/encode.rs` + build.rs entry.

All three prior tests (`padding`, `decode`, `api`) still green.

**But the `encode` test failed at test_opus_encode.c:501 — `enc_final_range != dec_final_range`.** This is a REAL ropus encoder bug, not a capi issue.

Bug diagnosis (from debug agent's standalone differential repro):
- Conditions: force-mode SILK_ONLY, bitrate 6809, 1920-sample frames, bandwidth alternating NB/MB/WB.
- Iterations 0, 1, 3-14: ropus-encoder.get_final_range matches both ropus-decoder and C-reference-decoder.
- **Iteration 2 (first WB activation):** encoder says `0x0c4c9290`, both decoders say `0x43f05790`. Bitstream is correct; encoder's internal bookkeeping is wrong on this one frame.
- XOR delta = `0x4fbcc500`. Hypothesis: `redundant_rng` XOR at `ropus/src/opus/encoder.rs:2564` fires on the first SILK→CELT bandwidth-switch frame when the decoder path doesn't match.

**This is exactly the class of bug the official suite was built to find** — a shared-with-C-style bug that our differential harness couldn't catch (because if our port faithfully copied a C encoder bug, both C and Rust would agree in differential). The test-suite oracle catches it because it asserts `enc_final_range == dec_final_range` directly, not relative to the other implementation.

Per Arthur's direction: dispatched a dedicated bug-fix agent (`a99570fa0e9e5ccb8`) to work in background. Brief: write design doc → self-review → implement → verify all tests → code-review → commit. Dedicated journal at `wrk_journals/2026.04.18 - JRN - encoder-final-range-bug.md`. Authorized to modify `ropus/src/opus/encoder.rs` (and harness tests if needed).

In parallel: moving to phase 5 (extensions). Phases are file-disjoint (bug fix touches `ropus/src/opus/encoder.rs`; phase 5 touches `capi/` and a new `ropus/src/opus/extensions.rs`), so concurrent work is safe.

**Phase 4 status:** capi work complete; blocked on ropus encoder bug fix. Test `encode` will go green once the bug-fix agent lands.

### 2026-04-18 — Phase 5 implementation

Phase 5 implementer (`aff2a59da125ad599`) delivered extensions port and internal-impl exports. Key findings:

- **Pre-existing implementation.** The phase 3 reviewer had flagged that `ropus/src/opus/repacketizer.rs` already contained `opus_packet_extensions_{count,parse,generate}` + `OpusExtensionIterator` as `pub(crate)`. Agent reused them with visibility escalation + new `_ext` variants that weren't present. Saves ~400 LOC of new porting.
- **`opus_packet_parse_impl` split.** The old signature didn't output padding info. Agent added `opus_packet_parse_impl_with_padding` (new pub fn returning a `PaddingInfo` struct) while keeping the old `opus_packet_parse_impl` as a delegator. Zero behavioural change for existing callers.
- **`nb_frames` mirror field on repacketizer handle.** `test_opus_extensions.c:679` reads `rp.nb_frames` directly via the stub-declared C layout. Agent added a mirrored `i32` at byte offset 24 of `OpusRepacketizerHandle`, pinned via `offset_of!` + `size_of` const asserts. `sync_nb_frames` called on every `cat()`. Stub header declares `struct OpusRepacketizer { unsigned char _prefix[24]; int nb_frames; unsigned char _pad[36]; }` matching `REPACKETIZER_STORAGE_SIZE=64`.
- **100M random iterations** on extension generate/parse round-trip pass in 515s (under 600s budget). Strong byte-exactness evidence.

Visibility escalations (all pub(crate) → pub):
- `OpusRepacketizer::out_range_impl`
- `opus_packet_extensions_count`, `_parse`, `_generate`

Test results at first run: `padding`, `decode`, `api`, `extensions` all green. `encode` still fails (pending concurrent bug fix).

**Round 2 — reviewer (`a46ebf9212759051f`):** clean bill. Two optional doc nits applied by `a04aa70fd9824d359`:
- packet_parse.rs comment clarification.
- Orphaned doc block deleted, heap-size rationale preserved inline.

**Round 3 — devil's advocate (`a53ec32c258ddd440`):** three conformance drifts vs C reference + one watch-list doc note. All applied by `a924b5745a44a862e`:
1. `opus_packet_parse_impl` rejected `data=NULL, len=0` with `OPUS_BAD_ARG` where C returns `OPUS_INVALID_PACKET`. Fixed: removed upfront rejection; let len==0 path return INVALID_PACKET via safe empty-slice construction.
2. `ropus/src/opus/extensions.rs` used `saturating_add` where C uses `int` wrap. Fixed: `wrapping_add`.
3. `opus_packet_extensions_parse` wrote `*nb_extensions = count` on `OPUS_BUFFER_TOO_SMALL` where C leaves it untouched. Fixed: conditional write gated on ret != BUFFER_TOO_SMALL. Applied to both parse and parse_ext wrappers.
4. Added `nb_frames > 48` bounds check in `opus_packet_extensions_count` to match sibling wrappers.
5. Doc note on `opus_repacketizer_out_range_impl`: `&self` non-mutating, no `sync_nb_frames` needed.

All six verifications green (build + 4 tests). `extensions` passes in 505s.

**Bug-fix agent completed** (`a99570fa0e9e5ccb8`): root-caused missing `redundant_rng = celt.rng;` capture at encoder.rs:2414 (should mirror C's `opus_encoder.c:2440`). Confidence 5/5. Fix committed at `190d4c6a`. Ropus unit tests clean (1788 passing, 0 failing, 5 more than baseline). New regression test `test_encoder_final_range_matches_decoder_on_celt_to_silk_redundancy` passes with fix / fails without — confirms bug-fix correspondence.

**BUT:** `encode` conformance test STILL FAILS. Bug-fix agent found a SECOND, distinct bug during verification — a CELT redundancy `redundant_rng` mismatch between encoder and decoder on the first j=7 CELT-mode frame (which due to mode-transition undo + bandwidth downgrade is actually encoded as SILK_ONLY with SILK→CELT redundancy). SILK base range matches; only CELT redundancy rng differs. Agent ran out of time budget; documented extensively in `wrk_journals/2026.04.18 - JRN - encoder-final-range-bug.md` "Next-bug notes" for the next agent. Also flagged a tertiary concern in `encode_multiframe` (`self.range_final` incorrectly overwritten with `celt.rng` after multi-frame loop — verified NOT the cause of current failure but a latent issue worth fixing).

**Phase 5 status:** DONE. All extensions infrastructure landed and verified. Test `extensions` green.

**Remaining:** dispatch a new bug-fix agent for the secondary CELT-rng divergence so `encode` goes green; then final audit.

Committing phase 4 + phase 5 capi work next (single atomic commit — build.rs and opus_private.h have interleaved additions from both phases).

### 2026-04-18 — Phase 4+5 committed; secondary bug-fix dispatched

Phases 4 + 5 committed as `a44f458` — single atomic commit covering encode wiring (FORCE_MODE, opus_private stub, regression_test stub, test file) and extensions (port + exports + tests). Clean build, 4 of 5 tests green.

Dispatched secondary bug-fix agent (`a8484292b554b95e2`) in background. Brief: continue the primary bug-fix journal; design doc → self-review → implement → verify → code-review → commit. Targets the SILK→CELT redundancy rng divergence the primary-fix agent discovered during verification. Hypothesis: mirror of primary bug (missing rng capture), but on the opposite redundancy direction.

**Summary status:**
| Test | State |
|---|---|
| `padding` | ✓ passes (phase 1) |
| `decode` | ✓ passes (phase 2) |
| `api` | ✓ passes (phase 3) |
| `encode` | ⚠ capi wired, pending 2nd ropus bug fix (phase 4) |
| `extensions` | ✓ passes (phase 5) |

**Commits landed so far on this HLD's work:**
- `e6ec9b7` and earlier — pre-existing baseline.
- `dcbc87a` / `f50767e` / etc — concurrent publish-as-crate work.
- **`190d4c6a`** — primary ropus encoder fix (capture redundant_rng on CELT→SILK).
- **Phase 1 capi skeleton** — earlier commit (pre-rename? — see git log).
- **Phase 2 capi decode** — earlier commit.
- **Phase 3 capi api** — `5792eeb`.
- **Phase 4+5 capi** — `a44f458` (just committed).

Audit deferred until secondary bug lands.

### 2026-04-18 — Secondary bug fixed; encode still blocked on tertiary

Secondary bug-fix agent (`a8484292b554b95e2`) committed `133155d`:
- **Root cause:** `OPUS_BITRATE_MAX` constant mismatch. Our Rust had `-1000`; C reference has `-1` (`opus_defines.h:212`). This caused SILK→CELT redundancy final-range divergence.
- **Why CELT→SILK direction (primary fix) didn't observably fail from the same constant mismatch:** `ResetState` excluded bitrate, masking the latent issue on that path.
- **Fix:** one-line constant change in `ropus/src/celt/encoder.rs` + 10-line explanatory doc comment. Regression test added to `harness/tests/c_ref_differential.rs` (Hybrid warmup → MODE_CELT_ONLY + WB + 60ms stereo).
- **Companion HLD:** `wrk_docs/2026.04.18 - HLD - encoder-celt-redundancy-rng-fix.md`.
- Self-code-review passed with no required changes.

**Test progress after secondary fix:**
- `padding`, `decode`, `api`, `extensions` — all remain green.
- **`encode` STILL FAILS** at `test_opus_encode.c:501` (`enc_final_range != dec_final_range`), but progresses much further. Specifically reaches "Mode LP FB encode VBR, 20918 bps OK" and "Mode LP FB encode VBR, 76757 bps OK" before the failing iteration. Crash type `STATUS_STACK_BUFFER_OVERRUN` is Windows's report for test_failed() → abort().

This is a TERTIARY encoder bookkeeping bug, distinct from primary (CELT→SILK rng capture) and secondary (BITRATE_MAX constant). The test is doing its oracle job — finding real codec-level defects that our differential harness couldn't.

**Open bugs carried forward:**
- **Tertiary:** unknown final_range divergence in a later iteration of test_opus_encode.c's `run_test1` (after the FB VBR warm-up cases).
- **Quaternary:** `encode_multiframe` overwrites `self.range_final` with `celt.rng` after multi-frame loop. Flagged by primary-fix agent, verified NOT the cause of current failure but latent.

**Status:** 4/5 conformance tests green. Each bug-fix iteration is a real encoder defect correction. The question for Arthur is whether to keep iterating or accept partial coverage.

