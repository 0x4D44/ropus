# 2026.05.04 - JRN - analysis-new-boxed-flake

## 2026-05-04 Initial context
- Task: investigate aggregate-only `cargo test --release -p ropus` failure in `opus::analysis::tests::test_new_boxed_is_equivalent_to_new`.
- Constraint: HLD-only phase; no production code edits.
- Read `/home/md/language/ropus/AGENTS.md`; repo validation guidance emphasizes scoped checks and deterministic repro.
- Existing unrelated untracked files are present; I will not touch them.
- Emotional temperature: focused, with mild caution because aggregate-only failures often mean hidden shared state or test-order coupling.

## 2026-05-04 Reproduction notes
- `timeout 300 cargo test --release -p ropus` passed once: 1867 unit tests and 3 doctests passed.
- `timeout 120 cargo test --release -p ropus opus::analysis::tests::test_new_boxed_is_equivalent_to_new -- --exact --nocapture` passed alone.
- `timeout 180 cargo test --release -p ropus -- --test-threads=1 --format terse` reproduced the failure consistently in the unit-test phase: 1866 passed, 1 failed.
- The failure is at `/home/md/language/ropus/ropus/src/opus/analysis.rs:1988`, comparing raw bytes from `TonalityAnalysisState::new()` and `TonalityAnalysisState::new_boxed()`.
- Current hypothesis: the test is reading and comparing padding bytes. `AnalysisInfo` has 63 bytes of explicit fields but a 64-byte `repr(C)` size, and `TonalityAnalysisState.info` is `[AnalysisInfo; 100]`, so stack constructor padding is not guaranteed to be zero even though every semantic field is zero.
- Emotional temperature: more confident now. The failure is annoying because the production heap-zeroing path is probably fine, while the test oracle is stricter than Rust's initialization guarantees.

## 2026-05-04 HLD completed
- Wrote `/home/md/language/ropus/wrk_docs/2026.05.04 - HLD - analysis-new-boxed-flake.md`.
- The HLD proposes a test-only fix: compare typed fields and keep any raw byte assertion scoped to the heap-zeroing contract, not stack-vs-heap equivalence.
- No production files were edited.
- Emotional temperature: settled. The smallest fix looks straightforward, and the main risk is keeping future test helpers exhaustive as the analysis state evolves.

## 2026-05-04 Implementation after sign-off
- Arthur signed off the HLD and asked for the analysis aggregate flake fix.
- Edited only `/home/md/language/ropus/ropus/src/opus/analysis.rs` for code: replaced stack-vs-heap whole-object byte equality with exhaustive semantic comparison helpers for `TonalityAnalysisState` and `AnalysisInfo`.
- Kept a scoped raw-byte assertion for `TonalityAnalysisState::new_boxed()` only, verifying the heap allocation bytes are zero after the constructor.
- Ran `timeout 60 rustfmt --edition 2024 ropus/src/opus/analysis.rs`: passed.
- Ran `timeout 120 cargo test --release -p ropus opus::analysis::tests::test_new_boxed_is_equivalent_to_new -- --exact --nocapture`: passed, 1 test.
- Ran `timeout 180 cargo test --release -p ropus --lib -- --test-threads=1 --format terse`: passed, 1867 tests.
- Emotional temperature: relieved. The deterministic repro is now green, and the fix stays aligned with the HLD rather than changing constructor behavior.
