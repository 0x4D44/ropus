2026-03-29 11:12

Task: Add matched RD-cost debug prints inside the NSQ delayed-decision inner loop in the C reference and Rust port, then build.

Notes:
- Located the equivalent RD candidate materialization sites in `reference/silk/NSQ_del_dec.c` and `src/silk/encoder.rs`.
- The user wording said `k == 0` for first subframe, but in the inner loop `k` is the delayed-decision state index. I used `subfr == 0` so output stays limited to the first subframe without dropping the per-state trace.

State:
- Focused. Slightly wary of adding noisy instrumentation in hot paths, but this is the right temporary cut for finding the first RD divergence.

2026-03-29 11:15

Validation:
- Ran `cargo clean`.
- Ran `cargo build`.
- Build completed successfully.
- The workspace still emits existing Rust warnings in untouched areas (`src/celt/*`, `src/opus/*`, and some unrelated `src/silk/encoder.rs` constant/paren warnings). This patch did not add any new warnings or errors.

State:
- Relieved that the instrumentation compiled cleanly on the first pass. Mild annoyance at the baseline warning volume because it obscures signal, but it did not block the task.

2026-03-29 11:20

Task: Add a narrowly scoped AR2 input-state trace at the delayed-decision warped allpass entry in both C and Rust for `subfr == 0`, `i == 1`, `k == 0`, then rebuild.

Notes:
- Added `[C NSQ_AR2]` immediately before the first `silk_SMLAWB` on `psDD->Diff_Q14` in `reference/silk/NSQ_del_dec.c`.
- Added `[RS NSQ_AR2]` immediately before the first `silk_smlawb_i32` on `dd.diff_q14` in `src/silk/encoder.rs`.
- Printed exactly the requested filter input state: diff, first five AR2 state entries, LF_AR, current shape sample, and warping coefficient.
- Left all existing debug instrumentation in place because `src/silk/encoder.rs` was already dirty and removing unrelated traces would have been a separate change.

Validation:
- Ran `cargo clean`.
- Ran `cargo build`.
- Build succeeded.
- Warnings remain in untouched baseline files under `src/celt/*`, `src/opus/*`, and existing areas of `src/silk/encoder.rs`.

State:
- Calm. This is the right diagnostic cut: either the printed AR2 inputs already disagree at `i=1`, or the math/update path after this point is the problem.
