# Performance Thresholds Apply-Feedback Journal

Date: 2026.05.06

## 00: Setup

I used the code-review-apply-feedback workflow against:

- `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - performance thresholds implementation.md`
- `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - performance thresholds review.md`
- `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - performance thresholds hld signoff.md`
- `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - performance release thresholds.md`

I limited edits to the allowed files and did not touch codec code or commit.

Emotional temperature: cautious. Performance gates are easy to sabotage by
making them "pass"; the correct fix here is to preserve the red signal and make
the decision explicit.

## 01: Feedback Decisions

Issue 1: unresolved `8.74x` decode smoke.

Decision: agree and address in documentation/journal, not by changing code or
thresholds. The live smoke is not calibrated enough to replace the threshold
table, but it is strong enough to say that the new gate would correctly fail
the current measurement. I recorded that this is unresolved release-blocking
evidence requiring a calibrated follow-up sweep or regression investigation
before claiming a clean release-performance pass.

Issue 2: missing unit tests for missing threshold and crashed row.

Decision: agree and fix with two pure synthetic tests in
`/home/md/language/ropus/full-test/src/bench.rs`. The tests assert
`banner_fail()`, the expected release-blocking issue text, and the row-level
`BenchThresholdStatus` values.

## 02: Validation

Passed:

```text
timeout 120 cargo test -p full-test bench
timeout 120 cargo test -p full-test banner
timeout 120 cargo fmt --check
timeout 30 git diff --check
```

`cargo test -p full-test bench` ran 39 tests, including the two new coverage
tests for missing threshold specs and crashed rows. `cargo test -p full-test
banner` ran 25 tests.

No commit made.

Emotional temperature: steady. The code now has the guard rails the signoff
asked for, and the measurement note is honest about the live red signal.
