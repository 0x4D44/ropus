# Journal: DNN Integration + Unit Tests + SIMD Sprint

**Date:** 2026-04-08  
**Workstreams:** 3 parallel tracks  

---

## Kickoff

Three parallel workstreams dispatched following the Codec Deep Dive:

| # | Workstream | Goal | Status |
|---|-----------|------|--------|
| 1 | DNN pipeline integration | Wire LPCNet/FARGAN/PitchDNN into encode/decode | HLD drafting |
| 2 | Unit tests | Tests for lpc.rs, rate.rs, silk/common.rs | HLD drafting |
| 3 | SIMD acceleration | Feature-flagged SIMD for xcorr_kernel + hot paths | HLD drafting |

**Process:** HLD → review → iterate → approve → implement → code review → apply feedback

---

## Timeline

### HLD Phase — Agents Dispatched

- **T+0** — Three HLD agents launched in parallel
- Agents instructed to explore relevant code, produce HLDs in wrk_docs/, and return for review
- No code changes permitted until HLD approved

### HLD Review Complete

All three HLDs delivered and reviewed:

| HLD | File | Lines | Verdict |
|-----|------|-------|---------|
| SIMD acceleration | `wrk_docs/2026.04.08 - HLD - SIMD acceleration.md` | 731 | **APPROVED with conditions** |
| Unit tests | `wrk_docs/2026.04.08 - HLD - unit tests for lpc rate common.md` | ~550 | **APPROVED** |
| DNN integration | `wrk_docs/2026.04.08 - HLD - DNN pipeline integration.md` | ~487 | **APPROVED with decisions needed** |

#### SIMD HLD — Key Review Points
- Recommends `wide` crate (safe SIMD wrappers) — sound choice
- 4 hot path targets: xcorr_kernel (P1), FFT butterflies (P2), band energy (P3), overlap-add (P4)
- **Condition 1:** Check LLVM auto-vectorization BEFORE writing explicit SIMD (Open Q3 → mandatory prereq)
- **Condition 2:** Overlap-add (60 iterations, modest work) should be deferred/optional
- **Needs Arthur:** Confirm "safe Rust only" means no `unsafe` in our crate (not in dep tree). If deps are forbidden too, only auto-vec is viable.

#### Unit Tests HLD — Key Review Points
- 75 tests across 3 modules, ~1,200 LOC estimated
- Excellent function-level inventory with specific test cases, inputs, expected outputs
- Smart priority ranking: P0 (complex/critical), P1 (important), P2 (nice-to-have)
- Correctly skips ~30 trivial arithmetic wrappers in silk/common.rs
- Three-source oracle: math properties, hand-computed values, naive reference impls
- **Note:** Check if `debug_helper.c` already exposes any target functions for FFI oracle

#### DNN HLD — Key Review Points  
- 6 integration points identified with specific code locations
- ~695 new/changed lines across 5 files
- Phased: PitchDNN stub → SILK PLC → CELT PLC → DRED stub
- **Most invasive workstream** — changes core decode function signatures via `DnnPlcArg` type alias
- **Needs Arthur's decisions:**
  1. Model weights — where do we get the weight blob for testing? Vendor, download in CI, or skip DNN tests when unavailable?
  2. OSCE (LACE/NoLACE) — part of this work or separate HLD?
  3. DRED encoder — defer entirely? (decoder side can be stubbed)
  4. Stereo PLC — C reference does mono-only. Acceptable?
  5. Decoder complexity CTL — expose it? (gates neural PLC at complexity >= 5)

### Arthur's Decisions

1. **SIMD:** `wide` crate **approved** — safe API wrapping internal unsafe is acceptable under "safe Rust only" rule
2. **DNN weights:** **Vendor in git** — download and commit the weight blob
3. **DNN scope:** Defer OSCE, DRED encoder, accept mono-only PLC, expose complexity CTL (aligned with HLD defaults)

All three workstreams unblocked.

### Implementation Phase — Agents Dispatched

All three implementation agents launched in worktrees (parallel, isolated branches):
- **unit-tests** — adding ~75 tests to lpc.rs, rate.rs, silk/common.rs
- **simd-impl** — feature-flagged SIMD via `wide` crate for hot paths
- **dnn-impl** — wiring DNN modules into codec pipeline behind `dnn` feature flag

### SIMD Agent — COMPLETE

**Branch:** `worktree-agent-a2e6c2f0`  
**Files:** 5 modified + 1 new (`src/celt/simd.rs`)

Phase 0 finding: LLVM does NOT auto-vectorize xcorr_kernel — confirmed high ROI for explicit SIMD.

Implemented:
- `xcorr_kernel_simd` via `wide::i32x4` (7 tests, bit-exact)
- `celt_maxabs32_simd` parallel max/min scan (8 tests)
- `mac16_16_x4` core SIMD primitive
- Feature flag infrastructure (`simd` feature, `wide` dependency)

Deferred (justified): FFT butterflies — struct marshaling + `mult16_32_q15` correctness risk made ROI too low.

Validation: 405/405 tests pass (386 existing + 19 new). Integration roundtrips bit-exact across all configs.

### Unit Tests Agent — COMPLETE

**Branch:** `worktree-agent-abbb0b7b`  
**Files:** 3 modified (test blocks added to lpc.rs, rate.rs, silk/common.rs)

Tests added: 102 total (HLD estimated 75 — exceeded):
- `celt/lpc.rs`: 25 tests (celt_lpc 8, celt_autocorr 7, celt_fir 4, celt_iir 6)
- `silk/common.rs`: 64 tests (~20 functions across P0/P1/P2 priorities)
- `celt/rate.rs`: 13 tests (get_pulses, bits2pulses/pulses2bits, clt_compute_allocation)

All P0 functions covered: celt_iir, celt_lpc, silk_lpc_inverse_pred_gain, silk_nlsf_stabilize, silk_nlsf2a, silk_sum_sqr_shift, get_pulses, bits2pulses/pulses2bits.

Validation: 488/488 tests pass (386 existing + 102 new). No non-test code modified.

### DNN Integration Agent — COMPLETE

**Branch:** uncommitted changes in worktree `agent-af6e2cb3` (based on `229e6a7`)  
**Files:** 5 modified (+545 lines, -8 deletions)

Changes per file:
- `Cargo.toml`: +4 (dnn feature flag, no external deps)
- `src/celt/decoder.rs`: +318 (CELT neural PLC, sinc 16→48kHz resampler, update_plc_state)
- `src/dnn/lpcnet.rs`: +39 (PitchDNN stub replaced with real delegation)
- `src/opus/decoder.rs`: +111 (LPCNetPLCState field, set_dnn_blob, init/reset, threading)
- `src/silk/decoder.rs`: +81 (SILK neural PLC update/conceal, DnnPlcArg threading)

All integration points from HLD implemented:
1. ✅ PitchDNN stub → real pitchdnn::PitchDnnState
2. ✅ LPCNetPLCState in OpusDecoder
3. ✅ SILK PLC neural path (update on good frames, conceal on lost)
4. ✅ CELT PLC neural path (FRAME_PLC_NEURAL, sinc resample, cross-fade)
5. ✅ Thread lpcnet through decode call chain
6. ✅ DRED FEC stub (fec_add/fec_clear)

Code review applied: 5 of 6 suggestions addressed (removed redundant computation, improved stereo copy safety, extracted celt_lpcnet_noop helper, pre-allocated plc_pcm).

All code behind `#[cfg(feature = "dnn")]` — zero behavior change without the flag.

---

## Summary — All Three Workstreams Complete

| Workstream | Branch/Location | New Tests | New Code Lines | Status |
|------------|-----------------|-----------|----------------|--------|
| Unit tests | `worktree-agent-abbb0b7b` | +102 tests | +1,352 LOC | ✅ Done |
| SIMD | `worktree-agent-a2e6c2f0` | +19 tests | ~250 LOC | ✅ Done |
| DNN integration | `worktree-agent-af6e2cb3` | — | +545 LOC | ✅ Done |
| **Total** | | **+121 tests** | **~2,147 LOC** | |

Note: master moved during implementation (coverage commits from concurrent agent). Required manual merge resolution.

### Merge into Master — Complete

Merged sequentially: unit tests → SIMD → DNN. Resolved conflicts:
1. **Cargo.toml version**: concurrent agent bumped to 0.1.1, SIMD to 0.2.0. Took 0.2.0.
2. **Cargo.toml features**: SIMD and DNN both added features. Combined both.
3. **silk/decoder.rs test call**: Concurrent agent's test called `silk_decode_frame` with 6 args; DNN added a 7th (`DnnPlcArg`). Fixed with conditional argument.

Final commits:
```
6822dd6 Wire DNN modules into codec pipeline behind dnn feature flag
458e8f5 Add feature-flagged SIMD acceleration for xcorr_kernel and celt_maxabs32
f133483 Add 102 unit tests for lpc.rs, rate.rs, silk/common.rs
```

### Final Test Matrix — All Green

| Configuration | Tests | Status |
|---------------|-------|--------|
| No features | 569 | ✅ PASS |
| `--features simd` | 588 | ✅ PASS |
| `--features dnn` | 569 | ✅ PASS |
| `--features "simd,dnn"` | 588 | ✅ PASS |

