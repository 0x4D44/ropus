# 2026.05.06 - JRN - legal frame duration roundtrip implementation

## Initial context

Task: Phase 3.3 Slice A only. Add legal frame-duration selection for `fuzz_roundtrip`, plus a shared pure helper and integration tests. Do not touch codec source, `fuzz_encode_multiframe.rs`, or `fuzz_decode_plc_seq.rs`. Do not start fuzz campaigns or commit.

Read `/home/md/language/ropus/AGENTS.md` and the signed HLD at `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - legal frame duration fuzz dimensions.md`. The implementation path is narrow and clear: reuse high bits from `data[7]`, preserve existing lower-bit config, lower the stale minimum-length guard, and keep bytes-needed validation after parsing.

Emotional temperature: focused, low friction. The only small irritation is the large fuzz corpus making broad file listings noisy; the actual code path is compact.

## Implementation

Added `/home/md/language/ropus/tests/fuzz/fuzz_targets/frame_duration.rs` with a pure selector-to-duration mapping for the six legal Opus frame durations and stable labels.

Added `/home/md/language/ropus/tests/fuzz/tests/frame_duration_tests.rs` to pin labels, sample counts for every supported sample rate, wrapping selector behavior, and the `fuzz_roundtrip` policy of deriving the selector from `data[7] >> 5`.

Updated `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs` only. The target now lowers the initial length guard to `8 + 40`, derives frame duration from the existing high bits of `data[7]`, computes the actual encode/decode `frame_size` from the helper, and includes duration context in assertion messages. The existing lower-bit DTX/loss interpretation and Phase 2.2 oracle flow are unchanged.

Emotional temperature: satisfied with the scope control. The main thing I kept checking was that no stateful sequence target or codec source slipped into the diff.

## Validation

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`: passed, 4 tests.
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`: passed, 20 active tests; 1 existing ignored operator report.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_roundtrip`: passed.
- `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`: passed.
- `timeout 120 cargo fmt --check`: passed.

Residual risk: this changes the semantic interpretation of existing `fuzz_roundtrip` corpus bytes because high bits of `data[7]` now select duration. That is intentional per HLD and old inputs remain parseable when long enough. No fuzz campaign was run.
