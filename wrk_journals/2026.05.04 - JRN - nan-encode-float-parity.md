# nan-encode-float parity journal

Date: 2026-05-04
Task: HLD-only investigation for
`/home/md/language/ropus/tests/fuzz/known_failures/nan-encode-float`

## Notes

- Read `/home/md/language/ropus/AGENTS.md` before edits.
- Kept the work to documentation only: no production code edits.
- Existing untracked docs and journals were present before this task. I left
  them alone.
- The known-failure README was accurate about the main direction: Rust maps
  NaN to `0` in `float2int16`, C maps NaN to `-32768`.
- The current fuzz replay is masked by parser-side sanitization. The replay
  command exits cleanly today because `fuzz_encode` maps non-finite floats to
  `0.0` before either encoder sees them.
- Parsed the fixture:
  `sr=12000`, `ch=1`, `app=2048`, `bitrate=49970`, `complexity=7`,
  `vbr=true`, `vbr_constraint=true`, `fec=2`, `dtx=true`,
  `loss_perc=22`, `frame_size=240`.
- Found four NaN samples in the first frame at indices `68`, `69`, `114`,
  and `115`.
- Important nuance: the actual codec parity bug appears to be NaN-specific.
  `+Inf` and `-Inf` already clamp the same way in the inspected Rust and C
  conversion paths. The fuzz workaround zeros both NaN and Inf, which is wider
  than the root issue.
- After the draft, the broader worktree showed concurrent `tests/fuzz` edits.
  I refreshed the sanitizer line references in the HLD and did not modify those
  source files.

## Commands Run

```bash
git status --short
```

```bash
timeout 120s cargo +nightly fuzz run --fuzz-dir tests/fuzz fuzz_encode \
  tests/fuzz/known_failures/nan-encode-float/crash-54acd282f7bd446d92811b719f183d6eea294f84
```

Result: exit code `0`; current parser workaround masks the fixture.

```bash
timeout 60s cargo test -p ropus test_division_and_conversion_helpers -- --nocapture
```

Result: `1 passed`.

## Emotional Temperature

Calm and fairly confident. Mild frustration that the known failure is no longer
a live repro because the workaround is doing its job; that makes the HLD need a
careful distinction between "fixture replay" and "codec gap repro". The good
part is that the C macro behavior is small and explicit, so the proposed fix can
stay very tight.

## Next Step

If Arthur approves implementation, make the C-order clamp change in
`/home/md/language/ropus/ropus/src/types.rs`, add direct unit coverage, add one
end-to-end C/Rust float encode test with injected NaNs, then remove the
differential fuzz parser workaround only after the known fixture replays cleanly
without it. Leave the safety-target sanitizer for a separate decision because
those targets can hit high-complexity analysis with original NaN PCM.

## Implementation Update

Arthur signed off the HLD and asked for implementation.

Changes made:

- Changed `float2int16` to clamp in C `MAX32` then `MIN32` order.
- Patch-bumped `ropus` to `0.12.6` in `ropus/Cargo.toml` and the root
  `Cargo.lock`.
- Note: the root `Cargo.lock` also shows a concurrent `full-test` version
  update from other in-flight work; I did not touch the `full-test` sources.
- Added NaN and infinity assertions to
  `types::tests::test_division_and_conversion_helpers`.
- Added `test_float_pcm_nan_encode_matches_c_reference` to the harness
  differential suite, injecting fixture-equivalent NaN bit patterns at indices
  `68`, `69`, `114`, and `115`.
- Removed the non-finite-to-zero parser workaround from `fuzz_encode` and
  `fuzz_roundtrip` only.
- Kept the safety-target sanitizer in place per HLD risk note.
- Replayed the original known-failure input successfully through both
  differential targets, then copied it into:
  `tests/fuzz/corpus/fuzz_encode/regression-nan-encode-float` and
  `tests/fuzz/corpus/fuzz_roundtrip/regression-nan-encode-float`.
- Updated the known-failure README as a resolved triage record instead of
  deleting the directory.

Commands/results:

```bash
timeout 120s cargo test -p ropus test_division_and_conversion_helpers -- --nocapture
```

Result: passed, `1 passed`.

```bash
timeout 300s cargo test -p ropus-harness --test c_ref_differential \
  test_float_pcm_nan_encode_matches_c_reference -- --nocapture
```

Result: passed, `1 passed`.

```bash
timeout 180s cargo +nightly fuzz run --fuzz-dir tests/fuzz fuzz_encode \
  tests/fuzz/known_failures/nan-encode-float/crash-54acd282f7bd446d92811b719f183d6eea294f84
```

Result: passed; fixed-input replay executed in `4 ms`.

```bash
timeout 180s cargo +nightly fuzz run --fuzz-dir tests/fuzz fuzz_roundtrip \
  tests/fuzz/known_failures/nan-encode-float/crash-54acd282f7bd446d92811b719f183d6eea294f84
```

Result: passed; fixed-input replay executed in `22 ms`.

After trimming format-only churn from the fuzz targets, reran both replays:

```bash
timeout 180s cargo +nightly fuzz run --fuzz-dir tests/fuzz fuzz_encode \
  tests/fuzz/known_failures/nan-encode-float/crash-54acd282f7bd446d92811b719f183d6eea294f84
```

Result: passed; fixed-input replay executed in `5 ms`.

```bash
timeout 180s cargo +nightly fuzz run --fuzz-dir tests/fuzz fuzz_roundtrip \
  tests/fuzz/known_failures/nan-encode-float/crash-54acd282f7bd446d92811b719f183d6eea294f84
```

Result: passed; fixed-input replay executed in `69 ms`.

```bash
timeout 120s cargo fmt -p ropus -p ropus-harness -- --check
```

Result: passed.

Final focused test reruns after formatting:

```bash
timeout 120s cargo test -p ropus test_division_and_conversion_helpers -- --nocapture
```

Result: passed, `1 passed`.

Added the second fixture NaN pattern (`0xffae_ffae`) to the direct unit
assertions and reran:

```bash
timeout 120s cargo test -p ropus test_division_and_conversion_helpers -- --nocapture
```

Result: passed, `1 passed`.

```bash
timeout 300s cargo test -p ropus-harness --test c_ref_differential \
  test_float_pcm_nan_encode_matches_c_reference -- --nocapture
```

Result: passed, `1 passed`.

Emotional temperature: steady. The fix was as small as expected. The main
thing I had to keep watching was scope: it is tempting to unmask every
non-finite fuzz path, but the safety targets are broader because analysis can
see the original float buffer. Leaving those for a separate decision still
feels technically correct.
