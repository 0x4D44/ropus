# Final Testing Gaps Reassessment Journal

Date: 2026.05.06

## Task

Arthur requested a documentation-only reassessment after commit
`e25a0fe Add platform sanitizer release breadth gate`.

Required outputs:

- `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - final testing gaps reassessment.md`
- `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - final testing gaps reassessment.md`

Constraints:

- Do not implement code.
- Do not commit.
- Do not revert or delete other agents' work.
- Distinguish testing-infrastructure gaps from codec implementation debts and
  performance regressions exposed by the new gates.

## Read Log

Read and cross-checked:

- Original review:
  `/home/md/language/ropus/wrk_docs/2026.05.05 - testing gaps review.md`
- Original plan:
  `/home/md/language/ropus/wrk_docs/2026.05.05 - PLN - testing improvement plan.md`
- Current oracle matrix:
  `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`
- Recent docs and journals through the platform/sanitizer gate, including the
  performance, platform/sanitizer, corpus, DRED/DNN, PLC, fuzz, and frame
  duration work.
- Remaining mutation candidates:
  `/home/md/language/ropus/.cargo/mutants.toml` and
  `/home/md/language/ropus/tools/mutation_test.sh`.

Read-only checks:

- `HEAD` is `e25a0fe Add platform sanitizer release breadth gate`.
- `cargo-mutants 27.0.0` is installed.
- The mutation runner still points at top-level `src/...`.
- There is no top-level `/home/md/language/ropus/src`; current code is under
  `/home/md/language/ropus/ropus/src`.

## Assessment

The infrastructure campaign has done the important thing: it converted vague
"tested" claims into explicit contracts. Core, fuzz, DRED/DNN, corpus,
performance, platform, and sanitizer lanes now have claim/no-claim/fail/debt
semantics instead of collapsing into a single green story.

The strict original phase list still has mutation testing revival open. I do
not think that should be treated as a release-preflight blocker, but it is
genuinely testing infrastructure rather than codec debt. The right final slice,
if Arthur wants literal plan closure, is a small HLD-first mutation audit
revival.

## Emotional Temperature

I feel mostly satisfied with the state of the release gate now. The temptation
is to keep digging because several red or uncomfortable facts remain, especially
DRED parity and the `8.74x` decode measurement. But those are no longer hidden
testing gaps. The infrastructure is now honest enough that continuing to expand
it without a narrow target would be busywork.

The mutation runner irritates me because it is small, stale, and clearly
unfinished relative to the original plan. It is also exactly the kind of thing
that can waste a lot of compute and produce misleading percentages if we rush
it. The final recommendation therefore keeps it narrow and periodic.

## Output

Wrote:

- `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - final testing gaps reassessment.md`
- `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - final testing gaps reassessment.md`

Recommendation recorded:

- Stop the release-confidence infrastructure campaign if measuring against the
  2026.05.05 success criteria.
- If measuring against every original phase item, run one final HLD-first
  mutation testing revival slice as a periodic audit lane.
