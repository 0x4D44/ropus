# Stage 7 — DNN Wiring + DNN-Adjacent CTLs (Supervisor Journal)

**Date opened:** 2026-04-19
**Supervisor:** Claude (Opus 4.7, 1M)
**Role:** dispatch + review agent teams; no direct coding/debugging.

## Scope (from earlier briefing)

Per `wrk_docs/2026.04.19 - HLD - stages-6-8-relaxed-oracles.md` (supersedes §6-8
acceptance) and `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md` §5/§10:

**Already done** (merged 2026-04-08, commit `6822dd6`):
- PitchDNN wiring in LPCNet
- `LPCNetPLCState` in `OpusDecoder` + `set_dnn_blob()` method
- SILK neural PLC update/conceal paths
- CELT `FRAME_PLC_NEURAL` path + sinc resampler + cross-fade
- LPCNet threaded through decode call chain
- DRED FEC stubs (`fec_add`/`fec_clear`)

**Open work** (this journal tracks):

| Pre-work (harness) | CTL routing (capi) | Tests |
|---|---|---|
| `-ffp-contract=off` in `harness/build.rs` (**already in tree**, verify) | `OPUS_SET_DNN_BLOB_REQUEST` round-tripped through `capi/src/ctl.rs` + `ctl_shim.c` | Tier-1: byte-exact encode w/ analysis + FARGAN |
| Remove `#define DISABLE_FLOAT_API 1` in `harness/config.h:23` | `OPUS_SET_COMPLEXITY` on decoder (gates neural PLC ≥5) | Tier-1: sample-exact PLC decode under seeded loss |
| Add DNN C sources (`nnet.c`, `fargan.c`, `nnet_default.c`) to `harness/build.rs` | Tier-2 fallback: SNR > 60 dB | |
| Enable `ENABLE_DEEP_PLC` in `harness/config.h` | Notable non-goals: `OSCE_BWE`, `DRED_DURATION`, `QEXT` ship with Stage 8 | CTL conformance: SET→GET round-trips |

## Acceptance (from HLD)

Try **tier 1** first (bit-exact); fall back to **tier 2** only if tier 1 fails:

**Tier 1:**
1. Byte-exact encode with analysis + FARGAN both enabled
2. Sample-exact decode under seeded packet-loss pattern
3. CTL conformance round-trip: `SET_DNN_BLOB`, `SET_COMPLEXITY` (decoder), `SET_IGNORE_EXTENSIONS`

**Tier 2 (fallback):**
1. FARGAN PCM quality: SNR > 60 dB vs C-decoded PCM under packet loss
2. CTL conformance (same)
3. No regressions on lossless paths (no-loss encode/decode still byte/sample exact)

## Plan (phases)

```
Phase 0  AUDIT           — verify current state vs HLD, produce punch list
Phase 1  CTL routing     — TDD: tests fail → wire OPUS_SET_DNN_BLOB_REQUEST → tests pass
Phase 2  Harness pre-work — config.h + build.rs + gate on weights presence
Phase 3  Integration tests — tier 1 attempt; tier 2 fallback if needed
Phase 4  Final verify    — two independent agents compare impl to HLD; iterate if <100%
```

Phase 1 is chosen before Phase 2 because CTL routing is **unblocked by weights**
(just pointer plumbing). Phase 2/3 need the DNN weights tarball fetched
(`cargo run -p fetch-assets -- weights`). If weights aren't available in this
sandbox, Phase 2/3 will emit clear skip/error messages.

## Agent team structure

- **Auditor** — reads HLD + current tree, produces current-state punch list
- **Implementer(s)** — one per logical chunk (CTL routing, harness config, tests)
- **Code-reviewer** — invokes `code-reviewer` skill on each implementer's diff
- **Apply-feedback** — invokes `code-review-apply-feedback` skill
- **Devils-advocate** — adversarial review, parallel to main reviewer; asks
  "what's missing / hiding / weakly specified?"
- **Verifier(s)** — final phase, two in parallel, comparing impl to HLD

Commit boundary: **after each phase's apply-feedback** and **after final verify**.

## Log

(entries below, newest at bottom)

### 2026-04-19 — kickoff

- Committed `tools/fetch-assets` (commit `19ffc1b`, rebased to `95629c0` after
  pull) — prerequisite for Stage 7.
- Arthur confirmed weights are now fetched:
  `reference/.opus_data-a5177ec6...installed` present;
  `reference/dnn/{fargan_data.c, lossgen_data.c, pitchdnn_data.c, dred_rdovae_*, lace_data.c, nolace_data.c, bbwenet_data.c}`
  all on disk.

### 2026-04-19 — audit complete (phase 0)

Auditor (Explore agent) produced a file-and-line-grounded punch list. Headline
findings:

| Area | Status |
|---|---|
| DNN Rust modules (10,318 LOC) | DONE — compile, pass unit tests |
| Encoder `SET/GET_COMPLEXITY` CTL | DONE |
| Decoder `SET/GET_IGNORE_EXTENSIONS` CTL | DONE |
| `-ffp-contract=off` in harness | DONE (`harness/build.rs:226`) |
| `DISABLE_FLOAT_API` removed | DONE (`harness/config.h:20-26`) |
| **`ENABLE_DEEP_PLC`** | NOT DONE (`harness/config.h:35` `/* #undef ENABLE_DEEP_PLC */`) |
| **DNN C sources in harness build** | NOT DONE (`harness/build.rs:200-203` has only `analysis.c`, `mlp.c`, `mlp_data.c`) |
| **`OPUS_SET_DNN_BLOB_REQUEST`** | NOT DEFINED, NOT ROUTED |
| **Decoder `SET/GET_COMPLEXITY`** | NOT ROUTED (decoder has `complexity` field but no CTL dispatch) |
| **`LPCNetPLCState` field on `OpusDecoder`** | NOT PRESENT |
| **`set_dnn_blob` method** | NOT PRESENT |
| **`FRAME_PLC_NEURAL` in CELT decoder** | NOT PRESENT |
| **`lpcnet` threaded through decode chain** | NOT PRESENT — `DnnPlcArg<'_> = ()` (`ropus/src/opus/decoder.rs:15-16`) |

### 2026-04-19 — root cause of missing wiring

Git archaeology (not speculation):
- Commit `6822dd6` (2026-04-08 12:56) **Wire DNN modules into codec pipeline
  behind dnn feature flag** (+547 lines across `src/opus/decoder.rs`,
  `src/silk/decoder.rs`, `src/celt/decoder.rs`, `src/dnn/lpcnet.rs`, `Cargo.toml`).
- Commit `fa031ab` (Stage 1 restructure) moved `src/*` → `ropus/src/*`.
- Commit `5abd1a9` (2026-04-18 16:16) **refactor(ropus): remove feature flags
  (Stage 2)** — message: "keep the default-enabled branches, delete the off-path
  bodies". In practice this kept the stub (no-DNN) branches and deleted the
  gated-on DNN branches, removing the 6822dd6 wiring.

Almost certainly unintentional regression from Stage 2, not a deliberate revert.

**Implication for Stage 7b**: the 6822dd6 diff is a usable reference for
re-porting the integration. Path translation `src/*` → `ropus/src/*`; the
`#[cfg(feature = "dnn")]` gates drop (features are gone); everything else
should map cleanly. Saves us from re-designing the integration from scratch.

### 2026-04-19 — scope decision (per Arthur)

Split into **7a** (quick, bankable) and **7b** (bigger, uses weights):

**Stage 7a** — no weights needed for plumbing, weights present for harness link:
- Define `OPUS_SET_DNN_BLOB_REQUEST` constant; route SET+GET in
  `capi/src/ctl.rs` + matching shim in `capi/src/ctl_shim.c`
- Route decoder `SET/GET_COMPLEXITY` CTLs
- Add round-trip tests (SET→GET) for each new CTL
- Enable `ENABLE_DEEP_PLC` in `harness/config.h`
- Add the DNN C sources to `harness/build.rs` (scalar only: `nnet.c`,
  `nnet_default.c`, `fargan.c`, `pitchdnn.c`, `lpcnet.c`, `lpcnet_plc.c`,
  `lpcnet_enc.c`, `lossgen.c`, the corresponding `*_data.c` weight files,
  plus support: `nndsp.c`, `freq.c`, `burg.c`, `kiss99.c`)
- Verify harness still compiles with weights present
- Commit

**Stage 7b** — port 6822dd6 adapted for current tree; run tier-1/tier-2 tests:
- Use `git show 6822dd6` as a reference; translate paths `src/*` → `ropus/src/*`;
  drop `#[cfg(feature = "dnn")]` gates
- Implement `LPCNetPLCState` field + `set_dnn_blob` on `OpusDecoder`
- Thread `lpcnet` through silk decode + celt decode; add `FRAME_PLC_NEURAL`
- Replace `DnnPlcArg<'_> = ()` stub with real reference
- Wire PitchDNN stub replacement in `lpcnet.rs`
- Run tier-1 attempt: byte-exact encode, sample-exact PLC decode
- Tier-2 fallback on f32-divergence failures
- Commit

### 2026-04-19 — Stage 7a implementer report (agent a6204f90)

**CTL routing landed cleanly:**
- `capi/src/ctl.rs` — `OPUS_SET_DNN_BLOB_REQUEST = 4052` defined; decoder SET/GET_COMPLEXITY routed; new pointer-dispatcher `mdopus_decoder_ctl_set_dnn_blob`.
- `capi/src/ctl_shim.c` — matching C variadic shims.
- `capi/src/lib.rs` — CTL-coverage comment updated.
- `ropus/src/opus/decoder.rs` — Stage-7a placeholder `set_dnn_blob(&[u8]) -> Result<(), i32>` (always Ok), 3 new round-trip tests all green.
- `cargo build -p ropus|capi|ropus-harness` all clean.
- `cargo test -p ropus --lib` = 1810 pass / 1 pre-existing failure (verified independent of this work via stash).

**Harness pre-work BLOCKED:**
- `ENABLE_DEEP_PLC` + `FIXED_POINT=1` is unsupported in xiph upstream.
- Root: `reference/dnn/lpcnet_tables.c:198-206` has a static `kiss_fft_state kfft` initialized with 7 elements (float layout). `FIXED_POINT=1` adds a `scale_shift` field to `kiss_fft_state` per `reference/celt/kiss_fft.h:99-110` → 8 initializers needed → MSVC `C2078 too many initializers`.
- Evidence of upstream-unsupportedness: xiph's `.github/workflows/autotools.yml` EnableDNN job uses float-mode; fixed-point CI jobs don't enable DNN.
- Implementer staged the source list + include path but left feed-in commented out pending supervisor decision.

**Open questions from implementer:**
1. Switch harness C side to float mode for DEEP_PLC comparison?
2. Carry a tiny patched `lpcnet_tables.c` in the harness?
3. Should `OPUS_SET_DNN_BLOB` also route on the **encoder** side for DRED encoder weights? (`reference/src/opus_encoder.c:3325`) — flagged as Stage 7b/8 consideration.

### 2026-04-19 — dispatching reviewers (background)

Two parallel reviewers on the Stage 7a diff (CTL portion only — harness work is stubbed/commented and not in scope for review yet):
- **Reviewer A**: invokes `code-reviewer` skill on the uncommitted diff.
- **Reviewer B** (devils-advocate): adversarial — challenges assumptions, hunts for hidden costs, checks that the placeholder stub won't be forgotten by Stage 7b.

Blocker escalation to Arthur in parallel.

### 2026-04-19 — Reviewer A (code-reviewer skill) report

**Verdict: approve-with-nits.**

Verified independently: CTL `4052` matches the xiph convention (neighbouring
`DRED_DURATION` at 4050/4051, `OSCE_BWE` at 4054/4055; `GET_DNN_BLOB` at 4053
commented-out upstream, matching our write-only decision). Null-check +
`len<0` rejection in `capi/src/ctl.rs:368-383` mirrors
`reference/src/opus_decoder.c:1222-1225` exactly. Variadic `va_arg` types in
`capi/src/ctl_shim.c:289-294` are correct (`opus_int32 == int` on this
platform per `reference/include/opus_types.h:55,70,73`). Coverage comment in
`capi/src/lib.rs:82-84` is accurate.

Non-blocking nits:
1. `ropus/src/opus/decoder.rs:1453-1457` — placeholder lacks `TODO(stage-7b)`
   marker; easy to miss post-merge.
2. `ropus/src/opus/decoder.rs:3029-3044` — new CTL round-trip partly
   duplicates `set_complexity_range_and_getter` at line 4285. Keep both but
   note the intent, or consolidate.
3. Two sources of truth for `4052` (`capi/src/ctl.rs:72` + shim `#define`).
   A single runtime assertion would catch drift.

### 2026-04-19 — Reviewer B (devils-advocate) report

**Key findings beyond reviewer A:**

- **Upstream ACTIVELY prohibits `FIXED_POINT + DEEP_PLC`**: `reference/configure.ac:973`
  has `AC_MSG_ERROR`. The implementer's writeup called the combo "de facto
  unsupported"; DA strengthens this — it's an explicit error, not a gap in CI.
  Option C is legitimately a workaround of an upstream prohibition; worth a
  one-hour spike to confirm the issue is only `lpcnet_tables.c`'s kfft init and
  not wider FIXED_POINT entanglement.
- **Stub hardening (MED)**: `ropus/src/opus/decoder.rs:1453` `Ok(())` stub +
  tests at `decoder.rs:3050-3059` pinning `Ok(())` for non-empty blobs will
  mislead Stage 7b. Better: return `Err(OPUS_UNIMPLEMENTED)`, test pins that,
  Stage 7b flips the test when replacing the impl. Fail loudly, not silently.
- **FARGAN-is-decoder-only note**: `stages-6-8-relaxed-oracles.md:161-168`
  tier-1 text "byte-exact encode with analysis + FARGAN" conflates analysis
  (Stage 6, encoder) with FARGAN (decoder-only). Encoder tier-1 needs only
  analysis. Document, don't "fix" the HLD.
- **Tier-1 framing**: float DNN non-determinism across platforms means
  byte/sample-exact for DEEP_PLC is aspirational; tier-2 (SNR > 60 dB) is
  the realistic target for PLC. Stage 7b brief should plan for tier-2 as
  primary, tier-1 as bonus.
- **Encoder-side `OPUS_SET_DNN_BLOB`**: xiph exposes it under `#ifdef ENABLE_DRED`
  (`reference/src/opus_encoder.c:3333`), which pins it firmly as Stage 8 (DRED),
  not a 7b/8 hedge.

### 2026-04-19 — dispatching combined apply-feedback + Option C agent

Resuming the 7a implementer (agent `a6204f90`) with a combined brief:
1. Apply reviewer A nits: TODO markers, const drift check, test-dedup comment.
2. Apply DA stub hardening: `set_dnn_blob` returns `Err(OPUS_UNIMPLEMENTED)`; test pins this.
3. Implement Option C: separate `cc::Build` for DNN sources with `-UFIXED_POINT`.
4. Do the spike DA recommends BEFORE committing Option C — verify that only
   `lpcnet_tables.c`'s kfft init breaks under FIXED_POINT, not deeper coupling.
5. All existing tests must still pass.
