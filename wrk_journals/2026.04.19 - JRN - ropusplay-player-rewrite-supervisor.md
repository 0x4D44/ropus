# JRN: ropusplay player rewrite — supervisor log

Author: Arthur + Claude (supervisor role)
Date: 2026-04-19
HLD: `wrk_docs/2026.04.19 - HLD - ropusplay-directory-loop-status-keyboard.md` (rev 2)

Supervising a multi-agent implementation of the ropusplay player rewrite.
Not writing or debugging code myself — delegating to implementer / reviewer /
devil's-advocate / applier agents and recording outcomes here.

## Baseline

- Branch: `main`
- Last committed ropus-tools-core change: `799c818` (OpusTags landed).
- In-flight by other teams (do not touch):
  - `EncodeOptions` extensions in `options.rs`
  - `encode.rs`, `consts.rs`, `container/mod.rs`, `lib.rs`, `audio/mod.rs`
  - new `container/picture.rs`, `audio/downmix.rs`
  - `ropus/src/dnn/dred.rs`, `ropusenc/src/main.rs`
  - `harness/Cargo.toml`, `Cargo.lock`
- My write set (minimal, non-overlapping):
  - `ropus-tools-core/Cargo.toml` — add crossterm, bump version
  - `ropus-tools-core/src/options.rs` — append `LoopMode` + extend `PlayOptions` (bottom of file; no overlap with in-flight `EncodeOptions` edits at top)
  - `ropus-tools-core/src/commands/play.rs` — full rewrite (no other agent touching)
  - `ropusplay/src/main.rs` — add `--loop` flag, wire options
  - `ropusplay/Cargo.toml` — bump version

## Plan

Five stages. Each stage: implementer → (reviewer ∥ devil's-advocate) → applier → commit.

1. Scaffolding + deps + types
2. Pure-logic helpers w/ TDD (build_playlist, format_status_line, advance_on_error)
3. Player rewrite (main FSM, raw mode, event loop, decode UX, error handling)
4. CLI wiring (--loop flag, version bumps verified)
5. HLD conformance audit

Commits land after each stage. Build is scoped to the crates I touch
(`cargo build -p ropus-tools-core -p ropusplay`), not the full workspace —
other teams' in-flight code may not compile and that's their problem.

## Agent roster

- **impl-A**: general-purpose, writes code per stage brief.
- **review-A**: general-purpose, invokes `code-reviewer` skill on staged diff.
- **devil-A**: general-purpose, adversarial — looks for gaps impl-A missed
  and things review-A might rubber-stamp.
- **apply-A**: general-purpose, invokes `code-review-apply-feedback` skill to
  action review comments.
- **audit-A**: general-purpose, stage-5 conformance check vs HLD.

Review and devil's-advocate run in parallel after implementation; applier
runs once their findings are collected.

---

## Stage 1 — scaffolding + deps

Status: COMPLETE

Impl-A (`a5b9b35078f29366d`): delivered clean build. Six files touched
(scope rippled from the two named in the HLD to six: the three sibling CLI
Cargo.tomls needed dep-requirement bumps because cargo resolves path deps
against the version field, and one `main.rs` call-site fix for the new
required field — both legitimate mechanical consequences). Impl agent
correctly stopped on the `main.rs` conflict and asked rather than
unilaterally touching a file the brief said was off-limits. Approved the
one-liner.

### Review pass (parallel)

- **Review-A** (`a85b3b61e25a124b6`) — ✅ ship. Two nits: crossterm 0.29.0
  now on crates.io (pinning to 0.28 is fine, one minor behind), line-ending
  churn across six Cargo.lock touches is pre-existing Windows noise.
- **Devil-A** (`a31fb986fa349fe58`) — three challenges:
  1. Revert sibling Cargo.toml bumps as "cosmetic". **Rejected**: the claim
     that path-dep `version` is only consulted for publishing is wrong.
     Cargo enforces `version` as a requirement; `^0.1` does not match
     `0.2.0` path crate → resolver error. Bumps were necessary.
  2. Semver noise for back-to-back 0.x bumps. **Not actionable**; devil
     themselves conceded "acceptable as-is".
  3. Add `// TODO(stage4)` marker in `main.rs` against silent forget.
     **Rejected**: this is exactly the class of comment CLAUDE.md tells us
     not to write (task-referencing, will rot). Task #4 and this journal
     are the forget-prevention mechanism; a load-bearing `TODO` comment
     that duplicates them is worse than both.

Conclusion: no apply-feedback pass needed. Ship as-is.

### Commit

`7d64c30` — `feat(ropusplay): scaffold crossterm dep and PlayOptions.loop_mode default`.

Noted: concurrent commit `c4e7640` (opus-tools-parity team) swept my
uncommitted `LoopMode` + `PlayOptions.loop_mode` edits into their commit
via `git add -A`. The LoopMode shape on HEAD is mine; the attribution is
theirs. My commit lands the rest (crossterm, version bumps, main.rs
one-liner). Not worth reverting their commit — the sweep was accidental
and the code itself is correct. Lesson for future stages: commit fast,
don't leave work uncommitted across agent boundaries in a multi-agent
tree.

---

## Stage 2 — pure-logic helpers with TDD

Status: COMPLETE

**impl-B** (`aa0a6af0b98f4ebdc`): delivered `build_playlist`,
`resolve_display_name`, `format_status_line`, `advance_on_error` +
sub-helpers + `ScratchDir` RAII tempdir + 27 unit tests. 65/65 tests pass.
Existing `pub fn play(...)` untouched per brief. File grew ~570 lines
(mostly tests).

### Review pass (parallel)

- **Review-B** (`ae3ff47c976c87d3e`) — ✅ ship. HLD coverage audit table
  shows every bullet in Status line / Width handling has a named test.
  Two out-of-scope nits (rustfmt column overflow cosmetic, empty-path
  fallback clarity). `cargo test`: 65 pass reproduced.
- **Devil-B** (`a2518fadf2f404a93`) — two findings. Six others
  investigated and rejected.

### Findings + resolution

1. **Pause-glyph cell-width mismatch** (medium). `⏸ U+23F8` is
   Emoji-classified and renders as 2 display cells on Windows Terminal;
   `chars().count()` returns 1. Truncation budget uses `chars().count()`,
   so the paused line got a 1-char-too-generous title budget and could
   wrap on narrow terminals. Exactly the flicker the HLD tried to avoid.

   **Applier-B** (`ae598cae54bf394e3`) — first pass padded the play
   glyph `"▶ "` to equalize visual rendering (3 cells each). Visually
   state-invariant, but the char-count budget math remained off by 1 in
   pause state. Redirected via SendMessage: swap pause glyph to
   `‖ U+2016 DOUBLE VERTICAL LINE` (cell-width 1, `chars().count()` == 1
   cells). Fix applied: 1-line glyph change + 1-line test update.
   Budget math now correct for both states; no new deps, no char-vs-str
   gymnastics. Final: 65 pass, clean build, zero clippy on `play.rs`.

2. **`#[allow(dead_code)]` × 4 on staging helpers** (low). Devil flagged
   the forget-risk. Accepted for now per the staging discipline; stage 3
   wires all four helpers in and removes the allows. The supervisor
   journal (this document) + task list are the forget-prevention
   mechanism.

### Commit

Pending.
