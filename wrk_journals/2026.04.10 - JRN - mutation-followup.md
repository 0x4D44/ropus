# Journal: Mutation Testing Follow-up Actions

Date: 2026-04-10

## Context

Following up on the mutation testing pilot report (2026-04-08). Two workstreams identified
as highest-impact follow-ups:

1. **Dead code cleanup** — Remove 3 dead functions from `math_ops.rs` + 20 dead wrappers from `types.rs`
2. **Newton iteration FFI differential tests** — Add exact-match FFI tests for `celt_rsqrt_norm`, `celt_rcp_norm16`, `celt_rcp_norm32`, `celt_rcp`

## Approach

- Two agents dispatched in parallel, one per workstream
- Each agent writes an HLD before coding; I review and sign off
- After implementation, agents run `/code-reviewer` and `/code-review-apply-feedback`
- Journal updated throughout

## Timeline

### 10:00 — Dispatched agents for HLD design

Briefed both agents with codebase context:
- Dead code agent: knows the 3 functions, needs to identify the 20 types.rs wrappers
- Newton agent: knows existing FFI pattern (debug_helper.c + bindings.rs), knows `celt_rsqrt_norm32` already has a wrapper but the others don't

Waiting for HLD proposals before signing off on implementation.

### HLD reviews completed — both approved

**Dead code cleanup HLD:**
- Agent found 18 dead wrappers in types.rs (not 20 as the report estimated). Verified each
  function individually across 72 total functions. `shr` correctly identified as alive
  (internal callers from live functions like `mult16_16_q15`).
- Also caught `celt_float2int16` as additional dead code not in original report.
- 3-way classification: DELETE (19 functions), `#[cfg(test)]` (3 functions), keep (5 false positives).
- Minor concern: `#[cfg(test)]` gating may not fully eliminate mutation noise since
  `cargo-mutants` still sees `#[cfg(test)]` code. Clean deletion would be more effective.
  Decided to accept the compromise — tests have marginal math validation value.
- **Approved** — dispatching for implementation.

**Newton iteration FFI tests HLD:**
- 4 new C wrappers + FFI bindings, plus upgrade of existing `celt_rsqrt_norm32` sweep.
- Smart strategy: exhaustive sweeps for 16-bit functions (49K + 16K values), 200K-point
  dense sweeps for 32-bit, strategic multi-range sweep for `celt_rcp`.
- Exact match assertions (==), no tolerance — the whole point of this exercise.
- Correctly handled `celt_rcp_norm16` not being in header (extern declaration pattern).
- **Approved** — dispatching for implementation.

### Dispatched agents for implementation

Both agents ran in isolated worktrees to avoid conflicts.

### Implementation complete — both agents finished

**Dead code cleanup results:**
- Deleted `celt_atan2p` (14 lines, zero callers)
- Marked `#[cfg(test)]`: `celt_atan01` (became dead when its sole caller was deleted),
  `celt_float2int16`, `opus_limit2_checkwithin1`
- **HLD deviation**: `celt_cos_norm32` was NOT marked `#[cfg(test)]` — the agent correctly
  discovered it's used by the `mdopus-compare` binary in `tests/harness/main.rs`. The HLD
  only checked `src/` for callers and missed this. Good catch.
- Deleted 18 dead wrappers from types.rs, cleaned up test assertions
- 84/84 module tests pass

**Newton iteration FFI test results:**
- 4 C wrappers + FFI bindings added
- `celt_rsqrt_norm32` sweep upgraded 20K → 200K points, range expanded to [2^29, 2^31-1]
- Replaced misleadingly-named old "celt_rcp_norm32 sweep" (was re-testing sqrt32/rsqrt_norm32)
- All 468,633 test points pass with zero mismatches:
  - `celt_rsqrt_norm`: 49,152 values PASS (exhaustive)
  - `celt_rsqrt_norm32`: 200,003 values PASS
  - `celt_rcp_norm16`: 16,384 values PASS (exhaustive)
  - `celt_rcp_norm32`: 200,003 values PASS
  - `celt_rcp`: 3,091 values PASS (strategic sweep)

### Merged and verified

- Both patches applied to main repo without conflicts (disjoint file sets)
- `cargo build` — clean
- `cargo test` — 789 passed, 3 failed (all 3 are pre-existing: 2 DNN + 1 opus encoder)
- `cargo run --bin mdopus-compare -- mathcompare` — all sweeps PASS

## Summary

| Workstream | Files changed | Lines removed | Lines added | Tests added |
|------------|---------------|---------------|-------------|-------------|
| Dead code cleanup | 2 (math_ops.rs, types.rs) | ~156 | ~3 | 0 (removed dead test assertions) |
| Newton FFI tests | 3 (debug_helper.c, bindings.rs, main.rs) | ~33 | ~258 | 5 test sections, 468K+ test points |

Both workstreams are complete. The mutation testing pilot's top two recommendations
have been addressed.
