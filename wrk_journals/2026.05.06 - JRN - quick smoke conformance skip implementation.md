# 2026.05.06 - JRN - quick smoke conformance skip implementation

## Initial read

Read AGENTS.md and the signed HLD. This is nicely bounded: add an internal quick release-preflight Stage 2 skip policy, keep the parser unchanged, update help text, and make the printed Stage 2 command honest. I feel mildly wary of libtest's global substring `--skip`, but the HLD calls that risk out clearly and limits it to the smoke path.

## TDD pass

Added command-shape tests first and ran a narrow full-test unit target. It failed on the old three-argument `build_cargo_args` signature, which is the expected red state. Now wiring the policy through without touching parser behavior.

## Implementation check

Implemented the internal `quick && release_preflight` policy, added a small Stage 2 header helper, and kept the parser untouched except for help text. The full `cargo test -p full-test --locked` pass is green. Emotional temperature: steady; the only thing I am watching now is whether the smoke command exits cleanly inside the requested timeout.

## Validation result

The requested quick release-preflight smoke printed the corrected Stage 2 command with `--skip extensions`, then timed out after 300 seconds while still in Stage 2 with no additional output. I am stopping as instructed instead of starting a debugging loop. `cargo fmt --check` initially wanted rustfmt wrapping only; after `cargo fmt`, `cargo fmt --check` and `cargo test -p full-test --locked` both pass.
