# Worktree Commit Triage

Date: 2026-05-04

Arthur asked me to classify the uncommitted tree, decide what belongs in Git
versus `.gitignore`, group the work into sensible commits, include docs and
journals, and push.

Initial classification:

- Core parity fixes: `ropus` encoder/type/analysis changes plus C-reference
  differential tests. This covers float NaN clamp parity, multistream analysis
  parity, CTL return-code parity, and the `new_boxed` padding-sensitive test
  failure.
- Validation tooling: `full-test` now provisions IETF vectors before allowing
  the conformance gate to look green, with HTML/JSON reporting and safer fetch
  scripts.
- Fuzz/oracle work: new CTL sequence fuzz target, a seed input, C reference CTL
  constants, decoder oracle classification, and known-failure README updates.
- Process docs: HLDs, brief, and journals under `wrk_docs/` and
  `wrk_journals/`.

Ignore decision:

- No new `.gitignore` rule is needed from the visible untracked files. The only
  non-doc untracked files are Rust source/tests and a small tracked seed corpus
  input.
- Generated/local outputs are already ignored: `logs/`, `fuzz_crashes/`,
  `tests/vectors/ietf/`, fuzz build outputs, and `tests/results/`.

Emotional temperature: cautious. The tree is broad and clearly came from
several related investigation streams, so I am wary of over-splitting shared
manifest/lockfile changes into commits that would be internally inconsistent.
The sensible path is a small number of coherent commits with validation noted
explicitly.

Validation run before commit:

- `timeout 180 cargo fmt --all -- --check` passed.
- `timeout 180 cargo fmt --manifest-path tests/fuzz/Cargo.toml --all -- --check` passed.
- `timeout 300 cargo test -p ropus float2int16 -- --nocapture` passed.
- `timeout 300 cargo test -p ropus opus::analysis::tests::test_new_boxed_is_equivalent_to_new -- --exact --nocapture` passed.
- `timeout 420 cargo test -p ropus-harness --test c_ref_differential test_float_pcm_nan_encode_matches_c_reference -- --nocapture` passed both NaN encode tests.
- `timeout 300 cargo test -p full-test` passed, 136 tests.
- `timeout 300 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests --test ctl_sequence_parity` passed, with the expected C reference debug-build optimization warning.
- `timeout 300 cargo run -p ropus-harness --bin ropus-compare -- multistream` passed all three multistream checks.
- `timeout 60 bash -n tools/fetch_ietf_vectors.sh` passed.

Commit grouping decision after validation:

1. One code commit for the parity, fuzz oracle, CTL sequence, IETF
   provisioning, manifests, and public docs. This keeps shared lockfile version
   bumps consistent with the package manifests.
2. One process-doc commit for `wrk_docs/` and `wrk_journals/`, including the
   triage journal and concurrent journal additions that appeared while I was
   validating.

## Follow-up Entry

Arthur asked me to inspect the remaining uncommitted worktree, decide whether
the files should be committed or gitignored, group any commit-worthy work, then
commit and push.

Current classification:

- `wrk_journals/2026.05.04 - JRN - validation-and-fuzz-plan.md` has appended
  validation and fuzzing results, including the slow debug conformance gate and
  a reproducible `fuzz_decode` SNR failure.
- `wrk_journals/2026.05.04 - JRN - ietf-suite-run-lookup.md` records when the
  IETF test suite was last confirmed to have passed.
- `wrk_journals/2026.05.04 - JRN - overnight-fuzz-supervision.md` appeared
  during this pass and records the launch state for an in-progress overnight
  fuzz campaign.
- The visible ignored files and directories are generated or local-only:
  build output, reference checkout, logs, fuzz corpora/artifacts, panic-capture
  output, and fetched IETF vectors. Existing `.gitignore` rules already cover
  them, so no new ignore rule is needed.

Grouping decision: this remaining work is one process-documentation/journal
commit. There are no source changes in this pass, so I am not running codec
validation again; the files being committed are Markdown records of prior
validation.

Emotional temperature: steady. This is smaller than the earlier triage pass,
but I am still keeping the classification explicit because the ignored fuzz
tree is huge and easy to mistake for neglected source work.
