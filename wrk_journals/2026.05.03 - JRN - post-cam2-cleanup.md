# 2026.05.03 - JRN - post-cam2-cleanup

## Initial Context

Arthur asked for supervised multi-agent implementation of the post-Campaign-2 cleanup brief. I read the brief, repository AGENTS.md, CLAUDE.md, and the code review/apply-feedback skill instructions before delegating work.

The brief file is currently untracked in git. I will treat it as user-provided context and avoid committing it unless Arthur explicitly asks.

## Plan

1. Delegate implementation investigation and fixes for Task 1, including TDD/regression coverage and seed migration.
2. Delegate decode-side tolerance triage for Task 2.
3. Use a devils-advocate reviewer throughout to challenge assumptions and completeness.
4. After implementation, delegate code review using the code-reviewer skill.
5. Delegate feedback application using the code-review-apply-feedback skill.
6. Delegate final plan-vs-implementation audit and iterate if gaps remain.
7. Commit after each significant completed stage with only touched files.

## Emotional Temperature

Focused and cautious. This is parity-sensitive codec work, so the main risk is agents making broad “cleanup” changes that hide a bit-exactness issue instead of fixing it. I will keep scopes narrow and require exact validation evidence.

## Stage 1 Delegation

Spawned Worker A for Task 1 only: reproduce and fix `multiframe-vbr-fec-dtx-divergence`, migrate the six repros to permanent seeds, remove/narrow the `fuzz_encode_multiframe` skip filter, and provide exact validation evidence. Worker A was told not to commit.

Spawned a devils-advocate explorer for Task 1 to challenge assumptions, define convincing completion evidence, and flag suspicious over-broad changes. This should keep the implementation honest without duplicating the worker's debugging.

Current thought: the brief's suspicion around encoder rate-control cached state is plausible, but I do not want the team to anchor too hard on it. The acceptance test is byte-exactness on all six repros plus removal of the mask, not confirming the initial hypothesis.

## Stage 1 Devil's Advocate Findings

The devils-advocate found that the brief may be stale: the known-failure directory currently has seven `crash-*` files, not six. I redirected Worker A to treat all current crash files as in scope unless one can be proven stale or duplicate.

Acceptance bar tightened:

- No broad `sr=8000 && ch=2` skip may remain.
- Repro removal must be paired with equivalent permanent seeds.
- A convincing fix should include first-divergent-state evidence where practical, not only packet-length parity after the fact.
- Changes that relax equality/tolerance, reshape fuzzer inputs, special-case exact tuples, or rewrite hot paths broadly are suspicious.

Emotional temperature: more alert. The stale repro count is exactly the kind of mismatch that causes false closure if nobody checks the filesystem against the brief.

## Stage 1 Progress Check

Worker A is still running. High-level `git status` shows edits in `ropus/src/silk/encoder.rs` and `tests/fuzz/fuzz_targets/fuzz_encode_multiframe.rs`, deletion of the `multiframe-vbr-fec-dtx-divergence` known-failure directory, seven new `regression-vbr-fec-dtx-*` seeds, and a worker-specific journal.

I asked Worker A for a status checkpoint and explicitly called out that the known-failure deletion must be backed by all seven current repros passing byte-exact validation, including `crash-audio-cx9.bin`.

I am intentionally not debugging the codec locally; my current role is to check scope movement and make sure the worker does not accidentally close only the six repros listed in the stale brief.

## Stage 1 Worker Journal Readout

Worker A's journal reports:

- Seven current known-failure artifacts existed; four were already byte-exact and three still diverged.
- First root cause isolated to a C/Rust mismatch around mono-internal to stereo-internal transitions: C copies channel 0 resampler state into channel 1 after `silk_control_encoder`, while Rust copied too early before control could rewrite the state.
- Worker added the post-control resampler-state copy in `ropus/src/silk/encoder.rs`.
- After removing the broad skip, a 60-second fuzz smoke found one new unmasked artifact. Worker identified a second parity gap: C clamps `redundancy_bytes` in SILK-only as well as HYBRID; Rust only clamped HYBRID.
- Worker added the SILK-only redundancy clamp in `ropus/src/opus/encoder.rs`.
- Worker migrated the seven original artifacts plus the new smoke artifact to permanent `regression-vbr-fec-dtx-*` seeds.

This is a stronger result than the brief requested, because removing the mask immediately exposed an adjacent residual bug. I need the worker's final validation before accepting the stage; especially important are exact commands/outcomes and whether any longer fuzz smoke was skipped.

Emotional temperature: cautiously optimistic. The fixes sound targeted and C-reference-driven, but I will require review agents to scrutinize both codec changes because there are now two root causes, not one.

## Stage 1 Commits

Committed Stage 1 in four slices:

- `758f0a8` Record multiframe VBR FEC DTX investigation
- `0fc1261` Fix multiframe VBR FEC DTX encoder parity
- `e4b26f8` Migrate VBR FEC DTX repros to fuzz seeds
- `2f48541` Unmask multiframe encoder fuzz diff

The untracked brief remains uncommitted as user-provided context.

Stage 1 residual risk: the brief asked for a 1-hour fuzz smoke and full release tests. Worker A ran all regression seeds, focused release tests, and a 60-second fuzz smoke. The full release test had an unrelated existing `property_tests_codec::prop_plc_convergence` failure, and the 1-hour smoke was not run. This needs to be surfaced in final status and considered by review/audit agents.

## Stage 2 Delegation Plan

Next stage is Task 2 decode-side asymmetry triage. I will delegate implementation to Worker B and devils-advocate review to a separate explorer.

Expected outputs:

- Verdict for `multistream-decode-recovery-divergence`.
- Verdict for `multistream-decode-family-asymmetry`.
- Verdict for `multistream-family255-error-asymmetry`.
- README/journal/tolerance/test updates according to each verdict.
- No broad fix to the separate SILK recovery-divergence class.

Emotional temperature: steady but wary. Task 2 is less about code volume and more about not laundering real bugs as “documented asymmetries.”
