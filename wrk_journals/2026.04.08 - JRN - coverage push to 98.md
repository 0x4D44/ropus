# Coverage Push To 98

## 2026-04-08 09:25

Started with a full branch-coverage baseline instead of guessing. Good call. The suite itself is healthy; the real problem is that the tests mostly hug helper functions while the giant codec orchestrators stay cold.

Facts:
- `cargo llvm-cov --branch` needed nightly; stable dies immediately on `-Z coverage-options=branch`.
- Baseline is rough: 48.94% lines, 35.10% branches.
- SILK is the crater.
- The biggest misses are exactly where I would expect them: `silk/encoder.rs`, `silk/decoder.rs`, `opus/encoder.rs`, `celt/bands.rs`, `opus/multistream.rs`.

Emotional temperature:
- Focused.
- Slightly annoyed that the requested agent workflow is blocked by the sandbox, not by the repo.
- Also annoyed by how deceptive the existing test count is. There are hundreds of tests, but the aggregate number is still brutal.

Agent note:
- Tried `codex exec`; blocked by outbound network restrictions.
- Tried `claude -p`; blocked by sandbox EPERM on `reg.exe`.
- I’m not going to pretend that counts as a solved multi-agent setup. It doesn’t. The honest state is: attempted, blocked by environment, moving on locally.

Working hypothesis:
- Small untested modules are worth harvesting first because they are cheap and safe.
- The only meaningful route to a large jump is to deepen top-level Opus encode/decode tests so they drag SILK/CELT/Opus orchestrators through real paths.

Current mood:
- Calm, but skeptical about the literal 98% target in one pass from 35% branch coverage.
- The only defensible way forward is iterative measurement after each tranche.
