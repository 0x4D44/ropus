# Mutation Testing Implementation Journal

Date: 2026.05.06

## 00:00 - Scope Read-In

I read the mutation revival HLD, supervisor signoff, and assessment before
touching code. The signed contract is narrow and sensible: fix the stale runner,
remove hidden baseline skips, keep the lane unit-test-only, and do not let this
turn into release-preflight or C-reference wrapper work.

The existing worktree has unrelated journal changes. I am deliberately staying
inside the allowed mutation runner/config/report scope.

Emotional temperature: focused, with some caution. Bash audit scripts become
misleading quickly when they try to be too clever, so I am keeping the behavior
explicit and boring.

## 00:20 - Initial Implementation

Updated `tools/mutation_test.sh` to normalize `celt/math_ops`, `src/...`,
`ropus/src/...`, and absolute paths under the repo root to authoritative
`ropus/src/...` paths. The script now invokes cargo-mutants with
`--package ropus`, performs a zero-mutant inventory guard, runs a visible
`cargo test --package ropus --lib` baseline before mutations, and writes
`summary.md`, `commands.txt`, raw logs, native cargo-mutants output, and missed
mutant lists under `logs/mutation-testing/<timestamp>/`.

Removed hidden `additional_cargo_test_args` skips from `.cargo/mutants.toml`.
Strict baseline is now the default; any skip-degraded run must pass explicit
`--skip-test` filters at the script invocation point.

The report text states the key caveat every time: this is unit-test-only and
the C-reference differential harness is not in the mutation loop.

## 00:50 - Full Math Ops Audit Proved Too Slow

Started the signed scoped audit:

```text
timeout 1800 bash tools/mutation_test.sh --file celt/math_ops --jobs 2 --timeout-multiplier 3
```

The strict baseline passed, but the first mutant took roughly one minute. With
230 listed mutants that extrapolates beyond a reasonable first smoke on this
machine, so I stopped the run rather than waiting for the outer timeout. The
partial artifacts are under `logs/mutation-testing/20260506_170735/`.

That attempted run also exposed two implementation polish issues: a Bash
nameref warning and case-sensitive result parsing while cargo-mutants prints
lowercase `caught`/`missed` outcome lines. I fixed both before proceeding to a
bounded shard audit.

Emotional temperature: mildly annoyed at the shell warning, but it was useful
feedback from running the real path. The right next move is a small shard, not a
weaker policy.

## 17:23 - Fix/Finish Handoff

Focused fix pass took over after the interrupted audit attempt. The remaining
known implementation issue was the Bash nameref warning in live audit mode. Root
cause is local nameref shadowing between `append_cargo_mutants_test_args` and
`append_baseline_skips`; the fix is deliberately small, renaming the local
nameref variables and passing the original array name through.

The audit itself remains deferred. A prior full `celt/math_ops` run and a 1/16
shard were already too slow for this finish pass, so the goal here is clean
runner behavior, strict baseline policy, and honest documentation rather than a
partial mutation score.

Emotional temperature: more settled. The script is still shell, so I do not
want to add cleverness. The right finish is to make the warning disappear and
leave the long-running audit as an explicit follow-up.

## 17:31 - Fix/Finish Validation

Required validation passed:

```text
timeout 30 bash -n tools/mutation_test.sh
timeout 30 cargo mutants --version
timeout 60 bash tools/mutation_test.sh --list
timeout 60 bash tools/mutation_test.sh --file celt/math_ops --list
timeout 120 cargo test -p ropus --lib
timeout 30 git diff --check -- .cargo/mutants.toml tools/mutation_test.sh
```

Observed inventory:

```text
ropus/src/silk/common.rs   1199 mutants
ropus/src/opus/decoder.rs  1432 mutants
ropus/src/opus/encoder.rs  2221 mutants
ropus/src/silk/decoder.rs  1486 mutants
ropus/src/celt/decoder.rs  1115 mutants
ropus/src/celt/encoder.rs  2620 mutants
ropus/src/celt/math_ops.rs  230 mutants
```

The strict library baseline passed with `1866 passed; 0 failed`.

Optional warning check: ran a bounded live command with `--file celt/math_ops
--shard 1/230 --jobs 1 --timeout-multiplier 1` under a 45 second timeout. It
reached `Found 1 mutant to test` and timed out intentionally before completion.
There was no Bash nameref/circular-reference warning in the captured output.
The only warning was cargo-mutants' expected message that an explicit timeout is
recommended when using `--baseline=skip`.

No mutation audit score was published. The generated partial artifacts under
`logs/mutation-testing/20260506_172528/` are diagnostic only.
