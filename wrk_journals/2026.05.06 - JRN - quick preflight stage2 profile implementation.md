# Quick Preflight Stage 2 Profile Implementation Journal

## 2026-05-06

Read `/home/md/language/ropus/AGENTS.md` and the signed revised HLD. The current uncommitted patch is exactly the rejected shape: it threads a boolean named around skipping slow conformance and appends `--skip extensions` as a libtest substring filter.

Implementation direction is clear and narrower than the partial patch: replace the boolean skip with an explicit Stage 2 profile. Normal Stage 2 should remain the full workspace command. Quick release-preflight should select packages with Cargo excludes before `--lib --tests`, for both skip-coverage and coverage-enabled paths. The IETF vector fallback remains a separate libtest tail skip and synthetic failure.

Emotional temperature: steady. The rejected partial patch is small, so replacing it cleanly should be low-risk. Main thing I am watching is command/header drift, because misleading runner output would make the smoke path harder to trust.

Replaced the partial `--skip extensions` approach with a `Stage2Profile` enum. `QuickReleasePreflight` now contributes Cargo package-selection arguments before `--lib --tests`; `FullWorkspace` contributes none. This keeps normal Stage 2 unchanged while making skip-coverage, coverage-enabled, and the printed header share the same package profile. The IETF vector fallback still appends only `--skip testvector`, and the synthetic failure path was left untouched.

Validation results:

- `timeout 120 cargo test -p full-test --locked` passed: 152 tests passed.
- `timeout 240 cargo test --workspace --exclude conformance --exclude ropus-harness-control --exclude ropus-harness-deep-plc --lib --tests --no-fail-fast -- --test-threads=1` passed. The largest bucket was `ropus --lib`, which reported 1866 passed, 0 failed, 0 ignored in 91.95s; the full command completed inside the timeout.
- `timeout 300 cargo run --release -p full-test -- --quick --release-preflight --skip-coverage` passed. The printed Stage 2 header was `cargo test --workspace --exclude conformance --exclude ropus-harness-control --exclude ropus-harness-deep-plc --lib --tests --no-fail-fast -- --test-threads=1`; Stage 2 reported 2256 passed, 0 failed, 6 ignored in 105.70s; ambisonics then passed 5 orders in 10.48s.
- `timeout 120 cargo fmt --check` passed.

Emotional temperature: good. The implementation now reads like the policy in the HLD instead of a workaround. Small residual concern: the smoke still runs ambisonics because `--quick` only skips stages 1 and 4 by existing design; that is outside this Stage 2 slice and the run stayed well within the requested bound.
