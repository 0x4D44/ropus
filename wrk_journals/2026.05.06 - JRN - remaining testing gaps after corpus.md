# Journal - Remaining Testing Gaps After Corpus

Date: 2026.05.06

## Task

Arthur asked for a documentation-only reassessment after
`d70730f Add real world corpus release gate`, with no code implementation and no
commit. The output ownership is:

- `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - remaining testing gaps after corpus.md`
- `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - remaining testing gaps after corpus.md`

## Context Read

I read the May 5 testing gaps review and improvement plan, the current oracle
matrix, the DRED/DNN HLD and review, the corpus gate HLD/assessment/review, and
the current code for performance, preflight, corpus, platform, and mutation
testing candidates.

I also checked git state. The repo is at `d70730f` as requested. There is an
existing modified continuation journal and an older untracked journal that are
outside this reassessment. I left them alone.

## Assessment Notes

The shape of the problem has changed. Earlier in the day, the right next slice
was still hidden oracle work: DRED/DNN assets, PLC drift, corpus emptiness. Now
those are visible and gated enough that the next false-confidence problem is the
performance lane.

The current benchmark logic is not useless. It already parses ratios, has a
1.15 WARN threshold, and distinguishes missing fixtures from crashes. The
problem is release semantics: WARN exits 0, and the existing comment explicitly
keeps WARN non-blocking for local pre-commit comfort. That is good for local
iteration, but too soft for a release performance claim.

I considered recommending DRED exact parity next. I do not think that is the
right testing-improvement slice. It is valuable work, but it is codec/DNN parity
repair with high debugging risk. The matrix already says the debt exists. The
same applies to PLC full-frame exact parity. Chasing either now would blur the
testing-infrastructure campaign into codec implementation.

Platform/sanitizer breadth is important, especially because `.cargo/config.toml`
pins `x86-64-v3`. I still rank it behind performance because sanitizer/toolchain
work is more environment-sensitive, while performance policy is already wired
into `full-test` and can be improved with pure unit tests plus optional live
measurement.

Mutation testing revival remains useful but lower priority. The paths are stale
and the baseline skip list needs interpretation. It is a periodic audit, not a
release confidence gate.

## Recommendation

Recommend a narrow HLD-first slice:

```text
Performance release gate and severe-regression thresholds
```

The slice should keep local/default behavior WARN-only, but make non-quick
release-oriented performance claims explicit and fail on benchmark build
failure, parser failure, crashed vectors, and measured severe regressions. The
ratio FAIL threshold should be calibrated from current measurements rather than
invented in the plan.

## Emotional Temperature

Focused, with a bit of discipline required. It is tempting to keep pushing on
the codec debts because they are concrete and technically interesting. But the
brief is testing improvement, and the honest move is to keep tightening report
semantics before opening a broad parity debugging front.

## Validation

No code tests were run because this is documentation-only.

Passed:

```bash
timeout 30 git diff --check -- 'wrk_docs/2026.05.06 - PLN - remaining testing gaps after corpus.md' 'wrk_journals/2026.05.06 - JRN - remaining testing gaps after corpus.md'
```
