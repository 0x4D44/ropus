# Quick Smoke Conformance Skip HLD Journal

2026.05.06

Task: write the next HLD only for bounding the `full-test --quick --release-preflight`
smoke path so it does not run the very slow conformance `extensions` test.

Read the relevant runner files in `/home/md/language/ropus/full-test/src/` and
confirmed the diagnosis: Stage 2 has a single workspace test invocation, and
`--quick` currently skips quality and benchmarks but not tests. The conformance
`extensions` binary wraps upstream code that explicitly documents the
100,000,000-iteration random extension parse.

Design choice: recommend a small internal `options.quick &&
options.release_preflight` policy that appends libtest `--skip extensions`.
The substring/global nature is uncomfortable, but acceptable for a bounded
smoke path as long as the full non-quick path remains untouched and the skip is
visible in tests/reporting. I do not think a split Stage 2 runner is justified
yet; it would add aggregation complexity for a single known slow test.

Emotional temperature: mildly wary of global libtest skip semantics, but the
minimal design feels proportionate to the goal. The main thing to protect is
not letting this grow quietly into the real conformance gate.
