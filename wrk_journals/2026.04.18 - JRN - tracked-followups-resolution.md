# 2026-04-18 — Resolve 10 tracked follow-ups from high-level-API/CLI feature

Supervisor: Claude Opus 4.7 (1M context). Multi-agent process: agents do all
implementation, testing, debugging. My role is planning, dispatch, review of
reports, journaling, and final acceptance.

## Source list
From `wrk_journals/2026.04.18 - JRN - high-level-api-cli-feature.md`:

1. `test_opus_encode.c` line 501 STATUS_STACK_BUFFER_OVERRUN — **OWNED BY CONCURRENT WORKSTREAM** (see `2026.04.18 - JRN - encoder-final-range-bug.md`). Will leave alone per multi-agent rules.
2. `decode_fec: bool` → typed enum + frame-size silent inference foot-gun (API ergonomics; ropus 0.5.x).
3. `info` decodes whole stream; should parse `granule_position` from last Ogg page for fast metadata.
4. `ropus-cli/src/main.rs` 981 LoC could split into `commands/`, `audio/`, `container/` modules.
5. `WAVE_FORMAT_EXTENSIBLE` (0xFFFE) support in `common/wav.rs` (Audacity defaults trip our examples).
6. `build.rs` hardcoded `..` path to `.git` breaks `cargo install --path` outside the workspace.
7. `play` two-path routing (.opus through ropus, others through symphonia) — inconsistent UX.
8. Hardcoded `pre_skip = 312` should query ropus's actual lookahead.
9. `roundtrip` example now requires `--bitrate <bps>` (no default — minor UX nit).
10. License field cross-check across workspace members.

## Execution plan
- **Batch 1** (low-risk, parallel impl): #5, #6, #9, #10. Two parallel impl agents covering disjoint trees, then review/apply/commit.
- **Batch 2** (ropus-cli polish): #3, #7, #8. Design first (small), then impl/review/apply/commit.
- **Batch 3** (API change → ropus 0.5.0): #2 only. Hostile spec-read → design → impl/review/apply/DA/commit.
- **Batch 4** (refactor): #4 main.rs split. Last so it doesn't perpetually rebase the others.
- **Final**: compliance team verifies all 9 are done; DA challenges the lot; iterate to 100%.

Each batch: implement → code-reviewer → apply-feedback → devil's-advocate → commit.

## Hazards
- Concurrent commits: `02cd5ff perf(ropus): restore uc! macros` landed after our Stage 4 commit; encoder-final-range-bug is in flight; capi has uncommitted M to `capi/src/ctl.rs`, `capi/src/ctl_shim.c`, `tests/conformance/build.rs`. None overlap with our targets. Will re-check at every checkpoint.
- `ropus-cli/src/main.rs` is touched by three of the four batches (#3/#7/#8 in Batch 2, #4 in Batch 4). Order matters: Batch 2 first, then Batch 4.
- `ropus/examples/common/wav.rs` touched by #5 only. Disjoint.
- `ropus/Cargo.toml` touched by #2 (Batch 3 → 0.5.0). Disjoint from Batch 1 timing.
- `ropus-cli/build.rs` touched by #6 only.

## Decisions locked in upfront
- Follow-up #1 NOT in scope (concurrent workstream owns).
- Batch 3 (#2) is a SemVer minor bump if additive (typed enum exposed alongside bool overload), or major if bool removed. Will decide during design.
- Batch 4 (#4) is purely organizational; do NOT change behavior or public surface in the split.
- Hostile-spec-read pass only on Batches 3 & 4 (cost vs value: tiny items in Batches 1-2 don't warrant it).

## Status — Batch 1 (completed)
Two parallel impl agents on disjoint trees → single combined code-review.

**Agent-Batch1A (examples):**
- #5 WAVE_FORMAT_EXTENSIBLE in `ropus/examples/common/wav.rs:86-141` — parses cbSize, wValidBitsPerSample, dwChannelMask (advisory), 16-byte SubFormat GUID; validates KSDATAFORMAT_SUBTYPE_PCM. Normalises format_tag back to 1 so the rest of the parser is unchanged.
- #9 `--bitrate` default = 64_000 in `ropus/examples/roundtrip.rs:122-141`. Args accept 3-form (in/out only) or 5-form (with `--bitrate <bps>`).

**Agent-Batch1B (build/license):**
- #6 `ropus-cli/build.rs` already had Command-based fallback; agent hardened it: `head.exists()` guard so cargo install --path doesn't reference non-existent path, `.filter(|o| o.status.success())` for non-zero git exits, `.filter(|s| !s.is_empty())` so empty stdout doesn't write empty BUILD_GIT_SHA. Banner verified: `ropus-cli 0.2.0 (build 2026-04-18 20:10:06 UTC, sha 02cd5ff)`.
- #10 License audit: 4 internal crates (`capi`, `harness`, `tests/conformance`, `tests/fuzz`) had no `license` field. Added `BSD-3-Clause` to all. Workspace canonical = `ropus/Cargo.toml`. `cargo check --workspace` clean.

**Reviewer verdict: APPROVE-WITH-CHANGES (no required items, 4 minor suggestions, all skipped as not load-bearing).**

DA skipped on this batch — small surface, no public API, no security implications. DA ran on Batches 2-4.

**Batch 1 commit: `3b3e28a` — `fix(examples, cli, packaging): batch 1 follow-ups (#5/#6/#9/#10)`** (8 files, +157/-12).

## Status — Batch 2 (completed)

Plan agent → impl agent → code-reviewer + DA (parallel) → apply-feedback agent.

**Plan output (Plan agent):** correct ordering #8 → #3 → #7. Spotted symphonia's stub opus decoder issue and recommended unified-via-symphonia + ropus-decode-Opus-tracks pattern.

**Impl agent:**
- #8: `Encoder::lookahead()` accessor (`ropus/src/api.rs:436`); replaced `PRE_SKIP_SAMPLES` constant in cmd_encode.
- #3: `read_last_granule()` reverse-scan helper; `info` runs in 39 ms on a 177 s file (~1000× speedup) vs prior whole-stream decode.
- #7: `CodecPipeline::{Native, Opus}` enum; deleted `decode_opus_to_f32`/`is_opus_path`. Single demux pipeline through symphonia.
- Bumped `ropus-cli` 0.2.0 → 0.2.1 (play behavior change is material).

**Code-reviewer:** APPROVE-WITH-CHANGES. 2 required (64 KiB scan window too tight, 1 dead-fallback `unwrap_or(312)`), 5 suggested.

**DA: FIX-FIRST-THEN-COMMIT.** Real catches:
- We just removed `PRE_SKIP_SAMPLES = 312` in #8 and reintroduced it 50 lines later as `delay.unwrap_or(312)`. Fix: read pre_skip from `codec_params.extra_data` (OpusHead) when delay is None; bail otherwise.
- `u16::try_from(lookahead).unwrap_or(u16::MAX)` silently degrades — should hard-error.
- `packets` field appears/disappears in `info` between fast/slow paths — confusing for scripts.
- No unit tests for `read_last_granule`.
- 64 KiB scan window too tight (RFC 3533 caps page at 65,307; barely fits).

**Apply-feedback agent (6 items, all applied):**
1. SCAN_WINDOW 64 KiB → 128 KiB; refactored helper to `&mut impl Read + Seek` so unit tests can use `Cursor`.
2. Play pre_skip: `codec_params.delay` → OpusHead extra_data parse → `bail!`. No more 312 magic in play.
3. Lookahead `try_from` now hard-errors with clear message.
4. `packets` field dropped from `info` (Option A — not load-bearing).
5. 4 unit tests for `read_last_granule` (empty, valid, sentinel, wrong-serial).
6. Doc comment on `CodecPipeline::Opus.skipped`.

Tests: 1783 lib tests + 4 new CLI unit tests + 1 CLI integration test all pass. Smoke: encode 53 ms, info 26 ms (fast path, no `packets` line), play .opus shows 47,688 samples (= 48,000 − 312 trimmed), play .wav shows 48,000 samples.

**Batch 2 commit: `6d8b28f` — `fix(cli): batch 2 follow-ups (#3/#7/#8)`** (5 files, +359/-98).

## Status — Batch 3 (completed)

Hostile spec-read + design (Plan agent) → impl agent → code-reviewer + DA (parallel) → apply-feedback agent.

**Plan agent (hostile + design):**
- Confirmed real foot-gun: 481 samples @ 48 kHz mono passes through to deep `BadArg` from `frame_size_select` because everything between 2.5 ms and 120 ms sails through.
- Confirmed `decode_fec: bool` is bikeshed-tier "consistency" but worth doing now since unpublished.
- Recommended A: BREAKING (DecodeMode enum), B: NON-BREAKING signature with tightened internal validation, C: ADDITIVE `valid_frame_sizes()` accessor.
- 9 internal call sites identified; SemVer 0.4.0 → 0.5.0.

**Impl agent:**
- `DecodeMode { Normal, Fec }` enum (api.rs); decode/decode_float now take `mode: DecodeMode`.
- `is_valid_frame_size_per_channel()` validates encoder input against the legal 9-element set per sample rate.
- `Encoder::valid_frame_sizes() -> [usize; 9]` for discoverability.
- 8 call sites migrated (planner missed 1 — `main.rs:861` float decode in play pipeline; impl agent caught and fixed via grep).
- Tests: 1787 lib pass (4 new), 2 doc pass.

**Code-reviewer (APPROVE-WITH-CHANGES):**
- Required: `as_decode_fec` should return `bool` (matches inner) instead of `i32` then-coerced.
- Suggested: doc clarity on units, # Examples on DecodeMode, sharing arithmetic between `is_valid` and `valid_frame_sizes` (skipped — adds indirection).

**DA (SHIP-IT but with retrospective pushback):**
- Real risks: decoder lacks discoverability parity, DecodeMode is the only per-call enum, asymmetry with rest of API.
- Pushback: bundle Decoder discoverability into same major bump rather than ship 0.5.0 with one breaking change. Adopted this advice.
- Skipped DA-mentioned items: decoder frame-size validator (decoder buffer is a CAPACITY not a frame size — no foot-gun), PLC variant (design work), `Bitrate::Bits` removal (out of scope), bigger-picture API redesign (defer to ropus 1.0).

**Apply-feedback agent (5 items):**
1. `as_decode_fec -> bool` (drops dead translation).
2. `Decoder::max_frame_samples_per_channel() -> usize` returning `fs * 6 / 50` (5760 @ 48 kHz).
3. `Encoder::valid_frame_sizes` doc clarified ("samples per channel"; multiply by `channels`).
4. `# Examples` block added on `DecodeMode` doc.
5. Unit test asserting 5760 @ 48k and 960 @ 8k.

Tests: 1788 lib pass (1 new), 3 doc pass.

**Batch 3 commit: `f50767e` — `feat(ropus)!: typed DecodeMode + frame-size validator (#2) — 0.5.0`** (9 files, +237/-28).

## Status — Batch 4 (completed)

Plan agent (hostile + design) → impl agent → code-reviewer + DA (parallel) → apply-feedback agent.

**Plan agent:** Acyclic 11-module split with line-exact mapping. `commands.rs` = one file with 4 fns (not one-per-subcommand). Foot-guns enumerated for verbatim preservation.

**Impl agent:**
- main.rs 1190 LoC → 69 LoC entry point.
- 11 new files: `consts.rs` (21), `cli.rs` (118), `ui.rs` (34), `util.rs` (20), `audio/{mod,decode,resample,wav}.rs` (5+207+92+49 = 353), `container/{mod,ogg}.rs` (3+214 = 217), `commands.rs` (421).
- 4 unit tests for `read_last_granule` migrated with the helper to `container/ogg.rs`.
- BIT-IDENTICAL VERIFIED: encode SHA256 = `f7d77efe6242e74ef893e3910685155159017c1837c3498a680aaeca991355e3` (6699 B) before AND after refactor on `tests/vectors/48k_sine1k_loud.wav`. `--help` and `--version` byte-identical.
- 2 pre-existing clippy errors flagged (NOT introduced by refactor): `large_enum_variant` on CodecPipeline, `manual_is_multiple_of` on format_num.

**Code-reviewer:** APPROVE. Dep graph verified acyclic (`main → {cli, commands, ui}`; `commands → {audio::*, cli, consts, container::ogg, ui, util}`; `audio::decode → {consts, util}`; `audio::resample → {consts}`; `audio::wav, container::ogg, ui, util, consts, cli` are leaves). 8/8 foot-guns preserved verbatim. Visibility correct.

**DA: SHIP-IT** (with retrospective pushback on consts.rs and util.rs being too thin, commands.rs still being 421 LoC). Reversals proposed:
- Delete consts.rs — REJECTED (would create cycle: audio→commands→audio for OPUS_SR).
- Inline util.rs — DEFERRED (debatable structural choice, bikeshed-tier).
- Don't split commands.rs further — AGREED.

**Apply-feedback (4 items):**
1. Stale `cmd_encode` ref in consts.rs:18 → `commands::encode`.
2. Module-structure ASCII dep tree added to main.rs top-level doc.
3. `clippy::large_enum_variant` fixed: extracted `OpusState` struct; `CodecPipeline::Opus(Box<OpusState>)`. Internal to audio/decode.rs.
4. `clippy::manual_is_multiple_of` fixed in ui.rs:28.

After: `cargo clippy -p ropus-cli -- -D warnings` clean. Bit-identity re-verified (same SHA256). All tests pass.

**Batch 4 commit: `dcbc87a` — `refactor(ropus-cli): split main.rs into modules (#4) — pure structural change`** (13 files, +1250/-1136).

## Status — Final compliance + DA (completed)

Compliance team + final DA dispatched in parallel.

**Compliance: 9/9 PASS.** Each in-scope follow-up verified by file:line evidence. Smoke tests clean (`cargo build --workspace` clean; 1788 ropus lib tests pass; ropus-cli round-trip integration + 4 unit tests pass; `cargo clippy -p ropus-cli -- -D warnings` clean; ropus examples build clean).

**Concurrent #1 (test_opus_encode.c:501) status (verify-only, do NOT fix):**
- Primary CELT→SILK rng-capture bug landed during Batch 4 as `190d4c6 fix(ropus): capture CELT rng after CELT→SILK redundancy encode`.
- Secondary `enc_final_range != dec_final_range` bug remains at j=7 (SILK→CELT redundancy path).
- Workstream is active — uncommitted modifications across capi/, ropus/src/opus/, ropus/src/silk/, tests/conformance/.

**Final DA: CAMPAIGN-COMPLETE-WITH-OPEN-ITEMS.** Three concrete misses flagged that contradict campaign's own thesis:
1. `ropus/examples/encode.rs:46` STILL hardcodes `PRE_SKIP_SAMPLES = 312` — Batch 2 #8 fixed this in ropus-cli but missed the example.
2. `ropus/src/lib.rs:28` and `ropus/README.md:34` quickstart docs teach the LOW-LEVEL `OpusDecoder.decode(.., bool)` signature instead of the new high-level `Decoder + DecodeMode::Normal`. Not broken (low-level API unchanged) but contradicts the API ergonomics work.
3. No CHANGELOG entry for 0.5.0 breaking change.

**Supervisor disposition:**
- Misses 1 + 2: ACCEPT and fix in a closing commit. Both legitimately undermine the campaign's value.
- Miss 3: SKIP. CLAUDE.md prohibits creating new .md files without explicit user ask. Flag as user-decision item.

**Closing apply-feedback agent:**
- Fix 1 (`ropus/examples/encode.rs`): replaced const PRE_SKIP_SAMPLES with `let pre_skip = u16::try_from(encoder.lookahead()).expect(...)`. Smoke test confirms pre-skip 312 matches old hardcoded value (no behavior change for default Application::Audio encoder).
- Fix 2 (`ropus/src/lib.rs:13-31`): replaced low-level snippet with high-level `Encoder::builder` / `Decoder::new` / `DecodeMode::Normal` API. Doctest compiles and runs.
- Fix 3 (`ropus/README.md:23-36`): same migration applied to README quickstart.

Tests after closing fixes: 3/3 doctests pass; examples build clean.

## Final commit log
```
dcbc87a refactor(ropus-cli): split main.rs into modules (#4)
f50767e feat(ropus)!: typed DecodeMode + frame-size validator (#2) — 0.5.0
6d8b28f fix(cli): batch 2 follow-ups (#3/#7/#8)
3b3e28a fix(examples, cli, packaging): batch 1 follow-ups (#5/#6/#9/#10)
+ closing commit (this section): example + doctest sync to new API
```

## Process retrospective
- **Hostile spec-read pre-impl was high-leverage** on Batches 3 and 4 (the only two with public-API or structural impact). Pre-empted poor framings before they baked into impl prompts. Worth keeping as default for any batch that touches public surface.
- **Plan + impl + review + DA in parallel + apply** cadence repeated from Stage 1-2 of the parent feature. Kept catching real bugs (`unwrap_or(312)` dead-magic regression in Batch 2; `as_decode_fec -> i32` then-coerced double translation in Batch 3; missing pre-existing clippy errors in Batch 4).
- **DA's "items accepted too easily" prompt** is the most useful framing — it surfaces what got rubber-stamped without scrutiny, which a reviewer alone tends to miss because the reviewer is in the same agentic mindset as the impl.
- **Concurrent multi-agent environment** required `git status` checks at every checkpoint as before. One concurrent commit landed mid-Batch-4 (`190d4c6`) and one bug-fix commit (`14fcae7`) appeared between Batches 1 and 2. None conflicted with our work.
- **Closing fixes from final DA were genuine improvements** (the example was lying about ropus-cli; the lib.rs/README taught a deprecated API). The 15-minute fix prevents a future "your README doesn't compile against the crate it documents" issue.

## User-decision items (not in campaign scope, supervisor flags)
1. CHANGELOG.md for ropus — DA recommends; CLAUDE.md says don't create .md files without user ask. **Decision: defer to user.**
2. Multistream `decode_fec: bool` API at `ropus/src/opus/multistream.rs` (low-level — same foot-gun as high-level Decoder had pre-#2). Owned by codec API workstream; out of this campaign's scope.

## Plan complete. Closing the file.
