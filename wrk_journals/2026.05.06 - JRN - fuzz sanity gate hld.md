# 2026.05.06 - JRN - Fuzz Sanity Gate HLD

## Entry 1

Task: create an implementation-ready HLD for Phase 1.2, bounded fuzz sanity
gate, without modifying source code.

Read the repository instructions, the testing improvement plan, the status/next
slice note, current fuzz scripts, and the relevant `full-test` modules. The
important discovery is that Phase 1.1 is already present: target discovery is
centralized in `/home/md/language/ropus/tools/fuzz_targets.sh`, and both fuzz
runners consume it.

Current state:

- `/home/md/language/ropus/tools/fuzz_run.sh` already builds selected fuzz
  targets before either crash replay or campaign mode.
- `/home/md/language/ropus/tools/fuzz_run.sh --check-crashes` already replays
  committed crash files with `-runs=0`.
- There is no build-only or named sanity mode, so the bounded behavior is not
  obvious from the CLI surface.
- `full-test` has release preflight plumbing and banner/report integration, but
  no fuzz stage.
- There are committed crash files for `fuzz_decode`, so crash replay is not
  hypothetical.

Inner state: this design is pleasantly small, but the policy split is easy to
get wrong. I felt some friction around `--quick --release-preflight`: requiring
cargo-fuzz there would make the quick path less useful, but skipping all fuzz
checks would waste the chance to catch stale crash-corpus shape. Inventory-only
feels like the right compromise.

Decision captured in the HLD: shell owns the cargo-fuzz mechanics, `full-test`
owns policy and reporting. Full release preflight fails on missing fuzz tooling;
quick release preflight does not invoke cargo-fuzz and only validates manifest
and crash-corpus shape.

No blockers. No source code changed.
