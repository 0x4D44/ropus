# Quick Preflight Stage 2 Profile HLD Journal

Date: 2026.05.06

## Entry 1

Read the repository instructions, earlier HLD, Dalton review, Herschel diagnosis,
and the current uncommitted source diff. The partial patch is source-correct for
filtering the `extensions` conformance stress test, but the diagnosis shows the
design objective was too narrow: Stage 2 remains a broad workspace run and still
does not serve the quick smoke goal.

Emotional temperature: calm, mildly annoyed at the substring-filter path because
it is technically clever but semantically wrong for the revised goal. The right
shape is smaller and easier to reason about: choose the packages that belong in
the smoke profile instead of pretending a full workspace run becomes quick after
one or more global libtest skips.

## Entry 2

Decision for the revised HLD: replace the quick-preflight `--skip extensions`
partial patch with package-level excludes for `conformance`,
`ropus-harness-control`, and `ropus-harness-deep-plc`. Keep the independent IETF
vector `--skip testvector` fallback unchanged because it is tied to missing test
assets and synthetic failure accounting, not quick-preflight runtime policy.

Risk noted: the candidate package profile still includes the `ropus` library
suite, which Herschel measured at about 91 seconds serially. That is acceptable
for bounded-under-300s smoke but not for a sub-minute smoke target.
