# Journal: Worktree Triage And Push

**Date:** 2026-04-08

## Entry

Started with a dirty tree and needed to separate signal from local tooling junk before pushing. The split was straightforward once the file list was inspected: real design artifacts in `wrk_docs` and `wrk_journals`, local Codex sandboxes under `.codex-*`, and a validation scratch checkout under `.wrk_validate/`.

The only judgment call was `src/main.rs`. It had a tiny test that only proved `main()` can be called. That kind of change pads numbers without protecting behavior, so I cut it instead of laundering it into history. Slightly annoyed that it was sitting beside useful docs because it makes the worktree look less coherent than it is.

Added ignore rules for the local sandboxes and wrote a short review note so the commit history explains why some paths were kept and others were not. Emotional temperature: calm, mildly suspicious of coverage-driven noise, happier once the tree started looking intentional again.
