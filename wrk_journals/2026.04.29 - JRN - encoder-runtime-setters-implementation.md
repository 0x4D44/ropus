# Journal — Implementing encoder runtime setters

**Date opened:** 2026-04-29
**HLD:** `wrk_docs/2026.04.29 - HLD - encoder-runtime-setters.md` (V3 final)
**V2/V3 interim drafts:** retained on disk per supervisor convention
**Supervisor:** Claude

## Plan

Two implementation stages, each followed by code review + DA + apply-feedback + commit. Final completeness audit afterwards.

| # | Stage | Scope | Files |
|---|---|---|---|
| 1 | Foundation (additive only) | `InbandFec` enum + `from_c_int` helpers (Bitrate/Signal/ForceChannels/Bandwidth/InbandFec) + `pub(crate) get_user_bitrate_bps` on `OpusEncoder` + `check_ok_runtime` helper + their unit tests | `ropus/src/api.rs`, `ropus/src/opus/encoder.rs` |
| 2 | Facade methods | 10 runtime setters, 11 new getters (incl. `bitrate`, `effective_bitrate_bps`), builder `inband_fec` + `dtx` methods, renames+retypes of `get_vbr_constraint`→`vbr_constraint` (bool) and `get_packet_loss_perc`→`packet_loss_perc` (u8), all 9 HLD tests | `ropus/src/api.rs` |
| Audit | Completeness check | HLD acceptance items; iterate to 100% | — |

## Per-stage process

For each stage:
1. **Implementer** agent writes code, TDD where it fits.
2. **Code-reviewer** agent reviews using `code-reviewer` skill.
3. **Apply-feedback** agent applies findings using `code-review-apply-feedback` skill.
4. **Devil's advocate** agent challenges the work — looking for what reviewers missed.
5. Apply DA feedback if any.
6. Verify build / tests / clippy clean.
7. Commit.

## Pre-flight

Concurrent-agent check before kicking off Stage 1:
- `git status` clean
- `git log --oneline -5` for recent activity on target files

## Log

### 2026-04-29 — Setup

- Plan committed above.
- Journal opened.
- Dispatching Stage 1 implementer next.

### 2026-04-29 — Stage 1 — implementation

**Scope delivered (purely additive — no breaking API changes):**

- `ropus/src/api.rs`:
  - New `pub enum InbandFec { Disabled, Enabled, Forced }` mirroring the
    rustdoc / derive set used by `Bitrate`, `Signal`, etc.
    `pub fn as_c_int` (0/1/2) and `pub fn from_c_int` with `debug_assert!`
    fallback to `Disabled`.
  - `Bitrate::from_c_int` (`OPUS_AUTO`→`Auto`, `OPUS_BITRATE_MAX`→`Max`,
    `n>0`→`Bits(n as u32)`, anything else → debug_assert + `Auto`).
  - `Signal::from_c_int`, `ForceChannels::from_c_int`, `Bandwidth::from_c_int`
    — each with `debug_assert!` + documented default for unknown values.
  - `check_ok_runtime(code: i32) -> Result<(), EncodeError>` sibling of
    `check_ok`.
  - Five new unit tests under `#[cfg(test)] mod tests`: one per enum
    asserting `from_c_int(x.as_c_int()) == x` for all variants. Sixth
    test, gated `#[cfg(not(debug_assertions))]`, asserts the
    release-build fallback for `Bitrate::from_c_int(0)` /
    `from_c_int(-42)` returns `Bitrate::Auto`.
- `ropus/src/opus/encoder.rs`:
  - `pub(crate) fn get_user_bitrate_bps(&self) -> i32` adjacent to
    `get_bitrate`, with the rustdoc text from the HLD verbatim.
- `ropus/src/lib.rs`:
  - Added `InbandFec` to the `pub use api::{...}` re-export so the new
    enum is reachable from the crate root. (Without this, the new `pub`
    enum is dead-code from the lint's perspective because `mod api` is
    private — same pattern as `Bitrate`, `Signal`, etc.)

**Deviations from the HLD:**

- Added `#[allow(dead_code)]` on each Stage-1-only-plumbing helper
  (`Bitrate::from_c_int`, `Signal::from_c_int`, `Bandwidth::from_c_int`,
  `ForceChannels::from_c_int`, `OpusEncoder::get_user_bitrate_bps`,
  `check_ok_runtime`). Rationale: clippy with `-D warnings` is in the
  Stage 1 verification gate, and these helpers are intentionally unused
  until Stage 2 wires them through the new getters/setters. The comment
  on each `#[allow]` references Stage 2 for context. Stage 2 will remove
  these attributes when the call sites land.

- HLD's "Stage 1 scope" wording at user-prompt level says
  `pub fn as_c_int / pub fn from_c_int` for `InbandFec` while the
  existing `as_c_int` methods on `Bitrate`/`Signal`/etc. are
  module-private. Followed the literal user prompt for `InbandFec`
  (public methods) and kept the existing pattern for the four added
  `from_c_int` helpers (private). The asymmetry only matters at the
  module-private boundary; both compile. Will revisit visibility in
  Stage 2 if the new getters need to call these helpers from outside the
  `api` module (they will not — getters live in `impl Encoder` inside
  the same module).

**Verification (all clean):**

- `cargo build -p ropus` — clean (one unrelated `cargo:warning` from
  `build.rs` about missing reference DNN weights, unaffected by this
  change).
- `cargo test -p ropus --lib api::tests` — 23/23 pass (18 pre-existing +
  5 new from_c_int round-trips).
- `cargo clippy -p ropus -- -D warnings` — clean.

**Blockers / risks for Stage 2 to note:**

- None. Stage 2 should remove the six `#[allow(dead_code)]` attributes
  added here as it wires each helper through to a real call site.
- The `#[cfg(not(debug_assertions))]` test for the Bitrate fallback only
  fires in release-mode test runs. CI's typical `cargo test` is debug;
  the load-bearing coverage is the round-trip test, as the user prompt
  acknowledged.

### 2026-04-29 — Stage 1 — review applied

Code reviewer's verdict: "Looks good — needs minor changes." One real
fix; one conditional nit (now moot).

**Changes:**

- `ropus/src/api.rs:306` — `pub fn as_c_int` → `fn as_c_int` on `InbandFec`.
- `ropus/src/api.rs:317` — `pub fn from_c_int` → `fn from_c_int` on `InbandFec`.
- `ropus/src/api.rs:305-306,316-317` — added `#[allow(dead_code)] // Wired
  up in Stage 2 alongside the runtime setters/getters.` to both methods.
  Required because dropping `pub` exposes the no-non-test-call-site
  status to the dead-code lint, and clippy `-D warnings` is in the gate.
  Mirrors the precedent on the four other `from_c_int` helpers added in
  Stage 1. Stage 2 will remove these two attributes (raising the running
  count from six to eight here, then removed in Stage 2 along with the
  original six as call sites land).

**Rationale:** brings `InbandFec`'s conversion helpers in line with the
six other typed-enum precedents in `api.rs` (`Channels`, `Application`,
`Bitrate`, `Signal`, `Bandwidth`, `ForceChannels` — all `fn`, not
`pub fn`). The Stage 1 deviation note above (lines 83–92) is hereby
resolved: the asymmetry the implementer flagged is removed, and Stage 2
will not need cross-module access.

**Conditional nit (skipped, now moot):** reviewer suggested rustdoc on
`as_c_int` if `pub` was kept. With `pub` dropped, parity with the other
private `as_c_int` methods (none of which carry rustdoc) restores the
precedent.

### 2026-04-29 — Stage 1 — devil's advocate

DA verdict: **ship Stage 1.** No critical findings. Watch-list items
carried into Stage 2 brief:

- **WL-a (`Bitrate::Bits(0)` round-trip lossy):** `Bits(0).as_c_int() = 0`, `from_c_int(0)` panics in dev / returns `Auto` in release. Encoder rejects `bitrate <= 0` so unreachable from stored state, but worth a one-line caveat on `Bitrate::from_c_int`'s rustdoc when Stage 2 lands.
- **WL-c (consistency vs absolute correctness):** the round-trip tests verify `from_c_int(as_c_int(v)) == v`, which would pass if both directions had cancelling typos. Recommend Stage 2 add at least one literal-value assertion per enum (e.g. `assert_eq!(InbandFec::Forced.as_c_int(), 2)`) to pin to libopus values rather than internal self-consistency.
- **WL-d (`#[allow(dead_code)]` count):** eight attributes by end of Stage 1. Add to Stage 2 acceptance: grep should return zero `#[allow(dead_code)]` newly-introduced in Stage 1.

DA scrutinised and confirmed fine: `OPUS_*` constant values match libopus, `pub(crate)` on `get_user_bitrate_bps` is correct, all five round-trip tests cover all variants exhaustively, no concurrent edits, no reachability bugs in the `from_c_int` helpers.

**Stage 1 ready to commit.**
precedent.

**Verification (all clean):**

- `cargo build -p ropus` — clean.
- `cargo test -p ropus --lib api::tests` — 23/23 pass.
- `cargo clippy -p ropus -- -D warnings` — clean.

### 2026-04-29 — Stage 2 — implementation

**Scope delivered (full HLD spec, no breaking surface beyond the planned
rename + retype):**

- `ropus/src/api.rs`:
  - 10 runtime setters on `impl Encoder`, each returning
    `Result<(), EncodeError>`: `set_bitrate`, `set_complexity`, `set_signal`,
    `set_vbr`, `set_vbr_constraint`, `set_force_channels`,
    `set_max_bandwidth`, `set_packet_loss_perc`, `set_inband_fec`,
    `set_dtx`. Each is a thin `check_ok_runtime(self.inner.set_*(...))`
    delegation.
  - 11 new getters on `impl Encoder`: `bitrate` (uses
    `get_user_bitrate_bps`, NOT `get_bitrate`),
    `effective_bitrate_bps` (uses `get_bitrate`; rustdoc names the
    `OPUS_GET_BITRATE` equivalence), `complexity`, `signal`, `vbr`,
    `vbr_constraint`, `force_channels`, `max_bandwidth`,
    `packet_loss_perc`, `inband_fec`, `dtx`. The latter two are net-new;
    `vbr_constraint` (bool) and `packet_loss_perc` (u8) replace the
    Stage-1 `get_vbr_constraint` (i32) / `get_packet_loss_perc` (i32)
    facade methods (rename + retype). The low-level
    `OpusEncoder::get_vbr_constraint` / `get_packet_loss_perc` are
    untouched, so capi/harness call sites at `capi/src/ctl.rs:198, 200,
    568, 580` and `harness/src/cli.rs:5821, 5827` keep working.
  - Builder: new `inband_fec(self, mode: InbandFec) -> Self` and
    `dtx(self, on: bool) -> Self` methods, matching `Option<...>` fields,
    matching `if let Some(...)` blocks in `build()`.
  - Removed all six Stage-1 `#[allow(dead_code)]` in `api.rs` (four
    `from_c_int` helpers, `InbandFec::as_c_int`/`from_c_int`,
    `check_ok_runtime`). All helpers now have real call sites in the new
    setters/getters. `grep -n "#\[allow(dead_code)\]" ropus/src/api.rs`
    returns nothing.
  - WL-a addressed: rustdoc on `Bitrate::from_c_int` extended with the
    one-line caveat that round-trip is faithful for any value the encoder
    accepted; `Bits(0)` is unreachable from encoder state.
  - WL-c addressed: new `typed_enum_wire_values_match_libopus` test pins
    each enum's wire integers to libopus literals (e.g.
    `Bitrate::Auto.as_c_int() == -1000`,
    `InbandFec::Forced.as_c_int() == 2`,
    `Bandwidth::Narrowband.as_c_int() == 1101`). Round-trip plus literal
    coverage means a one-sided typo in `as_c_int` or `from_c_int` would
    surface.
  - WL-d addressed: zero Stage-1-introduced `#[allow(dead_code)]`
    attributes survive into Stage 2 (verified by grep).
  - All 9 HLD-spec tests landed:
    1. `runtime_setters_round_trip_mono` — every applicable setter →
       getter on a mono fixture.
    2. `runtime_setters_round_trip_stereo` — `ForceChannels::Stereo` /
       `Mono` / `Auto` on a stereo fixture.
    3. `bitrate_setter_round_trips_all_three_forms` — `Auto`/`Max`/
       `Bits(64_000)` round-trip exact (the test V1 of the HLD would have
       failed).
    4. `effective_bitrate_bps_clamps_below_request` — request 750 kbps,
       confirm `effective_bitrate_bps()` is `≤ 750_000` and `> 0`.
    5. `runtime_setters_surface_bad_arg` — `set_complexity(11)` and
       `set_packet_loss_perc(101)` surface `EncodeError::BadArg`.
    6. `mid_stream_bitrate_flip_changes_packet_size` — 1 kHz sine fixture
       (NOT silence; DTX would mask), 100 frames at `Bits(16_000)` then
       100 at `Bits(96_000)`, asserts `mean(batch2) >= 3 * mean(batch1)`.
    7. `mid_stream_bitrate_flip_changes_packet_size_cbr` — same shape
       with `set_vbr(false)` first; covers the CBR `silk_mode.use_cbr`
       paired-write path at `encoder.rs:3515-3517`.
    8. `set_max_bandwidth_round_trips` — `Narrowband` round-trips through
       `max_bandwidth()`. Simpler than TOC parsing, same delegation
       coverage.
    9. `many_mid_stream_flips_do_not_panic_and_decode` — 100-iteration
       deterministic 5-op cycle over {bitrate, vbr, fec, packet_loss,
       dtx}; no `rand` crate dep. Asserts no panic AND that each emitted
       packet decodes successfully (catches malformed-packet emission).
  - Plus a tenth `builder_inband_fec_and_dtx_round_trip` test for the
    new builder methods — three FEC modes × two DTX modes round-trip
    through the matching getters.
  - Updated the existing `builder_vbr_constraint_round_trip` /
    `builder_packet_loss_perc_round_trip` tests to call the renamed
    facade methods (`vbr_constraint()` returns `bool`,
    `packet_loss_perc()` returns `u8`) per the HLD's "Acceptance" item.
- `ropus/src/opus/encoder.rs`:
  - Removed the Stage-1 `#[allow(dead_code)]` on
    `pub(crate) get_user_bitrate_bps` — it now has a real call site in
    `Encoder::bitrate()`.

**Deviations from the HLD:** none material. Two notes worth recording:

- The 9 HLD tests landed plus one extra (the builder coverage for the
  new `inband_fec`/`dtx` methods, mirroring the existing
  `builder_vbr_constraint_round_trip` / `builder_packet_loss_perc_round_trip`
  precedents). HLD didn't enumerate it but the symmetry made it the path
  of least resistance.
- Deterministic cycle for test 9: per the HLD's footnote (`rand` is not
  in `ropus/Cargo.toml`'s dev-deps), used `i % 5` plus inner
  deterministic state derived from `i / 5` to avoid any single op
  becoming a no-op trampoline. Decoder oracle catches both panic and
  malformed-packet emission, as the HLD requires.

**Verification (all clean):**

- `cargo build -p ropus` — clean.
- `cargo test -p ropus --lib api::tests` — 34/34 pass (23 carried
  forward + 11 new: 1 wire-value pin + 9 HLD tests + 1 builder
  inband_fec/dtx round-trip).
- `cargo clippy -p ropus -- -D warnings` — clean.
- `cargo build -p capi` — clean (rename was correctly scoped to the
  high-level facade only; `OpusEncoder::get_vbr_constraint` /
  `get_packet_loss_perc` untouched, so the C ABI shim is intact).
- `cargo test -p ropus --lib` — 1864/1864 pass (no regressions in any
  other module).
- `grep "#\[allow(dead_code)\]" ropus/src/api.rs` — empty (Stage-2
  acceptance gate).

**Stage 2 ready for review.**

### 2026-04-29 — Stage 2 — review applied

Reviewer's verdict was "ready to commit with one minor doc nit"; three
optional rustdoc improvements were suggested. All applied to
`ropus/src/api.rs`; no logic, signatures, or tests touched.

- `set_complexity` rustdoc (line 645): tightened "Values outside the
  range surface as `EncodeError::BadArg`" → "Values above 10 surface as
  `EncodeError::BadArg`". Rationale: `c: u8`, so the only out-of-range
  case is `> 10`; "outside" is misleading.
- `set_packet_loss_perc` rustdoc (line 684): same shape — "outside the
  range" → "above 100". Rationale: `pct: u8`, so only the upper bound
  is reachable.
- `inband_fec()` getter rustdoc (line 768): added cross-reference to
  the [`InbandFec`] enum and called out that `InbandFec::Forced` is
  libopus' "FEC always" mode for SILK/Hybrid. Rationale: the bare
  one-line summary left consumers without a path to the variant docs
  that explain the encoder/auto/forced trichotomy.

**Verification gates after rustdoc tweaks:**
- `cargo build -p ropus` — clean.
- `cargo test -p ropus --lib api::tests` — 34/34 pass.
- `cargo clippy -p ropus -- -D warnings` — clean (rustdoc-only edits,
  no lint surface affected).

**Stage 2 ready for devil's advocate pass + commit.**

### 2026-04-29 — Stage 2 — DA feedback applied

Devil's advocate pass surfaced two findings (C1, C2) — both about
honesty between rustdoc/HLD claims and the actual encoder behaviour.
Both fixed in `ropus/src/api.rs`; no other files touched, no logic
changes, no test count change (Test 5 just gained two assertions).

- **C1 — Rustdoc lied about `Bits(0)` clamping.**
  `OpusEncoder::set_bitrate` (`ropus/src/opus/encoder.rs:3468-3470`)
  returns `OPUS_BAD_ARG` for `bitrate <= 0` (excluding the `Auto` /
  `Max` sentinels); it does NOT silently clamp `0` up to 500. Two
  rustdoc blocks made the false claim:
  - `OpusEncoder::set_bitrate` rustdoc — rewrote to spell out the
    three regimes accurately: `<= 0` ⇒ `BadArg`, `[1, 499]` ⇒ clamp
    to 500, `> 750_000 * channels` ⇒ clamp to that upper bound.
  - `Bitrate::from_c_int` rustdoc — replaced the "round-trip is
    faithful for any `n >= 1`" wording with a four-bullet round-trip
    table covering: `Bits(0)` (unreachable, `BadArg`), `Bits(1..=499)`
    (unreachable, clamps to 500), `Bits(500..=750_000*ch)` (faithful),
    `Bits(>750_000*ch)` (unreachable, clamps to upper bound).
  Rationale: the previous wording made `Bitrate::Bits(0)` look like a
  valid input that round-trips through clamping, when in fact the
  setter rejects it outright. Misleading docs are worse than missing
  docs.

- **C2 — Tests didn't exercise the BadArg-and-clamp boundary.**
  Test 5 (`runtime_setters_surface_bad_arg`) covered `set_complexity(11)`
  and `set_packet_loss_perc(101)` but said nothing about the bitrate
  edges that C1 documents. Added two assertions on the existing mono
  fixture:
  1. `set_bitrate(Bitrate::Bits(0))` ⇒ `Err(BadArg)` (bottom edge).
  2. `set_bitrate(Bitrate::Bits(2_000_000_000))` succeeds; the
     subsequent `bitrate()` read-back asserts `b <= 750_000` (the
     mono cap), proving the silent top-edge clamp.
  Rationale: the rustdoc now makes specific behavioural claims; tests
  must lock those claims down so future refactors can't quietly
  regress them.

**Verification gates after DA fixes:**
- `cargo build -p ropus` — clean.
- `cargo test -p ropus --lib api::tests` — 34/34 pass (no count
  change; Test 5 carries two extra assertions inside the same `#[test]`).
- `cargo clippy -p ropus -- -D warnings` — clean.

**Stage 2 ready for commit.**

## Stage 2 — audit gap closure

The completeness auditor identified one remaining gap (Gap 1): the HLD's
"Default state" subsection mandates that runtime getters on a freshly-built
encoder report the libopus-mandated defaults (`Bitrate::Auto`, VBR on, FEC
disabled, DTX off, complexity 9, `Signal::Auto`, `ForceChannels::Auto`,
`Bandwidth::Fullband`), but no test pinned them down. The previous tests
all called a setter first, so an inner-encoder default drift could have
gone undetected.

**Fix.** Added `runtime_getters_reflect_default_state_after_build` in
`d:\language\ropus\ropus\src\api.rs` (positioned as "Test 0" before the
existing setter round-trip tests). The test builds a mono 48 kHz encoder
with no setter calls and asserts each of the eight getters returns the
HLD-mandated default.

**Default verification before writing the test.** Each predicted default
was checked against the source of truth in
`d:\language\ropus\ropus\src\opus\encoder.rs` `OpusEncoder::new`:
- `user_bitrate_bps = OPUS_AUTO` at line 1284 → `Bitrate::Auto` ✓
- `use_vbr = 1` at line 1280 → `vbr() == true` ✓
- `fec_config = 0` at line 1289 → `InbandFec::Disabled` ✓
- `use_dtx = 0` at line 1288 → `dtx() == false` ✓
- `silk_mode.complexity = 9` at line 1255 → `complexity() == 9` ✓
- `signal_type = OPUS_AUTO` at line 1275 → `Signal::Auto` ✓
- `force_channels = OPUS_AUTO` at line 1274 → `ForceChannels::Auto` ✓
- `max_bandwidth = OPUS_BANDWIDTH_FULLBAND` at line 1277 → `Bandwidth::Fullband` ✓

No discrepancy between HLD-predicted defaults and actual code. The test
matches the auditor's suggested body verbatim.

**Verification gates after gap closure:**
- `cargo build -p ropus` — clean.
- `cargo test -p ropus --lib api::tests` — 35/35 pass (was 34, +1 for
  `runtime_getters_reflect_default_state_after_build`).
- `cargo clippy -p ropus -- -D warnings` — clean.

Closes auditor Gap 1.

### 2026-04-29 — Re-audit and close-out

**Re-audit verdict: 100% complete — feature ready.**

Final commit chain:

| Commit | Stage |
|---|---|
| `be98636` | HLD V3 (final) docs + journal opened |
| `6928557` | Stage 1 — foundation (InbandFec enum, from_c_int helpers, get_user_bitrate_bps accessor, check_ok_runtime, +5 tests) |
| `c238989` | Stage 2 — facade methods (10 setters + 11 getters, builder additions, renames+retypes, +11 tests, dead-code attrs purged) |
| `5699c40` | Default-state test (closes audit Gap 1, +1 test) |

Test count progression: 18 → 23 (Stage 1) → 34 (Stage 2) → 35 (gap closure). Full lib test: 1864 → 1865.

**Verification at close:**
- `cargo test -p ropus --lib` — 1865/1865 pass
- `cargo build --workspace` — clean
- `cargo clippy -p ropus -- -D warnings` — clean
- `cargo build -p capi` — clean (rename correctly scoped to `api::Encoder` only; low-level `OpusEncoder::get_vbr_constraint` and `get_packet_loss_perc` untouched per HLD; capi callers at `capi/src/ctl.rs:198,200,568,580` and `harness/src/cli.rs:5821,5827` unaffected)
- `grep '#\[allow(dead_code)\]' ropus/src/api.rs` — zero matches (all 8 Stage-1-introduced attrs removed)
- `git diff be98636..HEAD -- ropus/Cargo.toml` — empty (no new dependencies)

**Audit observation (non-actionable):** The "Stage 2 — DA feedback applied" subsection above describes editing `OpusEncoder::set_bitrate` rustdoc; the actual edit was on the high-level `Encoder::set_bitrate` rustdoc at `api.rs:645-653` and `Bitrate::from_c_int` at `api.rs:139-162`. The low-level `OpusEncoder::set_bitrate` carries no rustdoc and is not the API surface the HLD targets. Consumer-facing rustdoc is correct; this is a journal phrasing inaccuracy only.

**Process recap:**
- Stage 1: implementer → reviewer → applier → DA → commit
- Stage 2: implementer → reviewer → applier → DA → applier (DA findings) → commit
- Audit: auditor → gap-closer → commit
- Re-audit: confirms 100%

Total real defects caught and fixed by the team across HLD review and implementation: nine.
- HLD round 1: 3 (InbandFec enum, bitrate getter footgun, false conformance citation)
- HLD round 2: 3 (phantom function name `compute_frame_size`, derived-cache caveat, deterministic test threshold)
- Stage 2 code review: 1 (visibility consistency on `InbandFec` helpers)
- Stage 2 DA: 2 (rustdoc lies about Bits(0), missing BadArg/clamp boundary tests)

**Feature complete.**
