# 2026-07-30 — macOS / ARM optimisation

## Goal

Make macOS on Apple Silicon a measured, bit-exact performance target:

- build the C comparison oracle correctly on AArch64;
- use a conservative Apple-Silicon compiler baseline;
- profile the fixed-point codec on Apple Silicon;
- complete only the safe SIMD kernels justified by evidence;
- preserve identical codec output and the existing cross-platform fallback.

Raw `std::arch::aarch64` intrinsics, a new dependency, internal codec
threading, and floating-point reassociation are out of scope unless the
evidence exposes a blocker that cannot be solved through the existing `wide`
dependency or compiler-friendly safe Rust.

## Worktree

- Path: `/Users/md/worktrees/ropus/20260730-TSK-HUM-macos-arm-optimisation`
- Branch: `task/20260730-TSK-HUM-macos-arm-optimisation`
- Base: `origin/main` at `1e888cd`
- Integration and push are not authorised.

## Live state

- Repository instructions read; no `lessons_learnt.md` exists.
- Pinned reference commit `788cc89ce4` is provisioned under ignored
  `reference/`.
- Native ARM build failure reproduced: Clang rejects `celt/x86/x86cpu.c`
  through x86-only `cpuid.h`.
- Initial audit confirms `.cargo/config.toml` tunes x86 only.
- `harness/build.rs`, `tests/fuzz/build.rs`, and `harness/config.h` select x86
  SIMD unconditionally, so the reference oracle needs architecture-aware
  wiring.
- `celt::simd::{xcorr_kernel_simd,celt_maxabs32_simd}` use `wide::i32x4`.
- `denormalise_band_simd` and `mdct_window_simd` are scalar-unrolled despite
  their names; their value must be measured rather than assumed.
- Two bounded read-only workers completed harness and SIMD/profiling audits.
- Draft HLD:
  `wrk_docs/2026.07.30 - HLD - macos arm optimisation.md`.

## Design checkpoint

- Status: `SELF-APPROVED`.
- Two independent adversarial reviews and both cross-refutation checks ran.
- A single Claude Opus fresh-eyes review ran in read-only plan mode.
- The external review found a critical macro defect: non-intrinsic
  `OPUS_ARM_PRESUME_NEON` would select an uncompiled ARMv7 pitch symbol.
  The design now uses only the three `_NEON_INTR` macros.
- The one targeted re-review confirmed that critical issue is resolved. It
  also confirmed that upstream's no-RTCD `silk_NSQ_del_dec` selector remains
  scalar with this safe macro set; object compilation is not treated as proof
  of execution.
- Other surviving findings tightened check-asm assertions, fuzz linking,
  exactness edge cases, profiling symbols, A/B retention, x86 isolation, and
  ARM-specific benchmark thresholds.

Residual risks at checkpoint:

- x86 source selection can be cross-compiled locally, but a fleet x86 runtime
  may not be reachable from this Mac; missing runtime evidence will be stated
  rather than hidden.
- Upstream ARM/scalar and x86 SSE disagree at the already-documented
  `celt_fir` negative saturation edge. This work preserves existing ropus
  output and treats the difference explicitly.
- Candidate SIMD work may be rejected if focused timings do not clear noise.

## Next

Implement and validate the architecture-aware C oracle first, then capture
the generic/A14 baseline before retaining compiler tuning or new SIMD.
