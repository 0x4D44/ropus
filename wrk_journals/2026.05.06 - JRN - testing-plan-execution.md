# 2026.05.06 - JRN - testing-plan-execution

## Initial Brief
- Arthur asked to systematically work through the testing improvement plan.
- Required workflow:
  - use an agent team,
  - have each agent write an HLD up-front,
  - review and sign off each HLD before any agent writes code,
  - use `code-reviewer` and `code-review-apply-feedback` practices,
  - maintain this journal throughout.

## Ground Rules Reconfirmed
- Workspace root: `/home/md/language/ropus`.
- Do not write outside the workspace tree.
- No git add, commit, or push.
- Keep source changes scoped; protect other agents' in-flight work.
- Use `timeout` for validation commands.

## Skills Loaded
- `software-testing`: for test strategy and oracle/gate design.
- `code-reviewer`: for post-implementation review stance.
- `code-review-apply-feedback`: for methodical handling of review findings.

## Team Structure
- Workstream A: Harness gate integrity.
  - Scope: `/home/md/language/ropus/harness/src/cli.rs`.
  - Goal: length-aware `CompareStats`, helper tests, non-zero headline compare
    exits on mismatch.
- Workstream B: Fuzz runner target discovery.
  - Scope:
    - `/home/md/language/ropus/tools/fuzz_cycle.sh`
    - `/home/md/language/ropus/tools/fuzz_run.sh`
    - read-only reference to `/home/md/language/ropus/tests/fuzz/Cargo.toml`.
  - Goal: eliminate stale hard-coded fuzz target lists.
- Workstream C: Release preflight and oracle matrix.
  - Scope: HLD only initially; likely follow-up files under
    `/home/md/language/ropus/full-test/src` and `/home/md/language/ropus/wrk_docs`.
  - Goal: define strict asset preflight and maintained oracle matrix.

## Emotional Temperature
- The process constraint is heavier than the code slices, but it is the right
  control for this repo. I need to keep the team from turning this into a broad
  testing rewrite; the useful move is narrow gate hardening first.

## HLD Review: Workstream B
- Mencius produced `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - fuzz target discovery.md`.
- Direction is correct: use `/home/md/language/ropus/tests/fuzz/Cargo.toml` as
  the single source of truth and share discovery between the two fuzz runners.
- Not signed off yet. I sent it back because the proposed
  `mapfile -t TARGETS < <(discover_fuzz_targets ...)` pattern can mask a
  nonzero helper exit; Bash process substitution does not make `mapfile`
  return the producer's status.
- Requested revision:
  - use a failure-propagating target loading pattern,
  - define consistent error handling,
  - discover targets before `fuzz_cycle.sh` creates timestamped log dirs,
  - include malformed-manifest validation without editing the real manifest,
  - clarify explicit invalid targets fail before cargo-fuzz build.

## Emotional Temperature Update
- Glad the first HLD had the right shape, but this is exactly why the design
  review gate matters. A stale-target fix that quietly ignores parser failures
  would recreate the same false-green problem in a different place.

## Workstream A HLD - Harness Gate Integrity
- Arthur assigned Workstream A as design-only for this round. I inspected
  `/home/md/language/ropus/harness/src/cli.rs` and
  `/home/md/language/ropus/wrk_docs/2026.05.05 - PLN - testing improvement plan.md`.
- The current harness comparison helper pads the absent side with zero, so
  trailing zero length mismatches can be counted as equality. The headline
  commands print failures but return normally.
- Wrote `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - harness gate integrity.md`
  with a smallest-scope design: length-aware `CompareStats`, helper-level unit
  tests, and a private command outcome enum used only by `encode`, `decode`, and
  `roundtrip`.
- Emotional temperature: mildly impatient with how easy this class of gate bug
  is to miss, but the fix shape is satisfyingly small. The main thing to resist
  is turning this into a command-runner rewrite.

## Workstream A Implementation - Harness Gate Integrity
- Arthur signed off the HLD and constrained implementation ownership to
  `/home/md/language/ropus/harness/src/cli.rs`.
- Implemented length-aware `CompareStats`, focused helper tests, and headline
  `encode`/`decode`/`roundtrip` outcome-to-exit handling while preserving
  explicit `SKIP` as exit `0`.
- Validation passed:
  - `timeout 120 cargo test -p ropus-harness compare_stats`
  - `timeout 120 cargo test -p ropus-harness exit_code_for_compare_outcome`
  - `timeout 120 cargo test -p ropus-harness`
- Emotional temperature: good, low drama. This was the right size of change:
  fix the oracle contract without dragging diagnostics or release preflight into
  the same patch.

## HLD Review: Workstream A
- Reviewed `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - harness gate integrity.md`.
- Signed off.
- Sign-off rationale:
  - implementation scope is one file: `/home/md/language/ropus/harness/src/cli.rs`;
  - `CompareStats` becomes length-aware without changing codec behavior;
  - helper tests directly cover the trailing-zero false-green bug;
  - only `encode`, `decode`, and `roundtrip` become process gates;
  - `SKIP` remains exit `0` for this diagnostic command slice, while release
    strictness is left to Workstream C.
- Note for implementation: if a length mismatch sets `first_diff_offset` at the
  overlap boundary, the print helpers must handle that offset safely for both
  byte and sample slices.

## Implementation Started: Workstream A
- Sent Halley the signed-off HLD and authorized implementation.
- Ownership constrained to `/home/md/language/ropus/harness/src/cli.rs`.
- Required validation:
  - focused compare helper tests,
  - focused outcome/exit mapping tests,
  - `timeout 120 cargo test -p ropus-harness` if scoped tests pass.

## HLD Review: Workstream C
- Mill produced `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - release preflight and oracle matrix.md`.
- Direction is correct: strict release preflight belongs in `full-test`, not in
  build scripts; the oracle matrix should label the claim behind each gate.
- Not signed off yet. I sent it back because the design needs a concrete first
  release profile. It currently uses `selected claim`, `not_claimed`, and
  `intentionally_skipped` language without specifying how the CLI tells the
  code which claims are selected.
- Requested revision:
  - define C1 `--release-preflight` as a core profile or propose concrete
    profile flags;
  - split C1 from future C2 neural/DRED profile work;
  - keep float-mode deep PLC assets report-only unless C1 explicitly claims
    neural PLC;
  - make required-vs-optional missing status machine-actionable;
  - avoid double-counting IETF absence because `full-test` already has a
    synthetic failure path.

## Emotional Temperature Update
- The team is converging, but the preflight slice is where scope could balloon.
  I am pushing it toward one concrete profile because otherwise the code will
  encode assumptions nobody actually agreed to.

## HLD Review: Workstream B Revised
- Reviewed the revised
  `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - fuzz target discovery.md`.
- Signed off.
- Sign-off rationale:
  - `/home/md/language/ropus/tests/fuzz/Cargo.toml` becomes the single source
    of truth;
  - the capture-first loading pattern propagates parser/helper failures;
  - `fuzz_cycle.sh` discovers targets before timestamped log directory creation;
  - invalid explicit targets fail before cargo-fuzz build;
  - malformed-manifest validation uses a temporary copy rather than touching
    the real manifest.
- Implementation caution: preserve a friendly `--help` path in `fuzz_run.sh` if
  it stays simple; otherwise do not let that preference complicate the fix.

## HLD Review: Workstream C Revised
- Reviewed the revised
  `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - release preflight and oracle matrix.md`.
- Signed off for C1.
- Sign-off rationale:
  - `--release-preflight` has one concrete initial `core` profile;
  - C1 required assets are fixed-point reference source, upstream conformance
    source, and IETF vectors;
  - DNN base weights, DRED RDOVAE weights, and float-mode deep PLC assets are
    report-only in C1;
  - statuses are machine-actionable (`present_required`,
    `provisioned_required`, `missing_required`, `present_optional`,
    `missing_optional`);
  - IETF absence remains accounted for through the existing Stage 2 synthetic
    failure instead of being double-counted.
- I am holding C1 implementation until Workstreams A/B settle, because A/B are
  smaller and directly protect the gate plumbing that C1 will report through.

## Workstream B Implementation
- Implemented signed-off fuzz target discovery:
  `/home/md/language/ropus/tools/fuzz_targets.sh` now provides shared `die`
  and `discover_fuzz_targets` helpers.
- Updated `/home/md/language/ropus/tools/fuzz_cycle.sh` and
  `/home/md/language/ropus/tools/fuzz_run.sh` to load targets from the fuzz
  manifest with capture-first error propagation.
- Validation passed for shell syntax, dry-run target listing, explicit invalid
  target rejection before cargo-fuzz build, and malformed copied-manifest
  helper failure.
- Emotional temperature: steady. The fix is small and removes a brittle manual
  list without dragging in a TOML dependency.

## Lead Validation Pass
- Re-ran high-signal checks locally after A/B implementations landed:
  - `timeout 120 cargo test -p ropus-harness compare_stats` passed: 5 focused
    tests.
  - `timeout 120 cargo test -p ropus-harness exit_code_for_compare_outcome`
    passed: 1 focused test.
  - `timeout 10s bash -n tools/fuzz_targets.sh && timeout 10s bash -n tools/fuzz_cycle.sh && timeout 10s bash -n tools/fuzz_run.sh`
    passed.
  - `timeout 10s bash tools/fuzz_cycle.sh 0` passed and listed 12 manifest
    targets.
  - `timeout 10s bash tools/fuzz_run.sh --list` passed and listed 12 manifest
    targets.
  - `timeout 10s bash tools/fuzz_run.sh --target fuzz_packet_parse --duration 0`
    failed as expected before the build phase with an undeclared-target error.
  - malformed copied-manifest helper test failed as expected with exit `2`.
  - `timeout 120 cargo test -p ropus-harness` passed.
- Note: the `fuzz_cycle.sh 0` dry run still creates an empty timestamped log
  directory, matching prior script structure. The signed-off requirement was
  that manifest discovery failures occur before log directory creation.

## Code Review Started
- Spawned independent reviewer Meitner to review the A/B implementation using
  the `code-reviewer` stance.
- Review scope is limited to:
  - `/home/md/language/ropus/harness/src/cli.rs`
  - `/home/md/language/ropus/tools/fuzz_targets.sh`
  - `/home/md/language/ropus/tools/fuzz_cycle.sh`
  - `/home/md/language/ropus/tools/fuzz_run.sh`
- I will apply any required feedback using the `code-review-apply-feedback`
  process before starting C1 implementation.

## Code Review Feedback Applied
- Meitner reported no blocking issues.
- Feedback item 1: `tools/fuzz_run.sh` valued flags used `$2` under `set -u`,
  so a missing operand produced Bash's unbound-variable error instead of our
  `die` path.
  - Decision: agree and fix. This is not a gate-integrity bug, but the scripts
    are operator-facing and should fail clearly.
  - Change: added `require_value()` and used it for `--duration`, `--target`,
    `--jobs`, and `--max-len`.
- Feedback item 2: `tools/fuzz_targets.sh` only matched bare `[[bin]]` table
  headers, even though TOML commonly permits trailing comments.
  - Decision: apply. This is a tiny parser hardening change and does not expand
    beyond the manifest shape we rely on.
  - Change: table-header detection now accepts optional leading whitespace and
    a trailing comment.
- Feedback item 3: optional subprocess test for harness command dispatch.
  - Decision: skip for this slice. Helper-level tests cover the new exit
    mapping, and command-level harness tests would need fixture/FFI setup for a
    synthetic failure path. That cost is not justified for this gate-plumbing
    patch.

## Post-Feedback Validation
- `timeout 10s bash -n tools/fuzz_targets.sh && timeout 10s bash -n tools/fuzz_cycle.sh && timeout 10s bash -n tools/fuzz_run.sh`
  passed.
- Missing operand checks now fail through `die` for `--duration`, `--target`,
  `--jobs`, and `--max-len`.
- Temporary manifest with `[[bin]] # fuzz target` now discovers all 12 targets.
- `timeout 10s bash tools/fuzz_run.sh --list` still lists the expected current
  manifest targets, including `fuzz_repacketizer_seq`, `fuzz_ctl_sequence`,
  `fuzz_dnn_blob`, `fuzz_multistream`, and `fuzz_decode_plc_seq`.

## Implementation Started: Workstream C C1
- Sent Mill the signed-off C1 HLD and authorized implementation.
- Scope constrained to `full-test` C1 preflight wiring and
  `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`.
- Explicit constraints:
  - `--release-preflight` is a fixed core profile only;
  - no neural/DRED hard gates in C1;
  - no build-script or codec-source changes;
  - IETF absence must not be double-counted because Stage 2 already injects a
    synthetic failure.

## Emotional Temperature Update
- A/B is in good shape after review. C1 is broader and more likely to produce
  integration friction, so I am keeping the first profile intentionally narrow.

## Workstream C C1 Implementation
- Mill implemented the C1 release preflight in `full-test` and added
  `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`.
- Reported validation:
  - `timeout 120 cargo test -p full-test preflight` passed.
  - `timeout 120 cargo test -p full-test cli` passed.
  - `timeout 120 cargo test -p full-test report` passed.
  - `timeout 120 cargo test -p full-test html` passed.
  - `timeout 120 cargo test -p full-test banner` passed.
  - `timeout 180 cargo test -p full-test` passed.
- Lead review found one required integration fix: `full-test/Cargo.toml` was
  bumped from `0.4.3` to `0.4.4`, but `Cargo.lock` still recorded
  `full-test` as `0.4.3`.
  - Decision: fix. A package version bump and lockfile disagreeing is
    unnecessary friction for locked builds.
  - Change: updated the `full-test` package entry in `Cargo.lock` to `0.4.4`.

## Lead Validation Pass: C1
- After the `Cargo.lock` fix, reran:
  - `timeout 120 cargo test -p full-test preflight --locked` passed.
  - `timeout 120 cargo test -p full-test banner --locked` passed.
  - `timeout 120 cargo test -p full-test cli --locked` passed.
  - `timeout 120 cargo test -p full-test report --locked` passed.
  - `timeout 120 cargo test -p full-test html --locked` passed.
  - `timeout 180 cargo test -p full-test --locked` passed: 143 tests.
  - `timeout 120 cargo fmt --check` passed.
  - `git diff --check` passed.
- I have not run the long `full-test --release-preflight` command yet; first I
  want the independent review to check the C1 semantics.

## C1 Code Review Feedback Applied
- Avicenna found no blocking Rust preflight semantics bug.
- Feedback item 1: `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`
  did not fully match the signed-off schema: no per-row `last_reviewed`, and
  several `strongest_oracle` cells used mixed/free-form values.
  - Decision: agree and fix. The matrix is the contract for oracle strength;
    fuzzy values undermine the point of adding it.
  - Change: added a per-row `Last reviewed` column and made each
    `Strongest oracle` cell use one controlled class. Split the fuzz row into
    separate weakened/exact rows rather than using "depending" language.
- Feedback item 2: JSON/HTML tests did not strongly lock the preflight report
  surface.
  - Decision: agree and fix. The core probe tests were solid, but report
    regressions could pass with only heading/count assertions.
  - Change: added a JSON contract test for concrete asset keys, statuses,
    requirements, `available`, and `banner_blocking`; added an HTML contract
    test for active core profile rendering and required/optional status rows.

## Optional Release-Preflight Smoke
- Started
  `timeout 1800 cargo run -p full-test -- --release-preflight --quick --skip-coverage --skip-benchmarks --skip-ambisonics`
  to exercise the integrated CLI/report path.
- The command compiled `full-test` and exposed one warning: `Outcome::inactive`
  and `Outcome::missing_required_for_banner` were only used by tests.
- The smoke then spent several minutes inside the workspace conformance
  `extensions` test binary with no output. I interrupted it rather than burn the
  rest of the turn on an optional smoke; the focused locked validations are the
  reliable acceptance signal for this patch.
- Fix applied: marked the two test-only preflight helpers with `#[cfg(test)]`
  so normal `full-test` builds do not warn.

## Final Validation Snapshot
- `timeout 120 cargo test -p full-test preflight --locked` passed: 9 tests.
- `timeout 120 cargo test -p full-test report --locked` passed: 14 tests.
- `timeout 120 cargo test -p full-test html --locked` passed: 23 tests.
- `timeout 120 cargo build -p full-test --locked` passed with no warnings.
- `timeout 180 cargo test -p full-test --locked` passed: 145 tests.
- `timeout 120 cargo test -p ropus-harness` passed.
- Final shell validation passed:
  - `timeout 10s bash -n tools/fuzz_targets.sh`
  - `timeout 10s bash -n tools/fuzz_cycle.sh`
  - `timeout 10s bash -n tools/fuzz_run.sh`
  - dry-run/list checks that include `fuzz_decode_plc_seq`.
- `timeout 120 cargo fmt --check` passed.
- `git diff --check` passed.

## Current State
- Completed and reviewed:
  - Workstream A: harness gate integrity.
  - Workstream B: fuzz target discovery.
  - Workstream C C1: release preflight and oracle matrix.
- Remaining plan work starts after this slice:
  - C2/future release profiles for neural/DRED only when the oracle matrix rows
    are strong enough;
  - stronger DRED payload parity gate;
  - legal frame-duration fuzz dimensions;
  - populated real-world corpus gate;
  - PLC/recovery drift follow-up.

## Final Emotional Temperature
- Good progress. The process overhead was real, but the HLD-review gate caught
  multiple useful things before they became code: fragile Bash process
  substitution, fuzzy release profile semantics, and a too-weak oracle matrix.
  The optional full-test smoke getting stuck in `extensions` is irritating but
  separate from the scoped changes; I do not want to hide that in the final
  answer.
## Workstream C C1 Implementation

Implemented the signed-off release preflight C1 scope in `full-test`:

- Added fixed-core `--release-preflight` CLI flag.
- Added `full-test/src/preflight.rs` with pure path probes for required core
  assets and optional/report-only DNN/DRED/deep-PLC assets.
- Wired preflight into setup, banner classification, JSON, and HTML.
- Kept IETF failure accounting on the existing Stage 2 synthetic failure path.
- Added the initial oracle matrix document.

Emotional temperature: focused and cautious. The repo has other active edits in
adjacent workstreams, so I kept this tightly inside the signed-off C1 file list.

## Final Note
- A late worker journal note landed after my final validation snapshot. Keeping
  it in place per the journal rule; this note marks the actual end of the turn.
- Confirmed no `full-test`, `cargo`, or conformance child processes are still
  running after the interrupted optional smoke.
- No `tests/results` report file was produced by the interrupted smoke.
