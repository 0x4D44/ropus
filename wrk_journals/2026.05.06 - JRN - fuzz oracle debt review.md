# Fuzz Oracle Debt Review Journal

## 2026.05.06

Started independent Phase 2.2 review for fuzz oracle debt reporting. Loaded the `code-reviewer` skill and refreshed repo instructions before looking at code. Emotional temperature: steady, slightly wary of reporting contracts because these small helper formats tend to become accidental APIs.

Read the signed HLD and current uncommitted diff for `/home/md/language/ropus/tests/fuzz/fuzz_targets/oracle.rs`, `/home/md/language/ropus/tests/fuzz/tests/oracle_tests.rs`, and `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`. The implementation is narrow and mostly follows the accepted Option B plus ignored report path. I checked surrounding classifier behavior and `fuzz_decode.rs` parsing to make sure the report helper is not inventing a different input interpretation.

Validation run:

```bash
timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests
```

Passed with 20 tests passing and 1 ignored. The C reference build emitted the expected optimization warning only.

Validation run:

```bash
timeout 120 cargo fmt --check
```

Passed.

Extra validation run because the ignored report reads committed directories:

```bash
timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests -- --ignored --nocapture decode_oracle_class_distribution_report
```

Passed and printed total 41, strong 33, weakened 8.

Main concern found: `format_report` presents rows with a comma-separated header, but the prose fields themselves contain commas and are not quoted. I do not think this blocks Phase 2.2 because the report is human-readable and deterministic, but it is a real footgun if someone treats the output as CSV later. Frustration level low; this is exactly the kind of small reporting ambiguity that is cheap to fix now and annoying once copied into docs or scripts.

Saved the review to `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - fuzz oracle debt reporting.md`.
