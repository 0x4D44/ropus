# 2026.05.06 - JRN - testing-plan-continuation

## Initial Brief
- Arthur asked to continue the testing improvement plan with me acting as
  supervisor.
- Required operating model:
  - use agents for legwork,
  - preserve supervisor context for guidance and direction,
  - do not personally dig into detailed testing/debugging where an agent can do
    it,
  - use implementation agents, review agents, and feedback-application agents,
  - use TDD as appropriate,
  - iterate until complete,
  - commit periodically.

## Starting Point
- Previous slice completed:
  - harness gate integrity,
  - fuzz target discovery,
  - release preflight C1 and oracle matrix.
- Known caveat from previous slice:
  - optional integrated smoke
    `cargo run -p full-test -- --release-preflight --quick --skip-coverage --skip-benchmarks --skip-ambisonics`
    stalled inside conformance `extensions`.
- Immediate supervisor goals:
  1. Commit the completed, reviewed A/B/C1 testing-gate slice.
  2. Delegate diagnosis of the integrated smoke hang.
  3. Use review/apply-feedback loop before accepting any fix.

## Emotional Temperature
- The repo is in a broad dirty state with useful completed work and process
  artifacts. I need to be careful not to mix the previous completed gate-hardening
  slice with new smoke-hang fixes. The right move is disciplined commit
  boundaries, then a separate diagnosis/fix cycle.

## Commit Boundary Assessment
- Delegated current-state assessment to Aquinas.
- Recommended committing the completed A/B/C1 testing-gate slice:
  - `Cargo.lock`
  - `full-test/**` C1 preflight changes
  - `harness/src/cli.rs`
  - `tools/fuzz_cycle.sh`, `tools/fuzz_run.sh`, `tools/fuzz_targets.sh`
  - 2026.05.06 HLDs and workstream journals
  - `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`
- Recommended leaving older 2026.05.05 planning/review docs and the
  2026.05.04 release/refuzz journal out of this commit boundary.
- Version pairing confirmed: `full-test/Cargo.toml` and `Cargo.lock` both carry
  `full-test` version `0.4.4`.

## Checkpoint Commit
- Committed completed A/B/C1 work as:
  - `79a4b96 Add testing gate integrity checks`
- Confirmed only older untracked process artifacts remain outside the commit:
  - 2026.05.05 testing plan/review docs,
  - 2026.05.04/2026.05.05 journals.
- Feeling: cleaner now. The worktree no longer blends finished testing-gate
  changes with the next diagnostic loop, which reduces the chance of accidentally
  committing unrelated notes or stepping on an agent's in-flight work.

## Smoke Diagnosis
- Planck completed the delegated smoke-hang diagnosis.
- Finding:
  - The quick release-preflight smoke is not hanging mysteriously.
  - Stage 2 still runs the full workspace test command.
  - The conformance `extensions` test enters upstream
    `test_random_extensions_parse()`, which loops 100,000,000 times.
- Recommended direction:
  - Make `--quick` skip the slow extension conformance test in Stage 2.
  - Keep the full non-quick path unchanged.
  - Add focused tests around command construction and skip-filter ordering.
- Main tradeoff:
  - `libtest --skip extensions` is substring-based and global to the workspace
    cargo invocation. If this proves too broad, split quick Stage 2 into smaller
    invocations. I want the HLD to evaluate this explicitly before code starts.
- Feeling:
  - This is a relief: it is an accidental quick-mode contract gap, not a deep
    test infrastructure failure. Still, the global `--skip` behavior deserves
    care because hidden over-skipping would be a bad testing regression.

## HLD Review: Quick Smoke Conformance Skip
- Heisenberg drafted
  `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - quick smoke conformance skip.md`.
- Initial review requested iteration because the HLD treated CLI help
  transparency as optional and did not pin down skip visibility tightly enough.
- Revised HLD accepted:
  - Policy is `options.quick && options.release_preflight`.
  - Stage 2 remains a single cargo invocation.
  - `--skip extensions` is appended only for this bounded smoke path.
  - The printed Stage 2 command/header and unit-tested args must show the skip.
  - CLI help must disclose the bounded exception.
  - No broad report-model refactor.
- Sign-off decision:
  - Approved for implementation. The global substring skip is not ideal, but it
    is proportionate for a smoke path and keeps the full validation path intact.

## Implementation Attempt: Quick Smoke Conformance Skip
- Boole implemented the signed-off design without committing.
- Changed files reported:
  - `/home/md/language/ropus/full-test/src/main.rs`
  - `/home/md/language/ropus/full-test/src/tests.rs`
  - `/home/md/language/ropus/full-test/src/cli.rs`
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - quick smoke conformance skip implementation.md`
- Validation reported by Boole:
  - `timeout 120 cargo test -p full-test --locked` passed, 150 tests.
  - `timeout 120 cargo fmt --check` passed after `cargo fmt`.
  - Smoke command still timed out after 300s:
    `timeout 300 cargo run -p full-test -- --release-preflight --quick --skip-coverage --skip-benchmarks --skip-ambisonics`
- Exact reported stopping point:
  - `[tests] cargo test --workspace --lib --tests --no-fail-fast -- --test-threads=1 --skip extensions`
- Supervisor decision:
  - Do not commit this slice yet. The command-shape work appears correct, but
    the user-facing smoke goal is not complete.
  - Delegate a focused code review of the current patch and a separate
    diagnosis of the next slow Stage 2 path.
- Feeling:
  - The first fix removed the known slow extension stress test but exposed that
    the whole Stage 2 workspace command is still too broad for the intended
    smoke budget. This is exactly why I want the next move diagnosed and
    reviewed rather than stacking guesses.

## Code Review: Quick Smoke Patch
- Dalton completed an independent review using the code-reviewer workflow.
- Review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - quick smoke conformance skip.md`
- Review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - quick smoke conformance skip review.md`
- Finding:
  - No blocking source-code defects in the current skip wiring.
  - One issue remains at the product/goal level: the patch is only a partial
    fix because the intended smoke command still timed out.
  - Dalton independently verified that
    `cargo test -p conformance --test extensions -- --test-threads=1 --skip extensions`
    filters the known slow test immediately.
- Supervisor response:
  - Treat the current patch as technically sound but insufficient until
    Herschel identifies the remaining slow Stage 2 path and we decide the next
    design step.

## Diagnosis: Remaining Stage 2 Timeout
- Herschel completed the focused diagnosis without source edits.
- Diagnosis saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - DGN - quick smoke stage2 timeout.md`
- Diagnosis journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - quick smoke stage2 timeout diagnosis.md`
- Root cause:
  - `--skip extensions` filters the known slow conformance extension binary,
    but Stage 2 is still a broad workspace run.
  - Bounded probes found additional slow or unsuitable quick-smoke packages:
    conformance encode/decode exceed 60s; harness control/deep PLC took about
    62s and exposed a separate failing deep PLC assertion.
- Recommendation:
  - Do not stack more substring `--skip` filters.
  - Introduce a narrower Stage 2 package profile for
    `--quick --release-preflight`, likely excluding `conformance`,
    `ropus-harness-control`, and `ropus-harness-deep-plc`.
- Supervisor response:
  - Move back to HLD before additional code. The current patch is useful but
    incomplete, and the next change alters the Stage 2 validation profile
    rather than just one libtest filter.
- Feeling:
  - This is the right moment to push against incremental skip sprawl. A package
    profile is more honest than pretending a few substring filters define a
    smoke test.

## HLD Review: Quick-Preflight Stage 2 Profile
- Anscombe drafted
  `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - quick preflight stage2 profile.md`.
- Accepted design:
  - Keep the internal trigger as `options.quick && options.release_preflight`.
  - Replace the partial `--skip extensions` approach with Cargo package
    excludes for:
    - `conformance`
    - `ropus-harness-control`
    - `ropus-harness-deep-plc`
  - Preserve the existing IETF-vector `--skip testvector` fallback and
    synthetic failure behavior.
  - Honor the same package profile in both skip-coverage and coverage-enabled
    command construction, while validating the explicit `--skip-coverage`
    smoke path as the acceptance priority.
  - Update CLI help so it describes a narrower Stage 2 smoke profile, not an
    extensions stress-test filter.
- Sign-off decision:
  - Approved for implementation. The design is more honest than stacking
    global libtest substring filters and keeps the full non-quick validation
    path intact.

## Implementation: Quick-Preflight Stage 2 Profile
- Ramanujan implemented the revised package-profile design and did not commit.
- Changed files reported:
  - `/home/md/language/ropus/full-test/src/tests.rs`
  - `/home/md/language/ropus/full-test/src/main.rs`
  - `/home/md/language/ropus/full-test/src/cli.rs`
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - quick preflight stage2 profile implementation.md`
- Implementation summary:
  - Added a `Stage2Profile`.
  - Replaced the rejected `--skip extensions` behavior with Cargo package
    excludes for quick release-preflight.
  - Applied the package profile in both `cargo test` and `cargo llvm-cov`
    command construction.
  - Kept `--skip testvector` as the only libtest skip.
- Validation reported by Ramanujan:
  - `timeout 120 cargo test -p full-test --locked` passed, 152 tests.
  - `timeout 240 cargo test --workspace --exclude conformance --exclude ropus-harness-control --exclude ropus-harness-deep-plc --lib --tests --no-fail-fast -- --test-threads=1` passed.
  - `timeout 300 cargo run --release -p full-test -- --quick --release-preflight --skip-coverage` passed.
  - `timeout 120 cargo fmt --check` passed.
- Supervisor decision:
  - This satisfies the main smoke objective, but the slice still needs an
    independent code review and a separate feedback-application pass before
    commit.
- Feeling:
  - Much better. The solution now matches the actual problem: quick preflight
    gets a bounded Stage 2 profile instead of a brittle stack of test-name
    filters.

## Code Review: Quick-Preflight Stage 2 Profile
- Einstein completed an independent code review using the code-reviewer
  workflow.
- Review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - quick preflight stage2 profile.md`
- Review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - quick preflight stage2 profile review.md`
- Findings:
  - No blocking issues.
  - One optional gap: `print_help()` output is not directly asserted, so the
    updated `--quick` help text is currently manually reviewed rather than
    test-covered.
- Reviewer validation:
  - `timeout 120 cargo test -p full-test --locked` passed.
  - `git diff --check -- full-test/src/cli.rs full-test/src/main.rs full-test/src/tests.rs` passed.
  - Package names confirmed via `cargo metadata`.
- Supervisor response:
  - Delegate feedback application for the optional CLI help test gap. It is
    small and worthwhile because the help text is part of the signed-off
    transparency requirement.

## Feedback Application
- Peirce applied the review feedback using the code-review-apply-feedback
  workflow.
- Feedback item:
  - Add direct test coverage for the updated quick release-preflight help text.
- Result:
  - Added a small testable `help_text()` helper and routed `print_help()`
    through it.
  - Added a focused assertion for the signed-HLD help line.
- Feedback journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - quick preflight stage2 profile feedback.md`
- Validation reported by Peirce:
  - `timeout 120 cargo test -p full-test --locked` passed, 153 tests.
  - `timeout 120 cargo fmt --check` passed.
- Supervisor response:
  - The known review gap is closed. Next step is a final completion review to
    confirm the whole slice is ready to commit.

## Completion Review
- Plato performed the final completion review.
- Completion review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - quick preflight completion.md`
- Completion review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - quick preflight completion review.md`
- Go/no-go:
  - Go.
- Plato confirmed:
  - `quick && release_preflight` uses package excludes for `conformance`,
    `ropus-harness-control`, and `ropus-harness-deep-plc`.
  - Rejected `--skip extensions` behavior is gone.
  - IETF `--skip testvector` fallback and synthetic failure behavior are
    preserved.
  - Normal non-quick Stage 2 command shape is unchanged.
  - CLI help text is transparent and directly test-covered.
- Validation passed:
  - `timeout 120 cargo test -p full-test --locked`
  - `timeout 240 cargo test --workspace --exclude conformance --exclude ropus-harness-control --exclude ropus-harness-deep-plc --lib --tests --no-fail-fast -- --test-threads=1`
  - `timeout 300 cargo run --release -p full-test -- --quick --release-preflight --skip-coverage`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Quick smoke result:
  - `PASS — ropus 0.12.6 @ main/79a4b96 — 2257 passed, 0 failed, 6 ignored`
- Residual risk:
  - Coverage-enabled quick-preflight runtime was accepted as out of scope for
    this slice; command shape is covered, but runtime remains unproven.
- Feeling:
  - This now feels commit-worthy. The completion review checked the exact
    behavioral constraints that mattered, and the smoke command gives the
    bounded feedback Arthur asked us to build.

## Commits And Next Plan Assessment
- Committed quick-preflight Stage 2 profile:
  - `85f76da Add quick preflight Stage 2 profile`
- Committed the original testing review/plan artifacts:
  - `4c04486 Add testing improvement plan docs`
- Left unrelated older release/refuzz journal uncommitted:
  - `/home/md/language/ropus/wrk_journals/2026.05.04 - JRN - release-and-refuzz.md`
- Harvey completed a plan-status assessment.
- Assessment saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - testing plan status and next slice.md`
- Assessment journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - testing plan status assessment.md`
- Status summary:
  - Done: 0.1, 0.2, 1.1, 2.1.
  - Partly done: 0.3, 1.2, 2.2, 3.1, 3.4, 4.1.
  - Not done: 3.2, 3.3, 4.2, 4.3.
- Recommended next slice:
  - Phase 1.2, narrowly: bounded fuzz sanity gate with manifest-discovered fuzz
    build and committed crash-corpus replay, not fuzz campaigns.
- Open design decisions before implementation:
  - Whether missing nightly/`cargo-fuzz` is FAIL under `--release-preflight`.
  - Whether all-target fuzz builds are excluded from `--quick --release-preflight`.

## HLD Review: Fuzz Sanity Gate
- Hilbert drafted:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - fuzz sanity gate.md`
- HLD journal:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz sanity gate hld.md`
- Accepted policy:
  - Default `full-test` does not run fuzz sanity and does not care about
    nightly or `cargo-fuzz`.
  - Non-quick `--release-preflight` runs all-target fuzz build plus committed
    crash-corpus replay and fails on missing tooling/build/replay problems.
  - `--quick --release-preflight` runs inventory-only checks and does not
    invoke cargo-fuzz.
  - Empty crash corpora pass with replay skipped.
  - Long fuzz campaigns remain out of scope.
- Supervisor sign-off:
  - Approved for implementation, with a constraint to keep the first version
    boring and bounded. Shell tooling should execute the cargo-fuzz mechanics;
    `full-test` should own policy/reporting.
- Feeling:
  - This is a bigger surface than the previous slice. I am wary of report-model
    churn, but the design has the right boundary: build/replay is operational
    shell work, and release meaning belongs in the runner.

## Implementation Attempt: Fuzz Sanity Gate
- Aristotle implemented the Phase 1.2 fuzz sanity gate and did not commit.
- Changed files reported:
  - `/home/md/language/ropus/tools/fuzz_run.sh`
  - `/home/md/language/ropus/full-test/src/fuzz.rs`
  - `/home/md/language/ropus/full-test/src/main.rs`
  - `/home/md/language/ropus/full-test/src/banner.rs`
  - `/home/md/language/ropus/full-test/src/report.rs`
  - `/home/md/language/ropus/full-test/src/html.rs`
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz sanity gate implementation.md`
- Implementation summary:
  - Added `tools/fuzz_run.sh --sanity`; kept `--check-crashes` compatible as
    an alias.
  - Sanity mode builds manifest-discovered targets and replays committed
    `.bin` crash files with `-runs=0`.
  - Empty crash corpora are skipped/pass.
  - Undeclared committed crash `.bin` files fail.
  - `full-test` now has fuzz policy/reporting:
    default `not_requested`, non-quick `--release-preflight` bounded sanity,
    quick release-preflight inventory-only.
- Validation reported by Aristotle:
  - `timeout 10 bash -n tools/fuzz_run.sh` passed.
  - `timeout 10 bash -n tools/fuzz_targets.sh` passed.
  - `timeout 10 bash tools/fuzz_run.sh --list` passed.
  - `timeout 120 cargo test -p full-test --locked` passed, 163 tests.
  - `timeout 300 cargo run --release -p full-test -- --quick --release-preflight --skip-coverage` passed, 2267 passed, 0 failed, 6 ignored.
  - `timeout 120 cargo fmt --check` passed.
- Blocker/residual risk:
  - `timeout 300 bash tools/fuzz_run.sh --sanity --target fuzz_decode`
    failed because all 4 committed `tests/fuzz/crashes/fuzz_decode/*.bin`
    still crash on replay.
- Supervisor response:
  - Treat the gate implementation as needing independent review, and the crash
    replay failures as a separate triage question. Do not bury real regressions
    just to get a green gate.
- Feeling:
  - This is useful discomfort. The gate did exactly what we wanted: it made a
    stale or unresolved crash corpus visible. Now we need to decide whether
    those crashes are expected debt, fixed-code regressions, or corpus files
    that should not be committed under the current policy.

## Code Review: Fuzz Sanity Gate
- Raman completed the independent code review.
- Review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - fuzz sanity gate.md`
- Review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz sanity gate review.md`
- Finding:
  - One issue: quick release-preflight Rust inventory can disagree with shell
    sanity semantics.
  - The Rust parser is looser than `tools/fuzz_targets.sh`, and Rust `.bin`
    crash counting is non-recursive while shell replay is recursive.
  - This can let `full-test --quick --release-preflight` pass or warn on an
    inventory shape that full sanity rejects.
- Reviewer assessment:
  - No blockers beyond that issue.
  - The known `fuzz_decode` replay failure is correct gate behavior.
- Reviewer validation:
  - `timeout 10s bash -n tools/fuzz_run.sh` passed.
  - `timeout 10s bash tools/fuzz_run.sh --list` passed.
  - `timeout 120s cargo test -p full-test fuzz` passed.
  - `timeout 120s cargo test -p full-test banner` passed.
- Supervisor response:
  - Delegate feedback application after crash triage returns, so the apply
    worker can account for both the consistency issue and the corpus status.

## Crash Replay Triage: Fuzz Decode
- Lovelace completed the crash replay triage.
- Diagnosis saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - DGN - fuzz decode crash replay.md`
- Diagnosis journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz decode crash replay diagnosis.md`
- Finding:
  - The four committed `tests/fuzz/crashes/fuzz_decode/*.bin` files do not
    currently crash `fuzz_decode` when replayed with the correct cargo-fuzz
    fixed-input argument order.
  - The new `--sanity` wrapper invokes replay as:
    `cargo +nightly fuzz run --fuzz-dir tests/fuzz fuzz_decode -- -runs=0 <crash_file>`
  - That treats the crash file as a libFuzzer argument after `--` and fails
    before the target runs with `Not a directory: <crash_file>; exiting`.
  - Correct replay is:
    `cargo +nightly fuzz run --fuzz-dir tests/fuzz fuzz_decode <crash_file> -- -runs=0`
- Classification:
  - Stale/resolved historical crash corpus plus a wrapper replay bug.
  - No evidence from this triage supports an unresolved decoder bug or oracle
    change for these four inputs.
- Supervisor response:
  - Do not quarantine or suppress the committed crash corpus.
  - Fix replay argument ordering and improve failure output so infra errors are
    not reported as target crashes.
  - Apply this together with Raman's inventory consistency feedback before the
    next review.

## Feedback Application: Fuzz Sanity Gate
- Leibniz applied the review and diagnosis feedback using the
  code-review-apply-feedback workflow.
- Fixes:
  - Tightened Rust quick-inventory target parsing to better match shell
    discovery semantics.
  - Made Rust `.bin` crash counting recursive.
  - Made `tools/fuzz_targets.sh` reject malformed `[[bin]]...` headers.
  - Fixed cargo-fuzz fixed-input replay ordering to put the crash file before
    `-- -runs=0`.
  - Added compact replay failure output: exit status and last 40 combined
    output lines.
  - Did not quarantine or suppress committed crash files.
- Feedback journal:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz sanity gate feedback.md`
- Validation reported by Leibniz:
  - `timeout 10 bash -n tools/fuzz_run.sh` passed.
  - `timeout 10 bash tools/fuzz_run.sh --list` passed, 12 targets listed.
  - `timeout 120 cargo test -p full-test --locked` passed, 167 tests.
  - `timeout 300 bash tools/fuzz_run.sh --sanity --target fuzz_decode` passed:
    `fuzz_decode build=pass crashes=4 replay=pass`.
  - `timeout 300 cargo run --release -p full-test -- --quick --release-preflight --skip-coverage` passed, 2271 passed, 0 failed, 6 ignored.
  - `timeout 120 cargo fmt --check` passed.
- Supervisor response:
  - Known issues are addressed. Next step is final completion review across
    implementation, feedback, docs, and validation before commit.

## Completion Review: Fuzz Sanity Gate
- Fermat performed the final completion review.
- Completion review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - fuzz sanity completion.md`
- Completion review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz sanity completion review.md`
- Go/no-go:
  - Go for commit within the requested verification scope.
- Validations passed:
  - `timeout 10 bash -n tools/fuzz_run.sh`
  - `timeout 10 bash -n tools/fuzz_targets.sh`
  - `timeout 10 bash tools/fuzz_run.sh --list`
  - `timeout 120 cargo test -p full-test --locked`
  - `timeout 300 bash tools/fuzz_run.sh --sanity --target fuzz_decode`
  - `timeout 300 cargo run --release -p full-test -- --quick --release-preflight --skip-coverage`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Confirmed:
  - `--sanity` is bounded and no-campaign.
  - `--check-crashes` remains compatible.
  - Default `full-test` does not require fuzz tooling.
  - Quick release-preflight is inventory-only.
  - `fuzz_decode` replay built and passed all four committed crash files.
  - Replay command ordering is fixed.
  - Prior quick/full inventory mismatch is covered by code and tests.
- Residual risk:
  - Full non-quick `full-test --release-preflight` was not run end-to-end in the
    final validation set; the path is covered by unit tests, code inspection,
    and direct shell sanity replay.
  - Rust quick inventory mirrors shell parsing rather than invoking the shell
    helper; future changes must keep these semantics aligned.
- Feeling:
  - Good checkpoint. The gate now finds real corpus issues without launching
    campaigns, and the one surprising failure turned into a concrete wrapper
    bug rather than a suppressed regression.

## Commit: Fuzz Sanity Gate
- Committed Phase 1.2 fuzz sanity gate as:
  - `b2aca87 Add fuzz sanity gate`
- Post-commit worktree check:
  - Only unrelated older release/refuzz journal remains untracked:
    `/home/md/language/ropus/wrk_journals/2026.05.04 - JRN - release-and-refuzz.md`
- Current recent testing commits:
  - `79a4b96 Add testing gate integrity checks`
  - `85f76da Add quick preflight Stage 2 profile`
  - `4c04486 Add testing improvement plan docs`
  - `b2aca87 Add fuzz sanity gate`
- Next supervisor action:
  - Reassess the plan after the fuzz sanity checkpoint and pick the next small,
    high-signal slice.

## Reassessment After Fuzz Sanity
- Nash reassessed the testing plan after `b2aca87`.
- Assessment saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - testing plan next slice after fuzz sanity.md`
- Assessment journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - next slice after fuzz sanity assessment.md`
- Status delta:
  - Phase 1.2 fuzz sanity is complete for the intended C1 scope.
  - The old Phase 0.3 fuzz-target enumeration gap is materially covered by
    full-test fuzz sanity.
  - Phase 2.2 remains the next plan-order gap.
- Recommended next slice:
  - Phase 2.2, narrowly: fuzz oracle class debt reporting around
    `/home/md/language/ropus/tests/fuzz/fuzz_targets/oracle.rs`,
    `/home/md/language/ropus/tests/fuzz/tests/oracle_tests.rs`, and
    `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`.
  - Defer legal frame-duration fuzzing until after oracle class debt is clearer.
  - Defer PLC/recovery drift because it is broader codec correctness work.
- Supervisor response:
  - Proceed to HLD for Phase 2.2 before any code.

## HLD Review: Fuzz Oracle Debt Reporting
- Gauss drafted:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - fuzz oracle debt reporting.md`
- HLD journal:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz oracle debt hld.md`
- Accepted design:
  - Keep `/home/md/language/ropus/tests/fuzz/fuzz_targets/oracle.rs` as the
    source of truth for stable class names, debt status, asserted properties,
    and unasserted properties.
  - Add pure, unit-testable count/report helpers rather than libFuzzer campaign
    counters.
  - Keep fuzz targets quiet and fast: no per-input output, no always-on
    counters, and no distribution thresholds.
  - Update the oracle matrix to split the weakened rows:
    `sample-count-only`, `recovery-or-dtx-only`, and
    `error-agreement-only`.
  - Leave Phase 3.3 frame-duration fuzzing out of scope.
- Supervisor constraint:
  - Do not add an optional shell wrapper in this slice unless it is genuinely
    trivial. The core value is metadata, tests, and matrix alignment.
- Feeling:
  - This is a good next move because it improves interpretability without
    adding more fuzz runtime or pretending weak oracles are strong.

## Implementation: Fuzz Oracle Debt Reporting
- Darwin implemented Phase 2.2 without committing.
- Changed files reported:
  - `/home/md/language/ropus/tests/fuzz/fuzz_targets/oracle.rs`
  - `/home/md/language/ropus/tests/fuzz/tests/oracle_tests.rs`
  - `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz oracle debt implementation.md`
- Implementation summary:
  - Added stable class metadata, debt properties, fixed-order count rows,
    totals, report formatting, and pure decode/multistream summary helpers.
  - Added metadata/count/report tests and classification boundary tests.
  - Added a bounded ignored operator report.
  - Split the matrix rows into strong CELT, SNR-gated SILK/Hybrid, and the
    three weakened debt classes.
- Validation reported by Darwin:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests` passed, 20 passed and 1 ignored.
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests -- --ignored --nocapture decode_oracle_class_distribution_report` passed, report total 41, strong 33, weakened 8.
  - `timeout 120 cargo fmt --check` passed.
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check` passed.
- Residual risks:
  - The ignored report summarizes committed fuzz decode seeds/crashes plus the
    standalone recovery fixture; it is not a live campaign metric.
  - Multistream arbitrary inputs were intentionally not parsed for the report.
- Supervisor response:
  - Proceed to independent review focused on metadata/source-of-truth accuracy,
    deterministic reporting, and matrix alignment.

## Code Review: Fuzz Oracle Debt Reporting
- Hegel completed the independent code review.
- Review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - fuzz oracle debt reporting.md`
- Review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz oracle debt review.md`
- Finding:
  - No blockers.
  - Minor: `format_report` emits comma-separated header/rows, but prose fields
    contain unescaped commas. It is deterministic and human-readable, but
    should not look like stable CSV unless escaped.
- Validation:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests` passed.
  - `timeout 120 cargo fmt --check` passed.
  - Ignored operator report passed with total 41, strong 33, weakened 8.
- Supervisor response:
  - Apply the minor feedback by making the report format explicitly
    human-readable instead of pseudo-CSV.

## Feedback Application: Fuzz Oracle Debt Reporting
- Kant applied the review feedback using the code-review-apply-feedback
  workflow.
- Fix:
  - Changed `format_report` from comma-separated pseudo-CSV to deterministic
    labeled-line output.
  - Updated report expectation tests.
- Feedback journal:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz oracle debt feedback.md`
- Validation reported:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests` passed, 20 passed and 1 ignored.
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests -- --ignored --nocapture decode_oracle_class_distribution_report` passed, report total 41, strong 33, weakened 8.
  - `timeout 120 cargo fmt --check` passed.
- Supervisor response:
  - Proceed to final completion review before commit.

## Completion Review: Fuzz Oracle Debt Reporting
- Archimedes performed the final completion review.
- Completion review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - fuzz oracle debt completion.md`
- Completion review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - fuzz oracle debt completion review.md`
- Go/no-go:
  - Go.
- Validations passed:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests -- --ignored --nocapture decode_oracle_class_distribution_report`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Report output:
  - `total: 41`, `strong: 33`, `weakened: 8`
- Residual risks:
  - The report is fixture/corpus reporting, not live fuzz campaign coverage.
  - Matrix/source wording could drift later without generated-doc checks.
  - `summarize_multistream_decode_packets` is thin and not directly tested in
    this slice, but not a Phase 2.2 blocker.
- Feeling:
  - This closes the interpretability loop for fuzz decode debt without adding
    hot-path noise. It is deliberately modest, which is the right shape here.

## Commit: Fuzz Oracle Debt Reporting
- Committed Phase 2.2 fuzz oracle debt reporting as:
  - `dd203bf Add fuzz oracle debt reporting`
- Post-commit worktree check:
  - Only unrelated older release/refuzz journal remains untracked:
    `/home/md/language/ropus/wrk_journals/2026.05.04 - JRN - release-and-refuzz.md`
- Current testing-plan checkpoint:
  - Gate integrity: committed.
  - Quick preflight Stage 2 profile: committed.
  - Fuzz sanity gate: committed.
  - Fuzz oracle debt reporting: committed.
- Recommended next implementation slice:
  - Phase 3.3 legal frame-duration fuzz dimensions. This is the next concrete
    fuzz coverage improvement now that oracle-strength reporting is clearer.
- Feeling:
  - We made real progress without jumping straight into fragile codec
    correctness work. The next slice will increase fuzz input breadth, and the
    oracle taxonomy now gives us better language for interpreting what those
    campaigns actually prove.

## HLD Review: Legal Frame-Duration Fuzz Dimensions
- Lorentz drafted:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - legal frame duration fuzz dimensions.md`
- HLD journal:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration fuzz hld.md`
- Accepted design direction:
  - Add a shared pure frame-duration helper for legal 2.5/5/10/20/40/60 ms
    sample-count mapping.
  - Vary actual encode/decode `frame_size`, not expert-frame-duration CTLs.
  - Keep Phase 2.2 oracle taxonomy intact.
  - Unit-test mapping and selector policy without running campaigns.
- Supervisor constraint:
  - Implement Slice A only first:
    - new shared frame-duration helper,
    - helper tests,
    - `fuzz_roundtrip` integration.
  - Defer `fuzz_encode_multiframe` and `fuzz_decode_plc_seq` to a separate
    slice after Slice A is reviewed and committed.
- Feeling:
  - The split is technically justified. `fuzz_roundtrip` is enough to prove the
    mapping and minimum-length behavior without mixing in stateful sequence
    failures.

## Implementation: Legal Frame-Duration Slice A
- Linnaeus implemented Slice A without committing.
- Changed files reported:
  - `/home/md/language/ropus/tests/fuzz/fuzz_targets/frame_duration.rs`
  - `/home/md/language/ropus/tests/fuzz/tests/frame_duration_tests.rs`
  - `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_roundtrip.rs`
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration roundtrip implementation.md`
- Implementation summary:
  - Added shared pure helper for legal 2.5/5/10/20/40/60 ms frame-duration
    mapping.
  - Added helper tests for labels, sample-rate frame sizes, selector wrapping,
    and `data[7] >> 5` policy.
  - Updated `fuzz_roundtrip` to use the selected legal frame size.
  - Lowered the stale minimum guard to `8 + 40`.
  - Kept the post-parse bytes-needed guard.
  - Did not touch `fuzz_encode_multiframe.rs` or `fuzz_decode_plc_seq.rs`.
- Validation reported:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests` passed.
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests` passed.
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_roundtrip` passed.
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check` passed.
  - `timeout 120 cargo fmt --check` passed.
- Residual risk:
  - Existing `fuzz_roundtrip` corpus inputs now reinterpret high bits of
    `data[7]` as duration selectors, intentionally per HLD.
- Supervisor response:
  - Proceed to independent review before commit.

## Code Review: Legal Frame-Duration Slice A
- Carson completed the independent review.
- Review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - legal frame duration roundtrip.md`
- Review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration roundtrip review.md`
- Findings:
  - No required changes.
  - Mapping helper matches the legal duration/sample-rate matrix.
  - Tests cover matrix, wrapping, and `data[7]` high-bit selection.
  - `fuzz_roundtrip` uses selected `frame_size` for encode/decode, packet
    duration checks, sample assertions, and existing oracles.
  - No diffs in `fuzz_encode_multiframe.rs` or `fuzz_decode_plc_seq.rs`.
- Validation passed:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_roundtrip`
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
- Residual risks:
  - No fuzz campaign was run.
  - Selector reuse means old corpus inputs can exercise different durations.
  - Selectors 0/1 are slightly overrepresented due to modulo wrapping, as
    accepted by the HLD.
- Supervisor response:
  - No feedback-application pass needed because there were no requested changes.
    Proceed to final completion verification before commit.

## Completion Review: Legal Frame-Duration Slice A
- Bohr performed the final completion review.
- Completion review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - legal frame duration roundtrip completion.md`
- Completion review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration roundtrip completion review.md`
- Go/no-go:
  - Go.
- Validation passed:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_roundtrip`
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Confirmed:
  - Helper covers all six legal durations at supported sample rates.
  - `fuzz_roundtrip` uses selected `frame_size` through encode/decode/assertions.
  - Min guard and dynamic bytes-needed guard are correct.
  - Oracle behavior is not weakened.
  - No campaign behavior was added.
  - `fuzz_encode_multiframe`, `fuzz_decode_plc_seq`, and `oracle.rs` remain
    untouched.
- Residual risks:
  - No fuzz campaign was run.
  - Selectors 6 and 7 wrap to 2.5 ms and 5 ms as designed.
  - Old corpus entries may now select different durations because `data[7]`
    high bits are intentionally reused.
- Feeling:
  - Slice A is a good incremental step: broader legal input coverage, small
    blast radius, and no change to oracle strength.

## Commit: Legal Frame-Duration Slice A
- Committed Slice A as:
  - `627974d Add legal frame durations to roundtrip fuzz`
- Post-commit worktree check:
  - Only unrelated older release/refuzz journal remains untracked:
    `/home/md/language/ropus/wrk_journals/2026.05.04 - JRN - release-and-refuzz.md`
- Next supervisor action:
  - Continue Phase 3.3 with the remaining stateful targets:
    `fuzz_encode_multiframe` and `fuzz_decode_plc_seq`.
  - Require a slice-specific HLD before implementation so the shared helper
    integration and PLC decode-capacity change are reviewed explicitly.

## HLD Review: Legal Frame-Duration Slice B
- Curie drafted:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - legal frame duration stateful fuzz.md`
- HLD journal:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration stateful hld.md`
- Accepted design:
  - Keep both remaining stateful targets in one implementation slice.
  - Use the existing Slice A helper only.
  - Use one selected legal duration per generated sequence.
  - For `fuzz_encode_multiframe`, derive duration and frame count from one
    shape byte.
  - For `fuzz_decode_plc_seq`, derive duration from high `drop_mask` bits.
  - Replace PLC requested decode frame size `5760` with the selected
    `frame_size`, while keeping output buffers sized at `5760 * channels`.
  - Do not alter oracle taxonomy/reporting or run fuzz campaigns.
- Sign-off:
  - Approved for implementation. The PLC requested-duration change is the main
    semantic point to protect in review.

## Implementation: Legal Frame-Duration Slice B
- Kepler implemented Slice B without committing.
- Changed files reported:
  - `/home/md/language/ropus/tests/fuzz/fuzz_targets/frame_duration.rs`
  - `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_encode_multiframe.rs`
  - `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_decode_plc_seq.rs`
  - `/home/md/language/ropus/tests/fuzz/tests/frame_duration_tests.rs`
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration stateful implementation.md`
- Implementation summary:
  - Added tiny selector helpers for multiframe shape bytes and PLC `drop_mask`
    high bits.
  - `fuzz_encode_multiframe` now selects one legal duration per sequence and
    uses it for PCM sizing, Rust encode, C encode, and decode sample-count
    checks.
  - `fuzz_decode_plc_seq` now selects one legal duration from `drop_mask`, uses
    it for generated PCM and encode, keeps decode buffers at
    `5760 * channels`, and replaces requested decode frame size `5760` with
    selected `frame_size` for Rust and C PLC decode.
  - Preserved oracle meaning; no edits to `oracle.rs`, `fuzz_roundtrip.rs`, or
    codec source.
- Validation reported:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests` passed.
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests` passed.
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe` passed.
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq` passed.
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check` passed.
  - `timeout 120 cargo fmt --check` passed.
  - `git diff --check` passed for touched files.
- Residual risks:
  - No fuzz campaigns were run.
  - Existing C reference `#pragma message` warning remains.
- Supervisor response:
  - Proceed to independent review, with special attention to PLC requested
    duration versus buffer capacity.

## Code Review: Legal Frame-Duration Slice B
- Hubble completed the independent review.
- Review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - legal frame duration stateful fuzz.md`
- Review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration stateful review.md`
- Findings:
  - No blocker or required source-code changes.
  - Residual risk: C PLC helper allocates `frame_size * channels` internally,
    while Rust target output buffer remains `5760 * channels`; selected
    `frame_size` is correctly passed as decode request.
  - Minor stale prose: `fuzz_decode_plc_seq.rs` comment still says `3..=12`
    while implementation/HLD use `2..=12`.
- Validation passed:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
- Supervisor response:
  - Apply the stale prose fix before final completion verification.

## Feedback Application: Legal Frame-Duration Slice B
- Popper applied the review feedback using the code-review-apply-feedback
  workflow.
- Fix:
  - Updated stale `fuzz_decode_plc_seq.rs` comment from `3..=12` to `2..=12`.
- Feedback journal:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration stateful feedback.md`
- Validation passed:
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
- Supervisor response:
  - Proceed to final completion verification.

## Completion Review: Legal Frame-Duration Slice B
- Huygens performed the final completion review.
- Completion review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - legal frame duration stateful completion.md`
- Completion review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration stateful completion review.md`
- Go/no-go:
  - Go.
- Validation passed:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Confirmed:
  - Source diff is limited to intended Slice B helper/target/test files.
  - Selector helpers are correct and tested.
  - Both stateful targets use one selected legal duration per sequence.
  - `fuzz_roundtrip`, oracle taxonomy/reporting, and campaign behavior are
    untouched.
- Residual risk:
  - Rust PLC decode buffer remains `5760 * channels`, while the C helper
    allocates `frame_size * channels`; this is legal because `frame_size` is the
    requested max output passed to C decode, but the allocation shapes are not
    literally symmetric.
- Feeling:
  - Phase 3.3 now has a coherent story: all three intended fuzz targets can
    reach legal frame durations, and the PLC target requests the actual stream
    duration instead of an accidental 120 ms PLC duration.

## Commit: Legal Frame-Duration Slice B
- Committed Slice B as:
  - `49b360f Add legal frame durations to stateful fuzz`
- Phase 3.3 status:
  - `fuzz_roundtrip` covers legal frame durations via Slice A.
  - `fuzz_encode_multiframe` and `fuzz_decode_plc_seq` cover legal frame
    durations via Slice B.
  - No fuzz campaigns were run; validations stayed at unit/build/fmt level.
- Worktree note:
  - Only process journals remain untracked. The older 2026.05.04 release/refuzz
    journal is unrelated to this testing-plan thread.
- Next supervisor action:
  - Reassess the remaining plan items and pick the next high-signal slice.

## Reassessment After Frame-Duration Fuzz
- Feynman reassessed the plan after Phase 3.3 completion.
- Assessment saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - testing plan next slice after frame durations.md`
- Assessment journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - next slice after frame durations assessment.md`
- Status delta:
  - Phase 3.3 is complete for the planned fuzz targets.
  - Phase 2 oracle reporting is complete enough to move on.
  - Remaining meaningful gaps include DRED/DNN parity, PLC/recovery drift,
    real-world corpus gate, and performance release thresholds.
- Recommended next slice:
  - Phase 3.2 PLC/recovery drift, scoped narrowly as an active tiered oracle
    for the existing decode24 PLC case.
  - Keep the exact first-288-sample guard.
  - Add a bounded full-frame drift test/report.
  - Update the oracle matrix.
  - Do not start by changing SILK math.
- Supervisor response:
  - Proceed to HLD for PLC/recovery drift before implementation.

## HLD Review: PLC Recovery Drift Oracle
- Zeno drafted:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - plc recovery drift oracle.md`
- HLD journal:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - plc recovery drift hld.md`
- Accepted design:
  - Keep the existing exact first-288-sample PLC regression.
  - Keep the full-frame decode24 exact parity test ignored as explicit debt.
  - Add a separate active bounded-drift oracle for the deterministic full PLC
    frame.
  - Use initial thresholds from current comments only after measuring the
    actual fixture in implementation: exact prefix at least 288 samples, max
    residual drift at most 17 16-bit LSB-equivalent units, drift count after
    prefix at most 615.
  - Update the oracle matrix to describe the active oracle as exact prefix plus
    bounded residual drift, not full parity.
- Supervisor constraint:
  - Do not change codec implementation or SILK/PLC math in this slice.
  - If measurement disagrees with the HLD thresholds, stop and report rather
    than widening bounds casually.
- Feeling:
  - This is the first slice in a while that touches a fragile correctness area.
    The right move is to make the drift visible and bounded, not pretend this
    is a parity fix.

## Implementation: PLC Recovery Drift Oracle
- Hypatia implemented the bounded PLC recovery oracle without committing.
- Changed files reported:
  - `/home/md/language/ropus/harness/tests/c_ref_differential.rs`
  - `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - plc recovery drift implementation.md`
- Implementation summary:
  - Added local decode24 PLC fixture constants.
  - Added drift metrics helper and helper tests.
  - Refreshed the ignored exact full-frame decode24 PLC debt test comment.
  - Added active `test_24bit_decode_plc_has_bounded_recovery_drift`.
  - Added `exact-prefix-plus-bounded-drift` to the oracle matrix.
- Measured fixture metrics before thresholds:
  - `rust_n=960`, `c_n=960`, `n_samples=960`
  - `exact_prefix_len=289`
  - `first_diff=Some(289)`
  - `max_abs_diff_raw_24=4352`
  - `max_abs_diff_16_lsb_equiv=17`
  - `drift_count_after_prefix=615`
- Validation reported:
  - `timeout 120 cargo test -p ropus-harness test_silk_plc_first_subframes_match_c_reference` passed.
  - `timeout 120 cargo test -p ropus-harness test_24bit_decode_plc_has_bounded_recovery_drift -- --nocapture` passed.
  - `timeout 120 cargo test -p ropus-harness decode24` passed, but only matched new helper tests.
  - `timeout 120 cargo test -p ropus-harness 24bit` passed with 5 active tests and the full-frame exact PLC test still ignored.
  - `timeout 120 cargo fmt --check` passed.
- Residual risk:
  - Bounds sit exactly at current measured drift (`17`, `615`) by design.
  - Full-frame exact decode24 PLC parity remains ignored debt.
- Supervisor response:
  - Proceed to independent review. The main review question is whether tight
    current-behavior bounds are acceptable and failure output is actionable.

## Code Review: PLC Recovery Drift Oracle
- Beauvoir completed the independent review.
- Review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - plc recovery drift oracle.md`
- Review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - plc recovery drift review.md`
- Findings:
  - No blocker or required-change findings.
- Residual risks noted:
  - Single deterministic PLC fixture only; no broader PLC/FEC coverage.
  - Drift conversion assumes current fixed-point `!ENABLE_RES24` decode24
    behavior.
  - Bounds are exactly at current measured edge, so intentional fixture
    movement needs deliberate threshold updates.
- Validation passed:
  - `timeout 120 cargo test -p ropus-harness test_silk_plc_first_subframes_match_c_reference`
  - `timeout 120 cargo test -p ropus-harness test_24bit_decode_plc_has_bounded_recovery_drift -- --nocapture`
  - `timeout 120 cargo test -p ropus-harness 24bit`
  - `timeout 120 cargo fmt --check`
- Supervisor response:
  - No feedback-application pass needed because there are no requested changes.
    Proceed to final completion verification.

## Completion Review: PLC Recovery Drift Oracle
- Hooke performed the final completion review.
- Completion review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - plc recovery drift completion.md`
- Completion review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - plc recovery drift completion review.md`
- Go/no-go:
  - Go.
- Confirmed:
  - No codec/SILK/PLC/decode24 implementation paths changed.
  - Exact first-288 sample test remains active and passed.
  - Full-frame exact decode24 PLC parity test remains ignored as explicit debt.
  - Active bounded drift test exists and emits actionable metrics.
  - Thresholds match current measured fixture exactly:
    `max_abs_diff_16_lsb_equiv=17`, `drift_count_after_prefix=615`.
  - Oracle matrix wording does not imply full parity.
- Validation passed:
  - `timeout 120 cargo test -p ropus-harness test_silk_plc_first_subframes_match_c_reference`
  - `timeout 120 cargo test -p ropus-harness test_24bit_decode_plc_has_bounded_recovery_drift -- --nocapture`
  - `timeout 120 cargo test -p ropus-harness 24bit`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Residual risks:
  - This covers one deterministic PLC fixture, not broad PLC/FEC parity.
  - Full-frame exact decode24 PLC parity remains debt.
  - Native residual 24-bit behavior would require revisiting threshold
    semantics.
- Feeling:
  - This is honest progress: the previously hidden drift window is now active
    and bounded without pretending we fixed full PLC parity.

## Commit Checkpoint: PLC Recovery Drift Oracle
- Committed completed slice:
  - `36774f7 Add bounded PLC recovery drift oracle`
- Scope included:
  - Active bounded PLC recovery drift oracle.
  - Completion/review/HLD/planning documents for the slice.
  - Oracle matrix update.
  - Journals from the slice.
- Current worktree note:
  - One older untracked journal remains present:
    `/home/md/language/ropus/wrk_journals/2026.05.04 - JRN - release-and-refuzz.md`
  - I am leaving it untouched because it appears unrelated to the current slice.
- Feeling:
  - Good checkpoint. I want to keep the next slice similarly narrow so the
    testing system gets stronger without mixing policy, harness, and codec
    changes into a single difficult-to-review batch.

## Delegation: Remaining Gap Assessment and DRED/DNN HLD
- Spawned Rawls to reassess remaining testing gaps after the PLC slice and
  recommend the next slice.
- Spawned Wegener to draft an HLD for a possible Phase 3.1 DRED/DNN
  parity/preflight gate.
- Constraints given:
  - Documentation only.
  - No implementation before supervisor design signoff.
  - No commits.
  - Use software-testing principles and save journals.
- Supervisor stance:
  - I am deliberately keeping my own work to coordination and checkpointing
    while the agents gather the detailed evidence.
- Feeling:
  - The main risk now is spreading into too many unfinished lines of work. I am
    keeping this as one candidate slice plus one independent prioritization
    check.

## Supervisor Review: DRED/DNN HLD First Pass
- Rawls recommended DRED/DNN as the next slice, scoped as coverage-contract
  and asset-backed oracle policy rather than broad DRED parity work.
- Wegener drafted the first HLD.
- I inspected the relevant `full-test` preflight/report/Stage 2 shape enough
  to review feasibility.
- Decision:
  - Direction is good.
  - Not signed off yet.
- Required HLD revisions sent to Wegener:
  - Fix release-preflight terminology so the UI/report does not keep saying
    only `core profile` after non-quick release-preflight starts claiming
    neural/DRED coverage.
  - Require `preflight::capture` to receive full `Options` or a derived policy
    enum, not just `release_preflight: bool`.
  - Require tempdir-simulated asset unit tests.
  - Keep implementation scope to `full-test` policy/report/docs.
  - Reconfirm no codec changes, no unignored exact DRED payload parity, and no
    DRED PCM reconstruction in this slice.
- Feeling:
  - This is the right kind of pushback. The design should prevent false claims
    in the reporting surface, not just flip a few blockers in preflight.

## HLD Signoff: DRED/DNN Coverage Contract
- Wegener revised the HLD and journal after supervisor feedback.
- I reviewed the revised HLD and signed it off.
- Signoff saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - dred dnn parity hld signoff.md`
- Implementation is approved only for:
  - `full-test` policy/report/help/tests.
  - `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`.
- Implementation is not approved for:
  - codec changes,
  - `harness-deep-plc` test changes,
  - unignoring exact DRED payload-byte parity,
  - DRED PCM reconstruction.
- Feeling:
  - Comfortable signing this off now. The key improvement is that the report
    claim itself becomes testable, which is exactly the kind of testing
    infrastructure hardening this plan needs.

## Delegation: DRED/DNN Implementation
- Committed planning/signoff checkpoint:
  - `6ac2819 Document DRED DNN parity gate design`
- Spawned Ohm to implement the signed-off DRED/DNN coverage contract.
- Allowed write scope:
  - `full-test` preflight/setup/cli/report/html/banner/main as needed.
  - `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md`.
  - Implementation journal.
- Forbidden scope repeated:
  - codec changes,
  - `harness-deep-plc` DRED test changes,
  - unignoring exact DRED payload parity,
  - DRED PCM reconstruction.
- Required validations:
  - focused `full-test` policy/report tests and `cargo fmt --check`.
- Supervisor note:
  - I will wait for implementation, then delegate independent code review
    using the code-reviewer workflow before any commit.

## Implementation Complete: DRED/DNN Coverage Contract
- Ohm completed the implementation with no commit.
- Reported changed scope:
  - `full-test/src/preflight.rs`
  - `full-test/src/setup.rs`
  - `full-test/src/main.rs`
  - `full-test/src/cli.rs`
  - `full-test/src/report.rs`
  - `full-test/src/html.rs`
  - `wrk_docs/testing-oracle-matrix.md`
  - implementation journal.
- Reported validations passed:
  - `timeout 120 cargo test -p full-test preflight`
  - `timeout 120 cargo test -p full-test cli::tests`
  - `timeout 120 cargo test -p full-test banner`
  - `timeout 120 cargo test -p full-test report`
  - `timeout 120 cargo test -p full-test html`
  - `timeout 120 cargo fmt --check`
  - `git diff --check` on assigned files.
- Delegated independent review to Helmholtz using the code-reviewer workflow.
- Feeling:
  - This is exactly where I want a separate reviewer. The behavior is mostly
    policy and reporting, so a missed wording or profile-drift issue would be
    easy to overlook if the implementer self-reviewed.

## Review Complete: DRED/DNN Coverage Contract
- Helmholtz completed the independent code review.
- Review saved to:
  - `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - dred dnn parity implementation.md`
- Review journal saved to:
  - `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - dred dnn parity review.md`
- Findings:
  - No blockers.
  - No issues.
  - No forbidden codec or `harness-deep-plc` changes detected.
  - Feedback-application pass not needed.
- Reviewer confirmed:
  - Non-quick release-preflight makes DNN/DRED assets required and
    banner-blocking.
  - Quick/default remain report-only and avoid neural/DRED coverage claims.
  - JSON/HTML/help expose the active claim without stale core-only wording.
  - Stage 2 profile selection and preflight policy are aligned from the same
    source.
  - Asset-policy tests use tempdirs.
- Validation passed in review:
  - `timeout 120 cargo test -p full-test preflight`
  - `timeout 120 cargo test -p full-test cli::tests`
  - `timeout 120 cargo test -p full-test banner`
  - `timeout 120 cargo test -p full-test report`
  - `timeout 120 cargo test -p full-test html`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Feeling:
  - This slice is ready to commit. It closes a false-green class without
    pretending the harder DRED parity debt is solved.
