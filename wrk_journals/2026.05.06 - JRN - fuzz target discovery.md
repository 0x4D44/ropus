# Fuzz Target Discovery Journal

## 2026-05-06

Task: Workstream B design-only pass for fuzz runner target discovery.

Inspected the workspace instructions and confirmed the request is explicitly design only. I read `/home/md/language/ropus/tests/fuzz/Cargo.toml`, `/home/md/language/ropus/tools/fuzz_cycle.sh`, and `/home/md/language/ropus/tools/fuzz_run.sh` with line numbers. The manifest has twelve `[[bin]]` targets, while both shell scripts hard-code nine and include stale names. That mismatch is exactly the kind of duplicated truth that quietly rots.

Decision: propose a small sourced Bash helper using `awk` to read `[[bin]] name = "..."` entries from the manifest. I considered cargo metadata or Python TOML parsing, but that feels heavier than the current repo needs for a simple manifest shape. Emotional temperature: calm, mildly irritated by the stale arrays because they are avoidable, but this is a contained fix once Arthur signs off.

No source code was edited. Created the requested HLD and this journal entry only.

## 2026-05-06 Update

Arthur reviewed the HLD and caught a real Bash footgun: `mapfile -t TARGETS < <(discover_fuzz_targets "$manifest")` would not reliably propagate a nonzero discovery status. Revised the design to use capture-first loading, add a shared `die` helper, define discovery before `fuzz_cycle.sh` log directory creation, and include a malformed-manifest validation path using a copied manifest under `mktemp -d`. Emotional temperature: slightly annoyed at myself for missing the process substitution status issue, but this is exactly why design review is useful.

Still design-only. No source code was edited.
