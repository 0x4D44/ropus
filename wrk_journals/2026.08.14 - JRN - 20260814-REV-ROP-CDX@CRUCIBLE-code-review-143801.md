# Journal — bounded `capi/` code-review pass

Reviewed the development-only C conformance adapter statically at baseline `ccd0830daf6e7814d99178abaca89714b775d748`. Separate correctness, security, performance, reliability, maintainability, test-coverage, and devil's-advocate lenses converged on five defects. I verified each against the local call path and, where parity mattered, the upstream Xiph source.

Filed `ROP-BUG-CRUCIBLE-00001` through `ROP-BUG-CRUCIBLE-00005` for incomplete fallible construction, projection size overflow/validation, ignored mapping-family validation, count-sized extension allocation, and pre-clamp frame scratch allocation. The null-packet extension inconsistency remained unverified because the upstream contract treats null plus positive length as invalid. Intentional conformance-only leaks, handle copying, and projection float ordering remained out of scope. No finding warranted a separate Draft requirement.

No source or build file changed. Per the review-pass contract, I did not build, run tests, launch the app, or run the exploratory/conformance harness. The next due area should be `ropus/`.
