# Fuzz Oracle Debt Feedback Journal

## 2026-05-06

Task: apply Phase 2.2 review feedback for fuzz oracle debt reporting.

Read the feedback and agree with the minor issue: the report should not be comma-separated while embedding prose with commas. I changed `format_report` to a deterministic labeled-line class list rather than adding CSV quoting. That keeps the output clearly human-facing and avoids implying a durable CSV schema.

Scope stayed intentionally narrow: formatting helper, its expectation test, and this journal. No fuzz runtime behavior changed.

Emotional temperature: calm. Mild frustration at CSV-shaped diagnostic text doing the usual thing where it looks more structured than it is, but this is exactly the kind of small ambiguity worth removing before it calcifies.

Validation note: first oracle test run failed because Rust's escaped multi-line string continuation stripped the indentation I expected in the generated report. The behavior was obvious from the assertion diff; I changed the format string to include explicit `\n  ` prefixes so the human-readable structure is stable.

Final validation:

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests` passed: 20 passed, 0 failed, 1 ignored.
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests -- --ignored --nocapture decode_oracle_class_distribution_report` passed: 1 passed, printed the labeled-line report with total 41, strong 33, weakened 8.
- `timeout 120 cargo fmt --check` passed.

Residual risk: this remains a human-readable diagnostic, not a formal machine schema. That is intentional for this feedback item.
