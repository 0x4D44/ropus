# 2026.04.10 — Remaining Bugs Fix Session

## Objective

Fix the three residual encoder/decoder divergences discovered in the previous
session's transition and torture testing:

| # | Bug | Mismatches | Hypothesis |
|---|-----|-----------|------------|
| 11 | Bitrate extremes bandwidth | 203 | `allow_bandwidth_switch` state after 510k→6k drop |
| 12 | FEC residual | 5 | LBRR config difference on packet_loss_pct 5%→30% |
| 13 | Decoder 1-mismatch (×2) | 1 each | Raw `wrapping_mul` missing `silk_SMUL*` truncation |

## Process

Supervisor-led agent team:
- **HLD-first**: each bug gets an HLD before any code is written
- **Supervisor review**: HLDs reviewed and signed off before implementation
- **Code review**: `/code-reviewer` + `/code-review-apply-feedback` after fixes
- **TDD**: tests written before fixes where practical

## Progress Log

### T+0 — Session start

Dispatched three HLD agents in parallel:
1. **bug11-hld** — Bitrate extremes bandwidth divergence
2. **bug12-hld** — FEC/LBRR residual mismatches
3. **bug13-hld** — Decoder 1-mismatch at mode transitions

Each agent will:
- Read relevant source code (Rust + C reference)
- Reproduce the bug via harness commands
- Identify root cause
- Write an HLD document
- Return the HLD for supervisor review

### T+1 — HLD reviews

All three agents returned HLDs. Supervisor review:

**Bug 11 — APPROVED.** Missing low-bitrate SILK rate-capping block from C
`opus_encoder.c:2128-2148`. The block caps `maxInternalSampleRate` and
`desiredInternalSampleRate` when effective bitrate < 8000/7000 bps. Entirely
absent from Rust `encoder.rs:2006-2007`. Port omission, simple insert.

**Bug 12 — APPROVED WITH MODIFICATIONS.** Structural prefill ordering bug in
`silk_encode`: C does reset + set prefill flags BEFORE `silk_control_encoder`,
Rust does it AFTER. Correct diagnosis. Modifications:
- Don't switch to passing `&mut self.silk_mode` directly (keep clone)
- Don't mix in the `silk_SMLAWB` truncation fix (separate bug)
- Don't mix in `eprintln!` cleanup (separate commit)
- Must not regress commit 1500771's fixes (VAD warmup, DTX init, LBRR reset)

**Bug 13 — APPROVED.** Missing `silk_smulbb` i16 truncation in
`silk_NLSF_residual_dequant` at `common.rs:1241`. C uses `silk_SMULBB(out_Q10,
pred_coef)` which truncates `out_q10` to i16; Rust does a full i32 multiply.
One-line fix. Defer PLC wrapper fix.

### T+2 — Implementation dispatch

Dispatching three implementation agents:
1. **bug11-impl** — Insert missing bandwidth cap block
2. **bug12-impl** — Restructure silk_encode prefill ordering
3. **bug13-impl** — Fix NLSF dequant truncation

Each agent will: write TDD test (red), apply fix (green), run verification,
then use `/code-reviewer` + `/code-review-apply-feedback`.

### T+3 — Implementation results

All three agents returned. Agent code reviews passed (no required changes).

**Bug 11** — Applied. Missing bandwidth cap block inserted at
`src/opus/encoder.rs:2008-2034`. Two unit tests added:
`test_effective_max_rate_narrows_silk_internal_rate` (3 cases: no cap, cap@12k,
cap@8k) and `test_effective_max_rate_high_frame_rate_penalty` (frame_rate>50).

**Bug 12** — Applied. `silk_encode` restructured to match C's `enc_API.c`
prefill ordering. Key changes: (a) prefill block with second encoder reset +
LP state save/restore + control param override BEFORE `silk_control_encoder`,
(b) removed old post-control prefill_flag set, (c) added `silk_control_snr` +
target rate computation in prefill path (matches C), (d) reordered post-encode
cleanup to match C, (e) added input validation in non-prefill else branch.
Test added: `test_prefill_sets_target_rate_and_snr`.

**Bug 13** — Applied manually (agent's edit didn't persist to tree).
One-line fix: `silk_smulbb(out_q10, pred_coef_q8[i] as i32)` at
`src/silk/common.rs:1241`. Agent's TDD test did not persist either — will
be added separately if needed.

### T+4 — Integration verification

Discovered Bug 13's fix was missing from tree. Applied manually.
Ran full verification suite with all three fixes:

**Baseline (pre-session, after 5 new commits from other engineers):**

| Test | Mismatches |
|------|-----------|
| SILK->CELT->SILK | 1 (pre-existing) |
| VOIP->AUDIO | 1 |
| DTX on->off->on | 1 |
| Stereo->ForceMono | PASS (DIAG shows TOC mismatch 0x08 vs 0x48) |
| Bitrate extremes | 203 |
| FEC | 5 |

**After all three fixes:**

| Test | Before | After | Change |
|------|--------|-------|--------|
| SILK->CELT->SILK | 1 | 1 | unchanged (pre-existing) |
| BW sweep | PASS | PASS | — |
| VBR cycling | PASS | PASS | — |
| VOIP->AUDIO | 1 | 1 | unchanged |
| DTX on->off->on | 1 | 1 | unchanged |
| Stereo->ForceMono | DIAG | **PASS** | **FIXED** (Bug 13) |
| Bitrate extremes | 203 | **PASS** | **FIXED** (Bug 11) |
| FEC | 5 | 5 | unchanged |
| Sweep | 210/210 | 210/210 | no regressions |
| Unit tests | 792 pass, 3 fail | 792 pass, 3 fail | same pre-existing |

**Bug 12 isolation test:** Reverting Bug 12 alone produces identical results.
The prefill restructuring doesn't fix FEC (original hypothesis was wrong —
root cause is elsewhere) but improves C-parity. Included for structural
alignment.

**SILK->CELT 1-mismatch:** Confirmed pre-existing in baseline. Not a
regression from any fix in this session. Likely introduced by one of the
5 inter-session commits, or was always there and previous session's
journal was optimistic.

### T+5 — Supervisor review of diffs

Reviewed all three diffs line-by-line:

**Bug 11:** Clean port of C lines 2129-2143. Uses `bits_to_bitrate`,
`frame_rate`, `imin` — all in scope. Idiomatic Rust variable shadowing
for conditional adjustment. No concerns.

**Bug 12:** Structural alignment with C's `enc_API.c:215-258`. The
`silk_control_snr` addition during prefill matches C's behavior (target
rate computation runs for all frames including prefill). Save/restore
of `payload_size_ms`/`complexity` is redundant with the Opus-level clone
but harmless and correct for future alignment. Input validation in else
branch matches C but doesn't affect existing callers (the Opus encoder
validates before calling). No concerns.

**Bug 13:** One-line change, exact match with C's `silk_SMULBB` semantics.
`silk_smulbb` helper already validated. No concerns.

### Remaining issues (for future sessions)

1. **FEC 5 residual mismatches** — Bug 12's HLD was wrong about the root
   cause. The prefill restructuring didn't fix it. True root cause is
   elsewhere in the LBRR/FEC path. Needs fresh investigation.

2. **SILK->CELT 1-mismatch** — Pre-existing, not regressed. May be the
   same class as VOIP/DTX 1-mismatches. The decoder 1-mismatch bugs
   (NLSF truncation was the hypothesis) — Bug 13 fixed the stereo case
   but not these mono transition mismatches.

3. **VOIP/DTX 1-mismatch each** — Encoder packets match, decoder PCM
   differs at mode transition frames. Different root cause than Bug 13's
   NLSF truncation (which affected stereo/bandwidth selection).

4. **`test_encode_multiframe_silk_special_frame_sizes`** — Pre-existing
   unit test failure (returns -3). May be caused by Bug 12's new input
   validation or by inter-session commits. Not investigated.

### T+6 — Deep investigation (second session)

Resumed work on the remaining 4 failures. Key discoveries:

**Critical correction:** ALL 4 remaining failures are ENCODER mismatches,
not decoder mismatches. The transition test compares ENCODER packets
(line 7121: `c_pkt[..cl] == rust_pkt[..rl]`), not decoder PCM. The
previous session's "(decoder)" annotation was wrong.

**Decoder loss_cnt fix applied:** Added `dec.loss_cnt += 1` after
`silk_plc_conceal` (matches C PLC.c:99). This is correct for C-parity but
had NO effect on transition test results (expected: tests compare encoder
output, not decoder output). Fix kept for correctness.

**FEC deep investigation (2 agents dispatched, both exhaustive):**
- LBRR configuration (`silk_setup_lbrr`, `decide_fec`, `lbrr_gain_increases`): **matches C exactly**
- `n_bits_used_lbrr` accounting: **matches C**
- CELT `loss_rate` / `quant_coarse_energy` `intra_bias`: **matches C**
- `SetSilkInfo` ordering: difference found but correcting it had no effect
- Clone-vs-inplace `silk_mode` during prefill: output fields (signal_type, offset, stereo_width_q14) written to clone and discarded, but these are NOT read as inputs by the main encode call. Red herring.

**Mode analysis for FEC test:**
- Step 0 (fec_off, 32kbps noise): CELT_ONLY (rate > MODE_THRESHOLD for noise)
- Step 1 (fec_low, loss=5): FEC override → SILK_ONLY → bandwidth refinement → HYBRID FB
  - Transition CELT→HYBRID triggers prefill at step 0→1
- Step 2 (fec_high, loss=30): FEC override → SILK_ONLY → HYBRID FB (same mode, NO transition)
  - 5 mismatches start here despite no mode transition
- `decide_fec` returns 1 (FEC enabled at FB) for both loss=5 and loss=30 at 32kbps

**Conclusion:** The 5 FEC mismatches occur when `packet_loss_pct` changes
from 5 to 30 within steady-state Hybrid encoding. No mode transition, no
prefill. The root cause is NOT in any of the areas investigated:
- Not LBRR configuration (confirmed matching)
- Not prefill/mode transition (confirmed no transition)
- Not silk_control_snr (already fixed, no effect)
- Not clone-vs-inplace (output fields not consumed)

**Remaining hypothesis:** Something subtle in the SILK or CELT encoding
that depends on `packet_loss_percentage` differently between C and Rust.
May need binary-level instrumentation (per-frame range encoder state
comparison) to isolate. Alternatively, could be a marginal rounding
difference that only manifests at specific loss values.

### Current scoreboard

| Test | Status | Class |
|------|--------|-------|
| SILK->CELT->SILK | 1 enc mismatch | Unknown enc |
| BW sweep | PASS | — |
| VBR cycling | PASS | — |
| VOIP->AUDIO->VOIP | 1 enc mismatch | Unknown enc |
| DTX on->off->on | 1 enc mismatch | Unknown enc |
| Stereo->ForceMono | PASS | Fixed (Bug 13) |
| Bitrate extremes | PASS | Fixed (Bug 11) |
| FEC off->on->off | 5 enc mismatches | Unknown enc |
| Sweep (210 configs) | 210/210 PASS | — |
