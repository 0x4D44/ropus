# PLC Recovery Drift Implementation Journal

Date: 2026-05-06

## 2026-05-06

Read `/home/md/language/ropus/AGENTS.md` and the signed HLD at `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - plc recovery drift oracle.md`. Supervisor scope is narrow: harness test, oracle matrix, and this journal only. I noticed unrelated dirty/untracked planning and journal files already in the worktree and left them alone.

Implemented a local decode24 PLC drift report helper in `/home/md/language/ropus/harness/tests/c_ref_differential.rs`, plus small pure helper tests for all-equal samples, first diff before the prefix, and first diff exactly at the 288-sample boundary. The active oracle is separate from the ignored full-frame exact parity test, so the exact debt remains visible.

Measurement run before locking thresholds:

```text
fixture sr=48000, ch=1, frame_size=960, seed=55
rust_n=960
c_n=960
n_samples=960
exact_prefix_len=289
first_diff=Some(289)
max_abs_diff_raw_24=4352
max_abs_diff_16_lsb_equiv=17
drift_count_after_prefix=615
first_mismatches_after_prefix=[(289, -852480, -852736, 256), (290, 677120, 676864, 256), (292, 355584, 355840, 256), (293, 1094656, 1094912, 256), (295, 95744, 95488, 256), (296, -510464, -510720, 256), (297, -1390336, -1391104, 768), (298, 2030848, 2030336, 512)]
```

The measured max drift and drift count exactly match the HLD's initial upper bounds, so I did not widen anything. Emotional temperature: mildly tense because the thresholds sit exactly on the measured ceiling, but that is also the honest contract Arthur asked for. I am comfortable with this because any regression will force a deliberate conversation instead of silently expanding the tolerated drift.

Updated `/home/md/language/ropus/wrk_docs/testing-oracle-matrix.md` with the `exact-prefix-plus-bounded-drift` class and the active decode24 PLC row. The row explicitly does not claim full-frame exact parity.

Validation:

```text
timeout 120 cargo test -p ropus-harness test_silk_plc_first_subframes_match_c_reference
result: passed; c_ref_differential ran 1 test.

timeout 120 cargo test -p ropus-harness test_24bit_decode_plc_has_bounded_recovery_drift -- --nocapture
result: passed; c_ref_differential ran 1 test and printed the measured drift report above.

timeout 120 cargo test -p ropus-harness decode24
result: passed; this literal filter matched the three pure decode24 drift helper tests.

timeout 120 cargo test -p ropus-harness 24bit
result: passed; adapted filter for existing 24-bit test names, with 5 passed and 1 ignored exact PLC debt test.

timeout 120 cargo fmt --check
result: passed.
```

Residual risk: the bounded oracle now sits exactly at the measured max drift and post-prefix drift count. That is deliberate, but it means legitimate fixture changes will need a measured threshold review rather than an automatic green run.
