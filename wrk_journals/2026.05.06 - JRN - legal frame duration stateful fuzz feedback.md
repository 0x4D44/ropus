# Legal Frame Duration Stateful Fuzz Feedback Journal

## 2026-05-06 19:33 BST

Started the feedback-application pass. I read the apply-feedback skill and review note, then confirmed the current PLC fuzz target already has the production `max_pcm = samples_per_frame` change from the prior agent. The gap is the reviewer's real point: without a focused regression test, that exact line could drift back to 120 ms capacity later.

Emotional temperature: steady. Mild caution because this is a multi-agent tree and the existing uncommitted PLC target change is not mine, so I am treating it as owned context rather than rewriting around it.

## 2026-05-06 19:36 BST

Added a tiny pure helper in `fuzz_decode_plc_seq.rs` for the selected PLC decode capacity and used it at the existing decode allocation site. Added a focused integration test asserting 48 kHz stereo at selected 60 ms uses `2880 * 2` samples and not the old `5760 * 2` 120 ms capacity. Updated the stale C helper comment to stop describing the old longest-plausible-frame policy.

Emotional temperature: good. The helper is a small amount of test seam, but it pays for itself because the regression test now exercises the same function the fuzz target uses.

## 2026-05-06 19:39 BST

The first focused test passed but produced a dead-code warning from importing the fuzz target as a test module. I tightened the import path by allowing dead code only on the nested `frame_duration` module and expanded the helper into request size plus capacity size, so the test now covers both dimensions the review called out.

Emotional temperature: still steady. This is the right level of ceremony: more than a pure source-text grep, less than trying to run a fuzz harness from a unit test.

## 2026-05-06 19:41 BST

The focused test passed again, but compiler output showed one unused local left in the arbitrary parser after routing frame-size selection through the helper. Removed that local and moved on to formatting.

Emotional temperature: mildly annoyed at the warning, but it was useful signal and the fix was clean.

## 2026-05-06 19:45 BST

Validation completed:

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`: passed, 8 tests.
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`: passed, 20 tests, 1 ignored.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`: passed.
- `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`: passed.
- `timeout 120 cargo fmt --check`: passed.
- `git diff --check`: passed.

Remaining risk: this is a build-and-regression-test pass, not a fuzz campaign. That matches Arthur's requested scope.

Emotional temperature: satisfied. The change is small, review-driven, and the old 120 ms number is no longer encoded in the selected PLC decode path or the helper comment.
