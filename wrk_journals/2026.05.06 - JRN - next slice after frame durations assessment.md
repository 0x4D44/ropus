# Journal: Next Slice After Frame Durations Assessment

Date: 2026.05.06

## Entry

Task: reassess the testing improvement plan after Phase 3.3 legal frame-duration fuzz completion and recommend one next implementation slice.

I inspected the original plan, recent commits, current status, prior next-slice docs, the PLC ignored test region, DRED payload parity test, corpus documentation, and benchmark stage plumbing. I avoided expensive validation and did not run fuzz campaigns or cargo test.

The main status change is that Phase 3.3 now looks complete for the named targets: roundtrip, multiframe encode, and PLC sequence fuzzing all have legal frame-duration dimensions backed by the shared helper/tests. The previous fuzz-oracle reporting slice also appears complete enough that it no longer needs to be the next recommendation.

My recommendation is Phase 3.2, but scoped as an oracle/reporting implementation rather than an immediate codec fix. The ignored full-frame decode24 PLC test is an uncomfortable kind of debt: it is deterministic, known, and close to being an active gate, but the current plan still lets "PLC tested" hide the post-288-sample residual drift. That is the right next small slice because it gives release reports a more honest signal without touching fragile SILK math first.

Emotional temperature: steady, mildly wary. DRED is tempting because it sounds higher-value, but the ignored test already points at upstream LPCNet feature drift. Calling that the next implementation slice would be setting someone up for a broad neural parity dig when the plan asked for small, testable work. Corpus work also matters, but fixture provenance and pinning will sprawl unless it is handled as its own policy slice.

Blockers or open decisions:

- Need Arthur's policy call, or an HLD default, on acceptable bounded drift terminology for PLC recovery.
- Need the implementation agent to measure current deterministic max drift instead of copying the historical `~17` comment blindly.
- Need to avoid silently converting bounded-drift debt into a claim of exact PLC parity.

Assessment saved to `/home/md/language/ropus/wrk_docs/2026.05.06 - PLN - testing plan next slice after frame durations.md`.
