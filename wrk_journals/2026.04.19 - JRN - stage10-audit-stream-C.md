# 2026-04-19 — Stage 10 audit — Stream C (Stages 8 + 9)

**Auditor.** Claude (read-only).
**Scope.** Spot-check Stage 8.10 audit; audit Stage 9 (Platform SIMD doc-deferral).
**Commits in scope.** `fe8c97e` .. `cfb9e23` (Stage 8); `bf1f0ca` (Stage 9).

---

## Stage 8 cross-check

### Test counts

All three required suites match the compliance audit's row verdicts:

| Suite | Claim | Observed |
|---|---|---|
| `cargo test -p ropus-harness-deep-plc` (DRED subset) | 8.6 `#[ignore]`d; all other DRED tests green | 1 ignored (`dred_encode_silk_frame_bytes_match_c_reference`); 3/3 `dred_integrated_encode`; 2/2 `dred_rdovae_dec_diff`; 1/1 `dred_rdovae_enc_diff`; 1/1 `dred_decode_payload_diff` |
| `cargo test -p ropus --lib opus::dred::` | 8 passed | 8 passed, 0 failed, 0 ignored |
| `cargo test -p ropus --lib dnn::dred::` | 10 passed | 10 passed, 0 failed, 0 ignored |

**Collateral (not Stage 8).** `tier2_snr::dnn_plc_tier2_snr_above_50db` fails at 9.29 dB vs a 50 dB gate (first diverge at sample 261, 14 packets lost). Stage 7b DEEP_PLC territory, unrelated to Stage 8 DRED. Flagging because anyone running `cargo test -p ropus-harness-deep-plc` blind will hit it. Test source: `harness-deep-plc/tests/tier2_snr.rs:238`.

### 8.6 `#[ignore]` status

Still ignored at `harness-deep-plc/tests/dred_encode_payload_diff.rs`. Test output: `ignored, Stage 7 LPCNet drift: Ex[]/FFT-path bit-exactness pending. See JRN 2026.04.19 stage8-dred-port 8.6.` Un-ignore criterion named in both the `#[ignore]` string and the test-body layered diagnostics. Matches compliance audit row 8.6.

### Adversarial fixes in `cfb9e23`

Verified via `git show --stat cfb9e23`:

- **Stereo rejection.** `ropus/src/opus/encoder.rs:3656-3658` — `if duration > 0 && self.channels > 1 { return OPUS_BAD_ARG; }`. Docstring at L3647-3651 explicitly cites the release-build silent-garbage failure mode the adversarial audit raised. ✅
- **`dred_stats.rs` dead_code drop.** `ropus/src/dnn/dred_stats.rs:10` now carries only `#![allow(non_upper_case_globals)]`. Stage-8.3 TODO and `#![allow(dead_code)]` bridge are gone. ✅

Commit message of `cfb9e23` lists both under "Applied from the adversarial audit:".

### Known-gap TODO markers

`TODO(stage-8` in `ropus/src/` returns 6 markers. The 4 Stream-C-relevant gaps:

| Gap | Inline `TODO(stage-8`? | Source of documentation |
|---|---|---|
| CELT-budget steal | ✅ `opus/encoder.rs:3051` | inline + supervisor journal |
| DTX first-frame shift | ✅ `opus/encoder.rs:2350` | inline + supervisor journal |
| Hardcoded quantiser (`q0=9, d_q=3, qmax=15`) | ❌ | prose comment at `encoder.rs:3665-3670` + supervisor journal |
| CTL dispatch | ❌ | `stage8-supervisor.md` "Known gaps carried forward" + compliance-audit "Stage 7 coordination points" |

The task prompt's shorthand "all 4 documented as inline TODOs" is not literally true: 2 live in prose comments / journals rather than grep-able markers. The audit's own language ("documented inline + journal" for G3, "Stage 7 coordination" for CTL) is accurate. Not material to Stage 8 closure.

**Verdict: confirms Stage 8.10 audit**, with the one labelling nuance above.

---

## Stage 9 audit

### Plan

Stage 9 is defined in `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md` §7 ("Platform SIMD (P5)", L175). Execution plan in `wrk_journals/2026.04.19 - JRN - deferred-work-closeout-execution.md` Stage 9 row: **"Platform SIMD (#7) — doc-only, deferred"**. Intent: acknowledge that `wide`-crate portable SIMD is at C parity and that hand-tuned per-architecture backends (ARM NEON, x86 AVX) are optimization-above-parity, not a shipping gate. Deliverable: a README note, not a code port.

### Delivery

Commit `bf1f0ca` (0x4D44, 2026-04-19 02:02) — `docs(ropus): document platform-SIMD as an intentional deferral`. Diff: `ropus/README.md` +10/-0, adding a new "Performance & SIMD" section. Verified via `git show bf1f0ca`.

New section reads (L62-68 post-change):

> ropus uses portable SIMD through the `wide` crate and runs at approximate parity with the xiph/opus C reference. Platform-specific intrinsics (ARM NEON, x86 AVX) are intentionally deferred: at C-parity, a hand-tuned per-architecture backend is optimization-above-parity rather than a correctness gate, so it is not a shipping requirement and not a blocker to adoption. The portable path is a single implementation that compiles everywhere the Rust target does.

A "Deferred" bullet at README L57 also lists "Platform-specific SIMD (ARM NEON, x86 AVX) — portable `wide` SIMD only" (pre-existing).

### Discoverability

Three readable surfaces record the deferral:

1. `ropus/README.md` L55-68 — Deferred bullet + dedicated Performance & SIMD paragraph.
2. `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md` §7 — problem, rationale, priority (P5), scope.
3. `wrk_journals/2026.04.19 - JRN - deferred-work-closeout-execution.md` §"Stage 9 — DONE" — commit SHA + outcome.

`MEMORY.md` reinforces that perf is a separate workstream and callers shouldn't re-raise it. A future reader hitting the README first sees both a one-line bullet and a paragraph of reasoning.

### Gap

None. Planned as doc-only, delivered as doc-only, recorded on all three of the surfaces a future reader would naturally consult. The absence of code changes is by design.

---

## Overall Stream C verdict

**Stage 8 — confirms Stage 8.10 audit.** Three required test suites match the audit's description. 8.6 `#[ignore]` still in place with un-ignore criterion. Both adversarial fixes (stereo reject, `dead_code` drop) landed in `cfb9e23`. Known gaps are all documented, with two of four residing in prose/journal rather than grep-able `TODO(stage-8` markers — wording nuance, not a scope nit. Collateral `tier2_snr` failure flagged but out of scope.

**Stage 9 — green.** Planned as doc-only, shipped as doc-only (`bf1f0ca`), discoverable across README + HLD + execution journal. No code delta expected, no code delta delivered.

**Stream C go/no-go: go.** Nothing in this slice blocks a Stage 10 close-out.
