# 2026.05.04 - JRN - four-failure-closeout

## Context
Arthur asked to close the remaining validation/known-failure work identified after the post-cam2 cleanup:

1. Aggregate-only `cargo test --release -p ropus` analysis flake.
2. `ropus-harness` multistream encode/roundtrip parity failure.
3. IETF vector provisioning before treating conformance as a gate.
4. `nan-encode-float` and SILK recovery divergence known-failure work.

Arthur explicitly requested an agent team, HLDs up-front, lead review/sign-off before code, code-reviewer and code-review-apply-feedback practice, and regular journal updates.

## Working Rules
- Stay within `/home/md/language/ropus`.
- Do not commit; Arthur's instructions say not to run git add/commit/push.
- Design docs go under `wrk_docs/` with `YYYY.MM.DD - HLD - ...`.
- First agent round is HLD-only. No production code changes until designs are reviewed and signed off.
- Split issue 4 into two workstreams because NaN float ingest and SILK recovery divergence have different root causes and validation oracles.

## Initial State
`git status --short` shows two untracked files before this journal:

- `wrk_docs/2026.05.03 - BRIEF - post-cam2-cleanup.md`
- `wrk_journals/2026.05.04 - JRN - fuzz-vs-mutation-triage.md`

The current branch is `main` at `28748c8` (`origin/main`), a merge commit after `b85b192`.

## Agent Plan
Five HLD-only workstreams:

1. Analysis aggregate flake.
2. Multistream encode/roundtrip parity.
3. IETF vector provisioning gate.
4. NaN encode-float parity.
5. SILK recovery divergence.

Emotional temperature: focused, cautious. The main risk is letting agents start coding before the evidence and oracle are clear. I will require concrete repro commands and a narrow success criterion in each HLD before implementation.

## HLD Agents Spawned
Initial full-history spawn attempt failed because the platform disallows setting `agent_type` with a full-history fork. Retried with explicit worker roles and self-contained prompts.

Agents:

- Worker A / Laplace: analysis `new_boxed` aggregate flake HLD.
- Worker B / Heisenberg: multistream encode/roundtrip parity HLD.
- Worker C / Darwin: IETF vector provisioning gate HLD.
- Worker D / Hubble: NaN encode-float HLD.
- Worker E / Carson: SILK recovery divergence HLD.

All agents were instructed to read `AGENTS.md`, write only HLD docs, avoid production-code edits, and report changed paths.

Emotional temperature: organized. The split is a little wide, but the workstreams are independent enough that parallel HLDs should reduce wall-clock time without coupling code changes prematurely.

## HLD Review
All five HLDs returned.

Reviewed and signed off:

- `/home/md/language/ropus/wrk_docs/2026.05.04 - HLD - analysis-new-boxed-flake.md`
- `/home/md/language/ropus/wrk_docs/2026.05.04 - HLD - ietf-vector-provisioning-gate.md`
- `/home/md/language/ropus/wrk_docs/2026.05.04 - HLD - nan-encode-float-parity.md`
- `/home/md/language/ropus/wrk_docs/2026.05.04 - HLD - silk-recovery-divergence.md`

The multistream HLD is signed off conditionally:

- `/home/md/language/ropus/wrk_docs/2026.05.04 - HLD - multistream-parity-closeout.md`

Condition: before changing shared encoder plumbing, collect the proposed complexity-control evidence. If the same deterministic fixture passes with analysis disabled or `complexity=9` and fails with `complexity=10`, the analysis-plumbing fix is approved. If that evidence does not hold, stop and revise the HLD instead of stacking guesses.

Worktree note: concurrent fuzz work appeared after the HLD agents started (`tests/fuzz` targets, tests, and corpus/seed files). I am treating those as someone else's in-flight changes. Any implementation that needs to touch fuzz files must first read the current diff and work with it.

Emotional temperature: cautiously positive. The analysis flake and NaN bug look narrow. The IETF gate is validation plumbing, not codec risk. The SILK recovery path should be kept as an oracle-classification task unless Arthur explicitly wants malformed recovery PCM parity. Multistream is the only one with meaningful shared-code risk.

## Implementation Round
Assigned implementation to the same five agents with disjoint ownership.

Completed streams:

- Analysis aggregate flake: test-only semantic comparison in `/home/md/language/ropus/ropus/src/opus/analysis.rs`; narrow test and single-threaded release lib aggregate passed.
- Multistream parity: complexity-control evidence confirmed `complexity=9` byte-exact and `complexity=10` failing at frame 0 byte 13 before patch; implementation threads original multistream PCM/downmix selectors into encoder analysis; multistream CLI and release smoke passed.
- SILK recovery divergence: decode oracle now classifies attacker-controlled SILK/Hybrid recovery/DTX sub-frames separately and re-enables SNR only for coded-comparable packets; known standalone and multistream fixtures replayed cleanly.
- NaN encode-float: `float2int16` now follows C clamp order so NaN maps to `-32768`; differential encode/roundtrip masks removed for the fixed targets; known fixture replays cleanly.
- IETF vectors: `full-test` provisions vectors before Stage 2 and records unavailable vectors as a fail-level synthetic conformance result instead of a green skip; focused full-test suite and clippy passed.

Review notes before formal `/code-reviewer` pass:

- The multistream patch is compact but touches shared encoder internals; review should focus on whether all analysis callers pass C-equivalent `c1`, `c2`, and channel counts.
- The SILK oracle classifier must stay narrow: frame size `<= 1` on SILK/Hybrid, not broad malformed-packet downgrades.
- The NaN fix intentionally leaves safety-target non-finite masking in place; that is now a separate high-complexity analysis-input policy, not the fixed `FLOAT2INT16` parity issue.
- PowerShell script syntax could not be executed locally because PowerShell is not installed.

Emotional temperature: better than expected, but not relaxed. We now have several passing focused gates, yet the combined diff is broad enough that a disciplined review pass matters. I will run code-reviewer-style review before applying feedback or broader validation.

## Code Review Feedback Applied
Used the `code-reviewer` skill through two read-only reviewers.

Validation-tooling review findings:

- Blocker: vector provisioning could hang indefinitely. Agreed and fixed with a Rust-side fetch child timeout plus script-level download timeouts for curl/wget/PowerShell.
- Issue: `--skip ::testvector` did not match the actual flat `testvectorNN_mono/stereo` test names. Agreed and fixed to `--skip testvector`, with comments and unit tests updated.
- Process blocker: new `full-test/src/ietf_vectors.rs` is untracked. Agreed as a process risk, but Arthur instructed no `git add`; final handoff must call out that this new file needs to be included when committing.

Codec/fuzz review findings:

- Blocker: NaN parity was fixed for `FLOAT2INT16` below the analysis gate, but `FLOAT2SIG` in the tonality-analysis path still used Rust NaN clamp semantics. Agreed and fixed production `float_to_sig`, the analysis test helper, and the harness downmix helper to follow C `MAX32`/`MIN32` order. Added a complexity-10, 16 kHz NaN differential test.
- Issue: fuzz files were not rustfmt-clean. Agreed and ran `cargo fmt --manifest-path tests/fuzz/Cargo.toml`.

Focused checks after feedback:

- `timeout 120s cargo fmt -p ropus -p ropus-harness -p full-test` passed.
- `timeout 120s cargo fmt --manifest-path tests/fuzz/Cargo.toml` passed.
- `timeout 10s bash -n tools/fetch_ietf_vectors.sh` passed.
- `timeout 180s cargo test -p full-test ietf_vectors -- --test-threads=1` passed.
- `timeout 300s cargo test -p ropus-harness --test c_ref_differential test_float_pcm_nan_encode_matches_c_reference -- --nocapture` passed, including the new analysis-path case.
- `timeout 120s cargo test -p ropus test_division_and_conversion_helpers -- --nocapture` passed.
- `timeout 180s cargo check --manifest-path tests/fuzz/Cargo.toml --bin fuzz_decode --bin fuzz_encode --bin fuzz_roundtrip --bin fuzz_multistream` passed with only the existing C reference debug-build pragma note.

Emotional temperature: grateful for the review pass. Both blockers were exactly the sort of thing that slips through when the first focused repros pass. The code is stronger after feedback, and the remaining concern is now broad validation time rather than design uncertainty.

## Final Validation
Second-pass reviews:

- Codec/fuzz reviewer found no blocking issues after the `FLOAT2SIG` fix and confirmed the new complexity-10 NaN differential crosses the analysis gate.
- Validation-tooling reviewer initially found the Rust timeout still vulnerable to pipe EOF blocking. I changed fetch output capture from pipes to temp files and removed the `wait_with_output` dependency. Final validation-tooling second pass found no blocking issues. Residual risk: direct shell-script execution on a system with only `wget` and no `timeout` command still lacks a true wall-clock cap, but `full-test` itself is bounded.

Broad/focused validation results:

- `timeout 240s cargo clippy -p full-test --all-targets -- -D warnings` passed.
- `timeout 900s cargo test --release -p ropus` passed: 1867 unit tests + 3 doc tests.
- `timeout 1200s cargo test --release -p ropus-harness --bin ropus-compare -- --nocapture` passed; multistream encode/decode/roundtrip passed.
- `timeout 180s cargo test -p full-test ietf_vectors -- --test-threads=1` passed.
- `timeout 180s cargo test -p full-test -- --test-threads=1` passed: 136 tests.
- `timeout 300s cargo test -p ropus-harness --test c_ref_differential test_float_pcm_nan_encode_matches_c_reference -- --nocapture` passed both below-analysis and analysis-gated NaN cases.
- `timeout 180s cargo check --manifest-path tests/fuzz/Cargo.toml --bin fuzz_decode --bin fuzz_encode --bin fuzz_roundtrip --bin fuzz_multistream` passed with only the C reference debug-build pragma note.
- `timeout 180s cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests -- --nocapture` passed: 12 tests.
- `timeout 240s cargo test --manifest-path tests/fuzz/Cargo.toml --test multistream_decode_asymmetries -- --nocapture` passed: 4 tests.
- `timeout 1800s bash tools/fetch_ietf_vectors.sh` downloaded, size/hash verified, and installed the vectors.
- `timeout 1800s cargo test -p conformance --test ietf_vectors -- --test-threads=1` passed: 24 tests.
- `git diff --check` passed.

Validation caveat:

- `timeout 2400s cargo run --release -p full-test -- --quick --skip-coverage` timed out while Stage 2 was still running. It had correctly detected provisioned vectors and invoked `cargo test --workspace --lib --tests --no-fail-fast -- --test-threads=1` without adding a vector skip. No assertion failure was surfaced before the outer timeout. I am recording this as an unfinished broad-run caveat, not as a failing test.

Worktree note:

- `tests/vectors/ietf/` was provisioned and remains gitignored.
- There is concurrent fuzz CTL-sequence work in the tree (`tests/fuzz/Cargo.*`, `c_reference.rs`, `fuzz_ctl_sequence.rs`, seeds/tests). The fuzz crate was rustfmt-formatted during the review-feedback cycle, so some adjacent dirty fuzz files have formatting deltas. I did not intentionally change their semantics and did not revert them because they appear to be another in-flight stream.

Emotional temperature: tired but satisfied. The four identified failures are either fixed or deliberately reframed with stronger gates. The one thing I dislike is the long `full-test` timeout; the focused evidence is strong, but the full workspace runner deserves a separate performance/hang look if it is expected to be a routine gate.
