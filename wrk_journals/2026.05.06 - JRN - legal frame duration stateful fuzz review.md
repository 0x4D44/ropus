# Legal Frame Duration Stateful Fuzz Review Journal

Date: 2026-05-06

## Entry

Started as independent review agent for Arthur's supervisor-led workflow. Read the code-reviewer skill and repository instructions, then inspected the HLD, current diff, stateful fuzz targets, shared frame-duration helper, tests, and C decode sequence helper. I kept hands off source code as requested and only wrote review artifacts.

The uncommitted source diff is very small: `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_decode_plc_seq.rs` changes the Rust PLC decode buffer from a fixed 120 ms capacity to the selected frame's sample count. The broader Slice B implementation appears to be in the latest commit and already wires the multiframe shape byte, PLC high-bit selector, helper-based frame sizes, fingerprints, and failure context.

Validation passed:

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe`
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`
- `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
- `timeout 120 cargo fmt --check`
- `git diff --check`

Emotional temperature: steady but a little uneasy about the test gap. The source change is obviously small and likely correct, but because the exact fixed bug is "do not accidentally use 120 ms PLC capacity/request", relying on fuzz build coverage alone feels too thin. I am also mildly frustrated by the stale review document already at the requested path; it said the Rust buffer still used `5760 * channels`, which was no longer true. I replaced it with the current review rather than preserving stale conclusions.

Residual concern: add a focused regression test or testable helper for PLC buffer sizing before calling this complete.
