# Fuzz Oracle Debt Implementation Journal

Date: 2026.05.06

## Entry 1

Read `/home/md/language/ropus/AGENTS.md`, the signed HLD, current
`tests/fuzz/fuzz_targets/oracle.rs`, existing `oracle_tests.rs`, and
`wrk_docs/testing-oracle-matrix.md`.

Implementation direction is narrow: keep fuzz runtime behavior unchanged, add
self-describing class metadata, deterministic count/report helpers, focused
unit tests, a bounded ignored operator report, and matrix alignment.

Emotional temperature: steady. The existing classifier is already a good base,
which makes this feel like contract hardening rather than risky behavior work.

## Entry 2

Added source metadata methods for stable names, weakened/debt status, asserted
properties, unasserted debt properties, and matrix classes. Added fixed-order
count rows, totals, deterministic report formatting, and pure decode /
multistream summary helpers.

Added integration tests for metadata uniqueness, strong/weakened boundaries,
count totals, deterministic report output including zero-count classes,
classification boundaries, and pure packet summarization. Added an ignored
bounded operator report that summarizes committed `fuzz_decode` seeds, crashes,
and the standalone recovery known-failure fixture without parsing multistream
arbitrary inputs.

Updated the oracle matrix to split the prior weakened fuzz row into
`sample-count-only`, `recovery-or-dtx-only`, and `error-agreement-only`, and to
add the missing SNR-gated strong class row.

Emotional temperature: mildly wary about report string brittleness, but it is
intentional here because the stable names and ordering are the contract being
tested.

## Entry 3

Validation completed:

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  passed with 20 tests run and one ignored operator-report test.
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests -- --ignored --nocapture decode_oracle_class_distribution_report`
  passed and printed a 41-input committed `fuzz_decode` distribution.
- `timeout 120 cargo fmt --check` passed.
- Extra sanity check `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
  passed after applying nested-manifest rustfmt wrapping.

Residual risk: the report summarizes committed `fuzz_decode` seeds/crashes and
one known-failure fixture, not a live fuzz campaign and not multistream
arbitrary inputs. That is deliberate for this phase, but it should be stated
when interpreting the report.

Emotional temperature: settled. The extra nested-manifest format check was
worth doing; root rustfmt did not cover the touched integration test.
