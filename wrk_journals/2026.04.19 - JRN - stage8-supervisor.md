# 2026-04-19 — Stage 8 supervisor journal (DRED port)

**Role.** Supervisor. High-level direction only; implementation /
review / feedback-application delegated to agents.
**HLD.** `wrk_docs/2026.04.19 - HLD - dred-port.md`
**Prior journal.** `wrk_journals/2026.04.19 - JRN - stage8-dred-port.md`
(8.1 execution record)

## Team structure

Per sub-stage (8.2 → 8.9):

| Role | Agent type | Skill | Input | Output |
|---|---|---|---|---|
| Implementer | general-purpose | rust-best-practices + software-testing | HLD sub-step + acceptance bar | Rust code + tests; journal entry |
| Code reviewer | general-purpose | code-reviewer | Diff + HLD | Structured review findings |
| Devil's advocate | general-purpose | (none — adversarial) | Diff + review output | Challenge memo: shortcuts, blind spots, risks |
| Change applier | general-purpose | code-review-apply-feedback | Reviews + devils-advocate | Revised code; reply to each finding |

Closing (8.10):

| Role | Agent | Task |
|---|---|---|
| Auditor | general-purpose (2 parallel) | Per-sub-step HLD-vs-impl comparison |
| Gap fixer | general-purpose | Iterate on any found gaps |

## Supervisor rules

1. Never read/write implementation source directly. Only read journals,
   HLD, review outputs, git log, test output.
2. Commit at end of each sub-stage (only after devil's-advocate has had
   its say and apply-feedback has run).
3. If a sub-stage fails tier 1, record the drop to tier 2 in the
   journal and keep going — do not redesign mid-stage.
4. If an agent returns "done" but tests weren't run, reject and send
   back. Trust-but-verify via `cargo test` exit status in their report.
5. Stage 7 owns DNN weight plumbing. We publish signatures; they wire.
   Do not cross that seam.

## Sub-stage state

| # | Sub-stage | Status | Notes |
|---|---|---|---|
| 8.1 | ec_laplace_p0 | ✅ complete | `fe8c97e`; both C-diff tests pass post-7a/7b.2 |
| 8.2 | dred_stats tables | ✅ complete | `10b1808`; reviewer + devils-advocate both clean; only change applied was a `TODO(stage-8.3)` marker on `#![allow(dead_code)]` |
| 8.3 | dred.rs skeleton | ✅ complete | 1190-line skeleton; reused Stage 7b.1.5 shared blob (+2.22 MB DRED layers, prefix-disjoint from pitchdnn/fargan/plc). Devils-advocate flagged missing LPCNet composite load; deferred to 8.6 via TODO. Also flagged assertion-bump rationale; added inline comment. |
| 8.4 | RDOVAE encoder fwd | ✅ complete | Tier 1 bit-exact on 8 frames in 7b.2's `harness-deep-plc` sibling harness via opaque-handle shim. 544-wide concat buffer compile-time asserted. Devils-advocate flagged correlated `linear_init` blind spot; documented as caveat; 8.6/8.7 downstream oracles close gap. |
| 8.5 | RDOVAE decoder fwd | ✅ complete | Tier 1 bit-exact on 8 frames, 576-wide buffer asserted, GLU + non-dilated convs. Carries 8.4 compensating-error caveat. Devils-advocate flagged that synthetic xorshift inputs don't test integer-aligned quantised values — folded forward to 8.6 task as an added test mode. |
| 8.6 | compute_latents + encode_silk_frame | 🟡 next | Forwarded: 8.5 quantised-input test mode; 8.3 LPCNet composite-load TODO |
| 8.4 | RDOVAE encoder fwd | ⏸ | |
| 8.5 | RDOVAE decoder fwd | ⏸ | |
| 8.6 | compute_latents + encode_silk_frame | ⏸ | |
| 8.7 | ec_decode + OpusDREDDecoder | ⏸ | |
| 8.8 | Encoder wiring | ⏸ | |
| 8.9 | FARGAN seam stub | ⏸ | |
| 8.10 | Audit + close | ⏸ | |

## Upstream / format pins (copied from 8.1 journal)

| Key | Value |
|---|---|
| Upstream sha | `788cc89` (2026-03-21) |
| DRED version | 12 (experimental) |
| Extension ID | 126 |

## Devil's-advocate prompt template

> You are reviewing a Stage 8 DRED port sub-commit. Assume the
> implementer and the code reviewer are both competent but may have
> missed things. Your job is to find the *holes*: untested edge cases,
> silent tier-1→tier-2 drops, guardrails removed for convenience, test
> cases that only cover the happy path, hidden dependencies on future
> work, misread C semantics. Produce a concise challenge memo. No
> praise; land specific, falsifiable observations.

## Open decisions for Arthur

None currently. If the blob pipeline in 8.3 is missing, I'll pause and
ask rather than invent one.
