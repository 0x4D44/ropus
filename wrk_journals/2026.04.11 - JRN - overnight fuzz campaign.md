# 2026.04.11 - Overnight Fuzz Campaign

**Started:** 22:00  
**Target end:** 06:00 (8 hours)  
**Goal:** Extended differential fuzz testing of all 5 targets, investigate and fix any crashes  
**Approach:** Supervisor pattern — agents do investigation/fixing, main thread monitors

## Campaign Configuration

| Setting | Value |
|---------|-------|
| Targets | fuzz_decode, fuzz_encode, fuzz_roundtrip, fuzz_repacketizer, fuzz_packet_parse |
| Duration per target | 90 min (5400s) |
| Total campaign time | ~7.5 hours |
| Seed corpus | 631 files across all targets |
| Max input length | 16384 bytes |
| Differential mode | ON (compares Rust vs C reference) |
| ASan | ON (MSVC clang_rt, leak detection suppressed) |
| unchecked-indexing | OFF (OOB panics instead of UB) |

## Corpus Inventory (pre-campaign)

| Target | Seeds | Crashes |
|--------|-------|---------|
| fuzz_decode | 126 | 0 |
| fuzz_encode | 63 | 0 |
| fuzz_roundtrip | 76 | 0 |
| fuzz_repacketizer | 53 | 0 |
| fuzz_packet_parse | 313 | 0 |

## Timeline

### 22:00 — Setup
- Verified nightly toolchain + cargo-fuzz installed
- All 5 targets build clean with MSVC linker + ASan runtime
- Windows ASan workaround: `CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_LINKER=link.exe`
- Note: VS 18 Enterprise has the correct ASan DLL at `/c/Program Files/Microsoft Visual Studio/18/Enterprise/VC/Tools/MSVC/14.50.35717/bin/Hostx64/x64/` (VS 2022 Community path is wrong)
- Journal created, launching campaign

---

### 22:10 — Round 1: Immediate Findings

Launched all 5 targets. Found 4 bugs within minutes:

#### Bug 1: SILK pulse decode OOB — FIXED
- **File:** `src/silk/decoder.rs:690`
- **Symptom:** `index out of bounds: len is 10 but index is 10`
- **Root cause:** Spurious `use_last` logic added `+1` to `N_RATE_LEVELS - 1` when `n_lshifts == 10`, overflowing `SILK_PULSES_PER_BLOCK_ICDF[10]` (only 10 entries, indices 0-9)
- **Fix:** Removed `use_last` — always index with `N_RATE_LEVELS - 1`, matching encoder and C reference
- **Status:** ✅ Fixed, all 36 SILK decoder tests pass

#### Bug 2: Hybrid SWB decode differential mismatch — INVESTIGATING
- **Target:** fuzz_decode
- **Symptom:** PCM mismatch at sr=48000, ch=1, pkt_len=256, samples=480
- **Details:** TOC byte 0x60 = config 12 = Hybrid SWB, 10ms, mono. Rust output ~[-320,+320], C output ~[-3600,+4400] — wildly different
- **Seed files:** `ones_255b.bin` or `zeros_255b.bin` in fuzz_decode corpus
- **Status:** 🔍 Investigation agent dispatched

#### Bug 3: SILK encoder subtraction overflow — INVESTIGATING
- **Target:** fuzz_encode
- **Symptom:** `attempt to subtract with overflow` at `silk/common.rs:1322`
- **Details:** Arithmetic overflow in fixed-point SILK code, found after ~2000 fuzzer iterations
- **Status:** 🔍 Investigation agent dispatched

#### Bug 4: Repacketizer out() length mismatch — INVESTIGATING
- **Target:** fuzz_repacketizer
- **Symptom:** Rust `out()` returns 30 bytes, C returns -3 (OPUS_BUFFER_TOO_SMALL)
- **Details:** Rust repacketizer produces output where C correctly rejects with buffer-too-small
- **Status:** 🔍 Investigation agent dispatched

#### fuzz_packet_parse — still running (no crash, healthy at 672MB RSS)

### 22:30 — Bug 3 Fixed, Continuing Campaign

- Bug 3 root cause: `(1i32 << 15) as i16 - delta_min_q15[order]` — `as i16` binds tighter than `-` in Rust, so 32768 becomes -32768 before subtraction, causing overflow. C does the subtraction in int then narrows. Fixed: `((1i32 << 15) - delta_min_q15[order] as i32) as i16`
- All 71 silk::common tests pass after fix
- Relaunched fuzz_encode with fix — running clean so far
- 3 investigation agents dispatched (decode mismatch, silk overflow [already fixed], repacketizer mismatch)
- Searched for similar cast-before-arithmetic patterns across codebase — no other instances found

**Running fuzz targets:**
- fuzz_encode round 2 (with fix) — active, 485MB RSS
- fuzz_packet_parse — active, 672MB RSS, no findings

**Blocked:**
- fuzz_decode — blocked on bug #2 investigation (hybrid decode mismatch)
- fuzz_roundtrip — blocked on bug #2 (same decode path)
- fuzz_repacketizer — blocked on bug #4 investigation

### 22:45 — Full Sweep Pass + Regression Suite

- **Sweep: 210/210 PASS** — all encode-decode configurations (SILK, CELT, Hybrid) produce bit-exact output vs C reference. Includes hybrid modes, all sample rates, all bitrates, FEC, DTX, stereo.
- **Regression suite: 797/797 PASS** — all unit + integration tests pass with both fixes applied
- **Key insight:** Bug #2 (decode mismatch) and Bug #4 (repacketizer) only occur with garbage/fuzzed packets, NOT with real encoded audio. Severity is robustness, not correctness.
- 3 investigation agents still running (deep code analysis)

---

