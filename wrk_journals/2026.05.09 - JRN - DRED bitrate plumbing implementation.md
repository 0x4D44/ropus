---
date: 2026-05-09
author: Claude (Opus 4.7) supervisor + agent team
status: in-progress
parent_audit: 2026.05.09 - JRN - encoder setter-sequence audit.md
---

# DRED bitrate plumbing — implementation campaign

## Why

The encoder setter-sequence audit (`2026.05.09 - JRN - encoder
setter-sequence audit.md`) cleared the surface area of the recurring
mode-transition defect class but flagged three known-gaps, all
DRED-related and all stemming from the same root cause: ropus does
not port C's `compute_dred_bitrate`. Findings:

- **F33** — DRED CBR override block (C `opus_encoder.c:2470-2477`):
  under `!use_vbr && dred_duration > 0`, C calls `OPUS_SET_VBR(1)`,
  `OPUS_SET_VBR_CONSTRAINT(0)`, `OPUS_SET_BITRATE(celt_bitrate)`. Rust
  skips the block — CELT runs CBR while C runs unconstrained VBR.
- **F53** — `st->bitrate_bps -= dred_bitrate_bps` (C 1338): Rust never
  computes `dred_bitrate_bps`, so the SILK/CELT budget over-allocates
  vs. C when DRED is active.
- **F48** — multi-frame `first_frame = (i==0) || (i==dtx_count)`
  (C 1777): Rust uses `i == 0` only. DRED attaches to the doomed
  frame when sub-frame 0 is DTX-dropped.
- **(quieter)** — `dred_q0/d_q/qmax/target_chunks` are statically
  initialised in `set_dred_duration` (`encoder.rs:1277-1280`) and
  never updated per-frame. C's `compute_dred_bitrate` writes them
  per-frame based on bitrate, packet-loss, and FEC config. Today's
  DRED extension emission therefore uses stale (wrong) quantizer
  levels relative to C.

All four divergences land on DRED-active inputs only and are
documented `TODO(stage-8.x)` in the source. Bundling because the
common dependency is `compute_dred_bitrate`.

## Quality bar

- **Bit-exact with C reference** under DRED-active inputs (the
  codec's standard Tier-1 bar). If f32 drift turns up, drop to Tier-2
  SNR-bounded **only with explicit notice in the journal** — not
  silently.
- **No regressions**: 1867/1867 lib tests, 17/17 differential param
  grid, all existing fuzz known-failures replay clean.
- **No new warnings**: `cargo build`, `cargo clippy` clean.
- **No backwards-compat shims**: this is greenfield code; the existing
  static init at `set_dred_duration` should be replaced, not left as
  a fallback.

## Plan (stages)

### Stage 1 — Detailed implementation plan
Plan agent walks the C source for `compute_dred_bitrate` /
`estimate_dred_bitrate` / `dred_bits_table` and the three call sites.
Produces an HLD-ish doc under `wrk_docs/` listing exact insertion
points, signatures, threading, test surface. No code yet.

### Stage 2 — TDD test scaffolding
Implementation agent writes failing unit tests against the to-be-ported
functions (using known C reference values for golden inputs/outputs)
and a differential test that compares Rust vs. C reference output for
DRED-enabled encode. Tests must compile and fail with clear messages.
**Commit at end of stage.**

### Stage 3 — Implementation
Implementation agent ports `dred_bits_table`, `estimate_dred_bitrate`,
`compute_dred_bitrate`, threads `dred_bitrate_bps` through
`encode_frame_native` / `encode_multiframe`, adds F33 setter block,
adds F53 subtraction, adds F48 first-frame fix, replaces the static
init at `set_dred_duration`. Tests from Stage 2 should now pass.
**Commit at end of stage.**

### Stage 4 — Code review (parallel: code-reviewer + devil's advocate)
- code-reviewer agent: standard review using `code-reviewer` skill.
- devil's advocate agent: actively challenges the implementation —
  argues for ways it could be wrong, looks for hidden assumptions,
  questions the f32 bit-exactness claim, probes edge cases.
Both produce written feedback. **No commit.**

### Stage 5 — Apply review feedback
Implementation agent applies feedback from both reviewers using the
`code-review-apply-feedback` skill. Re-runs all gates.
**Commit at end of stage.**

### Stage 6 — Re-review if material changes (skip if Stage 5 is light)
Decide based on Stage 5 diff size. If non-trivial, run another
code-reviewer pass. **Commit at end of stage if applied.**

### Stage 7 — Plan-completeness verification
Verification agent compares final state vs. Stage 1 plan, reports
any unmet items. If gaps: spawn another implementation agent to
close them, then re-verify. **Commit at end of stage.**

### Stage 8 — Closeout
Supervisor writes closeout journal section, archives crash artifacts
if any, confirms working tree clean. **Final commit if needed.**

## Supervisor rules

- Don't read/edit code or run tests directly — delegate every action.
- Read agent summaries; don't re-read files they read.
- After every agent: write a 2-3 line entry below with the verdict
  and decision.
- Commit at the boundary of each stage with a descriptive message.
- If an agent goes off-track, redirect with a short follow-up via
  SendMessage rather than spawning a fresh one.

## Working log

### 2026-05-09 11:45 — campaign kickoff

Plan written. Tasks set up. Baseline at commit `8a1c6e7`,
1867/1867 lib tests pass.

### 2026-05-09 12:10 — Stage 1 complete

Plan agent (read-only) produced a thorough HLD; supervisor persisted
it to `wrk_docs/2026.05.09 - HLD - DRED bitrate plumbing port.md`.

Highlights:
- Single production file touched (`ropus/src/opus/encoder.rs`); two
  new differential tests under `harness-deep-plc/tests/`.
- ~120-140 added LoC, ~6 deleted, 2 TODOs removed.
- `dtx_count` already plumbed in the multi-frame loop (F48 is one
  line).
- F33 has a sibling at C 2399-2411 (DRED-aware CELT budget steal)
  flagged as a TODO at `encoder.rs:3044`. Plan correctly bundles
  these together — they're the same DRED-aware-CELT picture.
- 10 unit tests + 2 differential harness tests + 1 existing
  integration test.
- Biggest risk: f32 determinism across the 5 ops in
  `estimate_dred_bitrate` / `compute_dred_bitrate`. Mitigations
  spelled out (no mul_add, f32 not f64 intermediates,
  `(0.5_f32 + bits).floor() as i32`).
- Important: the **fixed-point harness does NOT enable DRED**;
  differential tests live in `harness-deep-plc/`.

Decision: plan accepted as-is. Moving to Stage 2 (TDD tests).
Commit the plan + journal at the next stage boundary (after Stage 2
lands tests; tests are still part of the same atomic deliverable).

### 2026-05-09 13:30 — Stage 2 complete

Implementation agent (general-purpose) added 12 tests + compile-only
stubs (`STAGE-3-STUB`-tagged), confirmed `cargo build --workspace`
clean and 1867 pre-existing lib tests still pass. New tests fail
with clear messages.

Two HLD deviations:
- Field name is `dred_d_q` in Rust struct, not `dred_dQ` (C name).
- The `harness-deep-plc` C shim hard-codes some encoder knobs;
  the DTX differential test runs Rust-only and asserts the F48
  contract via output parsing.

Both reasonable. Committed as `755eb3b`.

For unit-test #2 (`test_estimate_dred_bitrate_golden`), the agent
computed expected outputs by simulating C `estimate_dred_bitrate`
in f32 and cross-checked with python:
- `(15, 5, 15, 4, 1_000_000)` → `(109, 2)`
- `(9,  3, 15, 100, 1_000_000)` → `(393, 26)`
- `(4,  5, 15, 100, 10_000)` → `(663, 26)`
- `(4,  3, 15, 8, 8000)` → `(290, 3)`
- `(15, 5, 15, 104, 0)` → `(263, 0)`

These will be the authoritative anchors for Stage 3.

Moving to Stage 3 (implementation).

### 2026-05-09 15:45 — Stage 3 complete (with caveats)

Implementation agent landed the full port. Final lib tests:
**1878 / 1878 pass** (1867 pre-existing + 10 Stage 2 + 1 new F48
lib test). Clippy clean, fmt clean. Versions bumped:
`ropus 0.12.14→0.12.15`, `harness-deep-plc 0.1.7→0.1.8`.

**Files modified:**
- `ropus/src/opus/encoder.rs` (~290 lines): real
  `DRED_BITS_TABLE`/`estimate_dred_bitrate`/`compute_dred_bitrate`
  bodies; F33 at lines 3298-3310; budget-steal sibling at 3215-3220;
  F53 carve-out at the post-CBR site; `dred_bitrate_bps` threaded
  through `encode_multiframe` (line 2380) and `encode_frame_native`
  (line 2510); F48 at lines 2418-2436; stale-quantizer init removed
  from `set_dred_duration`; one new unit test for the F48 contract;
  9 existing test call sites updated to pass `0` for the new param.
- `ropus/src/silk/encoder.rs`: 6-line whitespace shuffle from
  `cargo fmt`. Not a code change.
- 3 harness-deep-plc test files updated.

**Caveats / open issues for Stage 4 reviewers to probe:**

1. **Tier-1 byte-exact gate is not actually exercised.** At the
   `harness-deep-plc` C shim's hard-coded settings (FEC=1, loss=20,
   br=32k, dur=100, complexity=5), `compute_dred_bitrate` returns 0
   because `target_chunks = 1 < 2` (the early-out). With
   `dred_bitrate_bps = 0`, F33/F53/F48/budget-steal are all logical
   no-ops on this test. The differential test (`dred_bitrate_
   plumbing_diff.rs`) was dropped to Tier-2 SNR fallback; the agent
   correctly attributes residual drift to pre-existing SILK FEC
   divergence, NOT to the new f32 ops. **But this means we've
   landed an f32-sensitive port without a test that actually
   exercises its f32 ops.** Reviewers should challenge this.

2. **`dred_integrated_encode.rs` was modified** to add
   `set_inband_fec(1)` + `set_packet_loss_perc(20)`. The agent's
   claim: previous test passed only because static `target_chunks
   = 26` made Rust emit DRED at FEC=0/loss=0 settings where C
   would not. With the bug fixed, those settings correctly produce
   no DRED, so the test had to be reconfigured to actually exercise
   DRED emission. Plausible but should be verified by reviewers
   against the C contract.

3. **DTX differential test** runs Rust-only because synthetic PCM
   doesn't reliably trigger DTX in 10 successive frames; F48 is
   instead locked by a deterministic lib unit test the agent added.
   Acceptable but means C-side parity for F48 is unproven.

4. Pre-existing `dnn_plc_tier2_snr_above_50db` failure (34 dB vs
   50 dB threshold) is unrelated; verified failing on the
   unmodified Stage-2 commit.

**Decision:** commit Stage 3 as-is. Route concerns 1, 2, 3 to the
Stage 4 reviewers — code-reviewer for the standard pass and
devil's-advocate to specifically attack the no-op Tier-1 gate
problem.

### 2026-05-09 16:00 — Stage 4 reviewers spawned (parallel)

Two background agents:
- **code-reviewer** (general-purpose w/ `code-reviewer` skill).
  Standard rigorous review; APPROVE/APPROVE-WITH-CHANGES/REJECT
  verdict + required/recommended/nits/open-questions.
- **devil's advocate** (general-purpose). Adversarial review with
  10 hypotheses to attack: fake Tier-1 gate, integration test
  modification, DTX-test pragmatic Rust-only fallback, stale-
  quantizer poison values, regression check, f32 op order,
  HLD-as-oracle completeness, missing work, counterexample
  construction, and the bet-your-reputation test.

Both have full access to read the diff and run cargo / git
read-only. Waiting for results.

### 2026-05-09 16:30 — Stage 4 reviewers complete

**Code-reviewer**: APPROVE-WITH-CHANGES. Two required:
1. Real Tier-1 differential gate (current test at shim's settings
   makes `dred_bitrate_bps = 0`, exercising nothing).
2. Tautological F48 unit test (literally re-derives the predicate
   it's checking).
Plus 3 recommended, 3 nits, 3 open questions.

**Devil's advocate**: NO bet your reputation, conditional on
landing oracle-grounded fixtures. Findings beyond the
code-reviewer's:

- **NEW F33b — fifth unported divergence**: C
  `opus_encoder.c:2169` reads `if (st->mode == MODE_HYBRID ||
  dred_bitrate_bps > 0)` for the SILK CBR steal block; Rust at
  `encoder.rs:~2870` only checks HYBRID. Under SILK_ONLY +
  use_vbr=0 + dred_bitrate_bps > 0, C flips `silk_mode.useCBR=0`
  and reduces `maxBits`; Rust does not. **The HLD missed this.**
  Implementer faithfully built the HLD; the HLD was incomplete.
- Unit-test expected values are anchored on Stage-2 author's
  Python f32 simulation, not on captured C output. A misread of
  C semantics by the author would silently ratify a wrong port.
- DA's concrete counterexample: 16 kHz/40 kbps/CBR/loss=30/dur=100
  drives `compute_dred_bitrate > 0` AND should expose F33b's
  unported clause. Cannot run without harness shim extension.
- F48 Rust-only DTX differential is also degraded — the test
  `eprintln!`s + returns when DTX cannot be reproduced rather
  than failing.
- f32 op order in the port itself: REFUTED (faithful) modulo a
  subtle `target_bits as f32` cast hoist outside the loop, which
  is safe in practice (`target_bits ≤ 2^24`).
- Reverse-direction regressions: NONE; 1879/1879 lib + 17/17
  param-grid clean.

Decision: I'm bundling both reviews + the new F33b finding for
Stage 5 (apply feedback). The DA's counterexample is the right
acceptance gate — if a Tier-1 differential at that config passes
byte-exact, every f32 op is implicitly grounded against C. Stage
5 must not declare done without that gate or an explicit demotion.

### 2026-05-09 — Stage 5 apply-feedback: pivot to direct FFI scalar fixture

Background: the first Stage-5 attempt drove `compute_dred_bitrate >
0` via `dred_bitrate_plumbing_nonzero_diff.rs` (DA's counterexample
config: 16 kHz / 40 kbps / FEC=0 / loss=30 / CBR / dur=100). At
that config the test hits a 24 dB PCM SNR — F33b motion accounts
for ~3 dB but the residual is upstream SILK encoder drift unrelated
to this port. Supervisor pivot: validate the f32 ops directly via
FFI scalar comparison rather than transitively through encode.

What landed:
- `harness-deep-plc/dred_encode_shim.c`: VERBATIM COPY of the C
  `compute_dred_bitrate` and `estimate_dred_bitrate` bodies (both
  `static` in `opus_encoder.c` so not directly linkable). Vendor
  source untouched. Two `extern`-visible wrappers
  (`ropus_c_estimate_dred_bitrate`,
  `ropus_c_compute_dred_bitrate`) take the four scalar fields the
  C function reads off the encoder rather than `OpusEncoder *`,
  keeping the FFI surface flat.
- `harness-deep-plc/src/lib.rs`: matching FFI bindings.
- `ropus/src/opus/encoder.rs`: two `#[doc(hidden)] pub` test-only
  shims (`ropus_test_estimate_dred_bitrate`,
  `ropus_test_compute_dred_bitrate`) that forward to the private
  implementations so the FFI test can call them from outside the
  crate without widening the public surface.
- `harness-deep-plc/tests/dred_compute_bitrate_ffi_diff.rs`: 5
  `estimate_dred_bitrate` vectors (mirroring the Stage-2 unit-test
  goldens) + 10 `compute_dred_bitrate` vectors covering FEC on/off,
  loss zero/2/10/15/30, bitrate 5k/12k/24k/32k/40k/48k/64k/120k,
  frame sizes 160/320/480/960, sample rates 8k/16k/24k/48k, and
  edge cases (`dred_duration=0`, negative `bitrate-bitrate_offset`,
  short DRED duration). Asserts byte-exact return + all 4 out
  fields.
- `harness-deep-plc/tests/dred_bitrate_plumbing_nonzero_diff.rs`:
  demoted to `#[ignore]` with comment block pointing at the SILK
  drift open question and the new FFI fixture.
- Versions: `ropus` 0.12.16 → 0.12.17, `ropus-harness-deep-plc`
  0.1.9 → 0.1.10.

Test results:
- `dred_compute_bitrate_ffi_diff`: 2/2 pass (15/15 vectors
  byte-exact across return value and every out-parameter).
- `ropus` lib: 1877/1877 pass.
- `ropus-harness-deep-plc`: all green except the pre-existing
  `dnn_plc_tier2_snr_above_50db` failure (independent of this
  port; verified by running the same test on the stashed baseline
  before this work — same 34.34 dB result).

Outcome: f32 ops in `compute_dred_bitrate` and
`estimate_dred_bitrate` are byte-exact against the C reference at
15 input vectors covering every code path. The Stage-4
reviewers' "real Tier-1 gate" requirement is satisfied via direct
FFI scalar comparison rather than transitive encode-pipeline
validation.

### 2026-05-09 18:30 — Stage 7 verification + Stage 8 closeout

Stage 7 verifier (general-purpose agent) walked HLD §1 row-by-row
against the final code, diffed the verbatim C copies against
`opus_encoder.c:668-730` (Python normalisation: byte-for-byte
identical for table + both function bodies), confirmed FFI test
counts (5 estimate + 10 compute = 15 vectors, all asserting return
value + 4 out-params), confirmed `#[doc(hidden)] pub` on the test
shims (no public-API widening), confirmed anti-scope adherence
(no decoder, no LPCNet, no QEXT, no multistream, no docs leakage).

Verdict: COMPLETE-WITH-CAVEATS. Only deficiency: a single rustfmt
nit on `dred_compute_bitrate_ffi_diff.rs:43` (long FFI declaration
line). Fixed in commit `c1eca65` (rustfmt only, no logic change).

**Final gate confirmation:**

| Gate | Result |
|---|---|
| `cargo build --workspace` | clean |
| `cargo test --lib -p ropus` | **1877 / 1877 pass** |
| `cargo test -p ropus-harness-deep-plc --test dred_compute_bitrate_ffi_diff` | **2 / 2 pass** (15 / 15 vectors byte-exact) |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| `cargo fmt --all -- --check` | clean |
| `cargo run -p ropus-harness -- roundtrip 48k_sine1k_loud.wav --bitrate 64000` | PASS (48000 samples match, decode-only also matches) |

**What this campaign delivered:**

- Five C-vs-Rust setter-sequence divergences resolved (F33, F48,
  F53, stale-quantizer, and the F33b discovery from the
  devil's-advocate review).
- `compute_dred_bitrate` and `estimate_dred_bitrate` ported from
  C with byte-exact f32 fidelity, validated against linked C via
  a 15-vector FFI fixture covering every code path (FEC on/off,
  loss 0-30, bitrate 5k-120k, frame sizes 160/320/480/960, sample
  rates 8k/16k/24k/48k, and edge cases).
- `dred_q0/d_q/qmax/target_chunks` now updated per-frame
  (replacing static init in `set_dred_duration`); the DRED
  extension emission block now reads correct quantizer levels.
- Multi-frame `curr_max` DRED budget adjustment ported.
- `dred_bitrate_bps` parameter threaded through encoder
  pipeline mirroring C's parameter-passing model.
- New FFI infrastructure under `harness-deep-plc` exposes static
  helpers from `opus_encoder.c` for future scalar fixture work.
- ropus 0.12.14 → 0.12.17 (3 patch bumps); harness-deep-plc 0.1.7
  → 0.1.10 (3 patch bumps).
- 5 commits over the campaign:
  - `755eb3b` Stage 2 TDD scaffolding
  - `8f342bc` Stage 3 implementation
  - `9a420c4` Stage 5 apply feedback
  - `c1eca65` Stage 7 fmt nit
  - (plus the precursor audit at `9bfe500`)

**Open follow-up surfaced (not this campaign's scope):**

The Stage-5 first-pass integration test at the DA's
counterexample (fs=16k / 40 kbps / SILK_ONLY / CBR / FEC=0 /
loss=30) hit 24 dB SNR vs 60 dB target. F33b moved this by ~3 dB
(proving it's exercised), but the residual is upstream SILK
encoder drift unrelated to this port — verified by stash-and-
rerun on the pre-Stage-5 baseline. The test is `#[ignore]`'d
with a comment pointing at the SILK drift work item; it becomes
the regression check once that lands.

**Process observations:**

1. **The HLD missed F33b.** The Plan agent didn't catch the
   `|| dred_bitrate_bps > 0` clause at C 2168-2178; the
   implementer faithfully implemented an incomplete plan; only
   the devil's-advocate review caught it. **Lesson**: a
   plan-only stage isn't sufficient quality control on its own
   — the adversarial review found a real divergence the standard
   review missed.

2. **Transitive-through-encode validation failed because Rust
   SILK isn't byte-exact at all configs.** The first Stage-5
   approach (run a non-zero-DRED differential and assert byte
   equality) blocked on pre-existing upstream drift. The pivot
   to direct FFI scalar fixture was the right call — it
   eliminates the dependency on the rest of the encoder being
   bit-exact. **Lesson**: when validating a numerical port,
   directly compare scalar function outputs instead of relying
   on bit-exactness through an entire pipeline that may have
   independent drift.

3. **The fixed-point harness vs harness-deep-plc** distinction
   was not obvious from the file structure. The Stage-1 Plan
   agent caught it ("the fixed-point harness does NOT enable
   ENABLE_DRED — DRED testing must use harness-deep-plc"). This
   saved an entire round of confusion downstream.

4. **F33b was the same defect class as F33.** Recurring lesson
   from the campaign predecessor (the audit): per-frame
   state-mutation ordering between C and Rust. Just because
   you've audited once doesn't mean you've found every instance.
   The F33b find vindicates the closeout journal's claim that
   "this defect class may have more instances" — and it had at
   least one more.

**Status:** campaign closed. Working tree clean.

