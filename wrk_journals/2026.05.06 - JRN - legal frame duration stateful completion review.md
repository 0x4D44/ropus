# Legal Frame Duration Stateful Completion Review Journal

Date: 2026.05.06

## Entry

I reviewed the Phase 3.3 Slice B uncommitted diff for legal frame-duration stateful fuzzing. I treated Arthur's "do not edit files" instruction as no source edits, while still creating this requested journal and the requested completion review document.

The source diff is small and coherent. The multiframe target now takes a single shape byte, derives one legal duration selector and one frame count from it, and then uses the selected duration consistently for PCM sizing, Rust encode, C encode, and Rust decode sample-count checks. The PLC target derives its duration selector from the high bits of `drop_mask`, uses it for PCM sizing and encode, keeps the Rust decode scratch buffer at `5760 * channels`, and passes the selected duration as the requested decode/PLC frame size to both decoders.

I checked the untouched boundaries explicitly. There is no diff in `fuzz_roundtrip.rs` or `oracle.rs`, and I did not see any campaign behavior added. The only source/test files changed are the expected helper, two fuzz targets, and frame-duration tests. There are process-document changes in the worktree, including an existing modified journal, but those are outside codec/fuzz source behavior.

The one thing that made me uneasy is the wording around PLC decode buffers. The Rust-side target buffer is explicitly still `5760 * channels`, but the existing C helper allocates `frame_size * channels` internally. Since `frame_size` is also the requested maximum output passed to `opus_decode`, this is legal and not an overflow concern. Still, if Arthur meant literal symmetric `5760 * channels` buffers on both Rust and C helper paths, that is not currently true.

Emotional temperature: mostly steady. I am comfortable with the Slice B behavior, but I do not want to overstate the C helper buffer detail because completion verification is exactly where small wording mismatches matter.

## Validation Notes

Passed:

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe`
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`
- `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
- `timeout 120 cargo fmt --check`
- `git diff --check`

No files were committed.
