# 2026-08-19 — JRN — Code-review pass: harness-deep-plc/

Overnight review pass (Claude Fable 5, KILN). Area assigned/selected: `harness-deep-plc/`
(MOVED — reviewed 2026-07-31, changed 2026-08-02 by the FLUX bug-fix wave).

## What ran

Six parallel lens subagents (correctness/FFI, oracle validity, C-shim safety, build
reliability, maintainability, devil's advocate — the Workflow tool was denied in this
mode, so the fan-out went through the Agent tool), then a lead verification pass that
re-read every excerpt behind a ledger-bound finding. Static only; no build or test ran;
`reference/` is absent on this host, so layout claims about the xiph tree are labelled
UNVERIFIED where they could not be settled from in-repo evidence.

## Outcome

- 8 bugs filed: ROP-BUG-KIL-00021 (High: nine DRED gates skip-and-pass on empty
  WEIGHTS_BLOB) through KIL-00028 (arch-specific drift locks). Full table in
  `wrk_docs/2026.08.19 - CR - 20260819-REV-ROP-CLA@KILN-code-review-112641.md`.
- 4 Draft reqs: KIL-00029 (opus_dred_process differential), 00030 (test-support dedup),
  00031 (reference provenance + layout guards), 00032 (prove DEEP_PLC engagement).
- Coverage artifact written for `harness-deep-plc/` at 2026-08-19.

## Surprises / decisions

- The 2026-08-02 fix wave (FLUX-00068..72) hardened `src/lib.rs` and the parser gates
  well, but the same defect classes survive one layer out: NaN-unguarded bit-exact
  helpers in the two payload-diff files, skip-and-pass in the weights guard the fixes
  never touched, and INFINITY-SNR degeneracy in the release gate whose calibration
  sibling got the guard.
- Two reviewers disagreed on `peek_celt_trailing_base` (does the xiph tail include an
  `lpc[]` block before `oldBandE`?). Unresolvable without `reference/`; recorded inside
  KIL-00025 as a verification step for the fixer rather than asserted.
- Strongest devil's-advocate catch: `peek.c` lacks the `#ifdef HAVE_CONFIG_H` prologue
  its `harness/` siblings have, while its struct mirror is `ENABLE_DEEP_PLC`-gated —
  and `state_divergence.rs:11-15` records "garbage" reads where an OPUS_CLEARed decoder
  should read zeros. Filed as KIL-00025.
- Tooling note for Arthur/deltic: `deltic bugs new` on this host now mints host token
  `KIL` where every pre-existing record says `KILN` (sequence correctly unified —
  minted 21 after KILN-00020). Kept the allocator's IDs verbatim per doctrine; deltic's
  host-token derivation deserves a look.

## Not done here

Per the pass contract, no fixes, no commits, no integration — deltic owns git for this
worktree. Bugs drain via fix-open-bugs; reqs await Gate-1 triage.
