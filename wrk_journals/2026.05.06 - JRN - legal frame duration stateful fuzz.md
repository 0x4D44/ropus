# 2026.05.06 - JRN - legal frame duration stateful fuzz

## Initial Brief
- Arthur asked me to continue as supervisor and use agents for legwork.
- Target slice: Phase 3.3 Slice B from
  `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - legal frame duration fuzz dimensions.md`.
- Scope:
  - apply the shared legal frame-duration helper to
    `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_encode_multiframe.rs`;
  - apply it to
    `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_decode_plc_seq.rs`;
  - add/update tests using TDD where useful;
  - preserve the Phase 2.2 oracle taxonomy and avoid unrelated codec work.

## Supervisor Operating Model
- I will keep my own work to coordination, journal updates, go/no-go decisions,
  and final synthesis.
- Implementation, review, feedback application, and completion verification
  will be delegated to agents.
- I read the `code-reviewer` and `code-review-apply-feedback` skill
  instructions before starting the review loop.

## Starting State
- Current `HEAD`: `627974d Add legal frame durations to roundtrip fuzz`.
- Tracked files were clean before this journal was added.
- Existing untracked file before this task:
  `/home/md/language/ropus/wrk_journals/2026.05.04 - JRN - release-and-refuzz.md`.

## Emotional Temperature
- This is a good incremental slice. The risk is not conceptual complexity; it is
  accidentally changing oracle meaning or broadening stateful target schemas more
  than the HLD permits. I need to enforce scope and not personally dive into
  debugging if the implementation hits a subtle fuzz-build issue.

## Implementation Delegation
- Delegated implementation to Lovelace.
- Owned write scope:
  - `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_encode_multiframe.rs`
  - `/home/md/language/ropus/tests/fuzz/fuzz_targets/fuzz_decode_plc_seq.rs`
  - focused tests under `/home/md/language/ropus/tests/fuzz/tests/` if needed
  - one implementation journal under `/home/md/language/ropus/wrk_journals/`
- Constraints given:
  - do not edit `oracle.rs` unless a compile-only import issue forces it;
  - do not modify codec implementation files;
  - do not commit;
  - use TDD where practical;
  - run focused fuzz tests/builds and formatting checks.

## Acceptance Criteria
- `fuzz_encode_multiframe` uses one existing shape byte for both legal duration
  selection and frame-count derivation.
- `fuzz_decode_plc_seq` derives duration from unused high bits of `drop_mask`
  and requests the selected frame duration for PLC decode instead of `5760`.
- Byte parity, PCM/sample-count oracle stance, and Phase 2.2 oracle taxonomy are
  preserved.
- No fuzz campaign is added or required for completion.

## Implementation Result
- Lovelace reported that `fuzz_encode_multiframe.rs` already satisfied the Slice
  B HLD requirements from prior work, so it was not changed.
- Lovelace changed `fuzz_decode_plc_seq.rs` so Rust PLC decode buffer sizing uses
  the selected `samples_per_frame` instead of the old `5760 * channels`
  capacity.
- Lovelace appended implementation notes and validation results to this journal.
- Reported validation passed:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Supervisor response:
  - Proceed to an independent code review using the `code-reviewer` workflow.
  - The review must explicitly verify the claim that `fuzz_encode_multiframe.rs`
    already satisfies the HLD and that the PLC path no longer requests 120 ms
    PLC by accident.

## Code Review Result
- Plato completed an independent code review using the `code-reviewer` workflow.
- Review saved to:
  `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - legal frame duration stateful fuzz.md`
- Review journal saved to:
  `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration stateful fuzz review.md`
- Findings:
  - Issue: selected PLC capacity/request behavior is implemented, but no
    focused regression test would fail if it reverted to `5760 * channels`.
  - Nit: a comment in `c_reference.rs` still refers to the old
    longest-plausible-frame/5760 policy.
- Validation passed in review:
  - `frame_duration_tests`
  - `oracle_tests`
  - fuzz build for `fuzz_encode_multiframe`
  - fuzz build for `fuzz_decode_plc_seq`
  - both fmt checks
  - `git diff --check`
- Supervisor decision:
  - Apply feedback before completion review. The missing test is small and
    directly protects the key Slice B behavior.

## Feedback Application Result
- Russell applied the review feedback using the `code-review-apply-feedback`
  workflow.
- Feedback journal saved to:
  `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration stateful fuzz feedback.md`
- Changes reported:
  - added testable PLC selected-duration frame-size/capacity helpers in
    `fuzz_decode_plc_seq.rs`;
  - added a focused regression test asserting 48 kHz stereo 60 ms uses
    `2880 * 2`, not `5760 * 2`;
  - updated the stale C-reference helper comment.
- Validation reported:
  - `frame_duration_tests` passed with 8 tests;
  - `oracle_tests` passed with 20 passed, 1 ignored;
  - `cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq` passed;
  - both fmt checks passed;
  - `git diff --check` passed.
- Supervisor response:
  - Proceed to final completion review. The completion reviewer must check the
    whole Slice B contract, including the already-existing multiframe wiring,
    the newly tested PLC sizing helper, and untouched oracle semantics.

## Completion Review Result
- Hooke completed the final completion review.
- Completion review saved to:
  `/home/md/language/ropus/wrk_docs/2026.05.06 - CR - legal frame duration stateful fuzz completion.md`
- Completion review journal saved to:
  `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - legal frame duration stateful fuzz completion review.md`
- Go/no-go:
  - Go. Slice B is complete.
- Hooke confirmed:
  - `fuzz_encode_multiframe.rs` already satisfies the HLD requirements for
    `sequence_shape`, duration selector storage, shared helper sizing, duration
    context, and unchanged parity/sample-count semantics.
  - `fuzz_decode_plc_seq.rs` derives duration from `drop_mask >> 29`, uses the
    selected legal duration for PCM sizing, encode frame size, Rust PLC
    request/capacity, and C decode request.
  - The new focused test rejects a regression to `5760 * channels`.
  - The stale C-reference helper comment is fixed.
  - No codec implementation files or `oracle.rs` changed.
- Completion validation passed:
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
  - `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe`
  - `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`
  - `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
  - `timeout 120 cargo fmt --check`
  - `git diff --check`
- Final supervisor worktree observation:
  - tracked slice diffs are in `c_reference.rs`, `fuzz_decode_plc_seq.rs`,
    `frame_duration_tests.rs`, and the stateful fuzz review document;
  - untracked task docs/journals are present as expected;
  - the older `/home/md/language/ropus/wrk_journals/2026.05.04 - JRN - release-and-refuzz.md`
    remains unrelated and untracked.
- Feeling:
  - This landed in the right shape: one real behavior fix, one regression test,
    no oracle weakening, and no fuzz-campaign churn. I am satisfied with the
    completion review. I will not commit because Arthur's repo instructions say
    not to run git add/commit/push unless explicitly requested.

## Implementation Agent Pass
- I read the repo instructions, the Slice B HLD, the Slice A frame-duration
  helper/tests, and both stateful fuzz targets before editing.
- Starting tracked state was already close to the HLD: `fuzz_encode_multiframe`
  was using `sequence_shape`, storing `frame_duration_selector`, using the
  shared helper for frame size, and carrying duration context into the
  fingerprint/failures. `fuzz_decode_plc_seq` was deriving the selector from
  `drop_mask >> 29`, using the selected frame size for encode and C decode
  requests, and including duration context in failures.
- Gap found: Rust PLC sequence decode still allocated the old 120 ms capacity
  (`5760 * channels`) while C decode allocated `frame_size * channels`. I
  changed the Rust decode buffer to use the already-computed selected
  `samples_per_frame`, keeping the decode request at selected `frame_size`.
- Emotional temperature: low drama, but I am slightly wary of hidden fuzz-build
  issues because these targets depend on nightly/libFuzzer and the C reference
  build path. The actual code change is intentionally tiny.

## Validation
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests`
  passed: 7 passed.
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests`
  passed: 20 passed, 1 ignored operator-report test.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe`
  passed.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq`
  passed.
- `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check`
  passed.
- `timeout 120 cargo fmt --check`
  passed.

## Residual Notes
- I did not edit `oracle.rs` or codec implementation files.
- I did not run a fuzz campaign; the requested scope was target build
  validation, not campaign execution.
