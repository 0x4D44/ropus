# Journal: Platform Sanitizer HLD

Date: 2026.05.06

## Entry

Read the testing gaps review, the testing improvement plan, the current oracle
matrix, `.cargo/config.toml`, and the relevant `full-test` and fuzz runner code
for the platform/sanitizer breadth slice.

The important evidence is that the repo now has a much stronger release
contract than it did this morning. The performance gate has landed, and the
oracle matrix is explicit about most major lanes. Phase 4.2 is now the natural
next testing-infrastructure slice.

The `.cargo/config.toml` x86-64-v3 default is the core platform concern. It is a
good performance default, but we should not let it become the only correctness
story. A stable generic x86_64 `ropus --lib` smoke is the smallest useful
counterweight.

The sanitizer question was more subtle. My first instinct was to propose a
standalone ASan workspace test, but that smells like toolchain flake dressed up
as rigor. The repo already has a non-quick release fuzz sanity path, and
`cargo-fuzz` is the place where nightly ASan, C-reference sanitizer flags, crash
replay, and Windows runtime setup already live. The better first step is to make
that sanitizer claim explicit and avoid inventing another brittle ASan lane.

Local toolchain snapshot:

- `stable-x86_64-unknown-linux-gnu` active.
- `nightly-x86_64-unknown-linux-gnu` installed.
- installed target: `x86_64-unknown-linux-gnu`.
- `cargo-llvm-cov` available.
- `cargo miri` unavailable on stable.

Emotional temperature: cautiously satisfied. The temptation here is to build an
impressive matrix. That would be the wrong move. The useful slice is small,
boring, and honest: generic x86_64 smoke plus explicit ASan-backed fuzz sanity
reporting. Slight frustration that "sanitizer coverage" can sound broader than
it is, so I called out the limits directly in the HLD.

Files written:

- `/home/md/language/ropus/wrk_docs/2026.05.06 - HLD - platform sanitizer breadth.md`
- `/home/md/language/ropus/wrk_journals/2026.05.06 - JRN - platform sanitizer hld.md`

No implementation code changed and no commit was made.
