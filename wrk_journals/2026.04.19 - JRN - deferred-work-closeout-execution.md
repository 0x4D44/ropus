# 2026-04-19 — Deferred-work closeout execution journal

**Supervisor:** Claude (orchestrator)
**Plan:** `wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md`

## Role & operating mode

I am the supervisor. I do not implement, test, or debug directly. I:
- Delegate implementation to worker agents (general-purpose).
- Delegate code review to review agents invoking the `code-reviewer` skill.
- Run a devils-advocate agent after each review to challenge the work.
- Delegate feedback application to agents invoking `code-review-apply-feedback`.
- Update this journal at the close of each stage.
- Commit after each stage.

My context is protected: agents return concise summaries; I do not read large files myself.

## Concurrent-work awareness

Another agent is actively editing `ropus/src/celt/simd.rs`, `ropus/src/celt/vq.rs`, and `ropus/src/opus/encoder.rs` for the unchecked-indexing perf workstream (see `wrk_journals/2026.04.18 - JRN - celt-encoder-unchecked-indexing.md`). I will not touch their work and will coordinate when my stages land on `opus/encoder.rs`.

## Stage plan (from HLD priority order)

| Stage | Item | Size | Status |
|---|---|---|---|
| 0 | Kick-off + commit HLD | — | in progress |
| 1 | Ambisonic / projection matrices (#1) | S | pending |
| 2 | CVBR verification (#2) | XS | pending |
| 3 | Real-world bitstream corpus (#3) | S–M | pending |
| — | Checkpoint after #3 | — | — |
| 4 | Fuzzer cadence (#8) | XS | pending |
| 5 | Independent CTL plumbing (#9) | S | pending |
| 6 | Analysis / tonality detection (#4) | L | pending |
| 7 | DNN wiring (#5) + DNN-adjacent CTLs (#10) | L | pending |
| 8 | OSCE / DRED (#6) | XL | pending |
| 9 | Platform SIMD (#7) — doc-only, deferred | — | pending |
| 10 | Final plan-vs-implementation audit | — | pending |

Small items first, then the three large ports. I will pause and ask Arthur before committing to #7 (OSCE/DRED) given its XL size.

## Stage 0 — Kick-off

Starting state (git status short):
- `M capi/src/lib.rs` — my CTL-comment tightening from earlier this session
- `M harness/tests/c_ref_differential.rs` — NOT mine, leave alone
- `M ropus/src/opus/encoder.rs` — NOT mine (perf agent), leave alone
- Four modified `wrk_docs` / `wrk_journals` entries — NOT mine, leave alone
- `?? wrk_docs/2026.04.19 - HLD - deferred-work-closeout.md` — new, mine
- `?? wrk_journals/2026.04.19 - JRN - deferred-work-closeout-execution.md` — this file, new, mine

Commit plan for Stage 0: stage only my files — the HLD, this journal, and the capi comment fix. Do not stage others' in-flight work.

**Stage 0 result.** Commit `62c43d6` — HLD + journal + capi comment tightening. Clean stage, only my files.

---

## Stage 1 — Ambisonic / projection matrices (P0) — DONE

**Commit.** `1f43890` — 13 files, +1479/-27.

**Implementation agent.** Ported ten mapping matrices from `reference/src/mapping_matrix.c:289-996` (HLD said `mapping_matrix_data.h` but the real source lives in `.c`; agent found + used the correct path — good judgement). Added `harness/src/bin/projection_roundtrip.rs`, `tools/make_ambisonic_fixture.rs`, and an initial FOA fixture. All 1787 library tests stayed green; harness proved byte-exact encode + sample-exact decode vs C reference.

**Code reviewer.** Verdict: ships. Flagged two nits (stale `#[allow(dead_code)]`, leftover `ptr::null` scaffolding). Confirmed FFI signatures match reference headers; spot-checks of matrix values matched.

**Devils-advocate.** Confirmed test oracle is independent (C ref links its own matrices via cc crate — not a tautology). Transcription / row-column order / gain all PASS. **Raised one MUST-FIX: only FOA (1 of 5 orders) was actually tested end-to-end — SOA/TOA/4HOA/5HOA unverified, including the only matrix with non-zero gain.** Without fixture coverage, 80% of the ported data was effectively untested.

**Feedback application.** Agent extended the fixture generator + harness to cover all five orders (4/9/16/25/36 channels). All five pass byte-exact encode + sample-exact decode against C. No transcription errors discovered. Removed the dead-code attrs and `ptr::null` scaffolding.

**Outcome.** Stage closed with independent round-trip verification across all five ambisonic orders. The devils-advocate nudge was load-bearing — without it we'd have shipped claiming "ambisonics works" on evidence that only covered FOA.

---

## Stage 2 — CVBR verification (P1) — in progress

Verification-only stage. The CVBR flag is already wired to the CELT encoder (`opus/encoder.rs:2452`) per the HLD-revision pass; the task is to confirm it shapes output and add one output-size-assertion test, not to fix broken code.

Dispatching a single implementation agent with the code-reviewer + devils-advocate loop to follow.
