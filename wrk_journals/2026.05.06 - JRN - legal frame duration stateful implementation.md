# Legal Frame Duration Stateful Implementation Journal

## 2026-05-06

Started Phase 3.3 Slice B implementation for the two remaining stateful fuzz targets. Read `AGENTS.md`, the Slice B HLD, the existing duration helper/tests, and both target files before editing.

Initial emotional temperature: focused, slightly wary of the PLC path because replacing `5760` changes actual requested PLC behavior while the output buffer must remain at max capacity. The HLD is precise enough that the risk is mostly accidental oracle drift, so I am keeping changes to selector derivation, frame-size propagation, fingerprint/debug context, and pure tests.

Added small selector-policy helpers to `tests/fuzz/fuzz_targets/frame_duration.rs` so the multiframe shape byte and PLC drop-mask high-bit policies can be unit-tested directly. Updated `frame_duration_tests.rs` to verify multiframe selector/frame-count bounds and PLC high-bit selector behavior, including wrap-through of selectors 6 and 7.

Wired `fuzz_encode_multiframe.rs` to derive one legal duration and frame count from one sequence shape byte, size every PCM frame from the shared legal duration helper, include the selector in the fingerprint, and add duration/frame-size context to diagnostics.

Wired `fuzz_decode_plc_seq.rs` to derive one legal duration from high `drop_mask` bits, size generated PCM frames from that duration, keep decode output buffers at `5760 * channels`, and pass the selected `frame_size` as the requested decode size for both Rust and C decode paths.

Validation pass completed:

- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test frame_duration_tests` passed with 7 tests.
- `timeout 120 cargo test --manifest-path tests/fuzz/Cargo.toml --test oracle_tests` passed with 20 tests and 1 ignored report test.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_encode_multiframe` passed.
- `timeout 300 cargo +nightly fuzz build --fuzz-dir tests/fuzz fuzz_decode_plc_seq` passed.
- `timeout 120 cargo fmt --manifest-path tests/fuzz/Cargo.toml --check` passed.
- `timeout 120 cargo fmt --check` passed.

Residual risk: no fuzz campaigns were run by design, so this validates construction and compilation only. Emotional temperature now: relieved that the nightly fuzz builds were available and the PLC request-size change compiled cleanly; still attentive to the possibility that actual campaigns may uncover real duration-specific parity failures.
